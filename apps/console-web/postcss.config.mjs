// Tailwind CSS v4 via PostCSS - the exact setup Next 16's own bundled guide prescribes
// (node_modules/next/dist/docs/01-app/01-getting-started/11-css.md, R-491).
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
