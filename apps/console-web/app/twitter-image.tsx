/* Twitter/X card image (Next's `twitter-image` file convention): the same generated image as the
 * OpenGraph one, so both previews stay identical. */
export { default, alt, size, contentType } from "./opengraph-image";
