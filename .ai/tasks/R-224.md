# R-224 — Next.js console upgrade (DEFERRED — environment-blocked)

## Requirement

Upgrade the R-222 static platform console to a real Next.js (App Router, TypeScript) application under
`apps/console-web`, preserving the `overview.json` data contract and design, with security headers and
a production build.

New IDs continue after the workbook backlog's R-010..R-219 (this is R-224).

## Status: Deferred

This sandbox cannot install the Next.js toolchain: `pnpm install` for `next@15.5.4` repeatedly times
out fetching the native SWC binary (`@next/swc-darwin-arm64`) — attempted three times, including a
standalone install under `apps/console-web/nextjs/` with a 10-minute fetch timeout and increased
retries. Without a completed install there is no `next build` evidence, and committing an
un-installable app would break `task bootstrap` for others.

Per the rule "record real command evidence — never claim unexecuted tests", this task is **Deferred**
rather than marked Done. The R-222 dependency-free static console remains the working slice and is
fully verified.

## Resume plan (when a network/build environment is available)

1. In an environment with reliable npm registry access, create the Next.js app under `apps/console-web`
   (App Router, TypeScript): `package.json` (next/react), `tsconfig.json`, `next.config.mjs` (the
   security headers already drafted: CSP, `X-Frame-Options`, `nosniff`, referrer policy), `app/layout.tsx`,
   `app/page.tsx`, `lib/overview.ts`, importing the committed `data/overview.json`.
2. `pnpm install` then `pnpm --filter @omnistackai/console-web build` for real evidence.
3. Evolve `task bootstrap` from `--offline` to a normal frozen install (registry access required), and
   add `task console:build` / `console:dev`; update `scripts/console.sh` and the Taskfile.
4. Keep `task verify` independent of the web build; the `overview.json` contract and layout carry over
   unchanged from the static console.

## Model and cost trace

No model call; no cloud call. No code committed (install could not complete).
