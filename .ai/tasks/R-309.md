# Task R-309: Generated Accessible Reusable Pagination Component (components/pagination.tsx)

**Status:** DONE  
**Completed:** 2026-09-10  
**Phase:** MVP (Founder Stage 0)  
**Verification:** `task verify` PASS (1,029 tests, 12 new tests in `test_pagination_component.py`)

## Objectives
- [x] Implement `render_pagination_component() -> str` in `services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`.
- [x] Export `render_pagination_component` from `omnistackai_agent_engine.codegen`.
- [x] Register `components/pagination.tsx` in `NextjsWebAdapter.generate()`.
- [x] Support `PaginationProps`: `page`, `pageSize`, `total`, `totalPages`, `onPageChange`, `onPageSizeChange`, `pageSizeOptions`, `disabled`, `compact`, and `itemLabel`.
- [x] Implement WAI-ARIA 1.2 pagination structure: `<nav aria-label="Pagination">`, `aria-label="Previous page"`, `aria-label="Next page"`, `aria-current="page"` on active page button, and `<label htmlFor="pageSizeSelect">` with `<select id="pageSizeSelect" aria-label="Select page size">`.
- [x] Provide direct page number navigation buttons with ellipsis for wide page ranges in non-compact mode.
- [x] Support `compact` mode for constrained environments (e.g. drawers, detail subcollections, cards).
- [x] Guarantee 100% diff-invariance across `ir.description` changes.
- [x] Achieve 100% pass across unit tests, ruff linter, security checks, and demo builds.

## Evidence
- `task verify`: 1,029 passed in 1.483s.
- `task lint`: PASS.
- `task security:quick`: PASS.
- `task env:check`: PASS.
- `task builder:demo -- minimal-blog`: 48 files generated cleanly.
- `task builder:demo -- rideshare-favourites`: 45 files generated cleanly.
