# R-414: Accessible Futuristic Reusable Duration Input Suite (`components/duration-input.tsx`)

## Status
DONE

## Objective
Add an accessible, zero-dependency, desktop-and-mobile-grade, futuristic Duration Input compound component suite (`components/duration-input.tsx`): segmented numeric inputs for a configurable set of units (days/hours/minutes/seconds via a `UNIT_SECONDS` table) that convert to and from a single total-seconds value (`toSegments`/`fromSegments`), with `min`/`max` clamping (normalizing segments on clamp), a human-readable `formatDuration` helper, controlled + uncontrolled value (number of seconds), `onChange(totalSeconds)` callback, and WAI-ARIA (group with `aria-label`, per-segment `aria-label` + `inputMode="numeric"`); 4 futuristic visual styling variants ("default", "card", "glass", "neon"); 3 size scales ("sm", "md", "lg"); React ref forwarding (`forwardRef`); `useImperativeHandle` (`DurationInputHandle`: `getValue`/`setValue`/`getFormatted`/`clear`/`focus`); and explicit `displayName` across compound and semantic alias exports (`DurationInput`, `DurationField`, `TimeSpanInput`, `IntervalInput`, default export). Static module constant — never references `ir.name`/`ir.description`; ASCII-only source.

## Allowed Paths
- `services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`
- `services/agent-engine/src/omnistackai_agent_engine/codegen/__init__.py`
- `services/agent-engine/tests/test_duration_input_component.py`
- `.ai/tasks/R-414.md`
- `.ai/CURRENT_TASK.yaml`
- `.ai/PROJECT_STATE.yaml`
- `.ai/WORK_LOG.md`
- `.ai/HANDOFF.md`
- `PROJECT_STATE.md`
- `CHANGELOG.md`

## Forbidden Changes
- Do not introduce external npm dependencies.
- Do not break snapshot diff invariance across `ir.description`.
- Do not commit or push code to git without verification.

## Gates & Evidence
- Unit tests: `pytest services/agent-engine/tests/test_duration_input_component.py` (18 passed)
- Platform verification: `task verify` (2,735 tests passed)
- Linting: `task lint` (passed)
- Security scan: `task security:quick` (passed)
- Demo build: `task builder:demo -- minimal-blog` (generated 151 files)
