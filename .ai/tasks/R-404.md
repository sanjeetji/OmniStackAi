# R-404: Accessible Futuristic Reusable Masked / Pattern Input Suite (`components/masked-input.tsx`)

## Status
DONE

## Objective
Add an accessible, zero-dependency, desktop-and-mobile-grade, futuristic Masked / Pattern Input compound component suite (`components/masked-input.tsx`): token-based input masking (`9`=digit, `A`=letter, `*`=alphanumeric, any other char = literal) applied in real time as the user types, with built-in presets (`phone`/`date`/`card`/`time`/`ssn`) and custom masks; an `applyMask()` that produces both the formatted display value and the raw (unmasked) value plus a completeness flag; the caret kept at the end after reformatting (`requestAnimationFrame` + `setSelectionRange`); controlled + uncontrolled `value`; `onChange` (formatted, raw) and `onComplete` callbacks; `inputMode` pass-through for mobile keyboards; WAI-ARIA (`aria-label`, `aria-required` when required); 4 futuristic visual styling variants ("default", "card", "glass", "neon"); 3 size scales ("sm", "md", "lg"); React ref forwarding (`forwardRef`); `useImperativeHandle` (`MaskedInputHandle`: `getValue`/`getRawValue`/`setValue`/`clear`/`focus`); and explicit `displayName` across compound and semantic alias exports (`MaskedInput`, `InputMask`, `PatternInput`, `FormattedInput`, default export). Static module constant — never references `ir.name`/`ir.description`.

## Allowed Paths
- `services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`
- `services/agent-engine/src/omnistackai_agent_engine/codegen/__init__.py`
- `services/agent-engine/tests/test_masked_input_component.py`
- `.ai/tasks/R-404.md`
- `.ai/CURRENT_TASK.yaml`
- `.ai/PROJECT_STATE.yaml`
- `.ai/WORK_LOG.md`
- `.ai/HANDOFF.md`
- `PROJECT_STATE.md`
- `CHANGELOG.md`

## Forbidden Changes
- Do not introduce external npm dependencies.
- Do not break snapshot diff invariance across `ir.description`.
- Do not commit or push code to git without verification.

## Gates & Evidence
- Unit tests: `pytest services/agent-engine/tests/test_masked_input_component.py` (18 passed)
- Platform verification: `task verify` (2,556 tests passed)
- Linting: `task lint` (passed)
- Security scan: `task security:quick` (passed)
- Demo build: `task builder:demo -- minimal-blog` (generated 141 files)
