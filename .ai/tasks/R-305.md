# Standard AI Task Contract: R-305

## Metadata
- **Tracker ID**: R-305
- **Title**: Generated Keyboard Shortcuts Help Modal & Global Discovery Affordance
- **Milestone**: MVP
- **Category**: Builder / Codegen
- **Status**: Done
- **Author**: sanjeetji <sk698166@gmail.com>

## Objective
Elevate keyboard discoverability and power-user accessibility across generated Next.js web applications:
1. **Reusable ShortcutsDialog Component (`apps/web/components/shortcuts-dialog.tsx`)**:
   - `ShortcutsDialog` modal component rendering a backdrop overlay, dialog card container, header title with keyboard icon, close button, and organized shortcut groups.
   - WAI-ARIA compliance: `role="dialog"`, `aria-modal="true"`, `aria-labelledby="shortcuts-dialog-title"`.
   - Keyboard interaction: closes on `Escape` key press; clicking backdrop closes dialog.
   - Shortcut categories:
     - **Global Navigation**: `?` (Show / hide shortcuts), `Esc` (Close modal / dismiss / clear).
     - **Collection Screens**: `/` (Focus search input), `Esc` (Clear active search or filter criteria).
     - **Record Detail Screens**: `[` / `]` or `←` / `→` (Navigate previous / next record), `e` (Edit current record), `Esc` (Deselect active record).
     - **Form Editor Screens**: `Cmd+Enter` / `Ctrl+Enter` (Submit / save form), `Cmd+S` / `Ctrl+S` (Save form changes), `Esc` (Blur active input or discard changes).
   - Styled `<kbd>` badges with monospace font, subtle border, white background, and drop shadow.
2. **Navbar Header Integration (`apps/web/components/navbar.tsx`)**:
   - Imports and mounts `ShortcutsDialog` component with local `isOpen` state.
   - Registers a global `keydown` event listener for `?` (outside editable form elements like INPUT, TEXTAREA, SELECT, contentEditable) to toggle the modal.
   - Renders an accessible `Shortcuts (?)` trigger button with keyboard icon (`⌨`), text label, and `?` shortcut badge in the navbar header next to the quick-create CTA.
3. **Adapter Registration**:
   - `components/shortcuts-dialog.tsx` emitted as a static client component in `NextjsWebAdapter.generate()`.
   - Exported `render_shortcuts_dialog_component` in `omnistackai_agent_engine.codegen`.

## Invariance & Constraints
- 100% offline, stdlib-only Python codegen in `services/agent-engine`.
- Zero external runtime npm dependencies; inline styles matching existing system palette.
- Strict diff invariance across `ir.description`.
- Zero network or cloud model calls.
- Preserves all existing navbar routes, quick-create CTAs, and active route highlighting.

## Evidence & Verification
- [x] Dedicated unit test suite in `services/agent-engine/tests/test_shortcuts_dialog.py` passes (14/14 tests).
- [x] All 989 unit tests pass in `task verify` (1.080s).
- [x] `task lint` passed (ruff clean).
- [x] `task security:quick` passed (Stage 0 secret-policy clean).
- [x] `task env:check` passed.
- [x] Both builder demos (`minimal-blog` and `rideshare-favourites`) pass.
- [x] `R_&_D/OmniStackAI_Execution_Tracker_v6.xlsx` updated with R-305 (Row 9, table A4:M313, 0 `#REF!`, 94 Done, MVP 94/200 = 47.0%).
