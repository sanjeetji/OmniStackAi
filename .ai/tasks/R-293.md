# R-293 — Loading Skeletons for Form Initial Load & Detail Record-Selector List

**Status:** done
**Phase:** BASIC/MVP — Founder Stage 0
**Branch:** `main`
**Complexity:** L2 (deterministic codegen)
**Implementation SHA:** `9796f4a`

## Objective

Complete the R-292 loading-skeleton coverage by replacing the two remaining plain "Loading..." text
spots with layout-preserving skeleton placeholders: the form edit-mode initial-load banner
(`isEdit && fetchingInitial`, `_form_screen_page`) and the detail-screen record-selector "recent records"
list (`loadingList`, `_detail_screen_page`). Static inline-styled skeletons — no CSS `@keyframes`, no new
file, no dependency.

## Requirement

- Form initial-load banner: replace `Loading <name> details...` with a flex column mapping ~3 skeleton
  field blocks (`height: 34, background: "#e2e8f0", borderRadius: 6, opacity: 1 - i * 0.2`).
- Detail record-selector list loading: replace `{loadingList && <p>Loading <plural>...</p>}` with
  `{loadingList && (<div grid>...)}` mapping ~3 skeleton card blocks (`height: 56, background: "#f1f5f9"`)
  in the same `repeat(auto-fill, minmax(220px, 1fr))` grid as the recent-records cards.
- Other loading states (R-292), empty/error states, and data rendering unchanged. No hook/API-client/
  backend/IR change; deterministic; description-only-stable.

## Acceptance criteria

1. Form initial-load maps skeleton blocks; `Loading <name> details...` is gone.
2. Detail record-selector loading maps skeleton cards; its `Loading <plural>...` is gone.
3. Description-only IR changes remain byte-identical; both example projects still generate.
4. Focused + related form/detail tests + full offline gates + both demos pass.

## Model and cost trace

Deterministic; no model call, no network, no DB. local_calls 0 / cloud_calls 0.

## Verification Evidence

- `task verify` — 864 agent-engine tests pass (6 new in `test_loading_skeletons_extra.py`, written
  test-first; one `test_form_update_screens.py` initial-load assertion updated to the skeleton markup).
- `task lint`, `task security:quick`, `task env:check` — pass.
- `task builder:demo -- minimal-blog` and `-- rideshare-favourites` — pass.
- Generated TypeScript inspected: the form edit-mode initial-load banner maps three
  `height: 34, background: "#e2e8f0"` skeleton bars, and the detail record-selector list maps three
  `height: 56, background: "#f1f5f9"` skeleton cards in the same `repeat(auto-fill, minmax(220px, 1fr))`
  grid as the record cards. The R-292 skeletons and all other states are unchanged.
- Tracker — R-293 Done at `Phase_Roadmap!A9:M9`; table `A4:M301`; 293 unique IDs (0 dupes); 82 Done,
  1 Deferred, 210 Not Started; MVP 82/188 (43.6%); no `#REF!`; XLSX valid.
- Implementation `9796f4a`; offline/deterministic; 0 local / 0 cloud model calls; no DB connection.

## Result

**PASS** — loading skeletons now cover every generated data-loading state. Feat commit `9796f4a`; docs
commit records state/tracker. Next unstarted Tracker ID: **R-294**.
