# Task R-310: Generated Accessible Reusable Tabs Component (components/tabs.tsx)

**Status:** DONE  
**Completed:** 2026-09-10  
**Phase:** MVP (Founder Stage 0)  
**Verification:** `task verify` PASS (1,041 tests, 12 new tests in `test_tabs_component.py`)

## Objectives
- [x] Implement `render_tabs_component() -> str` in `services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`.
- [x] Export `render_tabs_component` from `omnistackai_agent_engine.codegen`.
- [x] Register `components/tabs.tsx` in `NextjsWebAdapter.generate()`.
- [x] Support `TabItem`, `TabsProps`, and `TabPanelProps` interfaces with `id`, `label`, `count`, `disabled`, `activeTab`, `onChange`, `ariaLabel`, and `variant` (`"line"` | `"pills"`).
- [x] Implement WAI-ARIA 1.2 Tabs structure:
  - `<div role="tablist" aria-label="...">`
  - `<button role="tab" id={`tab-${id}`} aria-selected={isActive} aria-controls={`tabpanel-${id}`} tabIndex={isActive ? 0 : -1}>`
  - Accessible count badge display when `count !== undefined`
  - `TabPanel` component rendering `<div role="tabpanel" id={`tabpanel-${id}`} aria-labelledby={`tab-${id}`} tabIndex={0} hidden={activeTab !== id}>`
- [x] Full keyboard navigation: `ArrowRight` (next tab), `ArrowLeft` (previous tab), `Home` (first tab), `End` (last tab).
- [x] Guarantee 100% diff-invariance across `ir.description` changes.
- [x] Achieve 100% pass across unit tests, ruff linter, security checks, and demo builds.

## Evidence
- `task verify`: 1,041 passed in 1.245s.
- `task lint`: PASS.
- `task security:quick`: PASS.
- `task env:check`: PASS.
- `task builder:demo -- minimal-blog`: 49 files generated cleanly.
- `task builder:demo -- rideshare-favourites`: 46 files generated cleanly.
