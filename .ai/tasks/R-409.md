# R-409: Accessible Futuristic Reusable Currency / Money Input Suite (`components/currency-input.tsx`)

## Status
DONE

## Objective
Add an accessible, zero-dependency, desktop-and-mobile-grade, futuristic Currency / Money Input compound component suite (`components/currency-input.tsx`): a numeric money field that sanitizes typed input to a clean numeric string, parses it to a number, clamps to `min`/`max` on blur, and formats the value as locale-aware currency via the built-in `Intl.NumberFormat` (`style: 'currency'`) when unfocused (plain numeric while focused for easy editing); configurable `currency` + `locale`, `min`/`max`/`step`, `allowNegative`; controlled + uncontrolled `value` (number); `onChange(value|null, formatted)` and `onBlur` callbacks; WAI-ARIA (labeled input, `aria-invalid`, `aria-required`, `inputMode="decimal"`); 4 futuristic visual styling variants ("default", "card", "glass", "neon"); 3 size scales ("sm", "md", "lg"); React ref forwarding (`forwardRef`); `useImperativeHandle` (`CurrencyInputHandle`: `getValue`/`getFormatted`/`setValue`/`clear`/`focus`); and explicit `displayName` across compound and semantic alias exports (`CurrencyInput`, `MoneyInput`, `CurrencyField`, `PriceInput`, default export). Static module constant — never references `ir.name`/`ir.description`.

## Allowed Paths
- `services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`
- `services/agent-engine/src/omnistackai_agent_engine/codegen/__init__.py`
- `services/agent-engine/tests/test_currency_input_component.py`
- `.ai/tasks/R-409.md`
- `.ai/CURRENT_TASK.yaml`
- `.ai/PROJECT_STATE.yaml`
- `.ai/WORK_LOG.md`
- `.ai/HANDOFF.md`
- `PROJECT_STATE.md`
- `CHANGELOG.md`

## Forbidden Changes
- Do not introduce external npm dependencies.
- Do not break snapshot diff invariance across `ir.description`.
- Do not commit or push code to git without verification.

## Gates & Evidence
- Unit tests: `pytest services/agent-engine/tests/test_currency_input_component.py` (17 passed)
- Platform verification: `task verify` (2,645 tests passed)
- Linting: `task lint` (passed)
- Security scan: `task security:quick` (passed)
- Demo build: `task builder:demo -- minimal-blog` (generated 146 files)
