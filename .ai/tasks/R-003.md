# R-003 — Local Ollama Stage 0 bootstrap

## Requirement

Make local Ollama a working Stage 0 dependency with one configured coding model, matching Brief
Sections 79, 80.4, and 84.1. Keep the endpoint on loopback and make model selection configurable.

The supplied tracker omits R-003 through R-009. This task is reconstructed from the Founder Build
Sequence with the founder's instruction to continue.

## Acceptance criteria

- A tracked environment variable selects the local coding model; product code does not hardcode it.
- Stage 0 validation rejects non-loopback Ollama endpoints.
- Root commands validate configuration, inspect health/models, pull the selected model, run the
  server, and perform a real local inference smoke test.
- The smoke test discovers the selected model from `/api/tags` before calling `/api/generate`.
- Static repository verification remains deterministic and does not require a running Ollama server.
- No cloud model, API key, product provider adapter, extra infrastructure service, or native work is
  added.

## Expected blast radius

Only local-model scripts, commands, environment placeholders, related documentation/tests, tracker
evidence, and portable task/state/handoff files may change.

## Model and cost trace

One local Ollama smoke-test call using the configured coder model. Zero cloud calls and zero paid
provider usage.
