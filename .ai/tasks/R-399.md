# R-399: Accessible Futuristic Reusable Particle Network & Interactive Constellation Canvas Suite (`components/particle-network.tsx`)

## Status
DONE

## Objective
Add an accessible, zero-dependency, desktop-and-mobile-grade, futuristic Particle Network & Interactive Constellation Canvas compound component suite (`components/particle-network.tsx`) — an ambient, decorative-by-default animated background (distinct from the data-driven `network-graph`): particles are generated internally (no required data props) from a `count`/`density` derivation, animate on an HTML5 Canvas 2D `requestAnimationFrame` loop with edge-bounce motion, draw proximity link lines between particles with distance-proportional `globalAlpha`, react to the pointer (attraction + accent-colored cursor links within `interactionRadius`), use device-pixel-ratio-aware canvas sizing, honor `prefers-reduced-motion` in JS by rendering a single static frame and scheduling no animation (with a live change listener), expose an imperative `ParticleNetworkHandle` (`pause`, `resume`, `toggle`, `restart`, `isPaused`, `getCanvas`) via `useImperativeHandle`, carry WAI-ARIA decorative semantics (wrapper `role="img"` + `aria-label`, `aria-hidden="true"` canvas, no focus trap / not keyboard-interactive), offer 4 futuristic visual styling variants ("default", "card", "glass", "neon") and 3 size scales ("sm", "md", "lg"), support React ref forwarding (`forwardRef`), semantic alias exports (`ParticleNetwork`, `ConstellationCanvas`, `ParticleField`, `StarfieldBackground`), and explicit `displayName` across compound exports. Static module constant — never references `ir.name`/`ir.description`, so generation stays deterministic and diff-invariant.

## Allowed Paths
- `services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`
- `services/agent-engine/src/omnistackai_agent_engine/codegen/__init__.py`
- `services/agent-engine/tests/test_particle_network_component.py`
- `.ai/tasks/R-399.md`
- `.ai/CURRENT_TASK.yaml`
- `.ai/PROJECT_STATE.yaml`
- `.ai/WORK_LOG.md`
- `.ai/HANDOFF.md`
- `PROJECT_STATE.md`
- `CHANGELOG.md`

## Forbidden Changes
- Do not introduce external npm dependencies.
- Do not break snapshot diff invariance across `ir.description`.
- Do not commit or push code to git without verification.

## Gates & Evidence
- Unit tests: `pytest services/agent-engine/tests/test_particle_network_component.py` (18 passed)
- Platform verification: `task verify` (2,466 tests passed)
- Linting: `task lint` (passed)
- Security scan: `task security:quick` (passed)
- Demo build: `task builder:demo -- minimal-blog` (generated 136 files)
