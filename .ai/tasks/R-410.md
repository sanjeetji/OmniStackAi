# R-410: Accessible Futuristic Reusable Password Generator Suite (`components/password-generator.tsx`)

## Status
DONE

## Objective
Add an accessible, zero-dependency, desktop-and-mobile-grade, futuristic Password Generator compound component suite (`components/password-generator.tsx`): generates a secure random password using `crypto.getRandomValues` (with a `Math.random` fallback) from configurable character sets (uppercase, lowercase, numbers, symbols) with an optional exclude-ambiguous filter, guaranteeing at least one character from each enabled set and shuffling with Fisher-Yates; a length slider (min/max), set toggles, a strength meter, a read-only output, copy-to-clipboard (`navigator.clipboard` with `execCommand` fallback) with copied feedback, and a regenerate action; SSR-safe (crypto only accessed in handlers/effects; auto-generates on mount); controlled length/options via props; `onGenerate` + `onCopy` callbacks; WAI-ARIA (`role="group"`, labeled controls, `aria-live` copied announcement); 4 futuristic visual styling variants ("default", "card", "glass", "neon"); 3 size scales ("sm", "md", "lg"); React ref forwarding (`forwardRef`); `useImperativeHandle` (`PasswordGeneratorHandle`: `generate`/`getValue`/`copy`/`setLength`); and explicit `displayName` across compound and semantic alias exports (`PasswordGenerator`, `PasswordCreator`, `SecurePasswordGenerator`, `PasswordMaker`, default export). Static module constant — never references `ir.name`/`ir.description`.

## Allowed Paths
- `services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`
- `services/agent-engine/src/omnistackai_agent_engine/codegen/__init__.py`
- `services/agent-engine/tests/test_password_generator_component.py`
- `.ai/tasks/R-410.md`
- `.ai/CURRENT_TASK.yaml`
- `.ai/PROJECT_STATE.yaml`
- `.ai/WORK_LOG.md`
- `.ai/HANDOFF.md`
- `PROJECT_STATE.md`
- `CHANGELOG.md`

## Forbidden Changes
- Do not introduce external npm dependencies.
- Do not break snapshot diff invariance across `ir.description`.
- Do not commit or push code to git without verification.

## Gates & Evidence
- Unit tests: `pytest services/agent-engine/tests/test_password_generator_component.py` (18 passed)
- Platform verification: `task verify` (2,663 tests passed)
- Linting: `task lint` (passed)
- Security scan: `task security:quick` (passed)
- Demo build: `task builder:demo -- minimal-blog` (generated 147 files)
