# R-412: Accessible Futuristic Reusable Character & Word Counter Textarea Suite (`components/character-counter.tsx`)

## Status
DONE

## Objective
Add an accessible, zero-dependency, desktop-and-mobile-grade, futuristic Character & Word Counter Textarea compound component suite (`components/character-counter.tsx`): a textarea with live Unicode-safe character counting (`Array.from` code points) and word counting (trim + whitespace split), configurable `maxLength` / `maxWords` with a computed stats object (`{characters, words, remaining, overLimit}`), an optional hard limit that blocks input past `maxLength`, a warn threshold that recolors near the limit, an optional progress bar, controlled + uncontrolled value, `onChange(value, stats)` callback, and WAI-ARIA (labeled textarea, `aria-describedby` wiring to a `role="status"` `aria-live` counter region); 4 futuristic visual styling variants ("default", "card", "glass", "neon"); 3 size scales ("sm", "md", "lg"); React ref forwarding (`forwardRef`); `useImperativeHandle` (`CharacterCounterHandle`: `getValue`/`setValue`/`getStats`/`clear`/`focus`); and explicit `displayName` across compound and semantic alias exports (`CharacterCounter`, `CharCounter`, `WordCounter`, `TextCounter`, default export). Static module constant — never references `ir.name`/`ir.description`.

## Allowed Paths
- `services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`
- `services/agent-engine/src/omnistackai_agent_engine/codegen/__init__.py`
- `services/agent-engine/tests/test_character_counter_component.py`
- `.ai/tasks/R-412.md`
- `.ai/CURRENT_TASK.yaml`
- `.ai/PROJECT_STATE.yaml`
- `.ai/WORK_LOG.md`
- `.ai/HANDOFF.md`
- `PROJECT_STATE.md`
- `CHANGELOG.md`

## Forbidden Changes
- Do not introduce external npm dependencies.
- Do not break snapshot diff invariance across `ir.description`.
- Do not commit or push code to git without verification.

## Gates & Evidence
- Unit tests: `pytest services/agent-engine/tests/test_character_counter_component.py` (18 passed)
- Platform verification: `task verify` (2,699 tests passed)
- Linting: `task lint` (passed)
- Security scan: `task security:quick` (passed)
- Demo build: `task builder:demo -- minimal-blog` (generated 149 files)
