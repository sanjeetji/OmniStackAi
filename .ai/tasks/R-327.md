# Task Contract: R-327

## Objective
Provide a standalone, comprehensive, accessible suite of form control components and input primitives at `apps/web/components/form-controls.tsx` in generated Next.js web applications, supporting WAI-ARIA form semantics, keyboard navigation, focus management, validation states, character counters, clear buttons, size variants, and full composability without external dependencies.

## Acceptance Criteria
- [x] Implement standalone compound form controls component (`apps/web/components/form-controls.tsx`) with client directive (`"use client";`).
- [x] Export subcomponents: `Input`, `Textarea`, `Select`, `Checkbox`, `RadioGroup`, `Radio`, `Label`, `FormField`, `FormMessage`, `FormHelperText`.
- [x] Export types: `InputSize`, `InputProps`, `TextareaProps`, `SelectProps`, `SelectOption`, `CheckboxProps`, `RadioGroupProps`, `RadioProps`, `LabelProps`, `FormFieldProps`, `FormMessageProps`, `FormHelperTextProps`.
- [x] Implement WAI-ARIA semantics: `aria-invalid` on inputs, `aria-describedby` binding helper/error text, `role="alert"` and `aria-live="polite"` on `FormMessage`.
- [x] Implement `RadioGroup` and `Radio` with `role="radiogroup"`, `role="radio"`, `aria-checked`, and keyboard arrow navigation.
- [x] Implement `Input` with size presets (`"sm"`, `"md"`, `"lg"`), optional prefix/suffix slots, and optional clear button (`onClear`).
- [x] Implement `Textarea` with auto/custom rows and optional live character counter (`showCount`, `maxLength`).
- [x] Implement `Select` with options array, placeholder, and error/disabled states.
- [x] Implement `Checkbox` with accessible custom/native indicator, focus ring, and label binding.
- [x] Implement `FormField` compound wrapper coordinating `htmlFor`, `id`, `aria-describedby`, error state, and helper text.
- [x] Zero external npm dependencies, pure React (React 18), TypeScript, and CSS custom properties.
- [x] 100% diff-invariant across changes to `ir.description`.
- [x] Export `render_form_controls_component()` in `omnistackai_agent_engine.codegen` and register `components/form-controls.tsx` in `NextjsWebAdapter.generate()`.
- [x] Comprehensive unit tests in `services/agent-engine/tests/test_form_controls_component.py` passing cleanly (13 tests).
- [x] Verification gates pass: `task verify` (1,232 tests), `task lint`, `task security:quick`, `task env:check`, `task builder:demo`.
- [x] Excel execution tracker updated at row 9 (`Phase_Roadmap` range `A4:M335`, Dashboard formulas incremented to row 335).
- [x] Strictly uncommitted in git (no commits or pushes).
