# Task Specification — R-460: Next.js Codegen End-to-End Route Handler Synthesis & Interactive CRUD Form Submission

## Status: COMPLETED
**Phase:** MVP  
**Tracker Row:** 9  
**Completed Date:** 2026-09-15  
**Assignee:** sanjeetji  
**Dependencies:** R-459  

## Objective
1. Eliminate scaffolded HTTP 501 `not_implemented` route stubs in generated Next.js web applications (`apps/web/app/<api_path>/route.ts`).
2. Synthesize real route handlers that forward requests directly to the FastAPI/Python backend (`BACKEND_INTERNAL_URL`, `NEXT_PUBLIC_API_URL`, or `http://127.0.0.1:8000`).
3. Forward HTTP methods (`GET`, `POST`, `PUT`, `DELETE`), query parameters, authorization headers, and JSON request bodies seamlessly.
4. Provide structured, reliable error handling with HTTP 503 fallback when the backend service is offline.
5. Scope `X-Frame-Options: DENY` to production only in generated `next.config.mjs` so the live Web Studio iframe preview renders without `ERR_BLOCKED_BY_RESPONSE`.
6. Add unit tests in `test_nextjs_routes_crud.py` and ensure full offline `task verify` pass (3,340 tests total).

## Verification Evidence
- `test_nextjs_routes_crud.py`: 3/3 passed.
- `test_nextjs_adapter.py`: 8/8 passed.
- `task verify`: 3,340 tests passed in 41.9s; lint, security, and environment checks all clean.
