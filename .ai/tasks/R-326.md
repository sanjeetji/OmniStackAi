# Task Contract: R-326

## Objective
Provide a robust, accessible, compound Dialog / Modal component at `apps/web/components/dialog.tsx` in generated Next.js web applications, supporting WAI-ARIA modal dialog semantics, keyboard navigation, focus management, backdrop dismiss, size variants, and full composability without external dependencies.

## Acceptance Criteria
- [x] Implement standalone compound Dialog component (`apps/web/components/dialog.tsx`) with client directive (`"use client";`).
- [x] Export subcomponents: `Dialog`, `DialogTrigger`, `DialogPortal`, `DialogOverlay`, `DialogContent`, `DialogHeader`, `DialogTitle`, `DialogDescription`, `DialogBody`, `DialogFooter`, `DialogClose`.
- [x] Export hook: `useDialog()`.
- [x] Export types: `DialogProps`, `DialogTriggerProps`, `DialogOverlayProps`, `DialogContentProps`, `DialogHeaderProps`, `DialogTitleProps`, `DialogDescriptionProps`, `DialogBodyProps`, `DialogFooterProps`, `DialogCloseProps`, `DialogSize`.
- [x] Implement WAI-ARIA modal dialog semantics: `role="dialog"`, `aria-modal="true"`, dynamic `aria-labelledby`, `aria-describedby`.
- [x] Implement keyboard interaction: Escape key dismiss with configurable `closeOnEscape` (default `true`).
- [x] Implement backdrop overlay dismiss with configurable `closeOnBackdropClick` (default `true`).
- [x] Implement focus restoration: returns focus to trigger element when dialog closes.
- [x] Implement body scroll lock: prevents background document scrolling while dialog is open.
- [x] Support size presets: `"sm"` (400px), `"md"` (500px), `"lg"` (640px), `"xl"` (768px), `"full"` (95vw).
- [x] Implement accessible close button with `aria-label="Close dialog"` and SVG icon.
- [x] Zero external npm dependencies, pure React (React 18), TypeScript, and CSS custom properties.
- [x] 100% diff-invariant across changes to `ir.description`.
- [x] Export `render_dialog_component()` in `omnistackai_agent_engine.codegen` and register `components/dialog.tsx` in `NextjsWebAdapter.generate()`.
- [x] Comprehensive unit tests in `services/agent-engine/tests/test_dialog_component.py` passing cleanly (13 tests).
- [x] Verification gates pass: `task verify` (1,219 tests), `task lint`, `task security:quick`, `task env:check`, `task builder:demo`.
- [x] Excel execution tracker updated at row 9 (`Phase_Roadmap` range `A4:M334`, Dashboard formulas incremented to row 334).
- [x] Strictly uncommitted in git (no commits or pushes).
