# R-294 — Generated Next.js App Router Resilience: Error Boundary, Global Error, Not-Found & Loading Fallback

**Status:** done
**Phase:** BASIC/MVP — Founder Stage 0
**Branch:** `main`
**Complexity:** L2 (deterministic codegen)
**Implementation SHA:** `c49bc5c`

## Objective

Give every generated Next.js web app real runtime resilience by emitting the four App Router "special
files" it currently lacks. These are first-class Next.js App Router conventions that the framework wires
automatically:

- `app/error.tsx` — route-segment error boundary (client component) with a `reset()` "Try again" recovery
  and a "Back to overview" link.
- `app/global-error.tsx` — root-layout error boundary (client component) that renders its own
  `<html>`/`<body>` and a `reset()` recovery.
- `app/not-found.tsx` — friendly 404 (server component) with a link back to the overview.
- `app/loading.tsx` — route-level Suspense fallback (server component) that reuses the R-292/293 skeleton
  visual language (gray rounded divs, `#e2e8f0` / `#f1f5f9`, opacity ramp).

All four are static, inline-styled to match the app aesthetic, dependency-free, and never reference
`ir.description` or `ir.name`, so generation stays deterministic and description-only-stable.

## Requirement

- `NextjsWebAdapter.generate()` appends the four files. Templates live as module-level constants
  (`_ERROR_PAGE`, `_GLOBAL_ERROR_PAGE`, `_NOT_FOUND_PAGE`, `_LOADING_PAGE`) with `render_error_page()`,
  `render_global_error_page()`, `render_not_found_page()`, `render_loading_page()` accessors exported from
  `codegen`, mirroring `render_toast_component()`.
- `error.tsx` / `global-error.tsx` start with `"use client"`, type props as
  `{ error: Error & { digest?: string }; reset: () => void }`, log via `useEffect`, and call `reset()`.
- `not-found.tsx` / `loading.tsx` have no `"use client"`; `loading.tsx` maps skeleton blocks.
- No new dependency; existing files, hooks, API client, backend, and IR unchanged; deterministic;
  description-only IR stability preserved; the console-snapshot description-edit diff set is unchanged.

## Acceptance criteria

1. A generated project contains all four `app/*.tsx` files with the correct client/server split.
2. `error.tsx`/`global-error.tsx` wire `reset()`; `global-error.tsx` renders its own `<html>`/`<body>`.
3. `loading.tsx` maps skeleton divs reusing the R-292/293 palette.
4. Byte-identical across `ir.description` changes; both example projects still generate; the
   `test_console_snapshot` description-edit diff set is unchanged.
5. Focused + related tests + full offline gates + both demos pass.

## Model and cost trace

Deterministic; no model call, no network, no DB. local_calls 0 / cloud_calls 0.

## Verification Evidence

- `task verify` — 873 agent-engine tests pass (9 new in `test_app_router_resilience.py`, written
  test-first; no existing assertion changed; the `test_console_snapshot` description-edit diff set is
  unchanged because the four files are static).
- `task lint`, `task security:quick`, `task env:check` — pass.
- `task builder:demo -- minimal-blog` and `-- rideshare-favourites` — pass.
- Generated TypeScript inspected: `app/error.tsx` + `app/global-error.tsx` are `"use client"` with
  `reset()` recovery (global-error renders its own `<html>`/`<body>`); `app/not-found.tsx` is a server 404
  linking to `/`; `app/loading.tsx` is a server Suspense fallback mapping skeleton divs that reuse the
  R-292/293 palette.
- Tracker — R-294 Done at `Phase_Roadmap!A9:M9`; table `A4:M302`; 294 unique IDs (0 dupes); 83 Done,
  1 Deferred, 210 Not Started; MVP 83/189 (43.9%); no `#REF!`; XLSX valid.
- Implementation `c49bc5c`; offline/deterministic; 0 local / 0 cloud model calls; no DB connection.

## Result

**PASS** — every generated app now ships the App Router resilience quartet (error boundary, global error
boundary, not-found, and a route-level loading fallback). Feat commit `c49bc5c`; docs commit records
state/tracker. Next unstarted Tracker ID: **R-295**.
