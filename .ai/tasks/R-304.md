# Standard AI Task Contract: R-304

## Metadata
- **Tracker ID**: R-304
- **Title**: Generated Accessible Confirmation Dialog — Replace window.confirm() with ConfirmDialog Component
- **Milestone**: MVP
- **Category**: Builder / Codegen
- **Status**: Done
- **Author**: sanjeetji <sk698166@gmail.com>

## Objective
Replace crude, blocking `window.confirm()` browser dialogs with an accessible, styled modal confirmation dialog component (`components/confirm-dialog.tsx`) and `useConfirm` hook across all generated Next.js web application screens:
1. **Reusable Modal Confirmation Component (`apps/web/components/confirm-dialog.tsx`)**:
   - `ConfirmDialog` component rendering modal backdrop, dialog container, header title, message body, and action buttons.
   - WAI-ARIA compliance: `role="dialog"`, `aria-modal="true"`, `aria-labelledby`, `aria-describedby`.
   - Focus management: traps Tab/Shift+Tab focus within dialog, focuses Confirm button on open, restores focus on close.
   - Keyboard interaction: closes/cancels dialog on `Escape` key press.
   - Visual variants: supports danger intent (crimson confirm button `#dc2626` for deletes) and neutral/primary intent (`#2563eb`).
   - `useConfirm` hook: returns `confirmAsync(title, message, options)` returning `Promise<boolean>` and `confirmProps` to spread onto `<ConfirmDialog />`.
2. **Collection Screens (`_collection_screen_page`)**:
   - Single item delete handler replaced with `await confirmAsync(...)`.
   - Batch/bulk delete handler replaced with `await confirmAsync(...)`.
   - Subcollection child delete handler replaced with `await confirmAsync(...)`.
   - Conditionally imports `useConfirm` and `ConfirmDialog` when deletable actions exist.
   - Renders `<ConfirmDialog {...confirmProps} />` before closing tag.
3. **Detail Screens (`_detail_screen_page`)**:
   - Record delete handler replaced with `await confirmAsync(...)`.
   - Master-detail subcollection child delete replaced with `await confirmAsync(...)`.
   - Conditionally imports `useConfirm` and `ConfirmDialog` when deletable actions exist.
   - Renders `<ConfirmDialog {...confirmProps} />` before closing tag.
4. **Form Screens (`_form_screen_page`)**:
   - Unsaved changes guard on Cancel button navigation replaced with `await confirmAsync(...)`.
   - Unsaved changes guard on `Escape` key press replaced with `await confirmAsync(...)`.
   - Form Reset button confirmation prompt replaced with `await confirmAsync(...)`.
   - Imports `useConfirm` and `ConfirmDialog`.
   - Renders `<ConfirmDialog {...confirmProps} />` before closing tag.
5. **Adapter Registration**:
   - `components/confirm-dialog.tsx` emitted as a static client component in `NextjsWebAdapter.generate()`.

## Invariance & Constraints
- 100% offline, stdlib-only Python codegen in `services/agent-engine`.
- Zero external runtime npm dependencies; inline styles matching existing system palette.
- Strict diff invariance across `ir.description`.
- Zero network or cloud model calls.
- Preserves all existing delete mutation hooks, form state, and toast feedback.

## Evidence & Verification
- [x] Dedicated unit test suite in `services/agent-engine/tests/test_confirm_dialog.py` passes (31/31 tests).
- [x] All 975 unit tests pass in `task verify` (1.082s).
- [x] `task lint` passed (ruff clean).
- [x] `task security:quick` passed (Stage 0 secret-policy clean).
- [x] `task env:check` passed.
- [x] Both builder demos (`minimal-blog` and `rideshare-favourites`) pass.
- [x] `R_&_D/OmniStackAI_Execution_Tracker_v6.xlsx` updated with R-304 (Row 9, table A4:M312, 0 `#REF!`, 93 Done, MVP 93/199 = 46.7%).
