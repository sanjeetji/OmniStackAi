# R-263: Interactive Screen Component Generator with Real Data Binding (apps/web/app/<screen>/page.tsx)

**Phase**: BASIC/MVP
**Stage**: Founder Stage 0
**Status**: done

## Context and Goal
Upgrade generated Next.js screen pages (`apps/web/app/<screen.id>/page.tsx`) into real, interactive, strongly-typed React client components:
- Connects screens to matching entities and generated `lib/hooks.ts` (`useList`, `useCreate`, etc.).
- Renders live collection views with search input, sort headers, loading/error states, and pagination controls.
- Renders creation forms with schema-derived inputs, validation, and submission handlers.
- Uses `"use client"` directive and standard React without external styling/component libraries.
- Pure codegen change; no new platform dep, no install/build/run/DB.

## Acceptance Criteria
- [x] Screen pages emit `"use client"` directive at top.
- [x] Screen pages import and use appropriate hooks from `../lib/hooks` and types from `../lib/types`.
- [x] Collection screens render live search input bound to `setSearch`.
- [x] Collection screens render pagination controls (Previous, Next, page indicator) bound to `setPage`.
- [x] Form/editor screens render interactive creation form using `useCreate` hook.
- [x] Detail screens or fallback screens render clean, structured UI without broken imports.
- [x] `_screen_page` does NOT reference `ir.description`, keeping generated screens byte-identical across description changes (preserving `test_console_snapshot.py`).
- [x] `task verify` green; 0 network calls (515 tests pass).

## Evidence
- `task verify` passed: 515 agent-engine tests, including 9 new in `test_screen_generation.py`.
- `task lint` passed.
- `task security:quick` passed.
- `task env:check` passed.
- `test_console_snapshot.py` diff invariance confirmed: hunk-level diffs unaffected.
- Zero external dependencies introduced; strictly uses React built-ins (`useState`), Next.js `<Link>`, and generated `lib/hooks.ts` / `lib/types.ts`.

