# R-447: Solution Pack Ecosystem Multi-Surface Cross-App Auth and Unified State Binding

## Standard AI Task Contract

### 1. Task Specification
- **Task ID:** R-447
- **Title:** Solution Pack Ecosystem Multi-Surface Cross-App Auth and Unified State Binding
- **Phase:** MVP
- **Category:** Feature / Engine / Solution Pack / Multi-Surface Ecosystem
- **Dependencies:** R-446 (Solution Pack Ecosystem Studio Live Multi-Surface Preview and Process Orchestration)

### 2. Objectives
1. Implement Ecosystem Cross-App Auth Engine (`solution_packs/ecosystem_auth.py`):
   - `EcosystemAuthContract`: Canonical, byte-stable specification of shared ecosystem JWT parameters (algorithm `HS256`, secret reference, issuer, audience, TTL, and role bindings).
   - `CrossAppAuthMatrix`: Formal role-to-surface and role-to-permission mapping verifying role isolation and preventing privilege leaks.
   - Python 3.13 stdlib-only deterministic JWT token minter & verifier (`mint_ecosystem_token`, `verify_ecosystem_token`) using `hmac`, `hashlib`, `base64`, and `json` (0 external dependencies, 100% offline).
   - Surface-specific test tokens generation (`generate_surface_tokens`) for seamless testing in Studio preview and automated suites.
2. Implement Ecosystem Unified State Binding Engine (`solution_packs/ecosystem_state.py`):
   - `EcosystemStateBinding`: Canonical specification capturing shared entities, authoritative vs reading/writing surfaces, lifecycle state flows, and cross-surface endpoint bindings.
   - `EntityStateFlow` & `StateTransition`: Formal state machines for domain entities with explicit role-gated transition authority.
   - `CrossAppEndpointBinding`: Maps frontend surfaces to shared backend API routes and required roles.
   - `SurfaceEnvBinding`: Standardized environment variable mapping (`NEXT_PUBLIC_API_URL`, `JWT_SECRET`, `DATABASE_URL`, demo auth tokens).
3. Synthesis & Integration:
   - `synthesize_ecosystem_auth(ecosystem_id, surfaces)` and `synthesize_ecosystem_state(ecosystem_id, surfaces)` for deterministic extraction from any ecosystem plan or pack.
   - Extend `EcosystemPackPackage` and `EcosystemPack` with optional `auth_contract` and `state_binding`.
   - Update `EcosystemPackRegistry` to expose auth and state bindings for registered ecosystem packs.
4. Studio Preview & Server Integration:
   - In `StudioPreviewManager`, inject shared `JWT_SECRET`, `DATABASE_URL`, and surface demo tokens into running surface environments.
   - In `studio/server.py`, expose `GET /api/ecosystem/auth` and `GET /api/ecosystem/state`.
   - In `studio/page.py`, render an auth badge with 1-click token copy and role switcher in preview controls (0 external network requests).
5. Ecosystem CLI:
   - Add `auth` and `state` subcommands to `solution_packs/ecosystem_cli.py` (`task agent-engine:solution-pack:ecosystem -- auth|state`).
6. 100% Offline Verification in `task verify` (0 model calls). Python 3.13 stdlib only.

### 3. Verification Gates
- `python3 -m unittest services/agent-engine/tests/test_ecosystem_auth_and_state.py` (16 tests, all passed)
- `task verify` (3,083 tests in 31.7s, all passed; 0 model calls)
- `task lint` (passed)
- `task security:quick` (passed)
- `task env:check` (passed)
- `task builder:demo -- minimal-blog` (passed)
- `task builder:demo -- rideshare-favourites` (passed)

### 4. Status
- Status: Completed
- Date: 2026-09-14
- Tests: 3,083 passed (0 failures, 0 errors)

