# R-402: Accessible Futuristic Reusable Cookie Consent & Preferences Manager Suite (`components/cookie-consent.tsx`)

## Status
DONE

## Objective
Add an accessible, zero-dependency, desktop-and-mobile-grade, futuristic Cookie Consent & Preferences Manager compound component suite (`components/cookie-consent.tsx`): a fixed-position consent banner with a compact view (Accept all / Reject all / Customize) and an expandable per-category preferences view with `role="switch"` toggles (required categories forced on and disabled); `localStorage` persistence (`getItem`/`setItem` under a configurable `storageKey`, wrapped in try/catch) so returning visitors are not re-prompted; SSR-safe mounting (renders `null` until mounted, real storage read only after mount to avoid hydration mismatch); configurable `categories`, title/description, optional privacy policy link, and button labels; `forceShow` override; `onAccept`/`onReject`/`onChange` callbacks; WAI-ARIA semantics (`role="region"` container with `aria-label`, `role="switch"` category toggles with `aria-checked`); 5 placements (`bottom`/`top`/`bottom-left`/`bottom-right`/`center`); 4 futuristic visual styling variants ("default", "card", "glass", "neon"); 3 size scales ("sm", "md", "lg"); React ref forwarding (`forwardRef`); `useImperativeHandle` (`CookieConsentHandle`: `open`/`close`/`accept`/`reject`/`getConsent`/`reset`); and explicit `displayName` across compound and semantic alias exports (`CookieConsent`, `ConsentBanner`, `CookieBanner`, `ConsentManager`, default export). Static module constant — never references `ir.name`/`ir.description`.

## Allowed Paths
- `services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`
- `services/agent-engine/src/omnistackai_agent_engine/codegen/__init__.py`
- `services/agent-engine/tests/test_cookie_consent_component.py`
- `.ai/tasks/R-402.md`
- `.ai/CURRENT_TASK.yaml`
- `.ai/PROJECT_STATE.yaml`
- `.ai/WORK_LOG.md`
- `.ai/HANDOFF.md`
- `PROJECT_STATE.md`
- `CHANGELOG.md`

## Forbidden Changes
- Do not introduce external npm dependencies.
- Do not break snapshot diff invariance across `ir.description`.
- Do not commit or push code to git without verification.

## Gates & Evidence
- Unit tests: `pytest services/agent-engine/tests/test_cookie_consent_component.py` (18 passed)
- Platform verification: `task verify` (2,520 tests passed)
- Linting: `task lint` (passed)
- Security scan: `task security:quick` (passed)
- Demo build: `task builder:demo -- minimal-blog` (generated 139 files)
