# Task R-531: Bazaar (Part 1 of 4) — API, Database, Domain Services & Deterministic Fixtures

## Status: DONE
Branch: `ai/R-531-bazaar-api`
Date: 2026-09-23

## Deliverables Completed
1. **Multi-Vendor Schema Migrations (`migrations/001..005`):**
   - `001_core.sql`: users, user_sessions, user_addresses, shops (KYC statuses, bank info, ratings).
   - `002_catalog.sql`: categories, products, product_variants (SKUs, options, pricing, stocks), inventory_logs.
   - `003_orders.sql`: carts, cart_items, orders, shipments (sub-orders), shipment_items, shipment_tracking_events, order_returns.
   - `004_ledger.sql`: accounts (platform, vendor, shopper), ledger_entries (double-entry accounting), settlement_batches.
   - `005_engagement.sql`: reviews (verified purchase flag), coupons (percentage & flat with caps), notifications, audit_logs.

2. **Domain Service Architecture (`services/api/src/`):**
   - `config.ts`, `db.ts`: PostgreSQL pool and withTransaction helper.
   - `lib/errors.ts`: Domain HTTP exceptions (HttpError, BadRequestError, UnauthorizedError, ForbiddenError, NotFoundError).
   - `lib/money.ts`: Currency formatting and commission splits (basis points).
   - `lib/events.ts`: EventBus broadcasting real-time updates.
   - `lib/shipment-state.ts`: Finite state machine for fulfillment (`placed` -> `accepted` -> `packed` -> `shipped` -> `delivered`, with `cancelled` and `returned`).
   - `auth/password.ts`: Standard scrypt password hashing and constant-time verification.
   - `auth/tokens.ts`: Native HS256 JWT generator and validator.
   - `auth/middleware.ts`: Hono bearer authentication and role verification (`shopper`, `vendor`, `admin`).
   - `providers/`: Mock payments (Card, UPI, COD), mock courier logistics (Delhivery, BlueDart, Shadowfax), and notification dispatcher.
   - `services/catalog.ts`: Category tree, filtered product search, variant matrix, and inventory management.
   - `services/orders.ts`: Shared cart, checkout splitting orders into per-vendor shipments, and inventory reservation.
   - `services/fulfillment.ts`: Vendor shipment stage progression and buyer returns.
   - `services/ledger.ts`: Multi-party double-entry accounting ledger and payout settlement batches.
   - `routes/`: Public storefront & auth (`public.ts`), shopper app (`shopper.ts`), vendor portal (`vendor.ts`), admin console (`admin.ts`), and SSE stream (`stream.ts`).
   - `app.ts` & `index.ts`: Hono server bootstrap on port 4000.

3. **Deterministic Demo Fixtures (`scripts/generate-seed.mjs` & `seed/001_demo.sql`):**
   - 3 Demo users:
     - Shopper: Priya Sharma (`priya@bazaar.test` / `Shopper@2026`)
     - Vendor: Aryan Gupta (`aryan@bazaar.test` / `Vendor@2026`, Craftloom Studio)
     - Admin: Vikram Mehta (`admin@bazaar.test` / `Admin@2026`)
   - 8 Verified shops across categories (Handloom, Terracotta, Brassware, Spices, Fine Silver, Ayurveda, Footwear).
   - 11 Products with 25+ variants, SKU matrix, pricing, and initial inventory logs.
   - Multi-vendor demo orders with independent shipment states, tracking histories, and verified reviews.
   - Double-entry ledger accounts and processed settlement batches.

4. **Unit Tests & Verification:**
   - `test/shipment-state.test.ts`: Lifecycle transitions, cancellations, terminal state enforcement.
   - `test/money-commission.test.ts`: Currency formatting and commission splits.
   - `test/auth.test.ts`: Scrypt hashing, JWT tokens, tampering rejection, expiry.
   - `test/coupons.test.ts`: Discounts, spend thresholds, and maximum caps.
   - `test/workflow.test.ts`: Split-order partitioning and accounting conservation.
   - 19/19 tests passed in 247ms via `node --test test/*.test.ts`.
   - `PYTHONPATH=services/agent-engine/src python3 -m unittest services/agent-engine/tests/test_studio_templates.py`: 40/40 tests passed.
   - `task verify`: 3,983 tests passed offline with 0 model calls.
