schema_version: 1
project: OmniStackAI
phase: BASIC/MVP
founder_stage: 0
current_task: R-470
current_status: done
branch: main
last_completed_task: R-470
# TASK COMPILATION AUDIT — 2026-09-16
# Performed to achieve 100% synchronization across all task-tracking documentation.
# Tracker workbook expanded to 464 tasks with R-464.
# All documentation (PROGRESS.md, RESUME_PROMPT.md, PROJECT_STATE.md, WORK_LOG.md, HANDOFF.md) updated.
task_compilation_audit:
  performed_at: "2026-09-16"
  total_tasks: 464
  done: 252
  deferred: 1
  not_started: 211
  mvp_total: 358
  mvp_done: 252
  mvp_completion_pct: 70.4
  overall_completion_pct: 54.3
  test_suite_count: 3593
  changelog_entries: 253
  missing_entries_backfilled:
    - R-272
    - R-314
    - R-374
    - R-375
    - R-376
  status_corrections:
    - task: R-360
      old_status: IN_PROGRESS
      new_status: DONE
      reason: "Task fully completed on 2026-09-11 with 35 tests. Status left stale during rapid development sprint."
last_completed_task_summary: >-
  Completed R-470 (2026-09-17): real Next.js console-web, wired to R-469's auth API. Phase B of
  R_&_D/OmniStackAI_Commercial_Platform_Kickoff_v1.md - replaced the static, dependency-free
  apps/console-web (plain HTML/CSS/JS reading a committed data/overview.json snapshot) with a real
  Next.js (App Router, TypeScript) app. Register/login/logout wired to R-469's control-plane via
  server-side Route Handlers (app/api/auth/*) that proxy the calls and set/clear an httpOnly
  session cookie - the raw token is never handed to client-side JavaScript, no CORS needed (the
  browser only ever talks to the Next.js origin). An authenticated home page shows the signed-in
  user's profile + credit balance; a /fabric page carries the old model/cost overview dashboard
  forward on the exact same data/overview.json data contract, now a React Server Component instead
  of hand-written DOM JS. No new UI dependency beyond React/Next.js itself (no Tailwind/component
  library) - the console's existing CSS design tokens carry forward into app/globals.css. New env
  var OMNISTACKAI_CONTROL_PLANE_URL (server-side only). Old index.html/app.js/styles.css removed.
  scripts/console.sh rewritten around next dev/build/lint/start; scripts/verify.sh gained console
  lint/typecheck/build steps (next build succeeds with no live control-plane - Route Handlers are
  compiled, not executed, at build time).
  Real compatibility issues found and fixed while implementing (full detail in
  .ai/tasks/R-470.md "Live discovery"): pnpm's default fetch timeout was too short for this
  network's throughput on large packages (fixed with a new root .npmrc); typescript@7.0.2 (npm's
  actual current latest) is not yet supported by typescript-eslint (pinned to typescript@6.0.3,
  matching eslint-config-next's own tested version); the documented FlatCompat eslint.config.mjs
  pattern crashed on a circular-reference bug inside @eslint/eslintrc's error formatter (fixed by
  importing eslint-config-next's own native flat-config export directly, dropping
  @eslint/eslintrc entirely); pnpm 11.19 stopped reading package.json's pnpm.* fields (moved
  onlyBuiltDependencies/allowBuilds to pnpm-workspace.yaml). Most importantly: the session cookie's
  Secure flag was originally derived from NODE_ENV (always "production" under next start regardless
  of the real protocol) rather than the actual request protocol - this would have silently broken
  login for anyone using the app locally over plain HTTP in a real browser (curl does not enforce
  the Secure attribute the way browsers do, so the first smoke test's 200/204s masked it; caught by
  inspecting the raw Set-Cookie header instead of trusting status codes alone). Fixed with a
  per-request isSecureRequest() helper; the full live smoke test was re-run afterward and confirmed
  correct (cookie issued without Secure over plain HTTP, verified in curl's own cookie-jar file).
  Gates: apps/console-web typecheck/lint/build all clean; task verify - Ran 3593 tests in 63.552s,
  OK, Stage 0 verification passed; live smoke against a real running Next.js server + the real
  R-469 control-plane container proved register(201, no token leaked) -> duplicate(409) ->
  home-authenticated(200, real email shown) -> fabric(200) -> home-no-cookie(307 to /login) ->
  logout(204) -> home-after-logout(307 to /login, real server-side invalidation) -> login(200, new
  cookie) end to end.
  NEXT R-471 (Phase C): bridge the control-plane's Job API to the unmodified agent-engine so a real
  generation call actually debits a user's credits (local Ollama stays credit-exempt).

_prior_last_completed_task_summary_R469: >-
  Completed R-469 (2026-09-17): control-plane foundation - users, auth, plans, credits. Phase A of
  R_&_D/OmniStackAI_Commercial_Platform_Kickoff_v1.md, the founder-approved decision (2026-09-17)
  to advance from the Stage-0 static console/stdlib Studio prototype toward the real commercial
  platform Implementation Brief Section 33 always specified. Gave the existing services/
  control-plane Go skeleton (health-check-only before this task) real users, authentication, and a
  plan/credit model: two roles only (super_admin, user - a user's access is gated entirely by plan,
  never a second role tier); plans reuse the Brief Section 22 tier names (free/developer/pro/
  agency/enterprise, byok as an add-on flag); every signup gets the free plan plus a starting
  credit grant recorded in an append-only credit_ledger. New migration 000002_users_auth_billing
  (users, credit_ledger, sessions) plus a small embedded migration runner (migrations package,
  go:embed *.up.sql) that replays every migration in order on every boot - safe because every
  migration file is idempotent, and necessary because docker-entrypoint-initdb.d only runs against
  a brand-new empty Postgres volume, never an existing one. Password hashing is PBKDF2-HMAC-SHA256
  implemented directly on crypto/hmac+crypto/sha256+crypto/subtle - zero new go.mod dependency
  (golang.org/x/crypto/bcrypt was considered and rejected for that reason), verified mechanically
  by a new scripts/test.sh check that go.mod's direct-dependency count stays exactly 1 (pgx).
  Session tokens are opaque crypto/rand bearer tokens; only their SHA-256 hash is ever persisted.
  Four new endpoints (POST /auth/register, /auth/login, /auth/logout, GET /auth/me) mounted
  alongside the existing health routes on one shared http.ServeMux (health.NewHandler became
  health.Register). Login failure is a generic 401 regardless of wrong-password vs. unknown-email,
  including a same-cost dummy-hash Verify call on the "not found" path - no email-enumeration
  timing leak, proven both in fake-store unit tests and against the real running container.
  internal/users' real PostgreSQL-backed Store is deliberately not unit-tested against a live
  database in go test (keeps control-plane:test exactly as hermetic as it already was, matching
  how internal/health tests a fake Pinger) - instead proven by a real Docker Compose + curl smoke
  test: register -> duplicate-register(409) -> login -> wrong-password(401) -> unknown-email(401,
  byte-identical error body) -> me(200) -> me-with-no-token(401) -> logout(204) ->
  me-after-logout(401, real session deletion) -> logout-again(204, idempotent). No frontend, no
  agent-engine bridge, no payment processor, no team/org model - all named as separate later
  Tracker IDs (Phases B/C/D/E) in the kickoff doc, not attempted here.
  Gates: go test (migrations 4, password 8, auth 15, health 3, config 2/9 subtests, all passing);
  control-plane lint/build passed; scripts/test.sh's new R-469 contract block passed; task verify
  (full pipeline, 3593 agent-engine tests, 62.4s, 0 model/network calls, no slowdown) passed;
  live Docker Compose smoke test passed end to end as above.
  NEXT R-470 (Phase B): replace the static apps/console-web with a real Next.js (App Router,
  TypeScript) app wired to this task's four auth endpoints - the point where task bootstrap/
  task doctor deliberately gain a real Node/npm toolchain requirement for the console.

_prior_last_completed_task_summary_R468: >-
  Completed R-468 (2026-09-17): Multi-turn chat / "continue editing this app" in the Studio. Every
  /api/build call used to be a fresh, stateless one-shot prompt with no way to say "now add a favorites
  feature" and have it land as a further commit on the same repo. New intake/app_delta.py is a generic
  (non-pack-coupled) sibling of solution_packs/ai_delta.py: AppDeltaProposal (entities/apis/screens/
  rationale, bounded+unique-validated), build_app_delta_messages (lists the app's REAL existing entities/
  apis/screens/roles as context, strict JSON-only, no-collision instruction), parse_app_delta_proposal
  (strict untrusted-JSON parsing, rejects any collision with the base IR at parse time - including a screen
  role that isn't one of the app's real roles, a gap found and fixed while implementing, see below),
  apply_app_delta (independent defense-in-depth collision re-check, then dataclasses.replace concatenation
  - base_ir.entities + proposal.entities etc., mirroring solution_packs/application.py's merge exactly -
  backstopped by ApplicationIR.__post_init__ and the same validate_ir/has_errors final check), and
  generate_app_delta_proposal (a bounded validate->feedback->retry loop, retry only on a validation
  rejection never on a provider exception, mirroring R-465's _synthesize_file idiom). The diff/commit half
  needed ZERO changes: edit/diff.py::plan_edit and edit/apply.py::commit_edit (proven end-to-end already by
  test_edit_loop.py) are reused exactly as-is. New studio/session.py's StudioSessionStore is a small,
  bounded (LRU-by-last-touched), in-memory, server-only store (same idiom as StudioBuildHistory) keyed by
  the same build id, holding the app's CURRENT ApplicationIR (never sent to the browser) and a bounded turn
  history; live_serve.py's _build starts a session only for the two build kinds with exactly one IR -
  plain-prompt and single-surface Ecosystem Pack builds. New POST /api/build/{id}/edit and
  GET /api/build/{id}/turns routes (server.py, via the R-467 _build_id_for_suffix pattern), wired
  unconditionally (no toolchain needed - just git and a model). studio/history.py gained .update() to
  refresh file_count/commit_sha/entities on an existing "Recent builds" entry in place after an edit.
  page.py: a small "Continue editing this app" chat box (turn list + input + Apply button) under the file
  browser; a successful edit re-renders entities/file-count/commit and refreshes the file browser; zero
  external assets preserved. v1 scope: ADDITIVE changes only (new entities/apis/screens; renaming/removing
  existing structure is rejected, never merged); Solution Pack builds (they already have their own AI-delta
  mechanism at build time) and "all surfaces" Ecosystem builds (no single IR) get an honest
  EditNotSupportedError (400) rather than being silently ignored. No changes to edit/, git_service/, or
  application_ir/ - every primitive reused exactly as-is.
  LIVE DISCOVERY while implementing (via the end-to-end test suite, not a live run): neither this new module
  nor solution_packs/ai_delta.py (the pattern it mirrors) validated that a proposed screen's role refers to
  one of the base IR's ACTUAL declared roles - only checked it was well-formed lower_snake_case.
  ApplicationIR.__post_init__ does catch an invalid role as a last resort (it did, in the first failing test
  run: minimal-blog-ecosystem's "author-studio" surface only declares an "author" role, unlike minimal-blog's
  author+reader), but only AFTER generate_app_delta_proposal had already returned successfully - too late
  for the retry-with-feedback loop to use it. Fixed: an explicit role-existence check added to
  parse_app_delta_proposal (same layer as every other base-IR collision check, gets the retry benefit) and
  to apply_app_delta (defense in depth, matching the existing pattern), plus the app's real roles are now
  listed in the prompt so the model has the information to avoid the mistake. New tests cover both the
  parse-time rejection and the merge-time defense-in-depth check.
  Gates: focused 214 passed; task verify 3,593 OK (0 model/network calls; 63.8s, no slowdown vs before -
  confirms no hidden calls were introduced); lint/security/env green; demos clean (359/353 files). Manual
  smoke (opt-in, real HTTP, build-only mode, no live model call - today's Groq daily quota was already
  exhausted per R-466/R-467): healthz ok; unknown-build turns -> {"turns": []} (200, honest empty view);
  unknown-build edit -> 404; a build id with a slash -> 404 (not matched); page HTML contains "Apply change".
  The actual delta->merge->diff->real-second-commit round trip (the core of this feature) is proven by
  test_studio_edit.py against real temporary git repos: a real SECOND git commit lands with the new entity's
  files actually present in the working tree; a second edit stacks a THIRD commit and keeps the first edit's
  entity; a collision is rejected with zero commits made and state unchanged; StudioBuildHistory's entry is
  updated in place (not appended); a provider exception is never retried and makes no commit; both excluded
  build kinds return the honest EditNotSupportedError. Previously:
  Completed R-467 (2026-09-17): Studio File Browser + Hybrid UI Toggle - the first slice of the hybrid
  engine reaching the actual product UI (the Studio a user opens in a browser, studio/server.py +
  live_serve.py + page.py - confirmed by direct reading to be a dependency-free stdlib http.server app with
  one server-rendered HTML+vanilla-JS page; no session/chat concept exists there at all). New pure
  studio/files.py (list_build_files, read_build_file - path safety mirrors edit/apply.py's
  _safe_destination; excludes .git/node_modules/__pycache__/.next/.venv and real .env* files, keeps
  .env.example) backs new GET /api/build/{id}/files and GET /api/build/{id}/file?path= routes in server.py
  (_build_id_for_suffix path matching; BuildNotFoundError->404, PathOutsideBuildError->400,
  FileNotFoundInBuildError->404), resolved via live_serve.py's _resolve_build_dir/_list_build_files/
  _read_build_file against StudioBuildHistory's recorded target_dir, wired unconditionally (build-only mode
  too - no toolchain needed). /api/build gains a hybrid_ui boolean: threaded into the plain-prompt path
  (build_app_from_prompt, always active - a provider is already mandatory there) and the Ecosystem Pack path
  (build_app_from_ir per surface, active only when a provider actually resolved - honestly reported via
  hybrid_ui_active) as synthesize_screens=True + a ui_outcomes sink; app_build_result_to_dict gained an
  optional ui_outcomes param (additive, byte-identical when omitted); StudioBuildHistory.record captures
  hybrid_ui_requested/active/ui_outcomes (bounded). The Solution Pack path has no such kwarg on
  build_solution_pack_project - hybrid_ui there reports hybrid_ui_active: false honestly, never silently
  ignored. page.py: the flat inert file list became a clickable two-pane browser (list + read-only viewer,
  fetches /api/build/{id}/file?path=, shows binary/truncated notices), a "Hybrid UI (experimental)" checkbox
  on the build form, and a one-line hybrid summary with a per-file robot badge - still zero external assets
  (verified: no http://, https://, src=, <link). Fixed a missing ApplicationIR import (latent NameError) in
  live_serve's ecosystem single-surface branch (custom_name/custom_description override path).
  LIVE DISCOVERY while implementing: with the founder's real Groq credentials now sitting in the repo's
  gitignored .env (added during the R-465/R-466 live proofs), running the pre-existing, UNMODIFIED Studio
  test suite made real outbound calls to Groq - pytest took minutes instead of seconds. Four pre-existing
  live_serve._build() test call sites never mocked resolve_generation_provider_from_env: the two already
  suspected in test_studio_ecosystem.py, PLUS two more found here in test_studio_server.py
  (test_live_serve_build_with_solution_pack, test_live_serve_build_with_ai_delta_features) - because ANY
  non-None provider reaching build_app_from_ir/build_solution_pack_project triggers R-462's overview-page
  LLM synthesis regardless of hybrid_ui. Timed precisely: resolve_generation_provider_from_env() alone takes
  0.02s; the unmocked ai-delta test took 23.5s of real, R-466-paced Groq traffic (413 too-large -> compact
  grounding retry -> validator rejection -> 429 rate-limited -> give-up) before falling back. This was a
  LIVE, ACTIVE violation of the standing "0 model/network calls under task verify" constraint on this
  machine today, not theoretical. All four now patch resolve_generation_provider_from_env to
  (None, "stub-model", 4096, 5.0). Gates: focused 144 passed; task verify 3,529 OK (0 model calls; full
  local suite 3,529 passed + 42 subtests in 68.80s, materially faster post-fix, confirming no hidden network
  calls remain); lint/security/env green; demos clean (359/353 files). Manual smoke (opt-in, real HTTP,
  build-only mode, no hybrid_ui to avoid today's exhausted Groq daily quota): a real 160-file build via
  POST /api/build; GET /api/build/1/files listed 160 real paths; GET /api/build/1/file?path=... returned
  real file content; unknown build id -> 404; a ../../../etc/passwd traversal attempt -> 400 "path escapes
  the build directory"; missing path query -> 400; server stopped cleanly. hybrid_ui end-to-end threading
  (synthesize_screens actually firing, a real page.tsx on disk carrying the LLM-Synthesized marker) was
  proven by stub-provider tests rather than a second live Groq call, since today's daily quota was already
  spent per R-466. Previously:
  Completed R-466 (2026-09-17): Compile-Level Repair for LLM-Written UI + Rate-Limit-Aware Pacing - the second
  brick of the hybrid engine, driven by what the live runs proved. model_gateway: ProviderHTTPError carries
  status_code + retry_after_seconds; new ProviderRateLimitedError (429); parse_retry_after (Retry-After header
  seconds/HTTP-date, else the body's "try again in Ns"); the cloud adapter's generate() waits the hint (+0.5s;
  2s/4s backoff without one) and re-sends the SAME request, bounded by OMNISTACKAI_RATE_LIMIT_RETRIES (2) and
  OMNISTACKAI_MAX_RETRY_AFTER_SECONDS (60; a longer ask fails fast), every wait/give-up logged, injectable sleep,
  knobs read by the bootstrap and documented in .env.example. verify/compile.py: CompileError/CompileReport,
  parse_tsc_output (`tsc --noEmit --pretty false`), compile_web_project (injectable runner), ensure_web_dependencies
  (present/linked/installed). codegen/hybrid_repair.py: llm_file_specs (the exact R-465 prompts + template
  fallbacks + compact prompts), compile_errors_message, repair_compiled_files (compiler errors through
  _repair_message; validator-gated; template fallback; UiSynthesisOutcome per file; deterministic files never
  rewritten), build_repair_diff (ProjectDiff of MODIFIED apps/web paths), compile_and_repair(_sync) (compile ->
  repair -> apply -> recompile for max_rounds, reverting still-failing LLM files on the last round). llm_ui:
  _Transcript shrinks on a "request too large" rejection (413, or a 400 that says so) - drop the echo, then
  switch to compact_grounding(ir) (every hook signature, one export per component; prompts <= 14k chars) -
  threaded through the synthesis entry points and the adapter (only with a provider; default bytes unchanged);
  outcome last_reason carries the HTTP status (ProviderHTTPError(413)), never the body. CLI Step 3/3: link or
  install node_modules, compile, repair, revert, commit the repair as the customer, print rounds and verdict.
  Tests: test_cloud_rate_limit (15), test_compile_report (11), test_hybrid_repair (12), test_llm_ui_compact (10);
  task verify 3,490 OK; lint/security/env green; demos clean; web-typecheck PASSED x2. Live (Groq free tier):
  the 3-step CLI ran end-to-end in 13s with every UI call failing on a non-429 status and the fallback repo
  compiling at 0 errors; a direct probe with the full grounded prompt succeeded on an empty window; a
  reproduction showed the 429 path working (Retry-After 112 honoured) but capped at 60s - the limiter was
  tokens per DAY (200k, 191k used), so the daily budget was spent. Pacing + status outcomes are live-verified;
  compile repair with real tsc errors and shrink-on-413 are stub-verified only. Previously:
  Completed R-465 (2026-09-17): Grounded Hybrid UI Synthesis - the first brick of the founder-approved HYBRID
  engine (LLM writes the modern UI over the deterministic typed data layer, with a bounded repair loop and a
  template fallback; model calls opt-in, never in task verify). codegen/nextjs.py gained summarize_data_layer
  (parses the REAL generated types/hooks/api so the prompt cannot drift - the R-462 seed hallucinated refresh(),
  page/pageSize params and missed use<Entity>/useUpdate/useDelete/useList<Child>By<Rel>), _component_files +
  summarize_components (the real ~110 component files with real export names; auth-provider only when
  needs_auth), and summarize_design_tokens (real --color-*/--space-*/... names; the hardcode-hex rule is gone).
  codegen/llm_ui.py was rewritten around one _synthesize_file core: validator rejection feeds the reason back
  (ASSISTANT echo capped 8k chars + corrective USER turn), up to 3 attempts, retry ONLY on validator rejection
  and NEVER on exceptions, then the deterministic template with no marker; a JSON-safe secret-free
  UiSynthesisOutcome per file; import hardening (multi-line statements, exact react/react-dom, react-* /
  require / dynamic import rejected). The never-set OMNISTACKAI_SYNTHESIZE_ALL_SCREENS env gate is replaced by an
  explicit synthesize_screens flag threaded generate -> assemble_project -> build_app_from_ir/prompt (default
  False; default output byte-identical; import os removed). Opt-in CLI `task agent-engine:ui:synthesize`
  (intake/ui_synthesize_run.py; provider via OMNISTACKAI_CLOUD_PROVIDER; clean bounded exit on provider errors).
  tests/test_llm_ui_grounding.py (15 tests: grounding, no-hallucination, drift guard, tokens, components,
  repair success/exhaustion/exception, switch, byte-identical default, import hardening, threading); the R-462
  test IR gained real APIs and the drifted page?/pageSize? assertions became the real limit?/offset?. Gates:
  task verify 3,442 OK; lint/security/env green; demos 157/153; web-typecheck PASSED for both examples. Live
  proof (founder's Groq key, free on_demand tier, 8,000 TPM, one accessible model openai/gpt-oss-120b): the
  model returned a page, the validator rejected it with an exact reason (truncated at max_output 4096), the
  repair loop engaged, the repair call was rate-limited (429), and the engine fell back gracefully with a truthful
  outcome record; a full LLM-page-compiles proof needs >8k TPM (GOOGLE_API_KEY / Groq Dev tier). docs/HYBRID_UI.md
  documents the approach. Previously: Completed R-464: Full-Stack Platform Feature Completeness across 4 phases:
  Phase 1: Verified frontend search, pagination & filter UI controls across collection screens, and updated LLM prompt synthesis with complete hook signatures (page, pageSize, totalPages, params, setSearch, setPage, setPageSize, setSort, setFilter, clearFilters, refetch).
  Phase 2: Audit timestamps (created_at, updated_at) with automatic PostgreSQL set_updated_at() trigger on all entity tables, Go and Python models, TypeScript interfaces, and record detail footer.
  Phase 3: RBAC / Row Ownership (created_by) conditional on needs_auth(ir) across PostgreSQL schema (FK to users table), data access layer, require_owner (Python) and RequireOwner (Go) middleware, TypeScript interfaces, and ownerOnly list hook support.
  Phase 4: S3-compatible file uploads with FieldType.ATTACHMENT in IR, schema mapping (TEXT), Go/Python/TypeScript types, and STORAGE_* environment contracts in .env.example.
  14 unit tests in test_platform_feature_completeness.py; 3,427 tests passing across the repository.
  Previously built R-460: Next.js Codegen End-to-End Route Handler Synthesis and Interactive CRUD Form Submission.
  Previously built R-459: Solution Pack Ecosystem Multi-Surface Documentation, Architecture Runbooks, and OpenAPI Aggregator Contracts
  (thirtieth brick of the differentiating spine and twenty-sixth Solution Pack task). Implemented ecosystem_docs.py
  with canonical DocPage, RunbookStep, ArchitectureRunbook, OpenAPIRoute, OpenAPIAggregationEntry, AggregatedAPISpec,
  and EcosystemDocsContract with full to_dict/from_dict/to_json/from_json roundtrips and deterministic SHA-256 digest();
  synthesize_ecosystem_docs() derives surface-specific architecture documentation pages, operational runbooks (local dev,
  production deployment, incident triage), and aggregated OpenAPI 3.1 specifications offline (0 model calls);
  in-process thread-safe EcosystemDocsEngine provides unified Markdown documentation bundle rendering, keyword and tag search,
  OpenAPI aggregation with route collision detection, and export dry-run simulations (markdown, json, openapi_bundle, runbook_checklist);
  updated EcosystemPackPackage in ecosystem_pack.py with docs_contract field, to_dict/parse/synthesize integration with whole-package checksum integrity;
  updated EcosystemPack in ecosystem_registry.py with docs_contract property and has_docs_contract flag;
  added get_docs_contract(ecosystem_id, version) to EcosystemPackRegistry and delegate proxy;
  updated StudioPreviewManager in studio/preview.py to synthesize and cache docs_contract and engine in replace_ecosystem(),
  inject has_docs, page_count, runbook_count, api_endpoint_count, docs_status into preview status payloads, and expose
  get_ecosystem_docs() and export_ecosystem_docs(); added GET /api/ecosystem/docs and POST /api/ecosystem/docs/export to studio/server.py
  and wired in studio/live_serve.py; updated Studio page.py with sky-blue/amber-themed #preview-docs-info panel (CSS, HTML,
  JS update logic, Export Docs and Refresh button handlers, strictly 0 external network requests); added docs subcommand to
  ecosystem_cli.py supporting formatted text, --json, --search, and --export; exported all docs symbols in solution_packs/__init__.py;
  twenty new tests in test_ecosystem_docs.py; 3,333 tests passing offline in task verify.
  Previously built R-458: Solution Pack Ecosystem Multi-Surface Governance, Compliance Policy, and Audit Evidence Contracts
  Previously built R-457: Solution Pack Ecosystem Multi-Surface SLA, SLO, and Error Budget Contracts
  Previously built R-456: Solution Pack Ecosystem Multi-Surface Alerting, Incident Runbooks, and Escalation Policies
  Previously built R-455: Solution Pack Ecosystem Multi-Surface Capacity Planning, Resource Quotas, and Unit Economics Budgeting
  Previously built R-454: Solution Pack Ecosystem Multi-Surface Disaster Recovery, Snapshot Backup, and Rollback Orchestration
  (twenty-fifth brick of the differentiating spine and twenty-first Solution Pack task). Implemented ecosystem_recovery.py
  with canonical BackupTarget (target_id, surface_slug, target_kind, storage_uri, frequency, retention_days,
  encryption_required, tags), SnapshotManifest (snapshot_id, ecosystem_id, surface_slug, created_at_utc, checksum_sha256,
  size_bytes, metadata), RecoveryStep (step_id, sequence_order, surface_slug, action, target, timeout_seconds, critical,
  description), RollbackTrigger (trigger_id, condition, threshold, action, severity), and EcosystemDisasterRecoveryContract
  (ecosystem_id, version, backup_targets, recovery_steps, rollback_triggers) with full to_dict/from_dict/to_json/from_json
  roundtrips and deterministic SHA-256 digest(); synthesize_ecosystem_recovery() derives backup targets (database, state,
  configuration), ordered sequential recovery plan steps with traffic draining, data restoration, schema verification,
  service restart, and live cutover, and cross-surface rollback triggers (health_probe_failed, migration_error, timeout_exceeded);
  EcosystemRecoveryEngine is thread-safe (threading.Lock) and executes simulate_snapshot_creation(), simulate_recovery_plan(),
  simulate_rollback_triggers(), and simulate_full_dr_exercise() dry-run returning structured pass/fail summaries; updated
  EcosystemPackPackage in ecosystem_pack.py with recovery_contract field, to_dict/parse/synthesize integration; updated
  EcosystemPack in ecosystem_registry.py with recovery_contract property and has_recovery_contract flag in to_dict; added
  get_recovery_contract(ecosystem_id, version) to EcosystemPackRegistry and delegate proxy; updated StudioPreviewManager
  in studio/preview.py to synthesize and cache recovery_contract and engine in replace_ecosystem(), inject has_recovery,
  backup_target_count, recovery_step_count, rollback_trigger_count, dr_status into all three payload branches, and expose
  get_ecosystem_recovery() and simulate_ecosystem_recovery(); added GET /api/ecosystem/recovery and POST /api/ecosystem/recovery/simulate
  to studio/server.py and wired in studio/live_serve.py; updated Studio page.py with amber-themed #preview-recovery-info panel
  (CSS, HTML, JS update logic, Simulate DR and Refresh button handlers); added recovery subcommand to ecosystem_cli.py supporting
  --json and --simulate with structured human-readable output; exported all recovery symbols in solution_packs/__init__.py;
  twenty-one new focused tests in test_ecosystem_recovery.py covering model roundtrips, synthesis derivation, engine thread-safety,
  pack bundling integrity, registry lookup, HTTP endpoint simulation, and CLI recovery subcommand. Task verify: 3,223 passed
  fully offline (+21 net-new tests); lint, security, env, and both builder demos pass; 0 model calls.
  Previously built R-452: Solution Pack Ecosystem Multi-Surface CI/CD Workflow & GitHub Actions Orchestration (twenty-third brick
  of the differentiating spine and nineteenth Solution Pack task). Implemented Ecosystem Multi-Surface CI/CD and GitHub
  Actions Orchestration in solution_packs/ecosystem_cicd.py with canonical CIJobStep, CIJob, CIWorkflow, EcosystemCICDContract,
  deterministic Python 3.13 stdlib-only GitHub Actions YAML generation (generate_github_actions_workflow and to_workflow_yaml)
  with zero external dependencies (no PyYAML), in-process DAG dependency validation (Kahn's algorithm cycle detection) and
  deterministic dry-run pipeline simulation (EcosystemCICDEngine), and deterministic contract synthesis (synthesize_ecosystem_cicd)
  deriving surface verification jobs (Node.js/pnpm for web/admin, Python/pip + PostgreSQL service for FastAPI, Go + PostgreSQL for
  Go backends) and overarching ecosystem-integration verification job; updated EcosystemPackPackage, to_dict, parse_ecosystem_pack_package,
  and synthesize_ecosystem_pack in solution_packs/ecosystem_pack.py to serialize, parse, and verify CI/CD contracts with whole-package
  SHA-256 integrity; updated EcosystemPackRegistry and EcosystemPack in solution_packs/ecosystem_registry.py with cicd_contract and
  get_cicd_contract(); exported all CI/CD types and helper functions in solution_packs/__init__.py; updated StudioPreviewManager in
  studio/preview.py to track CI/CD contracts and simulation engines, injecting has_cicd, cicd_workflow_count, cicd_job_count, and
  cicd_status into preview status payloads, and exposing get_ecosystem_cicd(), to_workflow_yaml(), and simulate_cicd_run(); added
  GET /api/ecosystem/cicd, GET /api/ecosystem/cicd/yaml, and POST /api/ecosystem/cicd/simulate to Studio HTTP server in studio/server.py
  and wired handlers in studio/live_serve.py; updated Studio Web UI in studio/page.py with #preview-cicd-info container displaying workflow
  triggers, job chips, and 1-click "Copy GitHub Actions YAML", "Simulate Pipeline", and "Refresh CI/CD" buttons, strictly maintaining
  zero external network requests; added 'cicd' subcommand to ecosystem_cli.py supporting both file paths and registered ecosystem IDs
  with formatted text, --yaml, --json, and --simulate modes; updated Taskfile.yml and scripts/agent-engine.sh. Fifteen new focused tests
  in test_ecosystem_cicd.py; task verify: 3,173 passed fully offline (+15 net-new tests); lint, security, env, and both builder demos
  (152/149 files) pass; 0 model calls in test execution. No dependency, pack baseline/selection, Application IR schema/example, generator,
  generated output, provider, database, infrastructure, workbook, or .claude change.
  Previously built R-450: Solution Pack Ecosystem Multi-Surface Export, Deployment Manifest, and Live Gateway Orchestration (twenty-first brick
  of the differentiating spine and seventeenth Solution Pack task). Implemented Ecosystem Multi-Surface Deployment Manifest and Live
  Gateway Engine in solution_packs/ecosystem_deployment.py with canonical GatewayRoute, SurfaceDeploymentSpec, EcosystemDeploymentManifest,
  deterministic Python 3.13 stdlib-only Docker Compose YAML generation (to_compose_yaml()) with zero external dependencies, thread-safe
  in-process HTTP reverse-proxy EcosystemLiveGateway routing requests via longest-prefix matching with hop-by-hop header strip and
  forwarding headers injection, and deterministic deployment synthesis (synthesize_ecosystem_deployment) deriving non-colliding host
  ports, routes, and environment bindings across surfaces and PostgreSQL; updated EcosystemPackPackage, to_dict, parse_ecosystem_pack_package,
  and synthesize_ecosystem_pack in solution_packs/ecosystem_pack.py to serialize, parse, and verify deployment manifests with checksum
  integrity; updated EcosystemPackRegistry and EcosystemPack in solution_packs/ecosystem_registry.py with deployment_manifest and
  get_deployment_manifest(); exported symbols in solution_packs/__init__.py; updated StudioPreviewManager in studio/preview.py to track
  deployment manifests and live gateway instances, injecting has_deployment, deployment_surface_count, gateway_routes, gateway_port,
  and gateway_url into preview status payloads, and exposing get_ecosystem_deployment() and to_compose_yaml(); added GET /api/ecosystem/deployment
  and GET /api/ecosystem/deployment/compose to Studio HTTP server in studio/server.py and wired handlers in studio/live_serve.py; updated
  Studio Web UI in studio/page.py with #preview-deployment-info container displaying surface counts, route chips, and 1-click "Copy Compose YAML"
  / "Refresh Deployment" buttons, strictly maintaining zero external network requests; added 'deploy' subcommand to ecosystem_cli.py
  supporting both file paths and registered ecosystem IDs with text, --json, and --compose formatting; updated Taskfile.yml and
  scripts/agent-engine.sh. Thirteen new focused tests in test_ecosystem_deployment.py; task verify: 3,144 passed fully offline (+13 net-new tests);
  lint, security, env, and both builder demos (152/149 files) pass; 0 model calls in test execution. No dependency, pack baseline/selection,
  Application IR schema/example, generator, generated output, provider, database, infrastructure, workbook, or .claude change.
  Previously built R-449: Solution Pack Ecosystem Cross-Surface Telemetry, Audit Trails, and Distributed Tracing (twentieth brick of the
  differentiating spine and sixteenth Solution Pack task). Implemented Ecosystem Cross-Surface Telemetry, Audit Trails, and
  Distributed Tracing Engine in solution_packs/ecosystem_telemetry.py with canonical TelemetrySpan, AuditTrailEntry,
  DistributedTrace, TelemetrySamplingPolicy, TracedSurface, EcosystemTelemetryContract.
  EcosystemAuthContract, CrossAppAuthMatrix, Python 3.13 stdlib-only deterministic HS256 JWT token minter
  and verifier (mint_ecosystem_token, verify_ecosystem_token) with zero external dependencies, and demo
  token generation (generate_surface_tokens); implemented Unified State Binding Engine in
  solution_packs/ecosystem_state.py with SharedEntityBinding (authoritative/reading/writing surfaces),
  EntityStateFlow and StateTransition (formal entity lifecycle state machines with role-gated transition
  validation), CrossAppEndpointBinding, SurfaceEnvBinding, and EcosystemStateBinding; updated EcosystemPackPackage,
  parse_ecosystem_pack_package, and synthesize_ecosystem_pack in solution_packs/ecosystem_pack.py to serialize,
  parse, and verify auth contracts and state bindings with checksum integrity; updated EcosystemPackRegistry
  and EcosystemPack in solution_packs/ecosystem_registry.py with get_auth_contract and get_state_binding;
  updated StudioPreviewManager in studio/preview.py to track auth contracts and state bindings, generate surface
  demo tokens, inject active_role and active_token into live preview payloads, and expose get_ecosystem_auth()
  and get_ecosystem_state(); added GET /api/ecosystem/auth and GET /api/ecosystem/state to Studio HTTP server in
  studio/server.py and wired handlers in studio/live_serve.py; updated Studio Web UI in studio/page.py with
  #preview-auth-info container displaying active role badge and 1-click "Copy Demo JWT" button, strictly
  maintaining zero external network requests; added 'auth' and 'state' subcommands to ecosystem_cli.py supporting
  both file paths and registered ecosystem IDs with text and --json formatting; updated Taskfile.yml and
  scripts/agent-engine.sh. Sixteen new focused tests in test_ecosystem_auth_and_state.py; task verify: 3,083 passed
  fully offline (+16 net-new tests); lint, security, env, and both builder demos (152/149 files) pass; 0 model
  calls in test execution. No dependency, pack baseline/selection, Application IR schema/example, generator,
  generated output, provider, database, infrastructure, workbook, or .claude change.
  Previously built R-445: Solution Pack ecosystem pack registry integration, catalog discovery, and Studio
  multi-surface selection (sixteenth brick of the differentiating spine and twelfth Solution Pack
  task). Defined EcosystemPack, EcosystemPackRecommendation, and an immutable EcosystemPackRegistry
  in solution_packs/ecosystem_registry.py; pre-registered built-in ecosystem baselines (minimal-blog-ecosystem
  and rideshare-favourites-ecosystem) accessible via DEFAULT_ECOSYSTEM_PACK_REGISTRY; added load_surface_ir()
  to inspect clean, valid ApplicationIR for individual surfaces; exposed catalog discovery and recommendation
  endpoints on the Studio HTTP server (GET /api/ecosystem-packs and POST /api/ecosystem-packs/recommend);
  extended Studio POST /api/build in server.py and live_serve.py to accept ecosystem_id, ecosystem_version,
  and surface_slug to build an individual surface or complete multi-surface platform with 0 model calls;
  extended StudioBuildHistory in studio/history.py to record ecosystem_id, ecosystem_version, surface_slug,
  surface_kind, and is_ecosystem; enhanced Studio web UI in studio/page.py with tabs (Single App vs Multi-Surface
  Ecosystem), ecosystem dropdown (#eco-select), surface selector (#surface-select), surface cards (#surface-cards),
  real-time recommendations, and history badges, strictly preserving 0 external asset requests (no external
  http/https/src/link/fonts); added catalog subcommand to solution_packs/ecosystem_cli.py (task
  agent-engine:solution-pack:ecosystem -- catalog); fourteen new focused tests in test_ecosystem_pack_registry.py
  and test_studio_ecosystem.py; task verify: 3,055 passed fully offline (+14 net-new tests); lint, security, env,
  and both builder demos (152/149 files) pass; 0 model calls in test execution. No dependency, pack baseline/selection,
  Application IR schema/example, generator, generated output, provider, database, infrastructure, workbook, or .claude change.
  Previously built R-444: Solution Pack multi-surface ecosystem pack synthesis (fifteenth spine brick
  and eleventh Solution Pack task). Enabled synthesizing a complete, coordinated multi-surface
  business ecosystem (customer web, operator portal, admin dashboard) from a Solution Pack's
  unified data model; implemented synthesize_surface_ir and synthesize_ecosystem_pack in
  intake/ecosystem.py and solution_packs/ecosystem_pack.py to scope entity visibility, mutation
  authority, actor roles, APIs, and screens per surface while preserving relational closures and
  the pack's project strategy; defined EcosystemPackPackage and EcosystemSurfacePackage schemas
  bundling all surfaces with canonical IR digests, verify targets, and whole-ecosystem package_sha256
  checksums; implemented parse_ecosystem_pack_package and verify_ecosystem_pack with strict integrity
  validation, failing closed on tampering or corruption; enhanced plan_ecosystem to synthesize
  secondary surfaces from pack_result data models (setting is_synthesized=True); implemented
  ecosystem_cli.py supporting synthesize, verify, inspect, and build subcommands; and added Taskfile
  and scripts/agent-engine.sh commands (task agent-engine:solution-pack:ecosystem). Eighteen new focused
  tests in test_solution_pack_ecosystem.py; task verify: 3,041 passed fully offline (+18 net-new tests);
  lint, security, env, both builder demos (152/149 files), and CLI end-to-end pass; 0 model calls in
  test execution. No dependency, pack baseline/selection, Application IR schema/example, generator,
  generated output, provider, database, infrastructure, workbook, or .claude change.
  Previously built R-443: Solution Pack packaging, verification, and export CLI (fourteenth spine brick
  and tenth Solution Pack task). Defined canonical byte-stable SolutionPackPackage bundles pinning schema version ("1.0"),
  metadata, ir_sha256, canonical ir_dict, verify_plans, and package_sha256 checksum in solution_packs/package.py;
  implemented parse_solution_pack_package to strictly validate JSON/dict payloads, verify embedded Application IR with
  validate_ir, confirm ir_sha256 digest matching, verify whole-package SHA-256 integrity, and fail closed with
  SolutionPackError on corruption or drift; extended SolutionPackRegistry with dynamic package registration via
  register_package and SolutionPack.from_package; implemented package_cli.py providing export, verify, and inspect
  subcommands; and added Taskfile and scripts/agent-engine.sh commands (task agent-engine:solution-pack:package).
  Sixteen new focused tests; focused package suite: 16 passed; task verify: 3,023 passed fully offline (+16 net-new tests);
  lint, security, env, both builder demos (152/149 files), and CLI end-to-end pass; 0 model calls in test execution.
  No dependency, pack baseline/selection, Application IR schema/example, generator, generated output, provider, database,
  infrastructure, workbook, or .claude change.
  Previously built R-442: Studio AI-delta feature modification controls above Solution Packs (thirteenth spine brick
  and ninth Solution Pack task). Enabled natural-language feature additions on top of selected Solution
  Packs in the Studio: extended POST /api/build in server.py to accept ai_features and ai_delta_prompt options;
  wired live_serve.py to formulate typed ai-delta change intents, generate bounded AIDeltaProposal objects
  via generate_ai_delta_proposal (bypassing the model with 0 calls when no AI features are requested), and safely
  derive the Application IR via apply_solution_pack_manifest with complete applied_ai_delta_change_ids provenance;
  enhanced Studio history (StudioBuildHistory) to track applied_ai_delta_change_ids; and updated the Studio web
  interface (STUDIO_HTML in page.py) with an AI Feature Modifications input (#ai-features), live synthesis
  status messages, AI delta badge chips in history items, and applied AI delta provenance rendering, all while
  strictly maintaining 0 external resource requests (no http/https/src/<link). Four new focused tests; focused
  studio suite: 53 passed; task verify: 3,007 passed fully offline (+4 net-new tests); lint, security, env,
  and both builder demos (152/149 files) pass; 0 model calls in test execution. No dependency, pack baseline/selection,
  Application IR schema/example, generator, generated output, provider, database, infrastructure, workbook, or .claude change.
  Previously built R-441: live Studio integration and UI controls for Solution Pack selection, customization, and provenance
  (twelfth spine brick and eighth Solution Pack task). Added solution pack discovery and recommendation endpoints
  to the Studio HTTP server (GET /api/solution-packs and POST /api/solution-packs/recommend), updated POST /api/build
  to accept pack_id, pack_version, custom_name, custom_description, and configuration_changes, and wired
  live_serve.py to deterministically compile apps from Solution Packs (0 model calls) using create_solution_pack_manifest,
  apply_solution_pack_manifest, and build_solution_pack_project with full provenance recorded. Enhanced Studio
  history (StudioBuildHistory) to track pack_id and pack_version, and updated the Studio web UI (STUDIO_HTML in
  page.py) with a Solution Pack selection dropdown, real-time recommendation banner, custom metadata inputs,
  verified solution pack badges, and full SHA-256 / change provenance rendering, while strictly maintaining 0 external
  resource requests (no http/https/src/<link). Eight new tests; focused studio suite: 63 passed; task verify: 3,003
  passed fully offline (+8 net-new tests); lint, security, env, and both builder demos (152/149 files) pass; 0 model calls.
  No dependency, pack baseline/selection, Application IR schema/example, generator, generated output, provider,
  database, infrastructure, workbook, or .claude change.
  Previously built R-440: wire derived Solution Pack Application IRs into verified multi-repo builder pipelines
  and project generation (eleventh spine brick). Added Python-3.13-stdlib-only solution_packs/builder.py
  and solution_packs/build_cli.py. build_solution_pack_project assembles a Solution Pack derived
  ApplicationIR (from a SolutionPackApplicationResult or SolutionPackManifest + optional AIDeltaProposal)
  into an owned Git repository on disk using assemble_project and create_repository. Computes deterministic
  verify gate plans (verify_plans_for_ir) and records complete provenance in a frozen, byte-stable
  SolutionPackBuildResult (pack_id, pack_version, base_ir_sha256, derived_ir_sha256, applied_configuration_change_ids,
  applied_ai_delta_change_ids, unapplied_ai_delta_change_ids, target_dir, file_count, commit_sha, verify_targets).
  Extended plan_ecosystem and build_ecosystem with optional pack_result or pack_manifest (+ pack_proposal)
  arguments: checks pack compatibility against ecosystem domain (failing closed with SolutionPackError on domain
  mismatch), transparently substitutes the customer web surface with the pack-derived IR while preserving other
  ecosystem services, and records pack provenance in the ecosystem plan. Added task agent-engine:solution-pack:build
  and CLI build_cli.py for standalone repo generation. Eleven new tests; focused regressions 86 passed;
  task verify 2,995 passed fully offline (+11); lint, security, env, both builder demos (152/149 files), and
  build CLI pass; 0 model calls. No dependency, pack baseline/selection, Application IR schema/example, generator,
  generated output, provider, database, infrastructure, workbook, or .claude change.
  Previously built R-439: safely apply validated AI-delta proposals to Application IR (tenth spine brick).
  Enhanced solution_packs/application.py and SolutionPackApplicationResult to accept an optional
  proposal: AIDeltaProposal | None = None. When proposal is None, existing deterministic configuration
  application behavior is strictly preserved (applied configuration changes merged, all AI deltas marked
  unapplied, applied_ai_delta_change_ids empty). When proposal is provided: revalidates pack pins
  (proposal.pack_id, proposal.pack_version, and proposal.base_ir_sha256 match manifest); revalidates
  change IDs (all proposal.addressed_change_ids match pending ai-delta changes in manifest.changes);
  revalidates collisions (proposed entity names, API endpoints, and screen IDs do not collide with base IR);
  revalidates relation targets (all relations in proposed entities target declared base or proposed entities);
  immutably merges entities, APIs, and screens with allowlisted configuration updates into a fresh derived
  ApplicationIR; ensures the derived IR is validate_ir-clean (failing closed with SolutionPackError on any
  semantic issue); and records applied_ai_delta_change_ids and remaining unapplied_ai_delta_change_ids in
  the result. Fourteen new tests; focused regressions 66 passed; task verify 2,984 passed fully offline (+14);
  lint, security, env, both demos (152/149), and deterministic serialization inspection pass; 0 model calls.
  No dependency, pack baseline/selection, ecosystem plan, Application IR schema/example, generator,
  generated output, provider, database, infrastructure, workbook, or .claude change.
  Previously built R-437: deterministic Solution Pack configuration application (eighth spine brick). Manifest schema
  1.1 adds typed desired_text accepted only for configuration/update of project:name or project:description,
  bounded to the Application IR's 128/4000-character rules; summaries are never parsed and AI-delta/other
  targets cannot carry it. Strict parsing remains backward compatible with R-436 schema 1.0 and serializes it
  losslessly; an unvalued legacy project update cannot apply. New solution_packs/application.py revalidates
  the exact registry recommendation pin, preflights the entire configuration set and duplicate targets,
  loads a fresh baseline IR, immutably applies only the two allowlisted metadata targets, and requires a
  validate_ir-clean result. Unsupported configuration fails before a result. Frozen canonical provenance
  records the base/derived digests, applied configuration IDs, explicitly unapplied AI-delta IDs, and derived
  IR. Empty/AI-only manifests preserve the base digest; repeated application is byte-stable; registered
  baselines remain unchanged. Ten new tests; focused R-434-R-437 regressions 37 passed; task verify 2,955
  passed fully offline; lint/security/env and both demos (152/149) passed; deterministic proof passed; 0 model
  calls. No dependency, baseline/selection, ecosystem, IR schema/example, generator, provider, database,
  infra, tracker-workbook, or .claude change. Previously built R-436: pinned declarative Solution Pack customization manifests (seventh spine brick). New stdlib-only
  solution_packs/manifest.py defines frozen typed changes and factory-created manifests above a selected exact
  R-435 recommendation. Each manifest pins pack id/version/canonical IR SHA-256 plus the exact domain,
  capabilities, and targets query and revalidates that identity against the immutable registry. At most 32
  changes are canonicalized by unique ID; every change has a configuration or ai-delta source,
  add/update/remove operation, one of six semantic areas, an area-compatible semantic target, bounded summary,
  and 1-8 bounded acceptance criteria. Byte-stable canonical JSON and strict parsing reject unknown/missing
  keys, malformed types/enums/targets, controls, oversized values, duplicates, noncanonical order, no-match,
  and registry/query/version/digest drift. The schema has no file-path, patch, source-code, command,
  raw-model-output, or secret-value field and performs no pack load/application, IR mutation, source generation,
  build, or provider call. Eleven new tests; focused R-434/R-435/R-436 regressions 27 passed; task verify 2,945
  passed fully offline; lint/security/env and both demos (152/149) passed; canonical JSON round-trip passed;
  0 model calls. No dependency, pack baseline/registry behavior, ecosystem, IR/schema, generator, provider,
  database, infra, tracker-workbook, or .claude change. Previously built R-435: exact-compatible Solution Pack recommendations in ecosystem planning (sixth spine brick).
  Registry selection now optionally requires an exact target subset alongside its existing exact-domain and
  required-capability rules. Frozen SolutionPackRecommendation records a canonical query and only the
  selected pack id/version/digest/targets or explicit no-exact-match. Every EcosystemPlan derives required
  targets from the project plans of its already-built surface IRs and exposes the recommendation in JSON and
  the deterministic CLI; it never applies or loads the pack. Blog-cms (Next.js/Python) recommends
  minimal-blog@1.0.0. The current rideshare plan honestly reports no match because it is Python-backed while
  rideshare-favourites is Go-backed. Existing surface IRs and builder output are unchanged. Eight new tests;
  focused R-431/R-434/R-435 regressions 25 passed; task verify 2,934 passed fully offline; lint/security/env
  and both demos (152/149) passed; 0 model calls. No dependency, pack baseline, IR/schema, generator, provider,
  database, infra, tracker-workbook, or .claude change. Previously built R-434: immutable, versioned baseline Solution Pack registry (fifth spine brick). New stdlib-only
  solution_packs descriptors reference the already verified minimal-blog and rideshare-favourites example
  Application IR builders without copying source or restating IR. Each frozen descriptor pins semantic
  version, exact domains/capabilities, canonical IR SHA-256, and assembled targets. Registry construction
  validates the IR, digest, targets, and every target's verify plan and fails closed on malformed or duplicate
  descriptors and drift. Deterministic get/select/load APIs choose the newest exact-domain pack satisfying
  all required capabilities and return None rather than fabricating a fallback; loads are fresh and
  validate_ir-clean. `task agent-engine:solution-packs` provides JSON inspection/selection with no build,
  model, network, database, or live work. Eight new tests; task verify 2,926 passed fully offline;
  lint/security/env and both demos (152/149) passed; 0 model calls. No dependency, existing IR/example,
  generator, provider, database, infra, tracker-workbook, or .claude change. Previously built R-433:
  deterministic per-surface data and capability scoping (fourth spine brick). The existing
  curated and model-refined entity sets now pass through an explicit read/write surface policy before IR
  generation. All ten curated domains have complete, reviewed surface mappings; refined domains match
  entity names against bounded surface metadata and keep the complete validated model when intent is
  ambiguous. Recursive relation closure retains targets as read dependencies without inventing data.
  Each Application IR declares only its own normalized actor role with entity-qualified permissions;
  selected readable entities get list screens, only writable entities get editors and POST/PUT/DELETE,
  and every mutation requires the surface role. Public GET behavior remains unchanged for current previews.
  Food-delivery now yields Customer (3 readable, Order-only write), Merchant (3 read/write), Courier
  (Order + Restaurant dependency, Order-only write; 8 APIs/2 screens), and Admin (Order + Restaurant;
  11 APIs/4 screens), rather than four identical models. Plan JSON exposes roles/permissions/write scope.
  Seven new tests; focused R-431/R-432/R-433 suite 30 passed; task verify 2,918 passed fully offline;
  lint/security/env and both demos (152/149) passed; 0 model calls. No dependency, IR/schema, provider,
  generator, database, infra, tracker-workbook, or .claude change. Previously built R-432: opt-in cheap-model refinement for prompts outside the 10 curated domains (third spine
  brick). The deterministic R-430 classifier always runs first: known domains return the curated R-431
  ScopeProposal + entities with zero provider calls; only custom-application makes one explicit request
  through the platform ModelProvider boundary. New stdlib-only intake/scope_refinement.py builds the bounded
  instruction, strictly parses untrusted JSON into ScopeProposal + typed Entity records, and rejects unknown
  keys, invalid identifiers/types/actors/relations, >3 questions, credential fields, relation-derived FK
  collisions, and unsupported validation rules. An omitted empty relations list normalizes to (), without
  inventing business values. plan_refined_ecosystem feeds accepted data into R-431's deterministic CRUD/IR
  planner; surface_to_ir/plan_ecosystem gained an optional entities tuple with existing behavior unchanged.
  New explicit local-only `task agent-engine:ecosystem:refine` uses the existing Ollama ModelProvider and
  prints parsed bounded data, not raw output; no cloud fallback. Live apiary proof -> apiary-management,
  Beekeeper Dashboard + Admin Panel, 4 entities, 23 wired APIs, 8 screens per app. Five local calls outside
  verify were used while rejecting/hardening malformed outputs and proving the final boundary; 0 cloud calls.
  test_scope_refinement.py 14 passed; task verify 2,911 passed fully offline (0 real model calls); lint,
  security, env, curated food-delivery regression, and both demos (152/149) passed. PostgreSQL, IR, adapters,
  generated contracts, infrastructure, and .claude unchanged. Previously built R-431: Scope -> Application
  IRs (second brick of the differentiating spine). One prompt now
  materializes a full multi-app OWNED-CODE ecosystem. New Python-3.13-stdlib-only intake/ecosystem.py maps
  each proposed AppSurface (R-430) to a validate_ir-clean ApplicationIR from a curated per-domain data model
  (DOMAIN_ENTITIES: 2-4 typed entities + FK relations for all 10 domains + a custom-application fallback) plus
  a deterministic CRUD API/screen deriver whose endpoints WIRE to real repositories (LIST/GET/CREATE/UPDATE/
  DELETE + sub-collection LIST_BY, verified via wire_endpoint - no 501 stubs). surface_to_ir + plan_ecosystem
  build an EcosystemPlan (one IR per selected surface, respecting the Complete/Customer-only/Custom option);
  build_ecosystem materializes each app as its own owned Git repo under out/<slug>/ by reusing
  build_app_from_ir. Deterministic (no model/network); exported from intake/__init__.py. Deterministic plan
  CLI (task agent-engine:ecosystem:plan) + opt-in build CLI (:build). Demo: "Create a food delivery app ..."
  -> 4 apps (Customer Ordering App, Merchant Portal, Courier Dispatch App, Super-Admin Dashboard), each
  3 entities / 17 APIs / 6 screens; the build CLI wrote 4 owned repos (164 files each, author sanjeetji).
  VERIFIED the generated ecosystem apps COMPILE clean: tsc --noEmit on all four -> 0 errors, which exposed
  and fixed 4 generator compile bugs in codegen/nextjs.py that the new FK/multi-subcollection/filterable-child
  shapes triggered (minimal-blog/rideshare never generate them): over-braced FK <select> onChange (=> {{ ->
  => {); single-brace bool-badge inline style (}> -> }}>); a parent list with 2+ sub-collections rendered
  search-bar + filter-chips as two JSX roots (wrapped in a fragment <>...</>); and the entity interface
  omitted the scalar <relation>_id FK column the DB schema/API/editor use (now emitted for to-one relations).
  test_ecosystem.py (9 tests) covers valid IRs per domain, option app-counts, deterministic plan, CRUD
  wiring, a real 4-repo build, and an offline guard for the fixed compile-bug classes. task verify 2,897
  passed (offline; +9); lint/security/env green; both builder demos (152 / 149) unaffected; the R-429
  examples still pass web-typecheck (no regression); 0 model calls. NEXT: an opt-in cheap-LLM refinement
  layer over the deterministic classifier/data-models for prompts outside the curated domains. Previously
  built R-430: the Ecosystem Scope Compiler (first brick of the founder-approved DIFFERENTIATING SPINE, per
  Master Architecture Spec section 2). New Python-3.13-stdlib-only intake/scope_compiler.py turns a
  plain-English business prompt into a framework-neutral ScopeProposal: detected domain (+confidence +matched
  keywords), business actors, the multi-surface app ecosystem, and build-scope options (Complete Business
  Platform [recommended] / Customer Experience Only / Custom) plus <=3 high-materiality questions. A curated
  10-domain DOMAIN_LIBRARY (food-delivery, rideshare, marketplace, e-commerce, b2b-saas, healthcare-clinic,
  booking, learning, social, blog-cms); classify_domain() scores weighted keywords (multi-word phrases weigh
  more) with a stable tie-break and returns None on no match; propose_ecosystem() assembles the options +
  questions with a single-app custom-application fallback. 100% deterministic and pure (no model, network,
  clock, or randomness), so it runs under task verify. Frozen dataclasses (Actor/AppSurface/ScopeOption/
  ScopeProposal/DomainMatch/DomainSpec) with JSON-safe to_dict(); exported from intake/__init__.py. Added a
  deterministic CLI intake/scope_propose.py wired as `task agent-engine:scope:propose -- "<prompt>"` (+
  scripts/agent-engine.sh scope-propose): "Create a food delivery app ..." -> domain food-delivery (confidence
  1.0) -> Customer Ordering App + Merchant Portal + Courier Dispatch App + Super-Admin Dashboard, with
  Complete/Customer-only/Custom options and 2 materiality questions. This is the differentiating capability
  competitors lack (they turn "food delivery app" into a single customer screen). NOT yet wired into IR/repo
  generation (mapping each surface -> an Application IR is the next brick). test_scope_compiler.py (11 tests)
  covers classification, per-domain example self-classification, determinism, option composition, <=3
  questions, JSON round-trip, and the fallback. task verify 2,888 passed (offline; +11); lint/security/env
  green; both builder demos (152 / 149) unaffected; 0 model calls. Previously built R-429: made a generated
  Next.js app pass strict `tsc --noEmit` with ZERO errors (was 84 on a
  generated minimal-blog: 4 node_modules/next from a missing skipLibCheck + 80 in our code across 19
  files). All fixes at codegen/nextjs.py plus the generated tsconfig, in 8 classes: G0 tsconfig now sets
  skipLibCheck: true (Next's own default); G1 context-menu.tsx no longer double-exports names already
  `export const` (kept `export { ContextMenu }`); G2 internal sub-component aliases (audio-player/geo-map/
  pdf-viewer) typed `typeof XInner & { displayName?: string }` so `.displayName` is allowed; G3 compound
  components (color-picker, pin-input) rebuilt as a typed compound (forwardRef `…Base` cast to
  `typeof XBase & { Sub: … }`) so `.Sub =` and `<X.Sub/>` are on the type; G4 Banner/Carousel/Checkbox/
  CodeBlock(+CopyButton)Props now Omit the conflicting inherited DOM attribute (title/onSelect/
  defaultChecked/onCopy); G5 DOM/SVG element ref annotations changed React.RefObject<T | null> ->
  React.RefObject<T> (assignable to a JSX ref=; mutable MutableRefObject/value refs untouched); G6
  terminal.tsx variant destructuring default "default" -> "minimal" (a valid TerminalVariant, byte-identical
  render, scoped to the terminal template); G7 added isUnchanged?: boolean to SplitDiffRow; G8 the exported
  `api` object now includes the …WithCount LIST/LIST_BY methods (were emitted but omitted from the object)
  and the generated LIST/LIST_BY hooks always forward a fresh requestParams object literal with `...options`
  spread first (options can no longer override the hook's params/abort signal, matching the GET/CREATE
  hooks). The opt-in `task agent-engine:web-typecheck` gate now asserts a clean tsc exit and reports PASSED
  for BOTH minimal-blog and rideshare-favourites (fresh generate + pnpm install + tsc, 0 errors).
  test_generated_tsx_compile.py gained deterministic guards (skipLibCheck present, no duplicate exports, no
  React.RefObject<HTMLxxx | null>, every api.<method> a hook calls exists on the api object); 7 hook/
  component test files' exact assertions updated to the corrected emitted strings. task verify 2,877 passed
  (offline); lint/security/env green; both builder demos (152 / 149) pass; 0 model calls. This removes the
  last blocker to a production `next build` for generated apps. Previously built R-428: made generated
  Next.js apps actually compile and render, and added an opt-in TypeScript
  typecheck gate to find this class of bug. Added `task agent-engine:web-typecheck -- <example> [dir]`
  (generate + pnpm install + tsc --noEmit; opt-in/live, never in verify). Running it (and next dev) revealed
  and fixed the RUN-BLOCKING generated-code bugs: (1) search-input onChange / clear-button onClick handlers
  emitted `=> {{ ... }}}}` (extra braces, JSX syntax error) -> `=> { ... }}`; (2) components/pdf-viewer.tsx had
  a literal `\n` from a raw-string join (invalid character) -> real newline via adjacent concatenation; (3)
  nested screen pages imported depth-relative `../components/`/`../lib/` (broke module resolution one folder
  deep) -> the depth-independent `@/` path alias at all 24 generator import sites. Verified end-to-end: a
  generated minimal-blog app compiles under next dev and /, /post_list, /post_editor all serve HTTP 200 (were
  500), 0 module-resolution errors in generated code. Added test_generated_tsx_compile.py (over-braced
  handlers, relative ../components|../lib imports, literal backslash-n) alongside R-427's style test; updated
  9 screen/layout test files' import assertions from ../ to @/. 2,873 tests pass; lint/security/env and both
  builder demos (152 / 149) pass; diff-invariance unaffected; 0 model calls. REMAINING (follow-up R-429): ~82
  strict TYPE errors in the component library (duplicate exports, displayName on function components, prop-type
  conflicts) that do NOT block next dev/preview but would fail a production next build. Previously built R-427:
  fixed a real generated-code bug that made generated Next.js web apps fail to compile. Found via
  the Studio live preview - the backend ran fine (/posts 200) but the web app returned HTTP 500 with an SWC
  syntax error ("Expected '</', got ':'") on a single-brace JSX inline style in the generated form/list/detail
  screens. Root cause: 14 f-string templates in codegen/nextjs.py wrote style={{ ... }}, which Python collapses
  to single-brace style={ ... } in the emitted TSX (JSX requires double braces); the fix rewrites those to
  quadruple braces style={{{{ ... }}}} so they emit valid style={{ ... }}. Only f-string lines were changed
  (regular/raw component templates already correct were left untouched); no component template, screen layout,
  styling value, or logic changed. Added a deterministic regression test (test_generated_screen_styles.py) that
  generates both example IRs and forbids any single-brace object-literal inline style in the generated .tsx -
  RED before the fix (caught app/favourites/page.tsx and the blog screens), GREEN after - so this class of bug
  cannot recur. task verify never caught it before because it asserts generated code as strings and never
  compiles the TSX. 3 focused tests and task verify's 2,870 tests pass; lint/security/env and both builder
  demos (152 / 149 files) pass; diff-invariance/snapshot unaffected; 0 local/cloud model calls. Previously
  built R-426: remove a build from the Studio's in-session Recent builds list. StudioBuildHistory.remove(
  build_id) -> bool (thread-safe; drops the entry by id, returns whether it was present; unknown id is a safe
  no-op returning False). The stdlib Studio server gained POST /api/history/delete {id} via a new injected
  delete_build_fn(build_id) -> dict (reusing the generic _run_id_control body reader shared with re-preview/
  open); unset -> 404, missing id -> 400, errors -> 502. Because removing an entry only edits the bounded
  in-memory list (it never runs generated code, touches the DB, or deletes anything on disk), the delete
  handler is wired in BOTH Studio modes (alongside history listing) and returns the refreshed, bounded,
  secret-free history ({removed, builds}) so the page re-renders in one call. The Studio page adds a per-build
  "Remove" action that calls the route and re-renders the list, DOM-only. Build/preview/re-preview/open-folder/
  live status/status/stop/restart, collision-free ports, single-session cleanup, PostgreSQL, and
  studio:serve/studio:preview semantics unchanged. 70 focused tests (6 net-new: 2 history.remove, 4 delete-
  route/page) and task verify's 2,867 tests pass; lint/security/env and both builder demos (152 / 149 files)
  pass; a deterministic remove/delete inspection confirmed True/False removal and a bounded secret-free
  {removed, builds} payload; 0 local/cloud model calls. Previously built R-425: per-build repo actions in the
  Studio's Recent builds list. A purely client-side "Copy path"
  copies a recorded build's repo target_dir to the clipboard (navigator.clipboard with an execCommand
  fallback; works in both Studio modes, no server call), and an "Open folder" action opens the recorded repo
  directory in the OS file browser via a new trusted-local route POST /api/history/open {id}. The route reuses
  the injected-handler pattern (open_dir_fn(build_id) -> dict) and a generic _run_id_control body reader in the
  stdlib server (shared with re-preview); it is wired only in trusted-local preview mode (build-only 404).
  live_serve's _open_recorded_build looks up the build and launches the platform folder opener (_open_path:
  darwin open / win os.startfile / else xdg-open, best-effort, never raises or echoes a command) returning a
  bounded, secret-free status (opened/error); an unknown build id returns a bounded error. The opener is an
  opt-in/live path, injected/stubbed in tests. Build/preview/history/status/stop/restart, collision-free ports,
  single-session cleanup, PostgreSQL, and studio:serve/studio:preview semantics unchanged. 64 focused tests
  (4 net-new: 3 open-route, 1 page actions) and task verify's 2,861 tests pass; lint/security/env and both
  builder demos (152 / 149 files) pass; a deterministic open-route inspection (opener stubbed) confirmed the
  opened/error/unknown payloads are bounded and secret-free; 0 local/cloud model calls. Previously built
  R-424: live preview status. LocalAppSession.is_alive() reports whether a session is still running (not
  stopped and every owned background process poll() is None). StudioPreviewManager.status() is now
  liveness-aware: under its lock it refreshes the active session, and if the generated app's processes have
  exited on their own it stops/forgets the dead session exactly once and reports a bounded, secret-free
  "stopped" state instead of a stale "ready". The Studio page polls the existing R-422 GET /api/preview route
  every 5s while a preview is running and re-renders the preview surface on change, stopping the poll when the
  state is not "ready" and never reloading the embedded iframe when the preview URL is unchanged (no flicker).
  No new routes, server signature change, or live_serve wiring change - it reuses GET /api/preview and the
  R-421/R-422 manager. Collision-free ports, status/stop/restart + history controls, trusted-local
  studio:preview, build-only studio:serve, single-session cleanup, PostgreSQL, and architecture unchanged.
  60 focused tests (7 net-new: 4 is_alive, 2 status liveness, 1 page poll) and task verify's 2,857 tests pass;
  lint/security/env and both builder demos (152 / 149 files) pass; a deterministic liveness/status inspection
  confirmed the transition and secret-free payloads; 0 local/cloud model calls. Previously built R-423: the
  local Studio now keeps a bounded in-session history of recent builds and can re-preview any
  of them. New dependency-free, thread-safe StudioBuildHistory (stdlib only, in-memory ring capped at 10 most
  recent) records each successful build as a bounded, secret-free entry (id, truncated prompt, name, entities,
  file_count, target_dir, commit_sha, created_at; injected clock for deterministic tests). The stdlib Studio
  server gained GET /api/history (recent builds newest-first) and POST /api/history/preview {id} (re-previews a
  recorded build's already-materialized repo through the R-422 StudioPreviewManager), via optional injected
  handlers; unset handlers 404, missing/unknown id returns a bounded error. live_serve records every successful
  build (both modes), wires history_fn always and preview_build_fn only in trusted-local preview mode. The
  Studio page adds a "Recent builds" list that loads on start, refreshes after each build, and re-previews on
  click with textContent only (no HTML injection). Collision-free preview ports, status/stop/restart controls,
  trusted-local studio:preview, build-only studio:serve, single-session cleanup, PostgreSQL, and architecture
  unchanged. 53 focused tests (12 net-new: 7 history + 5 server routes) and task verify's 2,850 tests pass;
  lint/security/env and both builder demos (152 / 149 files) pass; a deterministic history-payload inspection
  confirmed bounded newest-first secret-free entries; 0 local/cloud model calls. Previously built R-422:
  collision-free Studio preview ports plus bounded status/stop/restart controls. Each trusted-
  local preview now allocates two distinct, currently-free loopback ports (allocate_preview_ports holds both
  sockets open while reading their OS-assigned ports) and threads them through the R-419 run plan (and thus
  the generated web app's NEXT_PUBLIC_API_URL) via a new start_preview_app boundary that delegates to the
  strict start_app, so a preview never fails on, or clobbers, an existing task app:run app on 3000/8000 or a
  prior preview. StudioPreviewManager (default start_fn now start_preview_app) gained bounded, JSON-safe,
  secret-free status()/stop()/restart(): it remembers a single last repo, restart re-previews it (idle no-op
  before any build), and stop is idempotent and reports "stopped". The stdlib Studio server gained optional
  injected status_fn/stop_fn/restart_fn -> GET /api/preview, POST /api/preview/stop, POST /api/preview/restart,
  wired only in trusted-local preview mode (build-only returns 404). The Studio page adds Stop/Restart buttons
  and a live status line that call the control routes and re-render preview state without HTML injection.
  studio:serve stays build-only, studio:preview stays trusted-local, single-session cleanup preserved,
  PostgreSQL and architecture unchanged. 53 focused tests (15 net-new) and task verify's 2,838 tests pass;
  lint/security/env and both builder demos (152 / 149 files) pass; a deterministic payload/port inspection
  confirmed distinct free ports and secret-free control payloads; 0 local/cloud model calls. Previously built
  R-421: an explicit trusted-local Studio mode now embeds the actual generated Next.js app in a
  sandboxed iframe. The existing studio:serve remains build-only; new studio:preview depends on db:up and
  opts into generated-code execution. LocalAppSession/start_app refactor the R-419 executor into a managed
  reusable boundary with API/web readiness, idempotent cleanup, partial-failure cleanup, occupied-port
  preflight, and exited-child protection; app:run preserves best-effort readiness. StudioPreviewManager
  serializes one session, stops it on replacement/shutdown, returns secret-free ready/error/unavailable
  payloads, and preserves successful build results when preview launch fails. 38 focused tests and task
  verify's 2,823 tests pass; lint/security/env and both demos pass; 0 model calls. Existing user processes
  on ports 3000/8000 were left untouched and now cause an honest preflight error rather than false preview
  readiness. Previously built R-420: generated PostgreSQL is safe for reserved-looking identifiers and FK dependencies.
  One defensive double-quote encoder is used across schema DDL, honest fixture INSERTs, Python
  repositories, and Go stores while logical paths, symbols, routes, and API names remain unchanged.
  Entity CREATE TABLE blocks and fixture groups now use a stable FK topological order, preserve source
  order between independent entities, permit self references, and reject non-self cycles with a stable
  ValueError. Seven focused safety tests and 186 related regressions pass; task verify passes all 2,808
  tests; lint/security/env and both builder demos pass; representative Python parses and Go stores pass
  gofmt. A reserved-name User/Order migration also executed successfully against local PostgreSQL inside
  a rollback-only transaction. 0 local model calls, 0 cloud calls. Previously built R-419, BRICK 4 of the
  "chat -> create an app" front door: the TURNKEY local run. New
  omnistackai_agent_engine/localrun/ package (Python 3.13 stdlib only), following the "deterministic plan +
  opt-in executor" pattern. plan.py: RunStep/RunPlan + build_run_plan(repo_dir, *, db_*, ports, jwt_secret)
  inspects a generated repo (python/go backend, migrations, web) and composes an ordered JSON-safe plan
  (RunPlan.to_dict masks the DB password): recreate a per-app Postgres DB, apply each migration, start the
  backend (uvicorn/go run with DATABASE_URL/JWT_SECRET), start the web app with `next dev` directly (NOT
  `pnpm dev`) + NEXT_PUBLIC_API_URL. run.py: opt-in executor (task agent-engine:app:run -- <dir>, Task deps
  on db:up) - runs setup synchronously (skips existing .venv/node_modules), launches both servers, polls
  backend /healthz, prints URLs, cleans up on Ctrl+C. VERIFIED END-TO-END on the founder's Mac: one command
  ran ~/omnistackai-blog-run - Postgres up, DB recreated, BOTH migrations applied, backend venv+install,
  uvicorn on :8000 (/healthz 200, /posts returned the 2 seeded posts), web install, Next.js on :3000 (200).
  This closes the local "chat -> create -> RUN" loop; the 6-step manual dance is now one command. Fixed a
  real bug along the way: bare `pnpm install` exits 1 on pnpm 11 ERR_PNPM_IGNORED_BUILDS (sharp) -> use
  `pnpm install --ignore-scripts`. Also SURFACED two pre-existing schema-generator bugs (invisible to
  task verify, which never executes SQL): unquoted reserved-word identifiers (bookstore "Order" -> CREATE
  TABLE order syntax error) and FK/table creation ordering (recipe box: ingredient before recipe) -> to be
  fixed in R-420. 2,801 tests pass (12 new in test_localrun_plan.py); lint/security/env green; builder:demo
  unchanged; 0 model calls in verify. Previously R-418 shipped brick 3: the user-facing chat studio WEB UI. New
  omnistackai_agent_engine/studio/ package (Python 3.13 stdlib http.server only - no npm/pnpm, no web
  framework): page.py (self-contained HTML with a prompt textarea + Build button + results panel, inline
  CSS/JS, zero external deps, diff-invariant), server.py (create_studio_server(build_fn, host, port)
  ThreadingHTTPServer - GET / serves the page, POST /api/build reads {prompt}, calls the INJECTED build_fn ->
  dict, returns JSON; 400 empty/invalid, 502 build failure - build_fn injected so task verify tests the HTTP
  layer via an ephemeral localhost server + in-memory stub, 0 model calls), live_serve.py (opt-in real
  local-Ollama build path on OMNISTACKAI_STUDIO_HOST/PORT, default 127.0.0.1:4173). Added
  app_build_result_to_dict() to intake/build_app.py (pure JSON-safe shape: name/description/entities/
  file_count/target_dir/commit_sha/files). Wired opt-in `task agent-engine:studio:serve`. VERIFIED
  END-TO-END on the founder's Mac: started the studio, GET / served the page (7,513 bytes), and POST
  /api/build with "Build a bookstore where users browse books, each book has an author, and users can place
  orders" -> local Ollama (qwen2.5-coder:14b) -> IR "Bookstore" (Book/Order) -> a 154-file owned Git repo.
  This completes the local user-facing "type a sentence -> see a real app get built" experience (minus a
  live in-browser preview). 2,789 tests pass (11 new in test_studio_server.py); lint/security/env green;
  builder:demo unchanged; 0 model calls in verify. Previously R-417 shipped brick 2: prompt -> generated app
  repo.
  intake/build_app.py adds build_app_from_ir(ir, target_dir, ...) (assemble_project -> create_repository ->
  AppBuildResult{prompt, ir, target_dir, file_count, commit_sha}) and async build_app_from_prompt(prompt,
  provider, target_dir, *, model_id, ...) which chains the R-416 intake agent so a sentence -> validated IR
  -> assembled monorepo -> real owned Git repo, depending only on the vendor-neutral ModelProvider protocol
  (task verify runs it against an in-memory stub into a temp dir via the local git CLI -> 0 model calls, 0
  network). Refactored the local-Ollama provider construction into intake/_ollama.py
  (build_ollama_provider_from_env), now shared by live_run.py and the new opt-in build_run.py wired as
  `task agent-engine:app:build -- "<description>"` (out dir via OMNISTACKAI_APP_OUT_DIR or a temp dir).
  VERIFIED END-TO-END on the founder's Mac: "Build a recipe box where users save recipes, each recipe has
  ingredients and cooking steps" -> local Ollama (qwen2.5-coder:14b) -> IR "Recipe Box" (entities
  Ingredient/Recipe) -> a materialized owned Git repo of 154 files with recipe-specific routes/screens,
  first commit authored sanjeetji. 2,778 tests pass (7 new in test_build_app.py); lint/security/env green;
  builder:demo unchanged; 0 model calls in verify. This completes the offline "chat -> real owned app"
  pipeline (minus the chat UI). Previously R-416 shipped the Prompt -> Application IR intake agent
  (omnistackai_agent_engine/intake/): a local, zero-new-dependency Prompt -> Application IR intake agent
  (omnistackai_agent_engine/intake/). build_intake_messages() constructs the system+user Message tuple with
  the schema taught BY EXAMPLE (embedding a real example_ir().to_dict() so it never drifts) plus an explicit
  allowed-field-types list derived from the FieldType enum; parse_ir_response() turns raw model text into a
  validated, normalized ApplicationIR (tolerates ```json fences + surrounding prose, injects schema_version,
  ApplicationIR.from_dict + normalize_ir, raises IntakeResponseError on malformed/invalid output);
  generate_ir() performs the single I/O step against the vendor-neutral ModelProvider protocol so task verify
  runs it against an in-memory stub (0 model calls, 0 network) and raises on validation errors. Ships
  IntakeResult + IntakeError/IntakeResponseError, and an opt-in live runner (intake/live_run.py) wired as
  `task agent-engine:intake:run -- "<description>"`. VERIFIED WORKING END-TO-END on the founder's Mac: local
  Ollama (qwen2.5-coder:14b) turned "Build a task tracker where users create projects and each project has
  tasks with a title, status and due date" into a valid Application IR (Project/Task entities, /projects +
  nested-task APIs, acceptance criteria) that feeds straight into the existing code generators. 2,771 tests
  pass (18 new in test_intake_nl_to_ir.py); lint/security/env green; builder:demo unchanged (152 files);
  0 model calls in verify. Previously R-415 shipped the Accessible Futuristic Reusable Phone Number Input
  Suite (components/phone-input.tsx); a genuinely functional international phone field — a country selector (ISO2 code + dial code from a curated 20-entry
  DEFAULT_COUNTRIES table, overridable via a countries prop) paired with a national-number input that strips
  non-digits (replace /[^0-9]/g), groups them loosely for display, assembles an E.164 string (dial + digits),
  and validates by digit length (6..14); a computed meta object ({country, dial, national, e164, valid});
  controlled + uncontrolled national value; onChange(e164, meta) callback; WAI-ARIA (labeled country select +
  tel input, aria-label, inputMode="tel", type="tel", autoComplete="tel-national", aria-invalid on invalid,
  aria-required); 4 futuristic variants ("default", "card", "glass", "neon"); 3 size scales ("sm", "md",
  "lg"); React ref forwarding (forwardRef); useImperativeHandle (PhoneInputHandle: getValue/getE164/setValue/
  getCountry/clear/focus); canonical TypeScript types (PhoneInputVariant, PhoneInputSize, PhoneCountry,
  PhoneInputMeta, PhoneInputHandle, PhoneInputProps); compound and semantic alias exports (PhoneInput,
  PhoneNumberInput, TelInput, PhoneField, default export) with explicit displayName; 100% diff-invariance
  across ir.description, ASCII-only source (ISO codes + dial codes, no flag emoji), 0 external runtime
  dependencies; exported render_phone_input_component in codegen; registered components/phone-input.tsx in
  NextjsWebAdapter; 2,753 tests pass (18 new tests in test_phone_input_component.py); 0 model calls.
  Previously R-414 shipped the Accessible Futuristic Reusable Duration Input Suite
  (components/duration-input.tsx); a genuinely
  functional duration field — segmented numeric inputs for a configurable set of units
  (days/hours/minutes/seconds via a UNIT_SECONDS table) that convert to/from a single total-seconds value
  (toSegments/fromSegments), with min/max clamping (normalizing segments on clamp), a human-readable
  formatDuration helper + live summary, controlled + uncontrolled value (seconds), onChange(totalSeconds),
  and WAI-ARIA (role="group" + aria-label, per-segment aria-label + inputMode="numeric"); 4 futuristic
  variants ("default", "card", "glass", "neon"); 3 size scales ("sm", "md", "lg"); React ref forwarding
  (forwardRef); useImperativeHandle (DurationInputHandle: getValue/setValue/getFormatted/clear/focus);
  canonical TypeScript types (DurationInputVariant, DurationInputSize, DurationUnit, DurationInputHandle,
  DurationInputProps); compound and semantic alias exports (DurationInput, DurationField, TimeSpanInput,
  IntervalInput, default export) with explicit displayName; 100% diff-invariance across ir.description,
  ASCII-only source, 0 external runtime dependencies; exported render_duration_input_component in codegen;
  registered components/duration-input.tsx in NextjsWebAdapter; 2,735 tests pass (18 new tests in
  test_duration_input_component.py); 0 model calls. Previously R-413 shipped the Copy-to-Clipboard Button
  Suite (components/copy-button.tsx); a
  genuinely functional copy button — copies a given value via navigator.clipboard.writeText with a
  document.execCommand('copy') textarea fallback, shows a transient "Copied" state (configurable timeout)
  with a swapped inline SVG icon (copy -> check) and an aria-live announcement, disables when there is
  nothing to copy, and exposes onCopy/onError callbacks; WAI-ARIA (button aria-label + visually-hidden
  aria-live status); 4 futuristic variants ("default", "card", "glass", "neon"); 3 size scales ("sm", "md",
  "lg"); React ref forwarding (forwardRef); useImperativeHandle (CopyButtonHandle: copy/isCopied/reset/focus);
  canonical TypeScript types (CopyButtonVariant, CopyButtonSize, CopyButtonHandle, CopyButtonProps); compound
  and semantic alias exports (CopyButton, CopyToClipboard, ClipboardButton, CopyIconButton, default export)
  with explicit displayName; 100% diff-invariance across ir.description, ASCII-only source (inline SVG icons),
  0 external runtime dependencies; exported render_copy_button_component in codegen; registered
  components/copy-button.tsx in NextjsWebAdapter; 2,717 tests pass (18 new tests in
  test_copy_button_component.py); 0 model calls. Previously R-412 shipped the Character & Word Counter
  Textarea Suite (components/character-counter.tsx); a genuinely functional counting textarea — live Unicode-safe character
  counting (Array.from code points) and word counting (trim + whitespace split), configurable maxLength /
  maxWords with a computed stats object ({characters, words, remaining, overLimit}), an optional hard limit
  that blocks input past maxLength, a warn threshold that recolors near the limit, an optional progress bar,
  controlled + uncontrolled value, onChange (value, stats) callback, and WAI-ARIA (labeled textarea,
  aria-describedby wiring to a role="status" aria-live counter region); 4 futuristic variants ("default",
  "card", "glass", "neon"); 3 size scales ("sm", "md", "lg"); React ref forwarding (forwardRef);
  useImperativeHandle (CharacterCounterHandle: getValue/setValue/getStats/clear/focus); canonical TypeScript
  types (CharacterCounterVariant, CharacterCounterSize, CharacterCounterStats, CharacterCounterHandle,
  CharacterCounterProps); compound and semantic alias exports (CharacterCounter, CharCounter, WordCounter,
  TextCounter, default export) with explicit displayName; 100% diff-invariance across ir.description, 0
  external runtime dependencies; exported render_character_counter_component in codegen; registered
  components/character-counter.tsx in NextjsWebAdapter; 2,699 tests pass (18 new tests in
  test_character_counter_component.py); 0 model calls. Previously R-411 shipped the Slug / URL Input Suite
  (components/slug-input.tsx); a genuinely
  functional slug field — a slugify() helper converts arbitrary text to a URL-safe slug in real time
  (Unicode NFKD normalization + charCodeAt filter to strip combining diacritics 0x300-0x36f, lowercase,
  non-alphanumeric runs collapsed to a configurable separator, leading/trailing separators trimmed);
  auto-sync from an optional source prop (e.g. a title) until the user manually edits; an optional prefix /
  base URL with a computed full URL; copy-to-clipboard (navigator.clipboard.writeText) with copied feedback;
  controlled + uncontrolled value; maxLength; onChange (slug) + onCopy (fullUrl) callbacks; WAI-ARIA (labeled
  input, aria-label, aria-live copied announcement); 4 futuristic variants ("default", "card", "glass",
  "neon"); 3 size scales ("sm", "md", "lg"); React ref forwarding (forwardRef); useImperativeHandle
  (SlugInputHandle: getValue/getFullUrl/setValue/slugify/clear/focus); canonical TypeScript types
  (SlugInputVariant, SlugInputSize, SlugInputHandle, SlugInputProps); compound and semantic alias exports
  (SlugInput, Slugify, UrlSlugInput, PermalinkInput, default export) with explicit displayName; 100%
  diff-invariance across ir.description, ASCII-only generated source, 0 external runtime dependencies;
  exported render_slug_input_component in codegen; registered components/slug-input.tsx in NextjsWebAdapter;
  2,681 tests pass (18 new tests in test_slug_input_component.py); 0 model calls. Previously R-410 shipped the
  Password Generator Suite (components/password-generator.tsx); a
  genuinely functional secure password generator — builds a password from configurable character sets
  (uppercase/lowercase/numbers/symbols, optional exclude-ambiguous) using crypto.getRandomValues (Uint32Array,
  Math.random fallback), guaranteeing at least one char from each enabled set and shuffling with Fisher-Yates;
  a length slider (min/max), set toggles, a strength meter, a read-only output, copy-to-clipboard
  (navigator.clipboard.writeText with a document.execCommand fallback) + copied feedback, and a regenerate
  action; SSR-safe (crypto only in handlers/effects; auto-generates on mount); onGenerate + onCopy callbacks;
  WAI-ARIA (role="group", labeled controls, aria-live copied announcement); 4 futuristic variants ("default",
  "card", "glass", "neon"); 3 size scales ("sm", "md", "lg"); React ref forwarding (forwardRef);
  useImperativeHandle (PasswordGeneratorHandle: generate/getValue/copy/setLength); canonical TypeScript types
  (PasswordGeneratorVariant, PasswordGeneratorSize, PasswordGeneratorOptions, PasswordGeneratorHandle,
  PasswordGeneratorProps); compound and semantic alias exports (PasswordGenerator, PasswordCreator,
  SecurePasswordGenerator, PasswordMaker, default export) with explicit displayName; 100% diff-invariance
  across ir.description, 0 external runtime dependencies; exported render_password_generator_component in
  codegen; registered components/password-generator.tsx in NextjsWebAdapter; 2,663 tests pass (18 new tests
  in test_password_generator_component.py); 0 model calls. Previously R-409 shipped the Currency / Money Input
  Suite (components/currency-input.tsx); a
  genuinely functional money field — sanitizes typed input to a clean numeric string, parses it to a number
  (parseFloat), clamps to min/max on blur, and formats the value as locale-aware currency via the built-in
  Intl.NumberFormat (style: 'currency') when unfocused (plain numeric while focused for easy editing);
  configurable currency + locale (default USD / en-US), min/max/step, allowNegative; controlled +
  uncontrolled value (number|null); onChange (value|null, formatted) + onBlur callbacks; WAI-ARIA (labeled
  input, aria-invalid on out-of-range, aria-required, inputMode="decimal", right-aligned tabular-nums);
  4 futuristic variants ("default", "card", "glass", "neon"); 3 size scales ("sm", "md", "lg"); React ref
  forwarding (forwardRef); useImperativeHandle (CurrencyInputHandle: getValue/getFormatted/setValue/clear/
  focus); canonical TypeScript types (CurrencyInputVariant, CurrencyInputSize, CurrencyInputHandle,
  CurrencyInputProps); compound and semantic alias exports (CurrencyInput, MoneyInput, CurrencyField,
  PriceInput, default export) with explicit displayName; 100% diff-invariance across ir.description, 0
  external runtime dependencies; exported render_currency_input_component in codegen; registered
  components/currency-input.tsx in NextjsWebAdapter; 2,645 tests pass (17 new tests in
  test_currency_input_component.py); 0 model calls. Previously R-408 shipped the Color Contrast Checker
  Suite (components/color-contrast.tsx); a
  genuinely functional WCAG contrast tool — parses foreground/background hex (#rgb and #rrggbb via parseHex),
  computes WCAG 2.x relative luminance (0.2126/0.7152/0.0722 + Math.pow gamma) and the contrast ratio
  ((lighter+0.05)/(darker+0.05)), and evaluates AA/AAA pass-fail for normal text (>=4.5 / >=7), large text
  (>=3 / >=4.5), and UI components (>=3); a computed ContrastResult ({ratio, aaNormal, aaaNormal, aaLarge,
  aaaLarge, uiComponent, rating}); native color inputs + hex text inputs, a swap action, a live preview
  swatch with sample text at normal and large sizes, and pass/fail badges; controlled + uncontrolled colors;
  onChange callback; WAI-ARIA (labeled inputs, a role="status" aria-live results region); 4 futuristic
  variants ("default", "card", "glass", "neon"); 3 size scales ("sm", "md", "lg"); React ref forwarding
  (forwardRef); useImperativeHandle (ColorContrastHandle: getRatio/getResult/setColors/swap); canonical
  TypeScript types (ColorContrastVariant, ColorContrastSize, ContrastResult, ColorContrastHandle,
  ColorContrastProps); compound and semantic alias exports (ColorContrast, ContrastChecker, WcagContrast,
  ContrastRatio, default export) with explicit displayName; 100% diff-invariance across ir.description, 0
  external runtime dependencies; exported render_color_contrast_component in codegen; registered
  components/color-contrast.tsx in NextjsWebAdapter; 2,628 tests pass (18 new tests in
  test_color_contrast_component.py); 0 model calls. Previously R-407 shipped the Credit Card Payment Field
  Suite (components/credit-card.tsx); a
  genuinely functional payment form field — card-number, expiry (MM/YY), CVC, and optional cardholder-name
  inputs with real-time formatting, IIN-prefix brand detection (visa/mastercard/amex/discover/unknown via
  detectBrand), Luhn checksum validation (luhnValid), expiry validity (valid month + not past), and
  brand-aware CVC length; a computed CreditCardMeta ({brand, numberValid, expiryValid, cvcValid, complete});
  an optional live gradient card preview; controlled + uncontrolled value (Partial); onChange (value, meta)
  and onComplete callbacks; WAI-ARIA (labeled inputs, aria-invalid, inputMode="numeric", autoComplete
  cc-* hints); 4 futuristic variants ("default", "card", "glass", "neon"); 3 size scales ("sm", "md", "lg");
  React ref forwarding (forwardRef); useImperativeHandle (CreditCardHandle: getValue/getMeta/clear/focus);
  canonical TypeScript types (CreditCardVariant, CreditCardSize, CardBrand, CreditCardValue, CreditCardMeta,
  CreditCardHandle, CreditCardProps); compound and semantic alias exports (CreditCard, CreditCardField,
  PaymentCardField, CardInput, default export) with explicit displayName; 100% diff-invariance across
  ir.description, 0 external runtime dependencies; exported render_credit_card_component in codegen;
  registered components/credit-card.tsx in NextjsWebAdapter; 2,610 tests pass (18 new tests in
  test_credit_card_component.py); 0 model calls. Previously R-406 shipped the Marquee / Ticker Suite
  (components/marquee.tsx); a seamless
  continuous scroller for arbitrary children (news tickers, logo walls, announcement bars) that duplicates
  its content once for a seamless -50% loop, driven by CSS @keyframes injected via an inline <style> element
  (omni-marquee-x / omni-marquee-y with animationDirection for left/right/up/down); configurable
  durationSeconds, gap, and edge gradient fade via CSS maskImage; pause-on-hover plus a controlled paused
  prop and an imperative pause/resume/toggle handle (animationPlayState); a CSS prefers-reduced-motion guard
  (in the injected style) that stops the animation; accessibility (the duplicated copy is aria-hidden, the
  container carries role="group" + aria-label); 4 futuristic variants ("default", "card", "glass", "neon");
  3 size scales ("sm", "md", "lg"); React ref forwarding (forwardRef); useImperativeHandle (MarqueeHandle:
  pause/resume/toggle/isPaused); canonical TypeScript types (MarqueeVariant, MarqueeSize, MarqueeDirection,
  MarqueeHandle, MarqueeProps); compound and semantic alias exports (Marquee, MarqueeTicker, ScrollingBanner,
  NewsTicker, default export) with explicit displayName; 100% diff-invariance across ir.description, 0
  external runtime dependencies; exported render_marquee_component in codegen; registered
  components/marquee.tsx in NextjsWebAdapter; 2,592 tests pass (18 new tests in test_marquee_component.py);
  0 model calls. Previously R-405 shipped the Mention / @-Autocomplete Textarea Suite (components/mention.tsx);
  a genuinely functional textarea that detects a configurable trigger char (default "@") at the caret via a
  detectTrigger() helper, opens a filtered suggestion listbox from an items prop, supports keyboard
  navigation (ArrowDown/ArrowUp/Enter/Tab to insert, Escape to close), inserts the chosen mention token and
  keeps typing flowing (caret repositioned via requestAnimationFrame + setSelectionRange), extracts the set
  of mentioned ids from the text, supports controlled + uncontrolled value, and fires onChange (value,
  mentions) + onMention callbacks; ARIA combobox/listbox pattern (textarea role="combobox" with
  aria-autocomplete="list", aria-expanded, aria-controls, aria-activedescendant; listbox role="listbox" with
  role="option" + aria-selected); 4 futuristic variants ("default", "card", "glass", "neon"); 3 size scales
  ("sm", "md", "lg"); React ref forwarding (forwardRef); useImperativeHandle (MentionHandle:
  getValue/setValue/getMentions/focus/clear); canonical TypeScript types (MentionVariant, MentionSize,
  MentionItem, MentionHandle, MentionProps); compound and semantic alias exports (Mention, MentionInput,
  MentionTextarea, AtMention, default export) with explicit displayName; 100% diff-invariance across
  ir.description, 0 external runtime dependencies; exported render_mention_component in codegen; registered
  components/mention.tsx in NextjsWebAdapter; 2,574 tests pass (18 new tests in test_mention_component.py);
  0 model calls. Previously R-404 shipped the Masked / Pattern Input Suite (components/masked-input.tsx); a
  genuinely functional token-based masked text input (9=digit, A=letter, *=alphanumeric, other chars =
  literals) applied in real time, with built-in presets (phone/date/card/time/ssn via PRESET_MASKS) and
  custom masks; an applyMask() producing both the formatted display value and the raw (unmasked) value plus
  a completeness flag; caret kept at end after reformat (requestAnimationFrame + setSelectionRange);
  controlled + uncontrolled value; onChange (formatted, raw) and onComplete callbacks; inputMode pass-through
  for mobile keyboards; WAI-ARIA (aria-label, aria-required when required); 4 futuristic variants ("default",
  "card", "glass", "neon"); 3 size scales ("sm", "md", "lg"); React ref forwarding (forwardRef);
  useImperativeHandle (MaskedInputHandle: getValue/getRawValue/setValue/clear/focus); canonical TypeScript
  types (MaskedInputVariant, MaskedInputSize, MaskedInputPreset, MaskedInputResult, MaskedInputHandle,
  MaskedInputProps); compound and semantic alias exports (MaskedInput, InputMask, PatternInput,
  FormattedInput, default export) with explicit displayName; 100% diff-invariance across ir.description, 0
  external runtime dependencies; exported render_masked_input_component in codegen; registered
  components/masked-input.tsx in NextjsWebAdapter; 2,556 tests pass (18 new tests in
  test_masked_input_component.py); 0 model calls. Previously R-403 shipped the Password Strength Meter &
  Requirements Suite (components/password-strength.tsx); a genuinely functional password field with live rule-based strength
  evaluation (empty/weak/fair/good/strong levels from passed-rule ratio), a 4-segment strength bar, a live
  requirements checklist (default rules: min length, uppercase, lowercase, number, symbol — overridable via
  a rules prop of {id,label,test}), a show/hide password toggle (aria-pressed), controlled + uncontrolled
  value, onChange + onStrengthChange callbacks, SSR-safe rendering with a JS prefers-reduced-motion guard on
  the bar transition, WAI-ARIA semantics (role="status" + aria-live strength text, aria-describedby wiring
  the input to the strength + requirements via useId), 4 futuristic variants ("default", "card", "glass",
  "neon"), 3 size scales ("sm", "md", "lg"), React ref forwarding (forwardRef), useImperativeHandle
  (PasswordStrengthHandle: getValue/setValue/getStrength/clear/focus), canonical TypeScript types
  (PasswordStrengthVariant, PasswordStrengthSize, PasswordStrengthLevel, PasswordRule, PasswordStrengthResult,
  PasswordStrengthHandle, PasswordStrengthProps), compound and semantic alias exports (PasswordStrength,
  PasswordStrengthMeter, PasswordInput, PasswordField, default export) with explicit displayName; 100%
  diff-invariance across ir.description, 0 external runtime dependencies; exported
  render_password_strength_component in codegen; registered components/password-strength.tsx in
  NextjsWebAdapter; 2,538 tests pass (18 new tests in test_password_strength_component.py); 0 model calls.
  Previously R-402 shipped the Cookie Consent & Preferences Manager Suite (components/cookie-consent.tsx); a genuinely functional (not decorative) consent banner with a compact view
  (Accept all / Reject all / Customize) and an expandable per-category preferences view with role="switch"
  toggles (required categories forced on + disabled); localStorage persistence (getItem/setItem under a
  configurable storageKey, try/catch-wrapped) so returning visitors are not re-prompted; SSR-safe (renders
  null until a mounted flag flips, real storage read only after mount to avoid hydration mismatch);
  configurable categories, title/description, optional privacy-policy link, and button labels; forceShow
  override; onAccept/onReject/onChange callbacks; WAI-ARIA semantics (role="region" + aria-label, role="switch"
  toggles with aria-checked); 5 placements (bottom/top/bottom-left/bottom-right/center); 4 futuristic variants
  ("default", "card", "glass", "neon"); 3 size scales ("sm", "md", "lg"); React ref forwarding (forwardRef);
  useImperativeHandle (CookieConsentHandle: open/close/accept/reject/getConsent/reset); canonical TypeScript
  types (CookieConsentVariant, CookieConsentSize, CookieConsentPosition, ConsentCategory, ConsentState,
  CookieConsentHandle, CookieConsentProps); compound and semantic alias exports (CookieConsent, ConsentBanner,
  CookieBanner, ConsentManager, default export) with explicit displayName; 100% diff-invariance across
  ir.description, 0 external runtime dependencies; exported render_cookie_consent_component in codegen;
  registered components/cookie-consent.tsx in NextjsWebAdapter; 2,520 tests pass (18 new tests in
  test_cookie_consent_component.py); 0 model calls. Previously R-401 shipped the Countdown Timer, Stopwatch &
  Live Clock Suite (components/countdown.tsx); three modes — countdown (to a targetDate or fixed duration), stopwatch, and
  live clock (12h/24h) — driven by a real setInterval tick with SSR-safe mounting (deterministic first
  paint via a mounted flag; real time via Date.now() only after mount to avoid hydration mismatch);
  day/hour/minute/second unit segments with optional labels and configurable separator, autoStart,
  controlled + uncontrolled paused, onComplete + onTick callbacks, a JS prefers-reduced-motion guard that
  disables the per-tick transition, WAI-ARIA semantics (role="timer", aria-atomic, visually-hidden
  aria-live completion announcement), 4 futuristic variants ("default", "card", "glass", "neon"), 3 size
  scales ("sm", "md", "lg"), React ref forwarding (forwardRef), useImperativeHandle (CountdownHandle:
  start/pause/reset/restart/getTime/isRunning), canonical TypeScript types (CountdownVariant, CountdownSize,
  CountdownMode, CountdownHandle, CountdownProps), compound and semantic alias exports (Countdown,
  CountdownTimer, Stopwatch, LiveClock, default export) with explicit displayName; 100% diff-invariance
  across ir.description, 0 external runtime dependencies; exported render_countdown_component in codegen;
  registered components/countdown.tsx in NextjsWebAdapter; 2,502 tests pass (18 new tests in
  test_countdown_component.py); 0 model calls. Previously R-400 shipped the Before/After Image Comparison
  Slider Suite (components/image-comparison.tsx); a genuinely interactive (non-cosmetic) two-layer image comparison
  revealer: an "after" base layer with a "before" layer clipped via CSS clip-path, a draggable divider with
  pointer capture, click/tap-to-position on the track, and a role="slider" handle with full keyboard control
  (Arrow keys by step, Home/End to 0/100, PageUp/PageDown by 10); horizontal and vertical orientations;
  controlled + uncontrolled position with onChange; optional before/after labels; gradient placeholder layers
  when no beforeSrc/afterSrc provided; disabled state; WAI-ARIA 1.2 semantics (role="group" container,
  role="slider" handle with aria-valuemin/max/now/valuetext/orientation); 4 futuristic variants
  ("default", "card", "glass" with backdropFilter blur, "neon" cyan glow), 3 size scales ("sm", "md", "lg"),
  React ref forwarding (forwardRef), useImperativeHandle (ImageComparisonHandle: setPosition/getPosition/reset),
  canonical TypeScript types (ImageComparisonVariant, ImageComparisonSize, ImageComparisonOrientation,
  ImageComparisonHandle, ImageComparisonProps), compound and semantic alias exports (ImageComparison,
  BeforeAfterSlider, CompareSlider, ImageReveal, default export) with explicit displayName; 100% diff-invariance
  across ir.description, 0 external runtime dependencies; exported render_image_comparison_component in codegen;
  registered components/image-comparison.tsx in NextjsWebAdapter; 2,484 tests pass (18 new tests in
  test_image_comparison_component.py); 0 model calls. Previously R-399 shipped the Particle Network &
  Interactive Constellation Canvas Suite (components/particle-network.tsx); an ambient, decorative-by-default animated background (distinct from the
  data-driven network-graph) with NO required data props — particles generated internally from a count/density
  derivation, animated on an HTML5 Canvas 2D requestAnimationFrame loop with edge-bounce motion, proximity link
  lines with distance-proportional globalAlpha, pointer reactivity (attraction + accent-colored cursor links within
  interactionRadius), device-pixel-ratio-aware canvas sizing, and a JS prefers-reduced-motion guard (single static
  frame, no rAF, live change listener — a net-new pattern since CSS reduced-motion cannot stop a canvas loop);
  imperative ParticleNetworkHandle (pause, resume, toggle, restart, isPaused, getCanvas) via useImperativeHandle;
  WAI-ARIA decorative semantics (wrapper role="img" + aria-label, aria-hidden="true" canvas, no focus trap / not
  keyboard-interactive); 4 futuristic visual styling variants ("default", "card", "glass" with backdropFilter blur,
  "neon" cyan glow); 3 size scales ("sm", "md", "lg"); React ref forwarding (forwardRef); canonical TypeScript types
  (ParticleNetworkVariant, ParticleNetworkSize, ParticleNetworkHandle, ParticleNetworkProps); compound and semantic
  alias exports (ParticleNetwork, ConstellationCanvas, ParticleField, StarfieldBackground, default export) with
  explicit displayName; 100% diff-invariance across ir.description, 0 external runtime dependencies; exported
  render_particle_network_component in codegen; registered components/particle-network.tsx in NextjsWebAdapter;
  2,466 tests pass (18 new tests in test_particle_network_component.py); 0 model calls
control_plane_database: postgresql
model_routing_mode: balanced
architecture_contract: R_&_D/OmniStackAI_Implementation_Brief_v6.md
master_spec: R_&_D/OmniStackAI_OS_Master_Architecture_Specification.md
tracker: R_&_D/OmniStackAI_Execution_Tracker_v6.xlsx
product_direction: >-
  Building the actual Emergent-class product per the brief, one Tracker ID at a time. The model fabric
  (R-005..R-223) is the engine; the product vertical slice starts here: Application IR (R-225) ->
  framework adapter contract -> Next.js adapter -> Git service -> preview/sandbox (needs cloud env).
id_note: >-
  Workbook backlog owns R-010..R-219. Founder-requested tasks use IDs after R-219: R-220..R-223 done,
  R-224 Next.js deferred (env-blocked), R-225..R-415 done, R-416..R-429 done (front-door bricks 1-4,
  generated-SQL hardening, embedded trusted-local preview, collision-free preview ports + controls, build
  history + re-preview, live preview status, per-build repo actions, remove-from-history, generated-JSX
  inline-style fix, generated-app compile fixes + opt-in tsc gate, and strict-type cleanup so generated apps
  pass tsc --noEmit clean), R-430 done (Ecosystem Scope Compiler = FIRST spine brick), R-431 done
  (Scope -> Application IRs = SECOND spine brick: one prompt -> multiple owned, clean-compiling app repos),
  R-432 done (opt-in unknown-domain refinement = THIRD spine brick), R-433 done (surface-specific
  data/capability scoping = FOURTH spine brick), R-434 done (immutable baseline Solution Pack registry
  = FIFTH spine brick), R-435 done (target-aware pack recommendations in planning = SIXTH spine brick), and
  R-436 done (pinned declarative customization manifests = SEVENTH spine brick), and R-437 done
  (deterministic allowlisted pack configuration application = EIGHTH spine brick).
  NOTE: the UI-COMPONENT SERIES is PAUSED at R-415 (110 components, resumable at a future free Tracker
  ID); R-416..R-429 built/hardened the "chat -> create -> run -> preview" front door; R-430..R-437 build SPINE.
blockers: []
next_action: >-
  R-468 is complete: the Studio now supports multi-turn "continue editing this app" - a generic, validated
  additive delta (intake/app_delta.py) merged onto the app's tracked IR (studio/session.py's
  StudioSessionStore) and applied as a real new git commit via the UNCHANGED edit/diff.py + edit/apply.py.
  NEXT R-469: wire R-466's toolchain-dependent compile_and_repair into the edit flow (so an edited hybrid-UI
  screen also gets type-checked/repaired), and/or an undo/revert UI over the real git history that now
  exists on disk for every edited build (git log/git revert already work from a terminal; nothing exposes
  them in the Studio yet). Consider extending app_delta beyond additive-only (rename/remove existing
  entities) as a later, separately-scoped follow-up - real design needed, higher risk of a confusing diff.
  Founder action to unlock a full live hybrid-UI/delta proof: after the Groq daily reset, or add
  GOOGLE_API_KEY (Gemini) to the gitignored .env.
  Keep task verify model/Docker/DB/install/network-free. The UI-component series remains paused/resumable; the
  tracker workbook still lags (ends at R-462).
