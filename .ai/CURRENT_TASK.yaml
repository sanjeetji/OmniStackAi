task_id: R-491
status: completed
phase: MVP
tracker_row: null
started_at: "2026-09-19"
completed_at: "2026-09-19"
objective: >-
  First of the six-task Console UI overhaul (R-491..R-496), approved by the founder after asking
  why the platform UI is "just simple and very ugly" ("I need proper best UI based platform not
  just simple a page"; reference bar: Emergent/Lovable/Dyad). Foundation only: retire the R-470
  scripts/test.sh gate that blocks UI dependencies (founder sign-off recorded), install Tailwind
  v4 (@tailwindcss/postcss, per Next 16's own bundled guide) + shadcn/ui (vendored primitives) +
  next-themes (dark-first) + lucide-react, load Geist/Geist Mono via next/font/google, replace the
  token system with an OKLCH dark-first scale (one desaturated accent, tinted shadows, real type
  scale) while re-pointing the legacy CSS variables so every existing class still renders. No
  page markup changes - every route must build and respond exactly as before. Screen redesigns
  are R-492..R-496.
allowed_paths:
  - apps/console-web/package.json
  - pnpm-lock.yaml
  - apps/console-web/postcss.config.mjs
  - apps/console-web/components.json
  - apps/console-web/components/ui/
  - apps/console-web/lib/utils.ts
  - apps/console-web/app/globals.css
  - apps/console-web/app/layout.tsx
  - apps/console-web/app/theme-provider.tsx
  - apps/console-web/app/icon.svg
  - apps/console-web/app/fabric/page.tsx
  - apps/console-web/tsconfig.json
  - apps/console-web/.gitignore
  - scripts/test.sh
  - .ai/tasks/R-491.md
  - .ai/CURRENT_TASK.yaml
  - .ai/PROJECT_STATE.yaml
  - .ai/WORK_LOG.md
  - .ai/HANDOFF.md
  - PROJECT_STATE.md
  - CHANGELOG.md
  - docs/PROGRESS.md
  - docs/RESUME_PROMPT.md
  - R_&_D/OmniStackAI_Commercial_Platform_Kickoff_v1.md
forbidden_changes:
  - No page/component markup changes beyond app/layout.tsx - screen redesigns are R-492..R-496; old CSS classes must keep rendering
  - No changes to lib/control-plane.ts, lib/session.ts, or any app/api/* route
  - No agent-engine or control-plane changes
  - Do not modify or commit the untracked .claude directory
  - No real network call in any automated gate (pnpm install runs once during the task, never in task verify)
required_gates:
  - "console-web: pnpm run typecheck, pnpm run lint, pnpm run build"
  - task verify
  - task lint
  - task security:quick
  - task env:check
  - "bash scripts/test.sh (R-470 gate retired, new R-491 block passes)"
  - "live smoke: next start on 4321, curl every page route (200) + one /api route via a real cookie session; founder eyeballs localhost:4321 for the font/theme change since no browser tool exists here"
model_tool_trace:
  routing: deterministic_only
  local_calls: 0
  cloud_calls: 0
  note: >-
    Pure TypeScript/CSS/config work, 0 model calls. One-time pnpm install of the new UI
    dependencies happens during the task, never inside an automated gate.
next_action: >-
  R-491 shipped: the console now runs on Tailwind v4 + shadcn/ui (Radix, Nova preset = Lucide/
  Geist) + next-themes (dark-first) with a tinted OKLCH token system, Geist/Geist Mono self-hosted
  via next/font, a branded icon.svg, and the R-470 no-UI-dependency gate retired with the
  founder's recorded decision. Real tooling drift (shadcn CLI v4.21's new --base/--preset model)
  and a real init bug (it clobbered the legacy --muted/--accent names) were both worked through
  honestly. 23 routes build; every route live-smoked; 3,738 agent-engine tests unaffected. The
  founder is asked to eyeball localhost:4321 (no browser tool here). Next: R-492 (auth + app
  shell: login/register redesign with real validation, top nav with current-page state, user
  menu, skip-link, branded 404, home/dashboard) - and the founder's Emergent/Lovable/Dyad
  screenshots, once shared, feed R-493/R-494's Studio screens.
