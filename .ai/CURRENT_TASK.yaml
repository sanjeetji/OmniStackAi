schema_version: 1
task_id: R-226
title: Framework adapter contract and generated file-set model
status: done
phase: BASIC/MVP
founder_stage: 0
branch: ai/R-226-adapter-contract
goal: >-
  Define the platform-owned code-generation boundary: an immutable, validated in-memory generated
  file-set and a FrameworkAdapter contract that turns an Application IR into a GeneratedProject for a
  target, plus a deterministic adapter registry. Pure, offline, deterministic; no disk writes, no code
  execution.
source:
  tracker_sheet: Phase_Roadmap
  tracker_row: reconstructed; new IDs continue after the workbook backlog's R-010..R-219
  brief_sections: [3, 9, 16, 17, 24, 25, 26, 27, 46, 47, 74, 75, 77, 91, 92]
project_profile:
  repository: platform_modular_monorepo
  service: python_agent_engine
  control_plane_database: postgresql
  model_routing: balanced
complexity: L2
completion:
  implementation_commit: f27bf416e99423d281e1c4e6f3eabc363848f95f
  verified_at: "2026-09-06T20:00:00+05:30"
  verification:
    - command: task verify
      result: pass
      evidence: 125 agent-engine tests (10 new codegen-contract tests) plus all Stage 0 checks
    - command: task agent-engine:lint
      result: pass
    - command: task security:quick
      result: pass
  acceptance_results:
    - criterion: GeneratedFile is immutable and validated with a safe relative POSIX path and string content.
      result: pass
    - criterion: GeneratedProject holds a target plus path-unique, deterministically ordered files; duplicates raise a stable error; supports lookup/paths/len/merge.
      result: pass
    - criterion: FrameworkAdapter is a runtime-checkable contract; AdapterRegistry registers by target with stable duplicate/unknown errors and selection only via the registry.
      result: pass
    - criterion: GenerationTarget is a bounded enum over the MVP targets.
      result: pass
    - criterion: Offline tests cover path safety, project uniqueness/ordering, a fake adapter via the registry, and registry error paths; nothing is written to disk or executed.
      result: pass
  note: Deterministic contracts; no model call. No cloud key set; cloud_calls zero.
blockers: []
next_action: >-
  R-227 — implement the first framework adapter (Next.js web) emitting a real app's source from the IR,
  verified offline by asserting emitted file contents.
next_command: task ai:status
