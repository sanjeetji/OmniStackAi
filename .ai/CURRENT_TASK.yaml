schema_version: 1
task_id: R-224
title: Next.js console upgrade
status: deferred
phase: BASIC/MVP
founder_stage: 0
branch: ai/R-224-console-nextjs
goal: >-
  Upgrade the R-222 static platform console to a real Next.js (App Router, TypeScript) app under
  apps/console-web, preserving the overview.json data contract and design, with security headers and a
  production build.
source:
  tracker_sheet: Phase_Roadmap
  tracker_row: reconstructed; new IDs continue after the workbook backlog's R-010..R-219
  brief_sections: [8, 40, 41, 44, 74, 91, 92]
project_profile:
  repository: platform_modular_monorepo
  service: console_web
  control_plane_database: postgresql
  model_routing: balanced
complexity: L2
status_reason: >-
  DEFERRED — this sandbox cannot install the Next.js toolchain: pnpm install for next@15.5.4 repeatedly
  times out fetching the native SWC binary (@next/swc-darwin-arm64), attempted three times including a
  standalone install with a 10-minute fetch timeout. Without a completed install there is no next build
  evidence, and committing an un-installable app would break task bootstrap. Per "record real command
  evidence — never claim unexecuted tests", this is Deferred, not Done. No application code was
  committed. The R-222 dependency-free static console remains the working, verified slice.
resume_plan:
  - In an environment with reliable npm registry access, scaffold the Next.js app under apps/console-web
    (package.json next/react, tsconfig, next.config.mjs security headers, app/, lib/ importing data/overview.json).
  - pnpm install then next build for real evidence.
  - Evolve task bootstrap from --offline to a normal frozen install; add task console:build/dev; keep task verify independent of the web build.
model_tool_trace:
  routing: local_only
  local_provider: ollama
  configured_model: qwen2.5-coder:14b
  local_calls: 0
  cloud_calls: 0
  note: No model call; no application code committed (install could not complete).
blockers:
  - Next.js SWC binary install times out in this sandbox; needs an environment with reliable registry access.
next_action: >-
  All safe founder-requested items are complete (R-220 streaming, R-221 fallback, R-222 console slice,
  R-223 env fallback wiring). R-224 (Next.js upgrade) is deferred to a network-capable environment.
  Native mobile / device-cloud remains deferred per Brief 25/91. Confirm the next Tracker ID.
next_command: task ai:status
