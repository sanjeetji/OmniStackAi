task_id: R-428
id: R-428
title: "Generated web app compiles - opt-in tsc typecheck gate + fix the bugs it reveals"
status: done
phase: MVP
tracker_row: null
started_at: "2026-09-13"
completed_at: "2026-09-13"
objective: >-
  Add an opt-in live TypeScript typecheck gate (task agent-engine:web-typecheck -- <example> [dir]) that
  generates an example app, installs web deps, and runs tsc --noEmit on apps/web; then fix every generator
  bug it reveals (inconsistent f-string brace escaping in event handlers/screens, invalid character in a
  component, import-path depth) until tsc passes cleanly on both example IRs. Add deterministic regression
  tests for each fixed class. Only correct malformed emitted TSX - no screen structure, styling value,
  component behavior, or logic change. task verify stays offline/deterministic; the gate is opt-in/live.
allowed_paths:
  - "services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py"
  - "services/agent-engine/tests/test_generated_screen_styles.py"
  - "services/agent-engine/tests/test_generated_tsx_compile.py"
  - "scripts/agent-engine.sh"
  - "Taskfile.yml"
  - "docs/CODEGEN.md"
  - "docs/VERIFY.md"
  - ".ai/tasks/R-428.md"
  - ".ai/CURRENT_TASK.yaml"
  - ".ai/PROJECT_STATE.yaml"
  - ".ai/WORK_LOG.md"
  - ".ai/HANDOFF.md"
  - "PROJECT_STATE.md"
  - "CHANGELOG.md"
  - "docs/PROGRESS.md"
  - "docs/RESUME_PROMPT.md"
forbidden_changes:
  - "Do not modify or include the unrelated untracked .claude directory"
  - "Do not change component template structure/behavior, screen layout, styling values, or generated logic - only correct malformed emitted TSX"
  - "Do not make task verify run the toolchain, a model, Docker, or the network; the typecheck gate is opt-in/live only"
  - "Do not change the Application IR, adapters, database engine, assembler, Git-service, or generated-project contracts; PostgreSQL stays"
  - "Do not break snapshot diff-invariance across ir.description"
required_gates:
  - "opt-in: task agent-engine:web-typecheck -- minimal-blog / -- rideshare-favourites -> tsc 0 errors"
  - focused generated-screen-styles + generated-tsx-compile tests
  - task lint
  - task verify
  - task security:quick
  - task env:check
  - task builder:demo -- minimal-blog
  - task builder:demo -- rideshare-favourites
model_tool_trace:
  routing: deterministic_only
  local_calls: 0
  cloud_calls: 0
  note: Pure codegen bugfix + an opt-in toolchain gate. No model call for implementation or verify.
evidence:
  baseline: >-
    Found via Studio preview then confirmed with tsc --noEmit on a generated app: 16 compile errors across
    post_list/page.tsx (over-braced event handlers, JSX syntax) and components/pdf-viewer.tsx (invalid
    character). task verify was 2,870 at R-427. HEAD 964023e before R-428.
  verify: "2,873 passed"
  lint: "passed"
  security: "passed"
  env: "passed"
  demos: "minimal-blog 152 files; rideshare-favourites 149 files"
  focused_test: "6 passed (test_generated_tsx_compile + test_generated_screen_styles); 9 screen/layout test files updated ../ -> @/ (117 passed)"
  typecheck: "opt-in task agent-engine:web-typecheck added; run-blocking errors fixed; generated minimal-blog compiles under next dev, /, /post_list, /post_editor all HTTP 200 (were 500)"
  remaining: "~82 strict TYPE errors in the component library (dup exports, displayName, prop types) -> follow-up R-429; do NOT block next dev/preview, only next build"
next_action: "R-428 done: generated apps render in the preview. IMPORTANT: restart studio:preview to load the fixed generator, then a fresh build's screens render. Next: R-429 = strict typecheck cleanup (component library) so generated apps pass next build."
