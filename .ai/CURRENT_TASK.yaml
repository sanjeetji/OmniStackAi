task_id: R-539
status: in_progress
phase: MVP
tracker_row: 8
started_at: "2026-09-23"
completed_at: null
branch: ai/R-539-careclinic-doctor-api
objective: >-
  Rebuild the CareClinic doctor workstation (apps/doctor) against the real API, the way R-538 did
  the clinic console: every screen reads live clinic data, the consultation writes real SOAP notes,
  prescriptions and lab orders, and diagnosis coding searches a real ICD-10 catalogue instead of a
  list inside the app. Then give the platform itself the two operational commands it lacked —
  `omnistack.sh fresh` (wipe local data and create the first owner) and `omnistack.sh admin`
  (administer accounts) — and document every command in COMMANDS.md.
allowed_paths:
  - templates/catalog/care-clinic/
  - services/control-plane/cmd/platformctl/
  - scripts/omnistack.sh
  - scripts/test.sh
  - services/agent-engine/tests/test_template_care_clinic.py
  - COMMANDS.md
  - README.md
  - docs/
  - .ai/
  - PROJECT_STATE.md
  - CHANGELOG.md
  - R_&_D/OmniStackAI_Template_Marketplace_Plan_v1.md
forbidden_changes:
  - No new template dependency beyond the pinned stack
  - task verify stays offline with 0 model calls
  - No real credentials in the template or the repository
  - platformctl must reuse the control-plane's own password hashing, never its own
required_gates:
  - Every doctor page reads live API data; no hard-coded table anywhere in apps/doctor
  - The consultation chain writes real records (SOAP, diagnosis, prescription, lab order)
  - Diagnosis coding searches a seeded ICD-10 catalogue
  - API unit tests and the live cross-app workflow tests pass
  - 48 screenshots recaptured from the running template
  - omnistack.sh fresh wipes and creates an owner who can sign in to the console
  - task verify passes
model_tool_trace:
  routing: deterministic_only
  local_calls: 0
  cloud_calls: 0
blockers: []
found_and_fixed:
  - "R-537 apps/doctor: every screen rendered hard-coded state. Only the dashboard, queue and
     patient chart called the API at all, and they overlaid mock values on the response. Rewritten
     against the API."
  - "The doctor earnings query joined invoices directly; a visit with both a consultation and a
     lab invoice counted its fee twice (gross 24,500 against a daily sum of 14,000). Replaced the
     join with a LATERAL that picks one invoice per appointment."
  - "R-536 shared types: Consultation, Diagnosis, Prescription, PrescriptionItem and LabTest named
     columns the API never returns, so the patient's prescription slip showed a blank medicine,
     form and dose on every real prescription."
found_not_fixed: []
