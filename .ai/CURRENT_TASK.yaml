schema_version: 1
task_id: R-287
title: Race-Safe Generated Detail Refetches
status: in_progress
phase: BASIC/MVP
founder_stage: 0
branch: main
goal: >-
  Make generated use<Entity> detail hooks race-safe so rapid record selection, deep-link, prev/next, or
  ID changes cannot let an older GET response overwrite the currently selected record's state. Reuse the
  proven R-286 LIST_BY AbortController design (signal passed after caller options), scoped to the detail
  hook only.
tracker_ref: "R-287"
requirements:
  - Each generated use<Entity> detail hook owns an AbortController ref (useRef, already imported).
  - refetch aborts the previous detail request before handling an empty/missing record ID.
  - Clearing the ID cancels in-flight work and resets record/loading/error state (setData(null), setError(null), setLoading(false)).
  - A valid ID creates and registers a new controller.
  - Pass the internally owned signal after caller-provided options so callers cannot replace it.
  - An aborted successful response must not update current record state.
  - AbortError and controller-aborted failures must not surface as user errors.
  - Only the currently active request may clear loading state.
  - Effect cleanup aborts the active request on ID change or unmount.
  - Preserve public hook name, params, return value, and current generated API-client behaviour.
  - Do not alter top-level list (useList<Entities>) or LIST_BY (useList<Child>By<Parent>) hooks.
  - Preserve deterministic generation and description-only IR stability.
acceptance_criteria:
  - The use<Entity> hook emits `const abortRef = useRef<AbortController | null>(null);` and `abortRef.current?.abort();` before the `if (!id)` reset.
  - The missing-ID branch resets setData(null), setError(null), setLoading(false).
  - The GET call is `api.get<Entity>(id, { ...options, signal: controller.signal })` (signal after options); the old `api.get<Entity>(id, options)` form is gone.
  - Success guarded by `if (controller.signal.aborted) return;` before setData; catch guarded by the AbortError check; finally clears loading only when not aborted.
  - Effect cleanup returns `() => abortRef.current?.abort();`.
  - List and LIST_BY hook output is unchanged; description-only IR changes remain byte-identical.
  - Focused R-287 tests, relevant detail/hook tests, full offline verification, lint, security, env check, and both builder demos pass.
expected_impact:
  - services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py
  - services/agent-engine/tests/test_detail_request_cancellation.py
  - services/agent-engine/tests/test_nextjs_hooks.py
  - .ai task/state/handoff/work-log files, PROJECT_STATE.md, CHANGELOG.md, docs/CODEGEN.md, docs/PROGRESS.md, docs/RESUME_PROMPT.md, and execution tracker
allowed_paths:
  - services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py
  - services/agent-engine/tests/test_detail_request_cancellation.py
  - services/agent-engine/tests/test_nextjs_hooks.py
  - .ai/**
  - CHANGELOG.md
  - PROJECT_STATE.md
  - docs/**
  - R_&_D/OmniStackAI_Execution_Tracker_v6.xlsx
forbidden_paths:
  - services/agent-engine/src/omnistackai_agent_engine/application_ir/**
  - services/agent-engine/src/omnistackai_agent_engine/codegen/data_access.py
  - services/agent-engine/src/omnistackai_agent_engine/codegen/backend_python.py
  - services/agent-engine/src/omnistackai_agent_engine/codegen/backend_go.py
  - services/agent-engine/src/omnistackai_agent_engine/codegen/openapi.py
  - package.json
  - pnpm-lock.yaml
must_not_change:
  - PostgreSQL, platform schemas/migrations, or backend GET semantics
  - Section 74 monorepo layout, provider boundaries, infrastructure, or native/mobile scope
  - Application IR schema, generated API-client behaviour, or public hook signatures
  - useList<Entities> (R-280) and useList<Child>By<Parent> (R-286) hook behaviour
gates:
  - focused detail cancellation tests
  - test_nextjs_hooks / test_detail_screen_lifecycle / test_detail_record_selector / test_subcollection_screens
  - task verify
  - task lint
  - task security:quick
  - task env:check
  - task builder:demo -- minimal-blog
  - task builder:demo -- rideshare-favourites
  - generated TypeScript artifact inspection
model_tool_trace:
  routing: deterministic_only
  local_calls: 0
  cloud_calls: 0
  note: Pure deterministic code generation and offline tests; no model call is needed.
evidence: []
blockers: []
next_action: >-
  Write focused failing tests, apply the smallest generator change to the use<Entity> block, run all
  gates, update the one existing detail-hook assertion, then tracker + docs + two commits + push.
next_command: >-
  env PYTHONPATH=services/agent-engine/src python3 -m unittest discover -s services/agent-engine/tests -p 'test_detail_request_cancellation.py' -v
