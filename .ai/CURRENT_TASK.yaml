schema_version: 1
task_id: R-221
title: Cross-provider fallback and circuit breaking for the model gateway
status: in_progress
phase: BASIC/MVP
founder_stage: 0
branch: ai/R-221-cross-provider-fallback
goal: >-
  Add explicit, allowlist-driven cross-provider fallback and per-provider circuit breaking to the
  Balanced gateway. An operator configures an ordered fallback chain of model descriptors; on a
  retriable failure (provider unavailable, timeout, or HTTP error) the gateway advances to the next
  allowed candidate that is registered and whose circuit is closed, never silently and never outside
  the configured chain. A per-provider circuit breaker opens after repeated failures and recovers
  after a cooldown. Accounting records each attempt. Default behavior (no fallback configured) is
  unchanged.
source:
  tracker_sheet: Phase_Roadmap
  tracker_row: reconstructed; new model-fabric IDs continue after the workbook backlog's R-010..R-219
  brief_sections: [18, 18.3, 25, 26, 27, 74, 84, 89, 90, 91, 92]
  founder_intent: >-
    Founder asked to complete both the console slice and cross-provider fallback, one by one. R-221 is
    fallback; the Next.js console slice follows as R-222.
project_profile:
  repository: platform_modular_monorepo
  service: python_agent_engine
  control_plane_database: postgresql
  model_routing: balanced
complexity: L2
expected_impact:
  - services/agent-engine/src/omnistackai_agent_engine/model_gateway/resilience.py
  - services/agent-engine/src/omnistackai_agent_engine/model_gateway/gateway.py
  - services/agent-engine/src/omnistackai_agent_engine/model_gateway/errors.py
  - services/agent-engine/src/omnistackai_agent_engine/model_gateway/__init__.py
  - services/agent-engine/tests/test_fallback.py
  - docs/MODEL_PROVIDER.md
  - portable state, changelog, handoff, task doc, and tracker evidence
allowed_paths:
  - .ai/**
  - CHANGELOG.md
  - PROJECT_STATE.md
  - R_&_D/OmniStackAI_Execution_Tracker_v6.xlsx
  - docs/MODEL_PROVIDER.md
  - services/agent-engine/src/omnistackai_agent_engine/model_gateway/**
  - services/agent-engine/tests/**
must_not_change:
  - PostgreSQL as the control-plane database or the existing schema
  - the R-005 records/registry, R-006 Ollama adapter, R-008 cloud adapters, R-009 accounting, or R-220 streaming public behavior
  - the existing single-provider routing outcome when no fallback chain is configured
  - local Compose services or infrastructure
  - auth, organizations, billing, Git integration, audit, or business APIs
  - native mobile or device-cloud functionality
  - the Section 74 top-level monorepo contract; no new top-level directory
  - the network-independence of static task verify
must_not_do:
  - fall back outside the explicitly configured allowlist chain (never silent)
  - fall back on non-retriable errors (budget, unknown model, unsupported request)
  - make any real cloud API call; add an external dependency or provider SDK; store secrets
acceptance:
  - RoutingPolicy accepts an optional ordered fallback chain of model descriptors; with none configured the gateway behaves exactly as before (single provider).
  - On a retriable failure the gateway advances to the next allowlisted candidate that is registered and whose circuit is closed; a non-retriable error is raised immediately without fallback.
  - A per-provider circuit breaker opens after a configurable consecutive-failure threshold, skips the provider until a cooldown elapses, then half-opens; success resets it. The breaker uses an injectable clock and is thread-safe.
  - When every candidate is unavailable, skipped, or failing, the gateway raises a stable AllProvidersFailedError; it never downgrades high-risk work or leaves the chain.
  - Each dispatch attempt (success or failure) is recorded by the accounting ledger when a recorder is configured.
  - Standard-library offline tests cover fallback success, non-retriable no-fallback, circuit open/skip/recover, all-failed, ordering, and accounting of attempts, with zero network access.
  - Static task verify stays network-independent and green.
required_gates:
  - impact_analysis
  - python_compile_test
  - fallback_ordering_and_retriable_classification_tests
  - circuit_breaker_open_skip_recover_tests
  - all_providers_failed_and_accounting_tests
  - vendor_import_exclusion
  - secret_exclusion_check
  - task_verify
  - blast_radius_check
  - tracker_evidence
model_tool_trace:
  routing: local_only
  local_provider: ollama
  configured_model: qwen2.5-coder:14b
  local_calls: 0
  cloud_calls: 0
  note: Fallback and circuit breaking are deterministic; exercised offline with fake providers. No cloud key set.
completion:
  implementation_commit: null
  verified_at: null
  verification: []
  acceptance_results: []
blockers: []
next_action: >-
  Implement the fallback chain and circuit breaker, integrate into the gateway, add offline tests, run
  the gates, record completion evidence, then start R-222 (Next.js console slice).
next_command: task agent-engine:test
