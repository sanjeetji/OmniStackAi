task_id: R-494
status: completed
phase: MVP
tracker_row: null
started_at: "2026-09-19"
completed_at: "2026-09-19"
objective: >-
  Fourth task of the Console UI overhaul (R-491..R-496): rebuild the four Studio tabs on the
  vendored shadcn Tabs (line variant, Lucide icons, live file/problem counts). Files becomes a
  real collapsible tree (file-tree-model.ts pure buildFileTree/ancestorsOf/countFiles +
  file-tree.tsx FileTree, dirs first, aria-expanded, selected-file ancestors expanded, no 200-item
  cap). Code becomes tree + viewer with a sticky header (path, language badge, line count, size),
  the untouched tokenizeCodeLine tokens on the existing .code-tok-* colors, tabular-nums gutter,
  skeleton loading, alert errors, honest binary/truncated notes. Problems gets the Check button
  with spinner, a result badge, empty/clean states, per-file groups whose header opens the file
  in Code, tsc "L12:5 TS2339: msg" lines parsed into chips (unparseable lines verbatim; no
  invented severities), raw output in <details>. Preview keeps its state machine byte-for-byte
  and gains a toolbar: status pill, truncated mono URL link, Open in new tab, Desktop/Tablet/Phone
  width presets (aria-pressed), Restart/Stop buttons, a taller responsive frame, skeleton starting
  state with real elapsed seconds, designed disabled/error states. Closes the R-477 degradation
  by fetching the file list on ?build= hydration (fetchBuildFiles at module scope).
  studio-icons.tsx deleted; the R-475 and R-493 assertions edited deliberately. Legacy
  tab/code/problems/preview CSS removed. No API/lib changes; no new deps.
allowed_paths:
  - apps/console-web/app/studio/studio-tabs.tsx
  - apps/console-web/app/studio/studio-preview.tsx
  - apps/console-web/app/studio/file-tree.tsx
  - apps/console-web/app/studio/file-tree-model.ts
  - apps/console-web/app/studio/studio-chat.tsx
  - apps/console-web/app/studio/studio-workspace.tsx
  - apps/console-web/app/studio/studio-icons.tsx
  - apps/console-web/app/globals.css
  - scripts/test.sh
  - .ai/tasks/R-494.md
  - .ai/CURRENT_TASK.yaml
  - .ai/PROJECT_STATE.yaml
  - .ai/WORK_LOG.md
  - .ai/HANDOFF.md
  - PROJECT_STATE.md
  - CHANGELOG.md
  - docs/PROGRESS.md
  - docs/RESUME_PROMPT.md
  - R_&_D/OmniStackAI_Commercial_Platform_Kickoff_v1.md
forbidden_changes:
  - No changes to code-highlight.ts, studio/page.tsx, studio/layout.tsx, lib/*, or any app/api/* route
  - No behavior change in the preview state machine, file read, problems check, or the chat's SSE/edit/404 logic (the hydration files fetch is the one scoped addition)
  - No new dependencies; no agent-engine or control-plane changes; no fabricated content or invented severities
  - Do not modify or commit the untracked .claude directory
required_gates:
  - "console-web: pnpm run typecheck, pnpm run lint, pnpm run build"
  - "bash scripts/test.sh (R-475 + R-493 assertions edited, new R-494 block)"
  - task verify
  - task lint
  - task security:quick
  - task env:check
  - "live smoke: next start on 4321; /studio?build=5 serves the new tabs; tree model run against the real 173-file list; real file read, real problems check (POST) and the preview route exercised through the API against the live stack; 0 server errors; founder eyeballs localhost:4321/studio?build=5 with the agent-engine in preview mode"
model_tool_trace:
  routing: deterministic_only
  local_calls: 0
  cloud_calls: 0
  note: Pure TypeScript/TSX/CSS work, 0 model calls. The smoke reused build 5 from R-493's real build; the problems check is a local tsc request (409 here - toolchain not installed in build-only mode).
next_action: >-
  R-494 shipped: the four Studio tabs are real Radix tabs with Lucide icons and live counts;
  Files is a collapsible tree (model verified against the real 173-file build: dirs first,
  alphabetical, 0 duplicates), Code is tree + viewer with a sticky header and skeleton loading,
  Problems parses tsc diagnostics per file with a click-through to Code, Preview has a real
  toolbar (status pill, URL, open-in-new-tab, Desktop/Tablet/Phone presets, Restart/Stop) over
  an unchanged state machine; the file list is fetched on ?build= hydration (R-477 degradation
  closed); studio-icons.tsx retired with the R-475/R-493 assertions edited deliberately (the
  contract tests caught the stale R-493 one). All gates green (3,738 tests); API paths for every
  tab exercised on build 5 with 0 server errors; the honest limits (no browser; build-only mode
  -> preview 404 and problems 409) are recorded. Console running detached on 4321. Next: R-495
  (Settings + Fabric): provider status as real settings cards (Ready / Needs key), the theme
  toggle in Settings (not a sun/moon switch), the fabric/cost overview with proper data
  typography on shadcn Card/Table-style markup; then R-496 (motion, states, responsive pass,
  OG/meta, legacy CSS sweep incl. .spinner/.pill and the .wrap/.masthead/.panel family).
