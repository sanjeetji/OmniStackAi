schema_version: 1
task_id: R-263
title: Interactive Screen Component Generator with Real Data Binding (apps/web/app/<screen>/page.tsx)
status: done
phase: BASIC/MVP
founder_stage: 0
branch: main
goal: >-
  Upgrade generated Next.js screen pages (apps/web/app/<screen.id>/page.tsx) into real, interactive,
  strongly-typed React client components:
  - Connects screens to matching entities and generated lib/hooks.ts (useList, useCreate, etc.).
  - Renders live collection views with search input, sort headers, loading/error states, and pagination controls.
  - Renders creation form with schema-derived inputs, validation, and submission handlers.
  - Uses "use client" directive and standard React without external styling/component libraries.
  - pure codegen change; no new platform dep, no install/build/run/DB.
source:
  tracker_sheet: Phase_Roadmap
  brief_sections: [43, 50, 74, 77, 91, 92]
  founder_note: >-
    Founder approved implementation plan for R-263.
project_profile:
  repository: platform_modular_monorepo
  service: python_agent_engine
  control_plane_database: postgresql
  model_routing: balanced
complexity: L2
expected_impact:
  - services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py
  - services/agent-engine/tests/test_screen_generation.py
  - docs/CODEGEN.md, docs/PROGRESS.md
  - portable state, changelog, handoff, task doc, tracker evidence
allowed_paths:
  - .ai/**
  - CHANGELOG.md
  - PROJECT_STATE.md
  - R_&_D/OmniStackAI_Execution_Tracker_v6.xlsx
  - docs/**
  - services/agent-engine/**
must_not_change:
  - the IR schema; no new IR field
  - network-independence of task verify
must_not_do:
  - add a platform Python dependency
  - break existing codegen contracts
  - connect to or run a database; make a network call; put a secret anywhere
acceptance:
  - Screen pages emit "use client" directive at top. (VERIFIED)
  - Screen pages import and use appropriate hooks from ../lib/hooks. (VERIFIED)
  - Collection screens render live search input bound to setSearch. (VERIFIED)
  - Collection screens render pagination controls (Previous, Next, page indicator) bound to setPage. (VERIFIED)
  - Form/editor screens render interactive creation form using useCreate hook. (VERIFIED)
  - task verify green; 0 network calls (515 tests pass). (VERIFIED)
required_gates:
  - screen_generation_test
  - task_verify
model_tool_trace:
  routing: local_only
  local_calls: 0
  cloud_calls: 0
  note: Deterministic codegen; no model call, no network, no DB. cloud_calls zero.
blockers: []
next_action: Task R-263 completed and verified. Ready for next task or commit.
next_command: task verify

