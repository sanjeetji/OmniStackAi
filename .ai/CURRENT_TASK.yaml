task_id: R-498
status: completed
phase: MVP
tracker_row: null
started_at: "2026-09-19"
completed_at: "2026-09-19"
objective: >-
  Answer the founder's platform-gap review with (a) a decision per feature, (b) an implementable
  spec per task so work can pause and resume, (c) an explanation of the two run modes, and (d) a
  single shell script to run the platform. Delivered: R_&_D/OmniStackAI_Platform_Buildout_v1.md
  (master: where we are, a build-now/gated/later/no decision for all 23 requested features, the
  GitHub mechanism, the Skills design, the dependency graph, the task table); R_&_D/specs/ with
  15 specs each carrying UI + database migration + API + flow + acceptance (F-01 projects, F-02
  preview, F-03 git, F-04 skills, F-05 secrets, F-06 AI usage, F-07 SEO, F-08 logs/chat, F-09
  database, F-10 security/tests, G-01 publish, G-02 domains, G-03 connectors, G-04 payments,
  G-05 analytics); scripts/omnistack.sh (up/down/restart/status/logs/build/doctor/verify/open,
  preview mode ON by default); Taskfile aliases and .run/ in .gitignore. No feature code.
allowed_paths:
  - R_&_D/OmniStackAI_Platform_Buildout_v1.md
  - R_&_D/specs/
  - R_&_D/OmniStackAI_Commercial_Platform_Kickoff_v1.md
  - scripts/omnistack.sh
  - Taskfile.yml
  - .gitignore
  - scripts/test.sh
  - .ai/tasks/R-498.md
  - .ai/CURRENT_TASK.yaml
  - .ai/PROJECT_STATE.yaml
  - .ai/WORK_LOG.md
  - .ai/HANDOFF.md
  - PROJECT_STATE.md
  - CHANGELOG.md
  - docs/PROGRESS.md
  - docs/RESUME_PROMPT.md
forbidden_changes:
  - No feature code (no console, control-plane or agent-engine changes)
  - No new dependencies; do not modify or commit the untracked .claude directory
required_gates:
  - "bash scripts/test.sh (new R-498 block)"
  - task verify
  - task lint
  - task security:quick
  - task env:check
  - "live: scripts/omnistack.sh down then a cold up, status, logs - all real"
model_tool_trace:
  routing: deterministic_only
  local_calls: 0
  cloud_calls: 1
  note: >-
    Planning and shell work, 0 model calls in any gate. One real streamed build was made during
    the investigation to prove the preview path works end to end (30s build, 5s preview,
    <title>Simple Note Taking</title> served from the running app).
next_action: >-
  R-498 shipped: the platform buildout plan (15 specs) and scripts/omnistack.sh are in the repo,
  so any session can resume cold. NEXT is F-01 (proposed R-499): projects persisted per user -
  control-plane projects table + agent-engine on-disk workspaces (state.json/turns.jsonl/ir.json)
  + project list, switcher and General panel, and the per-user ownership check that also closes
  today's cross-tenant read hole on /jobs/build/{id}/files. It is the prerequisite for every other
  Phase F/G task. Read R_&_D/specs/F-01-projects.md and implement it as written. Gated items still
  awaiting the founder: G-01 hosting model (recommendation: deploy from the user's GitHub to their
  own Vercel/Netlify), G-03 which connectors and their OAuth app registrations, G-04 Stripe and
  Razorpay test accounts.
