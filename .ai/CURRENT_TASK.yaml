task_id: PC-093
status: complete
phase: Platform Completion — 0 Foundation
tracker_row: null
started_at: "2026-09-26"
completed_at: "2026-09-26"
branch: platform-completion
objective: >-
  Intake repairs the model's commonest mistakes instead of rejecting the build: a schema name that
  is not a declared entity is resolved to the entity it means or dropped, and a status-change
  endpoint becomes a real workflow when the entity lists its states (or is removed when a workflow
  already exists). Every repair is reported in IntakeResult.repairs.
founder_gate: >-
  Queued by PC-047's comparison; founder on 2026-09-26: "continue task one by one without asking".
companions:
  - IntakeResult.repairs, filled by both generate_ir and its streaming twin
  - parse_ir_response_with_repairs, with parse_ir_response unchanged for existing callers
allowed_paths:
  - services/agent-engine/src/omnistackai_agent_engine/intake/
  - services/agent-engine/src/omnistackai_agent_engine/application_ir/validate.py
  - services/agent-engine/tests/
  - .ai/
  - R_&_D/Platform_Completion/
  - PROJECT_STATE.md
  - CHANGELOG.md
forbidden_changes:
  - A repair is never silent; a plan that needs nothing is unchanged
  - No lifecycle is invented for a field that lists no states
  - task verify stays offline with 0 model calls
required_gates:
  - The three real failure shapes from PC-047 now produce valid IRs
  - Live re-run of the same prompts
  - scripts/verify.sh passes
model_tool_trace:
  routing: explicit provider (live proof outside task verify)
  local_calls: 0
  cloud_calls: 6  # Gemini 3 Flash, 3 prompts x 2 rounds; task verify made 0
live_proof: >-
  Same three real prompts as PC-047, two rounds on Gemini 3 Flash: 6/6 valid (was 1/3), 9-14 s each,
  every plan carrying a lifecycle. Repairs applied: an undeclared 'json' body removed (1), an ad-hoc
  status endpoint removed because the entity already had a lifecycle (3).
blockers: []
deferred: []
found_and_fixed:
  - >-
    normalize_ir rebuilt the IR field by field and left out `capabilities` and `brand`, so every
    lifecycle a model declared was silently dropped at intake since R-566 — the model wrote the
    workflow, the parser read it, and normalize_ir threw it away. Documented as lossless. Fixed, with
    a test that checks every ApplicationIR field so a future field cannot be dropped the same way.
