task_id: R-495
status: completed
phase: MVP
tracker_row: null
started_at: "2026-09-19"
completed_at: "2026-09-19"
objective: >-
  Fifth task of the Console UI overhaul (R-491..R-496): rebuild the last two legacy screens on
  the R-491 stack. Settings becomes a real settings page - lg two-column with a sticky in-page
  nav; Account as a read-only dl (name, email, plan badge, tabular credits, role, BYOK) with the
  honest "Profile editing isn't available yet" line and no fake Save; Appearance with a new
  hydration-safe components/theme-switcher.tsx (System/Light/Dark segmented control on
  next-themes' useTheme, aria-pressed, role="group", useSyncExternalStore mounted flag - in
  Settings, not a header sun/moon switch); Model providers with the live Ready/Not ready "next
  build uses" summary, routing mode + cloud tier, and a Card per provider (tier badge, kind, mono
  default model, Active/Needs key badge and a real "Set KEY_ENV in .env" chip from
  ProviderInfo.keyEnv - never a key value). Fabric becomes a proper data page over the same
  data/overview.json snapshot (says it is regenerated by scripts/console.sh snapshot on every
  console build, shows snapshotVersion): routing ladder list with level chips, Providers and
  Price book as semantic Tailwind tables with tabular-nums and sr-only captions, Usage &
  resilience stat cards honest about the all-zero snapshot, Builder showcase cards and a
  DiffBlock that colors +/-/@@ lines. The dead legacy CSS family (266 lines) removed from
  globals.css - only .pill/.pill--accent/.spinner and the token aliases the R-475 assertion pins
  remain; .grain::before scoped from fixed to absolute. No API/lib/data changes; no deps.
allowed_paths:
  - apps/console-web/app/settings/page.tsx
  - apps/console-web/app/fabric/page.tsx
  - apps/console-web/components/theme-switcher.tsx
  - apps/console-web/app/globals.css
  - scripts/test.sh
  - .ai/tasks/R-495.md
  - .ai/CURRENT_TASK.yaml
  - .ai/PROJECT_STATE.yaml
  - .ai/WORK_LOG.md
  - .ai/HANDOFF.md
  - PROJECT_STATE.md
  - CHANGELOG.md
  - docs/PROGRESS.md
  - docs/RESUME_PROMPT.md
  - R_&_D/OmniStackAI_Commercial_Platform_Kickoff_v1.md
forbidden_changes:
  - No changes to lib/*, data/overview.json, scripts/console.sh, any app/api/* route, or the settings layout / auth gates
  - No fabricated data (no invented usage numbers, no Save that saves nothing, no key values)
  - No new dependencies; do not modify or commit the untracked .claude directory
required_gates:
  - "console-web: pnpm run typecheck, pnpm run lint, pnpm run build"
  - "bash scripts/test.sh (new R-495 block)"
  - task verify
  - task lint
  - task security:quick
  - task env:check
  - "live smoke: next start on 4321; /settings signed in (sections, theme control, live Ready summary, real keyEnv hints), /fabric signed out and in (snapshot badge, tables, ladder, showcase), no legacy selectors left in the stylesheet, other routes unchanged; 0 server errors; founder eyeballs both pages and flips the theme"
model_tool_trace:
  routing: deterministic_only
  local_calls: 0
  cloud_calls: 0
  note: Pure TypeScript/TSX/CSS work, 0 model calls; the Settings page's live provider status is the same read-only control-plane call it already made.
next_action: >-
  R-495 shipped: Settings is a real settings page (Account facts, hydration-safe three-way
  theme control, 11 live provider cards with actionable keyEnv hints and the Ready summary),
  Fabric is a proper data page over the honest snapshot (tables with captions and tabular-nums,
  stat cards, ladder, showcase diff), and the legacy CSS family is gone (266 lines). Three real
  gate catches were fixed first: the react-hooks/set-state-in-effect rule IS enabled (mounted
  flag now via useSyncExternalStore), a "*/" inside my own CSS comment broke the build, and a
  dead @media block still carried .wrap. All gates green (3,738 tests); live smoke of both pages
  with 0 server errors; console running detached on 4321. Next: R-496 (motion, states &
  polish): staggered entries on dashboard/settings cards, hover/active/focus sweep, a
  loading.tsx + error.tsx pass for the app routes, responsive check of the Studio grid on small
  screens, OG/meta (openGraph/twitter) in the root metadata, retire .pill/.pill--accent/.spinner
  + the legacy token aliases with the R-475 assertion edited deliberately, remove the stale
  "Legacy console classes" comments, and a final redesign-audit checklist recorded in the
  contract.
