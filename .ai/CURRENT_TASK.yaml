schema_version: 1
task_id: R-289
title: Debounced Live Subcollection Search in Generated Next.js Web App
status: done
phase: BASIC/MVP
founder_stage: 0
branch: main
goal: >-
  Make the generated subcollection (master-detail) search live and debounced, matching the top-level
  collection search (R-280). R-281 shipped a submit-only subcollection search form; now that R-286 made
  the useList<Child>By<Parent> hook race-safe (superseded LIST_BY requests are aborted), a live
  search-as-you-type is safe. Each subcollection gets a controlled search input backed by component state
  that commits to the hook's setSearch after a fixed 300ms debounce; the form submit still searches
  immediately. Applies to both subcollection render sites (collection master-detail and detail screen).
tracker_ref: "R-289"
requirements:
  - Each subcollection owns a controlled search input state (useState) declared next to its hook.
  - A 300ms debounce useEffect commits the trimmed input to <hook>.setSearch, guarded so it does not recommit an unchanged value.
  - The subcollection search input is controlled (value + onChange); the form onSubmit still commits immediately (Enter to search now).
  - Applies in both the collection master-detail screen and the dedicated detail screen.
  - Reuse the R-280 debounce pattern and the race-safe R-286 LIST_BY hook; no hook, IR, backend, or API-client change.
  - Preserve the sort select, filter controls (R-285), and pagination; preserve description-only IR stability.
acceptance_criteria:
  - Each subcollection emits `const <s_var>Search, set<...>Search = useState("")` and a `setTimeout(..., 300)` debounce useEffect committing to `<s_var>.setSearch`.
  - The subcollection search input uses `value={<s_var>Search}` + `onChange`, no longer `defaultValue` + FormData; the form onSubmit calls `<s_var>.setSearch(<s_var>Search.trim())`.
  - Both the collection master-detail and detail screens carry the debounced search per subcollection.
  - Entities without a subcollection emit no such state/effect; description-only IR changes remain byte-identical.
  - Focused tests, R-281 subcollection-controls tests (updated), full offline verification, lint, security, env check, and both builder demos pass.
expected_impact:
  - services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py
  - services/agent-engine/tests/test_subcollection_search_debounce.py
  - services/agent-engine/tests/test_subcollection_list_controls.py
  - .ai task/state/handoff/work-log files, PROJECT_STATE.md, CHANGELOG.md, docs/CODEGEN.md, docs/PROGRESS.md, docs/RESUME_PROMPT.md, and execution tracker
allowed_paths:
  - services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py
  - services/agent-engine/tests/test_subcollection_search_debounce.py
  - services/agent-engine/tests/test_subcollection_list_controls.py
  - .ai/**
  - CHANGELOG.md
  - PROJECT_STATE.md
  - docs/**
  - R_&_D/OmniStackAI_Execution_Tracker_v6.xlsx
forbidden_paths:
  - services/agent-engine/src/omnistackai_agent_engine/application_ir/**
  - services/agent-engine/src/omnistackai_agent_engine/codegen/data_access.py
  - services/agent-engine/src/omnistackai_agent_engine/codegen/backend_python.py
  - services/agent-engine/src/omnistackai_agent_engine/codegen/backend_go.py
  - services/agent-engine/src/omnistackai_agent_engine/codegen/openapi.py
  - package.json
  - pnpm-lock.yaml
must_not_change:
  - PostgreSQL, platform schemas/migrations, or backend semantics
  - Section 74 monorepo layout, provider boundaries, infrastructure, or native/mobile scope
  - Application IR schema, generated API-client behaviour, or the useList<Child>By<Parent> hook (R-286)
  - top-level collection search (R-280), sort, subcollection filters (R-285), and pagination behaviour
gates:
  - focused subcollection search debounce tests
  - test_subcollection_list_controls (updated) + all subcollection tests
  - task verify
  - task lint
  - task security:quick
  - task env:check
  - task builder:demo -- minimal-blog
  - task builder:demo -- rideshare-favourites
  - generated TypeScript artifact inspection
model_tool_trace:
  routing: deterministic_only
  local_calls: 0
  cloud_calls: 0
  note: Pure deterministic code generation and offline tests; no model call is needed.
evidence:
  - command: env PYTHONPATH=services/agent-engine/src python3 -m unittest discover -s services/agent-engine/tests -p 'test_subcollection_search_debounce.py'
    result: pass
    detail: 5 focused R-289 tests pass (they failed first against the pre-change submit-only form).
  - command: task verify
    result: pass
    detail: 834 agent-engine tests and all Stage 0 offline verification gates pass; R-281 subcollection-controls search assertions updated to the controlled form.
  - command: task lint
    result: pass
  - command: task security:quick
    result: pass
  - command: task env:check
    result: pass
  - command: task builder:demo -- minimal-blog
    result: pass
  - command: task builder:demo -- rideshare-favourites
    result: pass
  - command: generated TypeScript artifact inspection
    result: pass
    detail: >-
      Generated post_list subcollection carries `const [commentsSubcolSearch, setCommentsSubcolSearch] =
      useState("")` plus a 300ms setTimeout/clearTimeout debounce committing to commentsSubcol.setSearch
      (guarded against redundant recommit), and the search input is controlled (value/onChange). Tracker:
      289 unique IDs, R-289 Done, table A4:M297, dashboard formulas through row 297, no #REF!, valid XLSX;
      78 Done / 1 Deferred / 210 Not Started; MVP 78/184 (42.4%).
  - commit: b6a1af4
    detail: feat(codegen)- debounced live subcollection search [R-289]
blockers: []
next_action: >-
  R-289 is complete and verified. Next unstarted Tracker ID is R-290 (do not begin it). Generated-app
  search is now consistent (top-level + subcollection both live/debounced) and all request paths are
  race-safe. Recommended offline candidate: a further generated-app UX or robustness increment (e.g.
  optimistic UI updates with rollback, or loading skeletons) — propose with the founder at R-290 kickoff.
next_command: >-
  task ai:status
