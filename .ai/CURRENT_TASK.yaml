task_id: R-486
status: completed
phase: MVP
tracker_row: null
started_at: "2026-09-19"
completed_at: "2026-09-19"
objective: >-
  First of a five-task sequence (R-486..R-490) toward the founder's per-user isolation direction,
  chosen after research confirmed every serious 2025-2026 AI app-builder platform uses a managed
  microVM/gVisor sandbox provider rather than self-hosting Firecracker/K8s from scratch. This task:
  a new, additive SandboxHandle/SandboxLifecycleProvider contract (create/status/kill - the
  existing RuntimeProvider/PreviewPlan is a pure planner, not a real orchestrator, and stays
  untouched) plus the first real driver, E2BSandboxProvider, verified against E2B's actual
  documented REST API (POST/GET/DELETE https://api.e2b.app/sandboxes). No real API key exists in
  this environment, so every gate is offline via an injected fake HTTP transport; live-cloud
  verification is honestly marked pending a real key from the founder.
allowed_paths:
  - services/agent-engine/src/omnistackai_agent_engine/runtime/contracts.py
  - services/agent-engine/src/omnistackai_agent_engine/runtime/sandbox_http.py
  - services/agent-engine/src/omnistackai_agent_engine/runtime/e2b.py
  - services/agent-engine/src/omnistackai_agent_engine/runtime/__init__.py
  - services/agent-engine/tests/test_runtime_sandbox_http.py
  - services/agent-engine/tests/test_runtime_e2b.py
  - .env.example
  - scripts/test.sh
  - .ai/tasks/R-486.md
  - .ai/CURRENT_TASK.yaml
  - .ai/PROJECT_STATE.yaml
  - .ai/WORK_LOG.md
  - .ai/HANDOFF.md
  - PROJECT_STATE.md
  - CHANGELOG.md
  - docs/PROGRESS.md
  - docs/RESUME_PROMPT.md
forbidden_changes:
  - No changes to the existing RuntimeProvider/PreviewPlan/local.py/drivers.py - purely additive
  - No changes to studio/ - this driver is not wired into the Studio preview flow yet
  - Do not modify or commit the untracked .claude directory
  - No real API call in any automated gate
required_gates:
  - "agent-engine: task verify (0 model/network calls, fake HTTP transport stands in for E2B)"
  - task verify
  - task lint
  - task security:quick
  - task env:check
model_tool_trace:
  routing: deterministic_only
  local_calls: 0
  cloud_calls: 0
  note: >-
    Pure Python stdlib HTTP client implementation, 0 model/network calls in any automated gate.
    No real E2B_API_KEY exists in this environment - live-cloud verification is honestly deferred,
    not fabricated.
next_action: >-
  R-486 shipped: 3,680 agent-engine tests OK (22 new), repo gates all pass. E2B's real REST API
  was verified directly against docs.e2b.dev before writing any code. No real E2B_API_KEY exists
  in this environment, so live-cloud verification is honestly deferred, not faked. Next: R-487
  (Vercel Sandbox driver), R-488 (Daytona driver), R-489 (free WebContainers browser option, no
  cost/config - console-web, client-side, Node.js/frontend apps only), R-490 (provider-selection
  surface so a build can switch providers by cost/speed/smoothness, the founder's explicit ask).
