task_id: R-542
status: completed
phase: MVP
tracker_row: 9
started_at: "2026-09-24"
completed_at: "2026-09-24"
branch: ai/R-542-admin-console-reachable
objective: >-
  R-541 made every prompt assemble the admin console it had always been asking for, and the run
  plan starts it — but the Studio still shows a single preview app, so the console runs and lands
  in the user's repo while being unreachable in the product. Close that: a prompt-built project
  that has an admin console reports as a multi-app preview, so the console's existing
  MultiAppPreview switcher and proxy serve it with no console-web change. This follows the
  mechanism template previews already use: each Next app runs under its own BASE_PATH at
  /preview/<project>/<app>, and the generated next.config must honour it (today it does not, so
  the app would render at the wrong base and never load its assets).
allowed_paths:
  - services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py
  - services/agent-engine/src/omnistackai_agent_engine/localrun/plan.py
  - services/agent-engine/src/omnistackai_agent_engine/localrun/run.py
  - services/agent-engine/src/omnistackai_agent_engine/studio/preview.py
  - services/agent-engine/tests/
  - scripts/test.sh
  - .ai/
  - PROJECT_STATE.md
  - CHANGELOG.md
  - apps/console-web/data/overview.json
forbidden_changes:
  - No apps/console-web source changes — MultiAppPreview and the proxy already handle kind "multi"
  - No change to the template multi-app runner (localrun/multiapp.py); templates keep their path
  - A project with no admin console keeps exactly today's single-app preview behaviour
  - No new runtime dependency; generated projects stay on the pinned Next.js stack
  - task verify stays offline with 0 model calls
required_gates:
  - The generated next.config honours BASE_PATH, as the template apps' configs do
  - A project with an admin console reports kind "multi" with a web app, an admin app and its api
  - Each Next app is started under its own BASE_PATH and its API base points at the proxied path
  - A project without an admin console still reports a single-app preview, unchanged
  - Generated output stays byte-stable across two runs of the same IR
  - task verify passes offline with 0 model calls
model_tool_trace:
  routing: deterministic_only
  local_calls: 0
  cloud_calls: 0
blockers: []
evidence:
  - "task verify: 4,041 tests OK, offline, 0 model calls"
  - "scripts/test.sh: repository contract tests passed (new R-542 block)"
  - "next build with BASE_PATH=/preview/proj-1/web: exit 0, routes-manifest.json records that basePath"
  - "no config warnings from the pinned Next 15.5.4, so allowedDevOrigins is accepted there"
  - "12 new offline tests in tests/test_admin_console_reachable.py, three asserting the unchanged single-app path"
  - "lint and security:quick pass"
deferred:
  - R-543 the wider archetype family (storefront, blog, booking, directory) in both paths
  - R-544 guaranteed local-model fallback so the deterministic template is rarely user-visible
found_and_fixed:
  - "the generated next.config.mjs had no basePath at all, so an app served under the console's proxy would have rendered at the wrong base and never loaded its assets"
  - "the preview reported a single app even when the run plan had started two, leaving the admin console invisible in the Studio"
not_done:
  - "the live journey (prompt, build, open the preview, switch to the console in a browser) has not been run; the evidence is builds and offline tests"
considered_and_rejected:
  - "emitting omnistack.json and reusing the template multi-app runner: its validator requires a package.json for every listed app, which a Python or Go backend does not have"
