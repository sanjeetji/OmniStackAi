task_id: R-590
status: complete
phase: Platform Completion — 1 Vibe Mode live
tracker_row: null
started_at: "2026-09-26"
completed_at: "2026-09-26"
branch: platform-completion
objective: >-
  A lifecycle is operable from the screens, and only through its transitions. Web app and admin
  console detail pages and the mobile detail screen show the record's state and offer exactly the
  moves legal from it (hidden when the signed-in role cannot make them). The ordinary create/update
  endpoints stop writing the lifecycle field on every backend, and the forms stop offering it, so the
  role and from-state rules (R-566, R-589) cannot be bypassed.
founder_gate: >-
  Founder, 2026-09-26: "continue task one by one without asking"; small companion things are
  handled automatically.
companions:
  - The lifecycle field is no longer writable through create/update on Python, Go and Node
  - Web and mobile forms stop offering the lifecycle field as free text
  - Mobile entity calls use the contract paths (they called /api/<entity>s, which no backend serves)
allowed_paths:
  - services/agent-engine/src/omnistackai_agent_engine/codegen/
  - services/agent-engine/tests/
  - .ai/
  - R_&_D/Platform_Completion/
  - PROJECT_STATE.md
  - CHANGELOG.md
forbidden_changes:
  - A project without a workflow is unchanged
  - task verify stays offline with 0 model calls
required_gates:
  - Generated web and mobile apps type-check; generated backends build
  - Live run against PostgreSQL showing the bypass closed and transitions working
  - scripts/verify.sh passes
model_tool_trace:
  routing: deterministic_only
  local_calls: 0
  cloud_calls: 0
live_proof: >-
  32/32 against PostgreSQL on Python, Go, Node/Express and Node/Hono: create -> initial state even
  when the request says 'live'; an ordinary PUT changes the title but not the state; a role that may
  not move it -> 403; an illegal move -> 409; submit draft->review and publish review->live; the
  database holds the new state. Web app (list rows + detail panel), mobile app, Go, Express and Hono
  all type-check/build.
blockers: []
deferred:
  - "PC-004: Node repositories fall back to an in-memory map on any database error and report success; Python answers 500 for a malformed id."
  - "PC-011: self-registered users get role 'user', not a plan role; the admin console needs a role manager."
found_and_fixed:
  - "Create/update wrote the lifecycle field on every backend, so the transition rules could be skipped; the web and mobile forms offered it as free text."
  - "Node transitions moved state through update(); they now use a dedicated setter (otherwise they would have become no-ops)."
  - "Node update built SQL column names from the request's keys; now an explicit allow-list."
  - "Python could not create anything through its API: the model required a client-invented id, and the repository called a generator-only helper (NameError)."
  - "The mobile app called /api/<entity>s, a path no backend serves; every data screen got 404."
  - "Hono routes did not type-check: c.req.param() typed string|undefined, and R-589's c.get('user') typed never."
  - "Plans without a detail page could not operate a lifecycle anywhere; list rows now carry the moves." 
