task_id: R-526
status: completed
phase: MVP
tracker_row: null
started_at: "2026-09-21"
completed_at: "2026-09-22"
objective: >-
  RideNow template part 1/4: services/api (TypeScript on Node >= 22.18, Hono + pg), migrations,
  deterministic demo seed (generator shipped), trip state machine, dispatch with simulated drivers,
  fares/surge/promos, wallet ledger + mock payments, SSE, ratings, support, audit, OpenAPI; unit tests
  (node --test, offline) + live workflow test. Built under templates/catalog/_ride-now (hidden draft).
allowed_paths:
  - templates/catalog/_ride-now/
  - services/agent-engine/tests/test_template_ride_now.py
  - scripts/test.sh
  - .ai/
  - PROJECT_STATE.md
  - CHANGELOG.md
  - docs/PROGRESS.md
  - docs/RESUME_PROMPT.md
forbidden_changes:
  - Template stays hidden (_ride-now) until R-529
  - No real credentials; mock providers only; demo OTP only in preview
  - task verify stays offline with 0 model calls
required_gates:
  - node --test unit tests (offline, via agent-engine suite)
  - live: fresh DB migrations + seed + workflow test against the running API
  - task verify / lint / security:quick / env:check
model_tool_trace:
  routing: deterministic_only
  local_calls: 0
  cloud_calls: 0
evidence:
  typecheck: "tsc strict exit 0"
  unit: "20/20 (found and fixed a money rounding bug)"
  live: "fresh DB: migrations + seed load, ledger integrity 0 errors; workflow 4/4 against the running API"
  stage_0_verify: "task verify 3971 OK"
next_action: R-527 RideNow rider web app (apps/rider) + packages/shared
