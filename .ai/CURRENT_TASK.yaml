schema_version: 1
task_id: R-262
title: React Data-Fetching & Mutation Hooks Generator (apps/web/lib/hooks.ts)
status: done
phase: BASIC/MVP
founder_stage: 0
branch: main
goal: >-
  Generate strongly-typed, idiomatic React hooks (apps/web/lib/hooks.ts) in the generated Next.js web application:
  - useList<Entities>: loading, error, data, total count, pagination math (page, pageSize, totalPages, setPage), sorting (sort, order, setSort), search (q, setSearch), refetch.
  - use<Entity>(id): detail fetching hook with loading, error, refetch.
  - useCreate<Entity>, useUpdate<Entity>, useDelete<Entity>: mutation hooks with loading, error, and async execution.
  - useList<Entities>By<Rel>: subcollection list hook scoped by parent relation.
  - pure codegen change; no new platform dep, no install/build/run/DB.
source:
  tracker_sheet: Phase_Roadmap
  brief_sections: [43, 50, 74, 77, 91, 92]
  founder_note: >-
    Founder approved implementation plan for R-262.
project_profile:
  repository: platform_modular_monorepo
  service: python_agent_engine
  control_plane_database: postgresql
  model_routing: balanced
complexity: L2
expected_impact:
  - services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py
  - services/agent-engine/tests/test_nextjs_hooks.py
  - docs/CODEGEN.md, docs/PROGRESS.md
  - portable state, changelog, handoff, task doc, tracker evidence
allowed_paths:
  - .ai/**
  - CHANGELOG.md
  - PROJECT_STATE.md
  - R_&_D/OmniStackAI_Execution_Tracker_v6.xlsx
  - docs/**
  - services/agent-engine/**
must_not_change:
  - the IR schema; no new IR field
  - network-independence of task verify
must_not_do:
  - add a platform Python dependency
  - break existing codegen contracts
  - connect to or run a database; make a network call; put a secret anywhere
acceptance:
  - NextjsWebAdapter emits apps/web/lib/hooks.ts with "use client" directive.
  - useList<Entities> manages data, total, loading, error, page, pageSize, totalPages, setPage, setSearch, setSort, refetch.
  - use<Entity>(id) fetches entity detail when id is provided.
  - useCreate<Entity>, useUpdate<Entity>, useDelete<Entity> provide mutation triggers with loading and error states.
  - useList<Entities>By<Rel> provides scoped subcollection data fetching.
  - React imports use standard useState, useEffect, useCallback without external state libraries.
  - task verify green; 0 network calls.
required_gates:
  - nextjs_hooks_test
  - task_verify
model_tool_trace:
  routing: local_only
  local_calls: 0
  cloud_calls: 0
  note: Deterministic codegen; no model call, no network, no DB. cloud_calls zero.
blockers: []
next_action: Implement _hooks_file in codegen/nextjs.py and write test_nextjs_hooks.py.
next_command: task verify
