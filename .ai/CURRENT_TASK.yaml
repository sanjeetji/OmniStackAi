task_id: PC-003
status: complete
phase: Platform Completion — 0 Foundation
tracker_row: null
started_at: "2026-09-26"
completed_at: "2026-09-26"
branch: platform-completion
objective: >-
  The stack policy holds in code. What R-559 and R-562 already did was checked and kept: an app
  request with no stack named gets a React Native app, Flutter and native requests get React
  Native with a stated reason. The remaining gap was stacks the IR has no field for — React.js,
  Vue, Angular, Svelte, Ionic, Xamarin/MAUI, Spring, Laravel, Rails, .NET, Django/Flask, MySQL,
  MongoDB, SQLite, Firebase/Supabase — which the model silently mapped to Next.js, Python or
  PostgreSQL. The prompt is now read for them and each becomes a substitution note naming what
  was built instead, on both the single-app and ecosystem build paths.
founder_gate: >-
  Founder, 2026-09-26: forget Flutter and React.js; Next.js web/admin; app requests get React
  Native + PWA with QR; native when asked. "Continue task one by one without asking."
companions:
  - Notes for every layer (web, mobile, backend, database), not only the one named in the task
  - A layer already explained by resolve_stack is not explained twice
  - Notes only for layers that are actually built (no mobile note for a web-only project)
allowed_paths:
  - services/agent-engine/src/omnistackai_agent_engine/codegen/capabilities.py
  - services/agent-engine/src/omnistackai_agent_engine/intake/build_app.py
  - services/agent-engine/tests/
  - .ai/
  - R_&_D/Platform_Completion/
  - PROJECT_STATE.md
  - CHANGELOG.md
forbidden_changes:
  - No false alarms on ordinary words (spring sale, ruby jewellery, Maui, capacitors, javascript)
  - React Native is never reported as React.js
  - task verify stays offline with 0 model calls
required_gates:
  - Offline tests for every named stack, false-alarm cases and both build paths
  - scripts/verify.sh passes
model_tool_trace:
  routing: deterministic_only
  local_calls: 0
  cloud_calls: 0
blockers: []
deferred:
  - "The PWA + QR half of the app default is R-573 (queued at #8)."
found_and_fixed: []
