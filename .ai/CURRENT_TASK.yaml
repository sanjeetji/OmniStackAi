schema_version: 1
task_id: R-230
title: Go backend framework adapter — generate a Go net/http service from the IR
status: in_progress
phase: BASIC/MVP
founder_stage: 0
branch: main
goal: >-
  Add a third framework adapter: a GoBackendAdapter that turns an Application IR into a real,
  idiomatic Go standard-library net/http service as a GeneratedProject — go.mod, main.go (Go 1.22
  method+pattern routing + health), internal/models structs from entities, internal/handlers grouped
  by resource with 501 scaffolds and r.PathValue params, README/.gitignore/.env.example. Proves the
  one-IR -> many-targets differentiator (Next.js web + FastAPI + Go). Deterministic and offline;
  nothing installed, built, run, or written to disk. Registered via the AdapterRegistry.
source:
  tracker_sheet: Phase_Roadmap
  tracker_row: reconstructed; new IDs continue after the workbook backlog's R-010..R-219
  brief_sections: [9, 16, 17, 42, 43, 46, 47, 74, 77, 91, 92]
  founder_note: >-
    Work committed directly to main (only branch). Author solely sanjeetji; a Co-Authored-By trailer
    is added by tooling and may be stripped by the owner.
project_profile:
  repository: platform_modular_monorepo
  service: python_agent_engine
  control_plane_database: postgresql
  model_routing: balanced
complexity: L2
expected_impact:
  - services/agent-engine/src/omnistackai_agent_engine/codegen/backend_go.py
  - services/agent-engine/src/omnistackai_agent_engine/codegen/__init__.py
  - services/agent-engine/tests/test_backend_go_adapter.py
  - docs/CODEGEN.md
  - portable state, changelog, handoff, task doc, and tracker evidence
allowed_paths:
  - .ai/**
  - CHANGELOG.md
  - PROJECT_STATE.md
  - R_&_D/OmniStackAI_Execution_Tracker_v6.xlsx
  - docs/**
  - services/agent-engine/**
must_not_change:
  - PostgreSQL as the control-plane database or the existing schema
  - the model-gateway, Application IR, codegen contract, and Next.js/Python adapter public behavior (additive)
  - local Compose services or infrastructure; the Section 74 top-level contract; the offline bootstrap
  - the network-independence of static task verify
must_not_do:
  - install, build, or run the generated Go service; write generated files to disk in this task
  - add an external Python dependency to the platform; use any non-stdlib import in the generated Go
  - store secrets (generated .env.example holds placeholders only)
acceptance:
  - GoBackendAdapter implements FrameworkAdapter (target backend-go) and returns a GeneratedProject
    forming a coherent Go net/http service (go.mod, main.go, internal/models/models.go,
    internal/handlers/<segment>.go, README, .gitignore, .env.example).
  - Entities become Go structs (field types mapped; exported fields with json tags; non-required
    fields are pointers with ,omitempty). IR APIs become Go 1.22 mux routes ("METHOD /path" with
    {param}); handlers are grouped by first path segment, return 501, and read params via r.PathValue.
    main registers each route and a GET /healthz.
  - All paths are safe and deterministically ordered; generation is a pure function of the IR;
    standard-library only (platform + generated Go); nothing installed/built/run/written to disk.
  - Standard-library offline tests assert emitted paths and key contents for a representative IR,
    entity->struct mapping, route registration/params, and registry wiring.
  - task verify stays green and network-independent.
required_gates:
  - impact_analysis
  - go_emission_tests
  - struct_and_route_mapping_tests
  - registry_wiring_test
  - python_compile_test
  - vendor_import_exclusion
  - secret_exclusion_check
  - task_verify
  - blast_radius_check
  - tracker_evidence
model_tool_trace:
  routing: local_only
  local_provider: ollama
  configured_model: qwen2.5-coder:14b
  local_calls: 0
  cloud_calls: 0
  note: Deterministic codegen; no model call. No cloud key set; cloud_calls zero.
completion:
  implementation_commit: null
  verified_at: null
  verification: []
  acceptance_results: []
blockers: []
next_action: >-
  Implement the Go backend adapter and offline emission tests; run the gates; record completion. Then
  one IR emits web + Python + Go. Next real steps (sandbox/preview/deploy) need a cloud/network env.
next_command: task agent-engine:test
