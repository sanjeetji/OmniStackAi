task_id: R-434
id: R-434
title: "Versioned baseline Solution Pack registry"
status: done
phase: MVP
tracker_row: null
started_at: "2026-09-14"
completed_at: "2026-09-14"
objective: >-
  Fifth brick of the differentiating spine and first Solution Pack task. Register the already verified
  minimal-blog and rideshare-favourites Application IR examples as immutable, versioned baseline pack
  descriptors. Pin each descriptor to its canonical IR digest and assembled targets, validate registry
  integrity deterministically, and select packs by domain plus required capabilities without duplicating
  skeleton source or invoking a model/live path.
allowed_paths:
  - "services/agent-engine/src/omnistackai_agent_engine/solution_packs/__init__.py"
  - "services/agent-engine/src/omnistackai_agent_engine/solution_packs/registry.py"
  - "services/agent-engine/src/omnistackai_agent_engine/solution_packs/cli.py"
  - "services/agent-engine/tests/test_solution_packs.py"
  - "scripts/agent-engine.sh"
  - "Taskfile.yml"
  - "docs/SOLUTION_PACKS.md"
  - "docs/PROGRESS.md"
  - "docs/RESUME_PROMPT.md"
  - ".ai/tasks/R-434.md"
  - ".ai/CURRENT_TASK.yaml"
  - ".ai/PROJECT_STATE.yaml"
  - ".ai/WORK_LOG.md"
  - ".ai/HANDOFF.md"
  - "PROJECT_STATE.md"
  - "CHANGELOG.md"
forbidden_changes:
  - "Do not modify or include the unrelated untracked .claude directory"
  - "Do not copy generated skeleton files or duplicate the existing example Application IR definitions"
  - "Do not change Application IR examples, generators, adapters, R-430 through R-433 behavior, PostgreSQL, or infrastructure"
  - "No model, network, Docker, database, package install, generated-code execution, or toolchain use in task verify"
  - "No new dependency, mutable global registry, hidden fallback, secret, or unverified pack claim"
required_gates:
  - "focused test_solution_packs.py"
  - task verify
  - task lint
  - task security:quick
  - task env:check
  - task builder:demo -- minimal-blog
  - task builder:demo -- rideshare-favourites
  - "deterministic solution-pack CLI list and rideshare selection inspection"
model_tool_trace:
  routing: deterministic_only
  local_calls: 0
  cloud_calls: 0
  note: "R-434 is pure registry/validation/selection logic; no model call is authorized or needed."
acceptance_criteria:
  - "SolutionPack descriptors are frozen, structurally bounded, semantically versioned, JSON-safe, and pin an existing example IR by SHA-256 plus exact assembled targets."
  - "The registry contains minimal-blog and rideshare-favourites baseline packs without duplicating their IR or generated files."
  - "Registry construction fails closed on duplicate id/version pairs, missing examples, digest drift, target drift, invalid domains/capabilities, or malformed semantic versions."
  - "Selection is deterministic by exact domain and required-capability subset, chooses the newest valid version, and returns None rather than inventing a fallback."
  - "Loading a selected pack returns a fresh validate_ir-clean ApplicationIR and its project plan has verify ladders for every declared target."
  - "A deterministic CLI lists pack metadata and resolves a domain/capability query without model/network/live execution."
evidence:
  test_first_red: "Focused test import failed as expected: ModuleNotFoundError because solution_packs did not exist."
  focused_test: "test_solution_packs.py: 8 passed"
  verify: "2,926 passed (fully offline; +8; 0 model calls)"
  lint: "task agent-engine:lint and task lint passed"
  security: "task security:quick passed"
  env: "task env:check passed"
  demos: "minimal-blog 152 files; rideshare-favourites 149 files"
  deterministic_inspection: >-
    Registry list returned exactly two version-1.0.0 packs; rideshare + favourites selected
    rideshare-favourites@1.0.0 with its pinned digest and nextjs-web/backend-go targets.
  tracker: "No workbook row exists beyond R-358; workbook intentionally unchanged."
next_action: >-
  R-434 done. Next R-435: expose deterministic, exact-compatible Solution Pack recommendations in
  ecosystem planning without changing generated output. Report pack id/version/digest or no match; define
  the configuration/delta boundary explicitly and keep all model/live/build behavior out of verification.
