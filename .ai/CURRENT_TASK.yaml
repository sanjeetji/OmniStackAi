task_id: R-561
status: in_progress
phase: MVP
tracker_row: 28
started_at: "2026-09-25"
completed_at: null
branch: ai/R-561-verify-every-surface
objective: >-
  R-560 type-checks `apps/web` and nothing else. A generated project also ships an admin console, a
  React Native app and a backend, and none of them is ever compiled — by us or by any test.
  Measured on a freshly generated project: `apps/admin` compiles cleanly, the Python backend parses,
  and **the Go backend does not run at all**. It ships `go.mod` with no `go.sum`, its README tells
  the user to run `go run .`, and that command fails immediately with "missing go.sum entry" for
  every dependency. The preview does the same thing, so a Go backend is broken end to end — while
  the Python path gets a virtualenv and a `pip install` step the Go path was never given.
  Fix the Go backend, and extend verification to every surface so the next one cannot break in
  silence. Nothing but `apps/web` has a model-written file, so a failure anywhere else is a fault in
  our generator and is reported as one.
founder_gate: >-
  Founder on 2026-09-25: continue the roadmap in order; R-561 follows R-560.
allowed_paths:
  - services/agent-engine/src/omnistackai_agent_engine/intake/
  - services/agent-engine/src/omnistackai_agent_engine/codegen/
  - services/agent-engine/src/omnistackai_agent_engine/localrun/
  - services/agent-engine/src/omnistackai_agent_engine/verify/
  - services/agent-engine/tests/
  - scripts/
  - .ai/
  - PROJECT_STATE.md
  - CHANGELOG.md
forbidden_changes:
  - task verify stays offline with 0 model calls
  - A build is never failed because a surface could not be verified
  - Every skip states its reason, as in R-559 and R-560
  - A surface with no model-written file is never rewritten; its failures are reported as ours
  - No new dependency in a generated project
required_gates:
  - A generated Go backend starts from a clean checkout following its own README
  - The admin console, the mobile app and the backend are each compiled when the toolchain allows
  - A failure in any of them is attributed to the generator, not to the user
  - Each surface reports its own outcome rather than one combined verdict
  - task verify passes offline with 0 model calls
model_tool_trace:
  routing: deterministic_only
  local_calls: 0
  cloud_calls: 0
blockers: []
deferred: []
found_and_fixed: []
