# Claude Code Compatibility Notes

Follow `AGENTS.md` as the repository-wide contract. Use Claude-specific features only as
workflow conveniences; they must not weaken path scopes, approval gates, deterministic tests,
or the stop/resume protocol.

