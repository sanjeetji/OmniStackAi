# Deployment

No deployment exists in Founder Stage 0. Future releases must build once, promote the same
immutable digest, preserve source/toolchain/test provenance, observe health gates, and retain a
verified rollback or roll-forward path. Production changes require their risk-based approvals.

