# Repository architecture

The root layout follows Implementation Brief Section 74. `services/` names the eventual three
principal deployable boundaries. `modules/` defines logical product boundaries. `workers/`
defines future capability-specific execution targets. During R-001 these are documentation-only
placeholders, not running systems.

