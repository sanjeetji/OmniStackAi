# Changelog
 
2026-09-08  R-269  Page size selector & contextual empty state CTAs in generated Next.js screens (`apps/web/app/<screen>/page.tsx`, `apps/web/lib/hooks.ts`): NextjsWebAdapter adds configurable page size selection and contextual empty state call-to-actions; `UseListState<T>` interface declares `setPageSize: (size: number) => void;`; `useList<Entities>` and `useList<Children>By<Rel>` hooks implement `setPageSize` resetting offset to 0 and clamping page size to minimum 1, returning `pageSize` and `setPageSize` in hook state; collection screens (`_collection_screen_page`) destructure `pageSize` and `setPageSize` and render an accessible `<select id="pageSizeSelect">` dropdown (10, 25, 50, 100 per page) next to pagination buttons in footer; collection screen table empty state renders search mismatch message with interactive `Clear search` CTA button (`onClick={() => { setSearchInput(""); setSearch(""); }}`) when search is active, and `+ Create first <Entity>` CTA link when `form_screen` is available; subcollection empty states render `+ Add first <Child>` CTA link when `child_form` is available in both collection and detail views; strict diff invariance maintained across `ir.description`; pure codegen change, offline, 0 network, no DB (15 new tests, 598 total)
2026-09-08  R-268  Subcollection child item deletion & mutation feedback in master-detail views (`apps/web/app/<screen>/page.tsx`): NextjsWebAdapter adds child item deletion, confirmation dialogs, loading/disabled states, error feedback alerts, and automatic subcollection refetching to master-detail views in generated Next.js screens; `_subcollections_for_parent` sets `can_delete = True` on `SubcollectionInfo` when child entity supports `Op.DELETE`; fallback entity matching for `DELETE` endpoints lacking explicit `response_schema` (e.g. `ApiEndpoint(HttpMethod.DELETE, "/comments/{id}")`) across API client and hooks; collection screens (`_collection_screen_page`) and detail screens (`_detail_screen_page`) import and wire `useDelete<Child>` hooks from `"../lib/hooks"`; emits `handleDelete<Child>` handlers prompting confirmation dialog (`confirm("Are you sure you want to delete this <Child>?")`) with try/catch guard and triggering `<subcol>.refetch()`; child cards render styled Delete button with disabled state during mutation and `stopPropagation`; renders mutation error feedback alert banner (`{delete<Child>Error && ...}`) directly above child items if deletion fails; non-deletable subcollections omit deletion code; diff-invariant across `ir.description`; pure codegen change, offline, 0 network, no DB (13 new tests, 583 total)
2026-09-08  R-267  Foreign-key relation selectors & parent auto-population in generated Next.js forms (`apps/web/app/<screen>/page.tsx`): NextjsWebAdapter replaces raw foreign key text inputs with typed parent relation selectors (<select> dropdowns) and automatic query parameter pre-population; `_parent_relations_for_entity` helper detects `RelationKind.MANY_TO_ONE` relations and foreign key fields on child entities where parent entity has `Op.LIST`; child form screens (`_form_screen_page`) import and wire `useList<ParentPlural>()` from `"../lib/hooks"`; renders accessible `<select>` dropdowns displaying parent title/name options (`{String(item.title ?? item.name ?? item.id)}`), loading state placeholder (`Loading <parents>...` vs `Select <parent>...`), and required HTML5/ARIA attributes; displays contextual parent linkage badge (`&bull; Selected <Parent> linked`) when foreign key is selected; pre-populates foreign keys from `searchParams` on mount, supporting parameter aliases (`<field>`, `<rel>_id`, `<rel>Id`, `<rel>`); client-side required validation highlights unselected foreign keys with field-level errors; clean fallback safety for entities without relations (zero dropdowns/hooks emitted); diff-invariant across `ir.description`; pure codegen change, offline, 0 network, no DB (12 new tests, 570 total)
2026-09-08  R-266  Update/edit mode in generated Next.js forms & collection screen edit actions (`apps/web/app/<screen>/page.tsx`): NextjsWebAdapter connects generated frontend to existing backend update handlers (PUT/PATCH) and typed React mutation hooks (`useUpdate<Entity>` and `use<Entity>(id)`); form screen (`_form_screen_page`) operates in dual-mode (Create and Edit) when `Op.UPDATE` is wired, importing `use<Entity>`, `useUpdate<Entity>`, and `useSearchParams` from `"next/navigation"`, extracting `editId = searchParams.get("id")`, computing `isEdit = Boolean(editId)`, synchronizing initial data into `formData` via `useEffect`, branching submission to call `update(editId, formData)` in edit mode vs `create(formData)` in create mode, and dynamically updating headers, submit button labels, loading indicator, and success alerts; collection screen (`_collection_screen_page`) renders an "Edit" action link (`/{form_screen.id}?id=${(item as any).id}`) with `onClick={(e) => e.stopPropagation()}` to avoid toggling table row selection; subcollection master-detail view renders `+ New <Child>` link (`/{child_form.id}?{foreign_key}=${selectedId}`) when complementary form screen exists for child entity; fallback safety for entities without `Op.UPDATE` (zero update code emitted); diff-invariant across `ir.description`; pure codegen change, offline, 0 network, no DB (12 new tests, 558 total)
2026-09-08  R-265  Subcollection navigation & master-detail views in generated screens (`apps/web/app/<screen>/page.tsx`): NextjsWebAdapter connects parent collection screens to nested child subcollections; `_subcollections_for_parent` identifies relations where `rel.target_entity == parent_name` and an `Op.LIST_BY` endpoint exists; collection screens conditionally import subcollection hooks (`useList<Children>By<Rel>`) and child entity types (`import type { <Child> }`) on dedicated lines preserving substring match for parent imports; collection screen manages `selectedId` state with table row click selection and "View Details" / "Hide Details" button; renders master-detail subcollection section with live count badges (`{subcol.total}`), tabbed navigation for multi-subcollection parent entities, child item lists with key display fields, loading/error/empty states, and refetch; dedicated detail screen generator (`_detail_screen_page`) for `intent == "detail"` screens; fallback safety for entities without subcollections (zero subcollection code emitted); diff-invariant with respect to `ir.description`; pure codegen change, offline, 0 network, no DB (19 new tests, 546 total)
2026-09-08  R-264  Field-level validation & error feedback in generated Next.js forms (`apps/web/app/<screen>/page.tsx` & `apps/web/lib/api.ts`): NextjsWebAdapter emits `extractFieldErrors(error: unknown): Record<string, string>` normalizing Go backend structured errors (`{"errors": [...]}`) and FastAPI structured errors (`{"detail": [...]}`); form screens import `extractFieldErrors` and manage `fieldErrors` state (`Record<string, string>`); client-side pre-validation validates required fields, string `max_length`, numeric `min`/`max` constraints, and enum options before network requests, halting and setting `fieldErrors` on failure; submission failure extracts server errors via `extractFieldErrors(err)` and highlights inputs; inputs render red borders (`#ef4444`), `aria-invalid="true"`, and per-field error messages below inputs; reactive error clearing clears field errors on input edit (`onChange`); enum fields render interactive `<select>` dropdowns with options; reset button clears `fieldErrors` and form data; warning banner rendered when field errors exist; diff-invariant with respect to `ir.description`; pure codegen change, offline, 0 network, no DB (12 new tests, 527 total)
2026-09-08  R-263  Interactive screen component generator with real data binding (`apps/web/app/<screen>/page.tsx`) in NextjsWebAdapter: screen pages upgraded into interactive React client components with `"use client"` and React 18 hooks (`useState`); automatically matches screens to IR entities via multi-token scoring (`_match_entity`); detects screen intent (`_screen_intent`) as collection, form, or generic; collection screens bind to `useList<Entities>()` with live search input (`setSearch`), sortable headers (`setSort`), pagination controls (`setPage`, Previous/Next), loading/error/empty states, delete action (`useDelete<Entity>`) when `Op.DELETE` is wired, and navigation to complementary editor screens; form screens bind to `useCreate<Entity>()` with schema-derived inputs (checkbox for bool, textarea for text, number input for int/float, datetime-local for datetime, text for string), required indicators/attributes, submit handling (`create(formData)`), success banner, and reset/cancel controls; graceful fallback screen without broken imports when unwired; byte-identical output across IR description changes preserving diff stability; public `render_screen_page` exported in `codegen`; pure codegen change, offline, 0 network, no DB (9 new tests, 515 total)
2026-09-08  R-262  React data-fetching & mutation hooks generation (`apps/web/lib/hooks.ts`) from Application IR: NextjsWebAdapter emits strongly-typed, idiomatic React hooks using `"use client"` and React 18 built-ins (`useState`, `useEffect`, `useCallback`) with zero extra npm dependencies; exports shared interfaces UseListParams, UseListState<T>, UseDetailState<T>, UseMutationState<TData, TResult>; generates useList<Entities> managing data, total count, loading, error, pagination math (page, pageSize, totalPages, setPage), search (q, setSearch), sorting (sort, order, setSort), and refetch; generates use<Entity> for detail fetch by ID; generates useCreate<Entity>, useUpdate<Entity>, and useDelete<Entity> mutation hooks; generates useList<Entities>By<Rel> for parent-scoped subcollections; exports public render_hooks in codegen and registers in codegen.__init__; pure codegen change, offline, 0 network, no DB (15 new tests, 506 total)
2026-09-08  R-261  Full-text and keyword search filtering (`q` query parameter) on Op.LIST and Op.LIST_BY endpoints across Go, FastAPI, Next.js, and OpenAPI 3.1 targets: data_access._searchable_fields identifies fields of type STRING and TEXT; Go store emits `WHERE (field1 ILIKE $1 OR ...)` in List and Count methods with positional argument reuse for parameterized `"%"+q+"%"`, and subcollections scope with relation foreign key (`WHERE <rel>_id = $1 AND (...)`); Go handlers emit `parseSearch(r *http.Request) string` and pass `q` to store calls; FastAPI repositories emit `WHERE (col1 ILIKE %s OR ...)` in list_<table> and count_<table> passing `f"%{q}%"` wildcards; FastAPI routers declare `q: str | None = None` on list endpoints; Next.js lib/api.ts options.params includes `q?: string` for all list and list_by methods; OpenAPI 3.1 generator documents `q` query parameter on all LIST operations; non-text entities safely omit search clauses with 0 SQL errors; pure codegen change, offline, 0 network, no DB (16 new tests, 491 total)
2026-09-08  R-260  OpenAPI 3.1 specification generation (`render_openapi`, `render_openapi_json`) from Application IR: translates ApplicationIR into a canonical OpenAPI 3.1.0 document (Brief Section 43: Contracts: OpenAPI + generated clients, packages/contracts/openapi/); components.schemas maps entities to typed properties with validation metadata (maxLength, enum, minimum, maximum, required) plus standard ValidationError/Response and ErrorResponse schemas; components.securitySchemes declares BearerAuth; paths maps all endpoints with path parameters, query parameters (limit, offset, sort, order on LIST endpoints), request bodies, and responses with X-Total-Count header; security requirement attaches BearerAuth and required roles; assemble_project emits contracts/openapi.json in customer monorepos; backend_go and backend_python emit openapi.json in backend project root; test_console_snapshot updated for openapi.json in diff; pure codegen change, offline, 0 network, no DB (14 new tests, 475 total)
2026-09-08  R-259  Total count database queries and X-Total-Count HTTP response header on Op.LIST and Op.LIST_BY endpoints across Go, FastAPI, and Next.js targets: Go store emits Count<Entity> (`SELECT COUNT(*) FROM <table>`) and Count<Entity>By<Rel> (`SELECT COUNT(*) FROM <table> WHERE <rel>_id = $1`); Go handlers in internal/handlers/*.go query count before listing and emit `w.Header().Set("X-Total-Count", strconv.Itoa(total))`; Go corsMiddleware adds `Access-Control-Expose-Headers: X-Total-Count`; FastAPI repositories emit `async def count_<table>() -> int` and `count_<table>_by_<rel>(<rel>_id: str) -> int`; FastAPI routers inject `response: Response`, query count, set `response.headers["X-Total-Count"] = str(total)`, and expose in CORSMiddleware; Next.js lib/api.ts exports `PaginatedResult<T> { data: T; total: number }`, adds helper `requestWithMeta<T>`, and emits `list<Entity>WithCount` / `list<Entity>sBy<Rel>WithCount` returning `Promise<PaginatedResult<<Entity>[]>>`, keeping standard `list*` methods returning `Promise<<Entity>[]>` for complete backward compatibility; pure codegen change, offline, 0 network, no DB (15 new tests, 461 total)
2026-09-08  R-258  Query parameter sorting (`sort` and `order=asc|desc`) with SQL injection whitelist protection across Go and FastAPI backends and Next.js typed API client: Go store whitelists sort column against declared entity fields via switch statement (falling back to "id" default) and validates order direction ("DESC" on case-insensitive "desc", else "ASC"), formatting `ORDER BY %s %s LIMIT $1 OFFSET $2`; Go handlers emit `parseSort(r *http.Request) (string, string)` in shared handlers and pass sort and order to store.List and store.ListBy; FastAPI routers declare `sort: str = "id", order: str = "asc"` on Op.LIST and Op.LIST_BY and forward to repositories; Python repositories declare `ALLOWED_SORT_FIELDS`, whitelisting sort column (falling back to "id") and validating sort direction (falling back to "ASC"); Next.js typed API client (`apps/web/lib/api.ts`) types options params with `limit?: number; offset?: number; sort?: string; order?: "asc" | "desc"`; pure codegen change, offline, 0 network, no DB (14 new tests, 446 total)
2026-09-08  R-257  Full-stack connectivity — Next.js typed API client (`apps/web/lib/api.ts`) and backend CORS middleware (Go & FastAPI): NextjsWebAdapter emits a strongly-typed TypeScript API client using native fetch, supporting BASE_URL (`process.env.NEXT_PUBLIC_API_URL || ""`), ApiOptions (token for Bearer auth, query params for URLSearchParams), ApiError (status + data), request helper, typed CRUD functions for all wired entities (list with limit/offset pagination, get, create, update, delete, list_by subcollections), deterministic fallbacks for custom routes, and exported `api` namespace object; Go backend `main.go` gains corsMiddleware wrapping mux with Access-Control-Allow-Origin (* or env CORS_ALLOWED_ORIGIN), Methods, Headers, and OPTIONS 204 preflight; FastAPI backend `app/main.py` adds CORSMiddleware; .env.example files updated in all three adapters; additive, offline, 0 network, no DB (9 new tests, 432 total)
2026-09-08  R-256  Wired PUT handlers (full-replace update) in Go and FastAPI backends: route_wiring extends Op.UPDATE to match `method in ("PATCH", "PUT")` when endpoint ends in a single path parameter and `request_schema` matches a known entity; backend_go wires `Put<Entities><Id>` handler (body decode -> validateStruct if entity has rules -> store.Update<Entity> -> 404/200); backend_python wires `@router.put` (id_param + payload -> update_<table> -> 404 on None); negative shapes stay 501 scaffold; rule-free entities emit no validateStruct; example IRs unchanged; additive, offline, 0 network, no DB (14 new tests, 423 total)
2026-09-08  R-255  Added query parameter pagination (`limit` & `offset`) across both generated backends: Go `internal/handlers/handlers.go` emits `parsePagination(r *http.Request) (int, int)` returning limit (default 100, parsed if >0) and offset (default 0, parsed if >=0) via `strconv.Atoi`; Go wired `Op.LIST` and `Op.LIST_BY` handlers extract limit and offset and pass them to `store.List<Entity>` and `store.List<Entity>By<Rel>`; Go store emits `SELECT ... ORDER BY id LIMIT $1 OFFSET $2` (and `LIMIT $2 OFFSET $3` for filtered lists); FastAPI routers emit `limit: int = 100, offset: int = 0` query parameters on `@router.get` endpoints and pass them to repository calls; Python repositories parameterize `LIMIT %s OFFSET %s` with `(limit, offset)`; clients omitting parameters get default 100 items at offset 0; additive, offline, 0 network, no DB (11 new tests, 409 total)
2026-09-08  R-254  Replaced the flat "validation_failed" string with structured JSON error bodies in the generated Go backend: field_validation.go_validate_file() now emits a validationError struct {Field/Rule/Message string} and validateStruct(w http.ResponseWriter, v any) bool — on failure it iterates validator.ValidationErrors, builds []validationError{field:fe.Field(), rule:fe.Tag(), message:fmt.Sprintf(...)}, writes {"errors":[...]} via writeJSON(w, http.StatusBadRequest, ...) and returns false; on success returns true; handlers use "if !validateStruct(w, m) { return }" — a single line replacing the old two-line status+msg pattern; "fmt" added to validate.go imports; "validation_failed" string gone; FastAPI/Pydantic already return structured 422 detail by default (no change needed); rule-free gate, example IRs, and all other handler behaviour unchanged; additive and offline, no IR field, no platform dep, no install/build/run/DB (24 new tests, 398 total)
2026-09-08  R-253  Closed the missing PATCH/update CRUD verb in both generated backends: route_wiring gains Op.UPDATE; data_access emits Go UpdateEntity + Python update_table; backend_go wires Op.UPDATE handler (decode -> validateStruct -> store.Update -> 404/200); backend_python wires @router.patch; all SQL parameterized; rule-free no validate; example IRs unchanged; additive and offline (24 new tests, 374 total)
2026-09-08  R-252  Made the R-251 Go validate:"..." struct tags actually enforced at request time (FastAPI already enforced via Pydantic at construction; Go was the gap): codegen/field_validation.py gained VALIDATOR_REQUIRE (github.com/go-playground/validator/v10 v10.22.1) and go_validate_file(), which emits internal/handlers/validate.go — a shared var validate = validator.New() plus a validateStruct(v any) (int, string) helper returning http.StatusBadRequest/"validation_failed" on a tag violation ("" when valid); backend_go now computes has_validation (any wired CREATE handler whose entity carries rules) and, only then, adds the validator require to the generated go.mod, emits validate.go, and inserts a validateStruct(m) guard in each such create handler right after the JSON decode and before the store.Create call (400 on failure); rule-free projects and both example IRs emit none of it and stay byte-identical to R-251, the validator dependency lives only in the generated project (no platform Python dependency), and validation now holds at three layers — request model, request handler, and DB schema; additive and offline, no DB connection (5 new tests)
2026-09-08  R-251  Extended IR field validation to the Go backend and added numeric bounds: the codegen/field_validation parser gained min:<n> / max:<n> rules (raw literals, non-numeric ignored) and a go_validate_tag helper, so validation now flows into all three targets — the PostgreSQL schema renders numeric CHECK (col >= n)/CHECK (col <= n) (alongside the R-250 VARCHAR/enum CHECK), the FastAPI Pydantic models render Field(ge=n, le=n) beside max_length, and the Go models carry a go-playground validate:"max=,oneof=,gte=,lte=" struct tag on each field with rules (rule-free fields keep a plain json tag); tags are declarative (no go.mod dependency and no validator.Struct call this task — enforcement is a follow-up; the schema already enforces at the DB for both backends); additive and offline, example IRs unchanged, no DB connection (8 new tests)
2026-09-08  R-250  Made the IR Field.validation tuple meaningful with a small deterministic rule syntax (max_length:<int>, enum:<a>|<b>|<c>) threaded into two generated targets via codegen/field_validation.py: the PostgreSQL schema renders a STRING with max_length as VARCHAR(n) and an enum as CHECK (<col> IN ('a','b')) (values single-quote-escaped, id PK unaffected); the FastAPI Pydantic models render Field(max_length=n) (Field(default=None, max_length=n) when optional) and a Literal[...] type, importing Field/Literal only when used; unknown rules are ignored (forward-compatible), Go request-validation tags are deferred (the schema constrains Go writes at the DB); additive and offline with example IRs unchanged, no DB connection (10 new tests)
2026-09-08  R-249  Added schema indexes + unique constraints from the Application IR: Field gains a unique flag (a single-column UNIQUE constraint, never applied to the id primary key) and Entity gains indexes — a new Index record (fields + unique + optional name) whose fields are validated against the entity's own fields at construction; both round-trip through to_dict/from_dict with false/empty defaults (no schema-version bump); render_postgres_schema emits UNIQUE on unique non-id columns and one CREATE [UNIQUE] INDEX <name> ON <table> (<cols>); per entity index with a deterministic default name (<table>_<cols>_idx, _key when unique) when unnamed; rideshare-favourites gains a demo index on driver(name); pure/deterministic/offline, schema text only, nothing connects to or runs a database (11 new tests)
2026-09-08  R-248  Added honest seed data from explicit Application IR fixtures: a new Fixture record (entity + rows of column->JSON value) that is validated (entity + columns must be declared; a required column missing from a row is a WARNING) and round-trips through to_dict/from_dict/normalize_ir (additive IR field, empty default, no schema-version bump); render_postgres_seed (codegen/seed_sql.py) emits a deterministic migrations/0002_seed.sql with one INSERT per row using ONLY the declared columns/values — never inventing, defaulting, or guessing — and owns the codebase's first SQL literal quoter (single quotes doubled, bool->TRUE/FALSE, None->NULL, dict/list->jsonb); both backends emit the file only when the IR has fixtures and database_strategy=postgres (0001_init.sql unchanged), minimal-blog gains example fixtures and builder:demo prints the seed; deterministic and offline, no DB connection or migration run, no fabricated value (14 new tests)
2026-09-08  R-247  Made the real offline builder visible in the dependency-free static console: a new metadata-only console snapshot composes the R-245 ProjectPlan for the bundled Rideshare Favourites IR (apps/web + services/api with preview URLs and verification ladders) and an R-246 hunk-level unified patch produced from actual old/new Minimal Blog IR assemblies (five modified generated files); the responsive UI renders both with textContent-only DOM construction and same-origin JSON alongside the model fabric, with no generated app execution, database connection, external request, service, dependency, or secret value (5 new tests)
2026-09-07  R-246  Added hunk-level edit diffs + rename detection on top of the R-237 file-level ProjectDiff: diff_report(old, new) classifies each path as added/modified/deleted/renamed and attaches a git-style unified (hunk) diff for content changes, collapsing an exact-content delete+add into a single RENAMED record (old_path -> path); unified_patch(old, new) concatenates them into one byte-stable git-style patch string with rename headers, so an edit reads as a focused review-ready patch; standard-library difflib only, additive (diff_projects/apply_diff/plan_edit unchanged), pure and deterministic with no disk write or run (6 new tests)
2026-09-07  R-245  Added the combined project-plan surface: build_project_plan(ir) composes the assembler app layout (R-232), runtime preview plans (R-233), verify gate ladders (R-235), and optional deploy plans (R-234) into one per-app ProjectPlan a console/CLI can render — each AppPlan pairs an assembled app with its preview + verify plans (and a deploy plan only when a key-activated DeploymentProvider is passed); ProjectPlan.to_dict is JSON-serializable and secret-free, render() is a readable summary, and task plan:show prints it for an example IR; pure and deterministic, nothing is installed/run/verified/deployed and no key value is included (6 new tests)
2026-09-07  R-244  Wired the sub-collection GET pattern /<parents>/{parentId}/<children> to a parent-scoped list, clearing the main class of remaining 501 stubs: when the child entity (response_schema) is a repository entity with exactly one foreign-key relation (many_to_one/one_to_one), the endpoint lists it filtered by <rel>_id = the path parameter; route_wiring gains an Op.LIST_BY + fk_relations(ir) helper, and the data-access layer emits a filtered list per FK relation (Python list_<table>_by_<rel>, Go List<Entity>By<Rel>) with parameterized values and a fixed identifier column; Python routers and Go handlers call it; ambiguous endpoints (0 or >1 FK relations, multi-param, unknown entity) stay labelled 501; deterministic and offline, nothing runs; seed/fixture data deferred (the IR carries no seed values) (11 new tests)
2026-09-07  R-243  Added per-endpoint role enforcement on top of the R-242 JWT authentication: the Application IR ApiEndpoint gains an optional required_roles (role ids; a non-empty value implies auth, each must be a declared Role — validate_ir errors on an undeclared role) that round-trips through to_dict/from_dict (first additive IR change, default (), no schema-version bump); the generated guard returns 403 when the verified token's roles claim includes no required role — Python emits a require_roles(*required) dependency factory and role-gated routes declare dependencies=[Depends(require_roles("..."))], Go refactors to a shared verifyToken plus RequireRoles(next, required...)/hasAnyRole and main wraps role-gated endpoints; endpoints without required_roles keep the R-241/R-242 auth guard; deterministic and offline, nothing runs (6 new tests)
2026-09-07  R-242  Upgraded the generated auth guard from a bearer-presence check to real JWT verification: it decodes and verifies a JWT (HS256) using JWT_SECRET read from the environment (401 on a missing/invalid/expired token, 500 when the secret is unset), never hard-coding or defaulting the secret; Python require_auth uses PyJWT (jwt.decode with algorithms=["HS256"]) and returns the verified claims, Go RequireAuth uses github.com/golang-jwt/jwt/v5 and rejects non-HMAC tokens; the generated projects gain the JWT dependency (PyJWT in requirements.txt / golang-jwt in go.mod) and an empty JWT_SECRET in .env.example; the IR roles constant is retained for future per-endpoint authorization; platform code stays standard-library only (the JWT dep lives in the generated project); deterministic and offline, nothing signed/verified at generation time (3 new tests)
2026-09-07  R-241  Made the IR per-endpoint auth flag real: every auth=true endpoint enforces an authentication guard that rejects a request with no Authorization: Bearer credential (HTTP 401) before the handler runs; Python emits app/auth.py (require_auth dependency) and auth routes declare dependencies=[Depends(require_auth)], Go emits internal/handlers/auth.go (RequireAuth middleware) and main wraps exactly the auth=true registrations with handlers.RequireAuth(...); the IR roles are surfaced as a generated constant (Python ROLES, Go Roles) and token verification (signature/expiry/roles) is an explicit TODO in the generated code — never faked, no secret fabricated; emitted only when the IR declares an auth=true endpoint, public routes untouched; deterministic and offline, nothing runs (9 new tests)
2026-09-07  R-240  Wired the generated backends' HTTP handlers to the R-239 repository layer for the unambiguous CRUD shapes (route_wiring.wire_endpoint: GET collection->list, GET /x/{id}->get, POST with request_schema->create, DELETE /x/{id}->delete; entity from response_schema else request_schema; everything else stays a labelled 501 scaffold); Python routers import the repository/model and await the call (create takes a Pydantic body), Go handlers became methods on a Handlers struct holding *sql.DB (internal/handlers/handlers.go with New + writeJSON) with main opening store.Open() and registering h.<Handler>; emitted only when entities + database_strategy=postgres, non-DB backends keep the plain scaffolds unchanged; deterministic and offline, nothing runs (12 new tests)
2026-09-07  R-239  Added a per-entity data-access/repository layer so the generated backends read/write the R-238 tables: Python (FastAPI) gets app/db.py (async psycopg) + app/repositories/<entity>.py (list/get/create/delete) and psycopg in requirements.txt; Go gets internal/store/store.go (database/sql via pgx) + internal/store/<entity>.go (List/Get/Create/Delete scanning into the generated model structs) and a pgx require in go.mod; all query values are parameterized (%s / $N) with only fixed IR-derived identifiers inline, an id-only entity creates via DEFAULT VALUES; emitted only when entities + database_strategy=postgres; nothing connects to or queries a database; standard-library only, deterministic and offline (7 new tests)
2026-09-07  R-238  Added PostgreSQL schema/migration generation from the Application IR: render_postgres_schema turns entities + relations into deterministic SQL DDL (one CREATE TABLE per entity, snake_case names, FieldType->PG column types, NOT NULL for required fields, a UUID primary key from the entity id or a surrogate, foreign-key columns for many_to_one/one_to_one relations, and a join table per many_to_many); the FastAPI and Go backends emit it as migrations/0001_init.sql exactly when the IR has entities and database_strategy=postgres, changing no previously emitted file; byte-stable so the R-237 edit loop diffs it on IR change; nothing connects to or runs a database; standard-library only, deterministic and offline (11 new tests)
2026-09-07  R-237  Added the IR-diff -> patch-apply edit loop (edit an existing app): diff_projects classifies added/modified/deleted/unchanged between two generated projects (modified on content or executable change); plan_edit(old_ir, new_ir) assembles both IRs and diffs them so an IR change becomes exactly the files to rewrite; apply_diff writes added/modified and removes deleted files inside a path-safe target (escapes refused, empty dirs pruned) and returns an ApplyReport; commit_edit applies + commits as the customer identity via an additive git_service.commit_all, preserving history; deterministic and offline (diffing pure, applying a bounded path-safe disk step), no network or code execution (9 new tests)
2026-09-07  R-236  Expanded the model-provider catalog: added first-class OpenAI-compatible adapters for DeepSeek, xAI (Grok), Mistral, Together, and Fireworks alongside OpenAI/Anthropic/Google/OpenRouter/Groq, plus a generic env-driven custom-provider path (OMNISTACKAI_CUSTOM_PROVIDERS + per-provider BASE_URL/MODEL/API_KEY) so any OpenAI-compatible endpoint plugs in with no code change; resolve_provider_specs merges built-ins + custom and bootstrap/overview iterate it so every provider is key-activated, selectable as the cloud tier or a fallback, and listed in the metadata-only overview; illustrative price-book defaults for the priced new providers (openrouter/custom unpriced); keys stay env-only and never logged/stored/shown; standard-library only, deterministic and offline (14 new tests)
2026-09-07  R-235  Added the verifiable-engineering verify-plan layer: a deterministic per-target gate ladder (install/typecheck/lint/test/build) that generated code is engineered to pass, as pure validated data classified by gate kind; verify_plans_for_ir maps one Application IR to the verify plans for its assembled monorepo apps (apps/web, services/api) via a new additive assembled_targets; run_verify is an opt-in fail-fast executor returning a structured VerifyReport; task agent-engine:verify-plan prints a ladder; standard-library only, deterministic and offline (9 new tests), nothing installed/built/run by the platform
2026-09-07  R-234  Added a single OMNISTACKAI_TIER switch (0/1 local, 2 cloud) that resolves the runtime + deploy providers, and gave every declared provider a driver that emits a real command plan (deploy: vercel/netlify/render/fly; sandbox: e2b/daytona/fly-machines) with the key read from env at run time and never placed in a plan or logged; platform_status/format_status + task platform:status report the active tier and key presence, run_deploy executes a plan opt-in; deterministic and offline (13 new tests, task verify stays network-independent), nothing run or deployed
2026-09-07  R-233  Added the runtime & deployment provider layer: a local preview provider (Tier 0/1, no keys) with deterministic per-target preview plans and an opt-in executor, plus key-activated cloud sandbox (e2b/daytona/fly) and deploy (vercel/fly/render/netlify) providers via an env registry (Tier 2/3 = add a key); nothing is run or deployed by the platform and no key is logged/stored
2026-09-06  R-232  Added the customer project assembler: one Application IR is assembled into a complete customer monorepo (apps/web Next.js + services/api Go/Python) as a single GeneratedProject with root README/.gitignore, and materialized via the Git service into one customer-owned repo (default_registry preloads all three adapters); deterministic, offline
2026-09-06  R-231  Added an Application IR semantic validator (unknown API schema references = error; strategy/platform mismatch = warning), a deterministic canonical normalizer (idempotent, lossless), and example IR fixtures (rideshare-favourites, minimal-blog) that validate clean and generate via all three adapters
2026-09-06  R-230  Added the Go backend framework adapter: one Application IR now emits web (Next.js) + Python (FastAPI) + Go (net/http) — Go structs from entities, Go 1.22 method+pattern routes grouped by resource with r.PathValue params, main + health; standard-library only, deterministic, offline, no install/build/disk
2026-09-06  R-229  Added the Python (FastAPI) backend framework adapter: one Application IR now emits web (Next.js) + backend (FastAPI) together — Pydantic models from entities, routers from APIs grouped by resource with typed path params, main app + health; deterministic, offline, no install/build/disk
2026-09-06  R-228  Added the git service: materialize a GeneratedProject to disk and initialize a customer-owned Git repository with a first commit (explicit identity, no global config, writes only inside the target, offline) — completes the first end-to-end builder slice (IR -> app -> owned repo)
2026-09-06  R-227  Added the first framework code adapter (Next.js web): turns an Application IR into a real Next.js App Router TypeScript project (config, TS interfaces from entities, App Router API routes with {param}->[param], a page per screen, overview) as a deterministic in-memory GeneratedProject; no install/build/disk
2026-09-06  R-226  Added the code-generation boundary: an in-memory generated file-set (GeneratedFile/GeneratedProject, path-safe, deterministic) and the FrameworkAdapter contract + registry that turn an Application IR into a GeneratedProject per target; no disk writes or code execution
2026-09-06  R-225  Added the framework-neutral Application IR v1 (Brief 9) — the source of truth for code generation: validated, versioned records for application/strategy/roles/entities/APIs/screens/acceptance with cross-reference checks and lossless serialization; first real product primitive
2026-09-06  R-224  Deferred the Next.js console upgrade — the Next.js SWC binary install times out in this sandbox (attempted 3x); recorded a resume plan; no application code committed; the R-222 static console remains the working slice
2026-09-06  R-223  Wired the cross-provider fallback chain and circuit breaker into build_gateway_from_env via environment configuration (OMNISTACKAI_FALLBACK_PROVIDERS + breaker threshold/cooldown); the console overview surfaces the resilience settings; single-provider default unchanged
2026-09-06  R-222  Added the first platform console slice (apps/console-web): a dependency-free static console rendering providers, the routing ladder, the price book, and the usage/cost dashboard from a metadata-only Python-exported snapshot (no secrets, strict CSP, no external calls); Next.js upgrade deferred (SWC install times out in this environment)
2026-09-06  R-221  Added explicit allowlist-driven cross-provider fallback and a per-provider circuit breaker to the gateway: retriable failures fail over along the configured chain (never silent, never on non-retriable errors), the breaker opens/cools-down/half-opens, and every attempt is accounted
2026-09-06  R-220  Added true per-provider Server-Sent-Events streaming for the cloud adapters (OpenAI-compatible, Anthropic, Gemini): ordered delta events plus a final event with measured usage, reusing the HTTP-safety bounds (new IDs continue after the workbook backlog's R-010..R-219)
2026-09-06  R-009  Added deterministic usage and cost accounting for the model gateway: immutable metadata-only usage records (no content/secret), a configurable Decimal price book (local Ollama zero, unknown unpriced), and an aggregating ledger with per-provider/model breakdowns, p50/p95 latency, and cost per successful call
2026-09-06  R-008  Added key-activated cloud ModelProvider adapters (Anthropic, OpenAI, Gemini, OpenRouter, Groq) behind the shared boundary with an env bootstrap; standard-library only, no vendor SDK, keys env-only, local Ollama stays default and no key means no cloud call
2026-09-06  R-007  Added the deterministic Balanced Model Gateway router — escalation ladder (L0 refused), sub-L3 local Ollama routing, L3/L4 escalation-required, conservative context-budget guard, and no silent cloud fallback — with 14 offline tests
2026-09-06  R-006  Added the bounded loopback-only Ollama ModelProvider adapter with conservative model eligibility, offline conformance tests, and live generation/streaming proof
2026-09-06  R-005  Added the vendor-neutral Python ModelProvider contract, validated model-call records, deterministic registry, and CI-equivalent tests
2026-09-06  R-004  Added the minimal Go control-plane with typed config, PostgreSQL readiness, structured logs, graceful shutdown, tests, and live Compose verification
2026-09-06  R-003  Added loopback-only local Ollama configuration, model discovery, lifecycle commands, and live inference verification
2026-09-06  R-002  Added pinned local PostgreSQL+pgvector Compose bootstrap, versioned initial migration, and deterministic lifecycle/verification commands
2026-09-06  R-001  Bootstrapped the Section 74 monorepo skeleton and Stage 0 start, verification, state, and handoff contract
