# Changelog

2026-09-11  R-363  Generated Accessible Futuristic Reusable Tour & Onboarding Spotlight Guide Suite (components/tour.tsx) in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter emits a dedicated accessible futuristic Tour and Onboarding Spotlight Guide compound component suite (`apps/web/components/tour.tsx`) providing `TourVariant` ("default" | "card" | "glass" | "neon"), `TourSize` ("sm" | "md" | "lg"), `TourPlacement` ("top" | "bottom" | "left" | "right" | "center"), `TourStep` (target, title, description, placement, spotlightPadding, spotlightRadius, hideCloseButton, hideNavigation, nextLabel, prevLabel, finishLabel), `TourProps`, compound exports (`Tour`, `TourStepDot`, `TourProgressBadge`, `TourCloseButton`), dynamic target element bounding box measurement via `getBoundingClientRect()`, full-screen SVG cutout spotlight mask (`<mask id="...">` with `<rect fill="white"/>` and `<rect fill="black"/>`), floating tooltip card with directional arrow notch, automatic placement resolution and edge clamping against `window.innerWidth` and `window.innerHeight`, step navigation controls ("Next", "Back", "Finish", "Skip", "Close"), step dots indicator with clickable jump navigation, step counter badge ("X of Y"), full WAI-ARIA 1.2 dialog compliance (`role="dialog"`, `aria-modal="true"`, `aria-label`, `aria-describedby`), full keyboard navigation (Escape to dismiss, ArrowRight/Enter to advance, ArrowLeft to go back, Tab trapping within dialog), 4 futuristic visual styling variants ("default", "card", "glass" with backdropFilter blur, "neon" cyberpunk cyan glow), 3 size presets ("sm", "md", "lg"), React ref forwarding (`forwardRef`), `Tour.displayName = "Tour"`, 100% diff-invariance across ir.description; exports `render_tour_component` in `omnistackai_agent_engine.codegen` and registers `components/tour.tsx` in `NextjsWebAdapter.generate()`; pure codegen change, offline, 0 network, 0 model calls (19 new focused tests in test_tour_component.py; 1,857 total; lint/security/env and builder:demo pass)

2026-09-11  R-362  Generated Accessible Futuristic Reusable Sidebar & Side Navigation Suite (components/sidebar.tsx) in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter emits a dedicated accessible futuristic Sidebar and Side Navigation compound component suite (`apps/web/components/sidebar.tsx`) providing `SidebarVariant` ("default" | "card" | "glass" | "neon"), `SidebarSize` ("sm" | "md" | "lg"), `SidebarCollapsible` ("icon" | "offcanvas" | "none"), `SidebarSide` ("left" | "right"), `SidebarState` ("expanded" | "collapsed"), `SidebarContextValue`, `SidebarProps`, `SidebarHeaderProps`, `SidebarContentProps`, `SidebarFooterProps`, `SidebarGroupProps`, `SidebarGroupLabelProps`, `SidebarGroupContentProps`, `SidebarMenuProps`, `SidebarMenuItemProps`, `SidebarMenuButtonProps`, `SidebarMenuBadgeProps`, `SidebarMenuSubProps`, `SidebarMenuSubItemProps`, `SidebarMenuSubButtonProps`, `SidebarRailProps`, `SidebarTriggerProps`, compound subcomponents (`Sidebar`, `SidebarHeader`, `SidebarContent`, `SidebarFooter`, `SidebarGroup`, `SidebarGroupLabel`, `SidebarGroupContent`, `SidebarMenu`, `SidebarMenuItem`, `SidebarMenuButton`, `SidebarMenuBadge`, `SidebarMenuSub`, `SidebarMenuSubItem`, `SidebarMenuSubButton`, `SidebarRail`, `SidebarTrigger`, `SidebarToggle`, `SideNav`), collapsible icon rail and offcanvas sliding panel with mobile backdrop overlay, WAI-ARIA 1.2 navigation landmark and menu semantics (`role="navigation"`, `role="menu"`, `role="menuitem"`, `aria-label`, `aria-current="page"`, `aria-expanded`), keyboard navigation (`ArrowDown`, `ArrowUp`, `Home`, `End`), 5 built-in vector icons (`PanelLeftIcon`, `ChevronRightIcon`, `ChevronLeftIcon`, `ChevronDownIcon`, `MenuIcon`), tooltips on collapsed items, React ref forwarding (`forwardRef`), `Sidebar.displayName = "Sidebar"`, 100% diff-invariance across ir.description; exports `render_sidebar_component` in `omnistackai_agent_engine.codegen` and registers `components/sidebar.tsx` in `NextjsWebAdapter.generate()`; pure codegen change, offline, 0 network, 0 model calls (26 new focused tests in test_sidebar_component.py; 1,838 total; lint/security/env and builder:demo pass)

2026-09-11  R-361  Generated Accessible Futuristic Reusable Notification Center & Notification List Suite (components/notification-center.tsx) in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter emits a dedicated accessible futuristic Notification Center compound component suite (`apps/web/components/notification-center.tsx`) providing `NotificationVariant` ("default" | "card" | "glass" | "neon"), `NotificationSize` ("sm" | "md" | "lg"), `NotificationType` ("info" | "success" | "warning" | "error"), `NotificationItem` (id, title, message, timestamp, read, type, actionLabel, onAction, avatarUrl, link), `NotificationCenterProps`, `NotificationTriggerProps`, `NotificationPanelProps`, `NotificationListProps`, `NotificationItemProps`, compound subcomponents (`NotificationCenter`, `NotificationTrigger`, `NotificationPanel`, `NotificationList`, `NotificationItemComponent`, `NotificationBadge`, `NotificationEmptyState`), animated slide-in / dropdown panel, bell icon trigger with unread badge counter (boolean dot or numeric badge, capped at 99+), individual notification item cards with read/unread visual states (unread dot indicator, tinted background), action buttons ("Mark all as read", "Clear all", individual "Mark as read", custom item CTA), filter tabs (All / Unread), relative time formatting ("just now", "Xm ago", "Xh ago", "Xd ago"), category / type indicator vector icons (info, success, warning, error, bell, check, trash), full WAI-ARIA 1.2 dialog and listbox compliance (`role="dialog"`, `role="listbox"`, `role="option"`, `aria-label`, `aria-expanded`, `aria-haspopup="dialog"`, `aria-live="polite"`), full keyboard navigation (Escape to close, Tab trapping, Enter/Space activation), 4 futuristic visual styling variants ("default", "card", "glass" with backdropFilter blur, "neon" cyberpunk cyan glow), 3 size presets ("sm", "md", "lg"), React ref forwarding (`forwardRef`), `NotificationCenter.displayName = "NotificationCenter"`, 100% diff-invariance across ir.description; exports `render_notification_center_component` in `omnistackai_agent_engine.codegen` and registers `components/notification-center.tsx` in `NextjsWebAdapter.generate()`; pure codegen change, offline, 0 network, 0 model calls (44 new focused tests in test_notification_center_component.py; 1,812 total; lint/security/env and builder:demo pass)

2026-09-11  R-360  Generated Accessible Futuristic Reusable Number Input & Numeric Stepper Primitive (components/number-input.tsx) in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter emits a dedicated accessible Number Input & Numeric Stepper compound component suite (`apps/web/components/number-input.tsx`) providing `NumberInputVariant` ("default" | "card" | "glass" | "neon"), `NumberInputSize` ("sm" 32px | "md" 38px | "lg" 44px), `NumberInputFormat` ("plain" | "currency" | "percentage"), `NumberInputProps`, WAI-ARIA spinbutton pattern (`role="spinbutton"`, `aria-valuenow`, `aria-valuemin`, `aria-valuemax`, `aria-invalid`, `aria-required`, `aria-describedby`), full keyboard navigation (`ArrowUp` to increment, `ArrowDown` to decrement with `e.preventDefault()`), increment/decrement stepper buttons (`ChevronUpIcon`/`ChevronDownIcon`, `aria-label="Increment"/"Decrement"`, `tabIndex=-1`), disabled at min/max boundaries (`atMin`/`atMax`), `clamp()` helper for min/max enforcement, `precision`/`step` support, 3 value format modes via `Intl.NumberFormat` (plain locale-aware, currency with ISO 4217 code, percentage), `hideControls` to suppress stepper buttons, `leftSection`/`rightSection` slot nodes, label with `htmlFor` link, helper text, error message with `role="alert"` and `aria-live="polite"`, required asterisk affordance, controlled and uncontrolled modes, focus/blur raw-vs-formatted display toggle, `inputMode="decimal"` for mobile numeric keyboard, `fontVariantNumeric="tabular-nums"` for digit alignment, React ref forwarding (`forwardRef`), `NumberInput.displayName = "NumberInput"`, 100% diff-invariance across ir.description; exports `render_number_input_component` in `omnistackai_agent_engine.codegen` and registers `components/number-input.tsx` in `NextjsWebAdapter.generate()`; pure codegen change, offline, 0 network, 0 model calls (35 new focused tests in test_number_input_component.py; 1,768 total; lint/security/env and builder:demo pass)

2026-09-11  R-359  Generated Accessible Futuristic Reusable Mobile Bottom Navigation Bar Suite (components/bottom-nav.tsx) in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter emits a dedicated accessible mobile-first Bottom Navigation Bar compound component suite (`apps/web/components/bottom-nav.tsx`) providing `BottomNavVariant` ("default" | "glass" | "card" | "neon"), `BottomNavSize` ("sm" | "md" | "lg"), `BottomNavItem` (key, label, icon, badge, disabled, ariaLabel), `BottomNavProps`, WAI-ARIA 1.2 Tabs pattern compliance (`role="tablist"`, `role="tab"`, `aria-selected`, `aria-disabled`, `aria-controls`, `aria-orientation="horizontal"`, `aria-label`), full keyboard navigation (`ArrowRight`/`ArrowLeft`/`ArrowUp`/`ArrowDown` traversal with wrap-around, `Home`/`End` jump navigation), 4 futuristic visual styling variants ("default" surface+border, "glass" with backdropFilter blur, "card" with elevated top border-radius, "neon" cyberpunk cyan glow on dark), 3 size scales ("sm" 52px, "md" 60px, "lg" 72px), active pill indicator transition (opacity), badge notifications (boolean dot or number/string count chip via `BadgeChip` helper), optional floating-action-button (FAB) centre gap slot, full-screen backdrop tint (`backdropOpen`/`onBackdropClose`) for FAB menus, controlled and uncontrolled selection modes, React ref forwarding (`forwardRef`), `BottomNav.displayName = "BottomNav"`, 100% diff-invariance across ir.description; exports `render_bottom_nav_component` in `omnistackai_agent_engine.codegen` and registers `components/bottom-nav.tsx` in `NextjsWebAdapter.generate()`; pure codegen change, offline, 0 network, 0 model calls (26 new focused tests in test_bottom_nav_component.py; 1,733 total; lint/security/env and builder:demo pass)

2026-09-11  R-358  Generated Accessible Futuristic Reusable Searchable Combobox & Autocomplete Primitive (components/combobox.tsx) in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter emits a dedicated accessible futuristic Searchable Combobox and Autocomplete compound component suite (`apps/web/components/combobox.tsx`) providing `ComboboxVariant` ("default" | "card" | "glass" | "neon"), `ComboboxSize` ("sm" | "md" | "lg"), `ComboboxOptionItem`, `ComboboxProps`, WAI-ARIA 1.2 Combobox and Listbox pattern compliance (`role="combobox"`, `role="listbox"`, `role="option"`, `aria-expanded`, `aria-haspopup="listbox"`, `aria-controls`, `aria-activedescendant`, `aria-selected`, `aria-disabled`, `data-highlighted`), type-ahead live search filtering across label, description, and keywords, full keyboard navigation (`ArrowDown`/`ArrowUp` traversal with wrap-around, `Enter` to select, `Escape` to close, `Home`/`End` jump navigation), single-select string mode and multi-select string array mode with removable tag chips (`XIcon`), clear button affordance (`allowClear`), 4 futuristic visual styling variants ("default", "card", "glass" with backdrop blur, "neon" cyberpunk cyan glow), 3 size scales ("sm", "md", "lg"), form integration via hidden inputs (`name`), semantic aliasing (`Autocomplete = Combobox`), React ref forwarding (`forwardRef`), 100% diff-invariance across ir.description; exports `render_combobox_component` in `omnistackai_agent_engine.codegen` and registers in `NextjsWebAdapter.generate()`; pure codegen change, offline, 0 network, 0 model calls (15 new focused tests in test_combobox_component.py; 1,707 total; lint/security/env and builder:demo pass)

2026-09-11  R-357  Generated Accessible Futuristic Reusable Announcement Banner & Callout Suite (components/banner.tsx) in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter emits a dedicated accessible futuristic Announcement Banner and Callout compound component suite (`apps/web/components/banner.tsx`) providing `BannerVariant` ("info" | "success" | "warning" | "error" | "neon" | "gradient"), `BannerPosition` ("top" | "bottom" | "inline" | "floating"), `BannerSize` ("sm" | "md" | "lg"), `BannerProps`, `BannerCloseButtonProps`, WAI-ARIA live region compliance (`role="status"` | `role="alert"` for error/warning, `aria-live="polite"` | `aria-live="assertive"`), 4 layout positions ("top" sticky header, "bottom" sticky footer, "inline" card banner, "floating" center toast callout), 6 visual styling variants ("info", "success", "warning", "error", "neon" cyberpunk cyan glow, "gradient" violet-to-cyan), 3 size scales ("sm", "md", "lg"), dismissible state with onDismiss callback and accessible close button (`aria-label="Dismiss banner"`), action CTA slot container (`BannerAction`), icon slot container (`BannerIcon`) with built-in SVGs (`InfoIcon`, `SuccessIcon`, `WarningIcon`, `ErrorIcon`, `NeonIcon`, `CloseIcon`), compound exports (`Banner`, `BannerIcon`, `BannerAction`, `BannerCloseButton`, `AnnouncementBanner`, `Callout`), React ref forwarding (`forwardRef`), 100% diff-invariance across ir.description; exports `render_banner_component` in `omnistackai_agent_engine.codegen` and registers in `NextjsWebAdapter.generate()`; pure codegen change, offline, 0 network, 0 model calls (15 new focused tests in test_banner_component.py; 1,692 total; lint/security/env and builder:demo pass)

2026-09-11  R-356  Generated Accessible Futuristic Reusable Checkbox & Checkbox Group Primitive (components/checkbox.tsx) in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter emits a dedicated accessible futuristic Checkbox and Checkbox Group compound component suite (`apps/web/components/checkbox.tsx`) providing `CheckboxVariant` ("default" | "card" | "pill" | "neon"), `CheckboxSize` ("sm" | "md" | "lg"), `CheckedState` (boolean | "indeterminate"), `CheckboxProps`, `CheckboxGroupProps`, `CheckboxGroupContextValue`, WAI-ARIA 1.2 Checkbox pattern compliance (`role="checkbox"`, `role="group"`, `aria-checked="mixed"` for indeterminate / boolean, `aria-orientation`, `aria-disabled`, `aria-required`, `tabIndex`), full keyboard space toggling (`Space` key with `e.preventDefault()`), tri-state / indeterminate support with dedicated SVG minus vector and checkmark vector, vertical and horizontal flex layouts, 4 visual styling variants ("default" rounded square with blue fill, "card" selectable option tiles with title & description, "pill" segmented toggle buttons, "neon" cyberpunk glowing cyan/indigo border and glow shadow), 3 size scales ("sm", "md", "lg"), controlled and uncontrolled state management (`checked`, `defaultChecked`, `onCheckedChange`), multi-select array management inside `CheckboxGroup` (`value: string[]`, `onValueChange`), form integration via hidden inputs (`name`), options array convenience prop mapping alongside custom children, subcomponent aliasing (`CheckboxItem = Checkbox`), `useCheckboxGroup` context hook, full React ref forwarding (`forwardRef`) with `displayName`, 100% diff-invariance across ir.description; exports `render_checkbox_component` in `omnistackai_agent_engine.codegen` and registers in `NextjsWebAdapter.generate()`; pure codegen change, offline, 0 network, 0 model calls (15 new focused tests in test_checkbox_component.py; 1,677 total; lint/security/env and builder:demo pass)

2026-09-11  R-355  Generated Accessible Futuristic Reusable Radio Group Suite (components/radio-group.tsx) in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter emits a dedicated accessible futuristic Radio Group compound component suite (`apps/web/components/radio-group.tsx`) providing `RadioGroupVariant` ("default" | "card" | "pill" | "neon"), `RadioGroupSize` ("sm" | "md" | "lg"), `RadioGroupOrientation` ("vertical" | "horizontal"), `RadioGroupOption`, `RadioGroupProps`, `RadioGroupItemProps`, `RadioGroupContextValue`, WAI-ARIA 1.2 Radio Group pattern (`role="radiogroup"`, `role="radio"`, `aria-checked`, `aria-orientation`, `aria-disabled`, `aria-required`, roving `tabIndex`), arrow key navigation with wrap-around (`ArrowDown`/`ArrowRight` -> next, `ArrowUp`/`ArrowLeft` -> prev, `Space` -> select) and programmatic focus transfer, vertical (default) and horizontal flex layouts, 4 visual styling variants ("default", "card", "pill", "neon" cyan glow), 3 size scales ("sm", "md", "lg"), controlled and uncontrolled value management (`value`, `defaultValue`, `onValueChange`), hidden input field integration for native form submissions, compound exports (`RadioGroup`, `RadioGroupItem`, `Radio = RadioGroupItem`, `useRadioGroup` context hook), React `forwardRef` on group and items with `displayName`, 100% diff-invariance across ir.description; exports `render_radio_group_component` in `omnistackai_agent_engine.codegen` and registers in `NextjsWebAdapter.generate()`; pure codegen change, offline, 0 network, 0 model calls (15 new focused tests in test_radio_group_component.py; 1,662 total; lint/security/env and builder:demo pass)

2026-09-11  R-354  Generated Accessible Futuristic Reusable Keyboard Keycap Component (components/kbd.tsx) in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter emits a dedicated accessible futuristic Keyboard Keycap & Shortcut Badge component (`apps/web/components/kbd.tsx`) providing `KbdVariant` ("default" | "outline" | "subtle" | "ghost" | "neon"), `KbdSize` ("xs" | "sm" | "md" | "lg"), `KbdProps`, `KbdGroupProps`, `KbdShortcutProps`, semantic `<kbd>` HTML elements with WAI-ARIA compliance (`role="group"`, `aria-label`, `aria-keyshortcuts`), automatic modifier key symbol conversion ("meta"/"command" -> "⌘", "shift" -> "⇧", "ctrl" -> "⌃", "alt" -> "⌥", "enter" -> "↵", "backspace" -> "⌫"), 4 size scales ("xs", "sm", "md", "lg"), 5 futuristic visual variants ("default" 3D keycap with bottom border and shadow, "outline", "subtle", "ghost", "neon" cyberpunk glow), key combination arrays with configurable separators, composite container `KbdGroup`, string shortcut parser `KbdShortcut` (e.g. "⌘+K", "Ctrl+Shift+P"), full React ref forwarding (`forwardRef`), 100% diff-invariance across ir.description; exports `render_kbd_component` in `omnistackai_agent_engine.codegen` and registers in `NextjsWebAdapter.generate()`; pure codegen change, offline, 0 network, 0 model calls (16 new focused tests in test_kbd_component.py; 1,647 total; lint/security/env and builder:demo pass)

2026-09-11  R-353  Generated Accessible Futuristic Reusable Separator Component (components/separator.tsx) in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter emits a dedicated accessible futuristic Separator / Divider component (`apps/web/components/separator.tsx`) providing `SeparatorOrientation` ("horizontal" | "vertical"), `SeparatorVariant` ("neon" | "glass" | "gradient" | "bordered" | "minimal"), `SeparatorThickness` ("thin" | "md" | "thick"), `SeparatorLabelAlign` ("start" | "center" | "end"), `SeparatorProps`, WAI-ARIA 1.2 Separator pattern compliance (decorative mode: `role="none"`, `aria-hidden="true"` vs semantic mode: `role="separator"`, `aria-orientation`), horizontal and vertical layout orientations, thickness resolution for presets ("thin" 1px, "md" 2px, "thick" 4px) and custom pixel values, optional label/content slot along horizontal dividers with flexible alignment, dual flex-grow line segments around styled uppercase badge, 5 futuristic visual styling variants ("neon", "glass", "gradient", "bordered", "minimal"), full React ref forwarding (`forwardRef<HTMLDivElement, SeparatorProps>`), 100% diff-invariance across ir.description; exports `render_separator_component` in `omnistackai_agent_engine.codegen` and registers in `NextjsWebAdapter.generate()`; pure codegen change, offline, 0 network, 0 model calls (16 new focused tests in test_separator_component.py; 1,631 total; lint/security/env and builder:demo pass)

2026-09-11  R-352  Generated Accessible Futuristic Reusable Aspect Ratio Component (components/aspect-ratio.tsx) in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter emits a dedicated accessible futuristic Aspect Ratio Viewport Container component (`apps/web/components/aspect-ratio.tsx`) providing `AspectRatioPreset` ("16/9" | "4/3" | "1/1" | "21/9" | "9/16" | "3/2" | "2/3"), `AspectRatioVariant` ("neon" | "glass" | "bordered" | "minimal"), `AspectRatioProps`, zero Cumulative Layout Shift (CLS) space reservation via percentage padding-bottom fallback calculation (`paddingBottom = `${(1 / numericRatio) * 100}%``), modern CSS `aspectRatio` inline style property acceleration, absolute full-bleed child container layout (`position: "absolute"`, `inset: 0`, `width: "100%"`, `height: "100%"`), overflow clipping control (`overflowHidden` defaulting to `true`), full React ref forwarding (`forwardRef<HTMLDivElement, AspectRatioProps>`), 4 futuristic visual variants ("neon", "glass", "bordered", "minimal"), 100% diff-invariance across ir.description; exports `render_aspect_ratio_component` in `omnistackai_agent_engine.codegen` and registers in `NextjsWebAdapter.generate()`; pure codegen change, offline, 0 network, 0 model calls (16 new focused tests in test_aspect_ratio_component.py; 1,615 total; lint/security/env and builder:demo pass)

2026-09-11  R-351  Generated Accessible Futuristic Reusable Collapsible Component (components/collapsible.tsx) in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter emits a dedicated accessible futuristic Collapsible / Disclosure compound component suite (`apps/web/components/collapsible.tsx`) providing `CollapsibleVariant` ("neon" | "glass" | "bordered" | "minimal"), `CollapsibleSize` ("sm" | "md" | "lg"), `CollapsibleProps`, `CollapsibleTriggerProps`, `CollapsibleContentProps`, `CollapsibleContextValue`, compound subcomponents (`Collapsible`, `Collapsible.Trigger`, `Collapsible.Content`, `useCollapsible`), smooth CSS grid template rows expansion animation (`gridTemplateRows: open ? "1fr" : "0fr"`) with `overflow: "hidden"` container for layout-jump-free resizing, rotating indicator chevron (`transform: open ? "rotate(180deg)" : "rotate(0deg)"`), customizable indicator slot, `hideIndicator` option, controlled and uncontrolled open state (`open`, `defaultOpen`, `onOpenChange`), disabled state (`disabled`, `aria-disabled`), full WAI-ARIA 1.2 disclosure pattern compliance (`aria-expanded={open}`, `aria-controls={contentId}`, `id={triggerId}`, `role="region"`, `aria-labelledby={triggerId}`, `data-state={open ? "open" : "closed"}`), keyboard accessibility (`Enter` and `Space` activation on trigger), 4 futuristic visual variants ("neon", "glass", "bordered", "minimal"), 3 size presets ("sm", "md", "lg"), `forceMount` prop on content container; exports `render_collapsible_component` in `omnistackai_agent_engine.codegen` and registers in `NextjsWebAdapter.generate()`; 100% diff-invariant across ir.description; pure codegen change, offline, 0 network, 0 model calls (16 new focused tests in test_collapsible_component.py; 1,599 total; lint/security/env and builder:demo pass)

2026-09-11  R-350  Generated Accessible Futuristic Reusable Scroll Area Suite (components/scroll-area.tsx) in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter emits a dedicated accessible futuristic ScrollArea compound component suite (`apps/web/components/scroll-area.tsx`) providing `ScrollAreaVariant` ("neon" | "glass" | "bordered" | "minimal"), `ScrollAreaSize` ("sm" | "md" | "lg"), `ScrollAreaVisibility` ("auto" | "always" | "scroll" | "hover"), `ScrollAreaOrientation` ("vertical" | "horizontal" | "both"), `ScrollAreaProps`, `ScrollViewportProps`, `ScrollbarProps`, `ScrollThumbProps`, `ScrollCornerProps`, `ScrollAreaContextValue`, compound subcomponents (`ScrollArea`, `ScrollArea.Viewport`, `ScrollArea.Scrollbar`, `ScrollArea.Thumb`, `ScrollArea.Corner`, `useScrollArea`), cross-browser native scrollbar concealment via CSS (`scrollbarWidth: "none"`, `msOverflowStyle: "none"`, `WebkitOverflowScrolling: "touch"`), proportional thumb sizing and dynamic offset calculation with mouse/touch drag tracking (`setPointerCapture`/`releasePointerCapture`), direct track-click smooth jump scrolling, 4 visibility modes ("auto", "always", "scroll", "hover"), full WAI-ARIA 1.2 scrollbar semantics (`role="scrollbar"`, `aria-orientation`, `aria-valuenow`, `aria-valuemin`, `aria-valuemax`, `aria-controls`), viewport keyboard navigation (`tabIndex={0}`, `ArrowDown`/`Up`, `PageDown`/`Up`, `Home`, `End`), 4 futuristic visual variants ("neon", "glass", "bordered", "minimal"), 3 size presets; exports `render_scroll_area_component` in `omnistackai_agent_engine.codegen` and registers in `NextjsWebAdapter.generate()`; 100% diff-invariant across ir.description; pure codegen change, offline, 0 network, 0 model calls (16 new focused tests in test_scroll_area_component.py; 1,583 total; lint/security/env and builder:demo pass)

2026-09-11  R-349  Generated Accessible Futuristic Reusable Hover Card Suite (components/hover-card.tsx) in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter emits a dedicated accessible futuristic HoverCard compound component suite (`apps/web/components/hover-card.tsx`) providing `HoverCardVariant` ("neon" | "glass" | "bordered" | "minimal"), `HoverCardSize` ("sm" | "md" | "lg"), `HoverCardSide` ("top" | "bottom" | "left" | "right"), `HoverCardAlign` ("start" | "center" | "end"), `HoverCardProps`, `HoverCardTriggerProps`, `HoverCardContentProps`, `HoverCardArrowProps`, `HoverCardContextValue`, compound subcomponents (`HoverCard`, `HoverCard.Trigger`, `HoverCard.Content`, `HoverCard.Arrow`, `useHoverCard`), configurable entrance and exit delay timers (`openDelay` default 300ms, `closeDelay` default 200ms) with timeout clearance, smooth cursor pointer transit between trigger and content without premature card dismissal, viewport boundary collision prevention and edge flipping against `window.innerWidth` and `window.innerHeight` with safety padding, directional SVG pointer arrow notch (`HoverCard.Arrow`), Escape key dismissal with automatic trigger focus restoration, full WAI-ARIA 1.2 dialog semantics (`role="dialog"`, `aria-haspopup="dialog"`, `aria-expanded`, `aria-controls`, `aria-labelledby`, `tabIndex={-1}`), 4 futuristic visual variants ("neon", "glass", "bordered", "minimal"), 3 size presets; exports `render_hover_card_component` in `omnistackai_agent_engine.codegen` and registers in `NextjsWebAdapter.generate()`; 100% diff-invariant across ir.description; pure codegen change, offline, 0 network, 0 model calls (16 new focused tests in test_hover_card_component.py; 1,567 total; lint/security/env and builder:demo pass)

2026-09-11  R-348  Generated Accessible Futuristic Reusable Context Menu Suite (components/context-menu.tsx) in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter emits a dedicated accessible futuristic ContextMenu compound component suite (`apps/web/components/context-menu.tsx`) providing `ContextMenuVariant` ("neon" | "glass" | "bordered" | "minimal"), `ContextMenuSize` ("sm" | "md" | "lg"), `ContextMenuProps`, `ContextMenuTriggerProps`, `ContextMenuContentProps`, `ContextMenuItemProps`, `ContextMenuCheckboxItemProps`, `ContextMenuRadioGroupProps`, `ContextMenuRadioItemProps`, `ContextMenuSeparatorProps`, `ContextMenuLabelProps`, `ContextMenuSubProps`, `ContextMenuSubTriggerProps`, `ContextMenuSubContentProps`, `ContextMenuContextValue`, `ContextMenuSubContextValue`, compound subcomponents (`ContextMenu`, `ContextMenu.Trigger`, `ContextMenu.Content`, `ContextMenu.Item`, `ContextMenu.CheckboxItem`, `ContextMenu.RadioGroup`, `ContextMenu.RadioItem`, `ContextMenu.Separator`, `ContextMenu.Label`, `ContextMenu.Sub`, `ContextMenu.SubTrigger`, `ContextMenu.SubContent`), viewport boundary collision prevention and clamping (`window.innerWidth`, `window.innerHeight`), nested submenus with edge-flipping support, checkbox and radio items with custom vector indicators, keyboard shortcut badges (`shortcut?: string`) rendered via `<kbd>`, destructive item styling, outside click/scroll/resize dismissal and Escape key dismiss with focus restoration, full WAI-ARIA 1.2 Menu pattern semantics (`role="menu"`, `role="menuitem"`, `role="menuitemcheckbox"`, `role="menuitemradio"`, `role="separator"`, `role="group"`, `aria-checked`, `aria-disabled`, `aria-haspopup="menu"`, `aria-expanded`), full keyboard navigation (`Escape`, `ArrowDown`/`ArrowUp`, `ArrowRight`/`ArrowLeft`, `Home`/`End`, `Tab`, `Enter`/`Space`), 4 futuristic visual variants ("neon", "glass", "bordered", "minimal"), 3 size presets; exports `render_context_menu_component` in `omnistackai_agent_engine.codegen` and registers in `NextjsWebAdapter.generate()`; 100% diff-invariant across ir.description; pure codegen change, offline, 0 network, 0 model calls (16 new focused tests in test_context_menu_component.py; 1,551 total; lint/security/env and builder:demo pass)

2026-09-11  R-347  Generated Accessible Futuristic Reusable Speed Dial & Floating Action Button Component (components/speed-dial.tsx) in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter emits a dedicated accessible futuristic SpeedDial compound component suite (`apps/web/components/speed-dial.tsx`) providing `SpeedDialDirection` ("up" | "down" | "left" | "right"), `SpeedDialVariant` ("neon" | "glass" | "bordered" | "minimal"), `SpeedDialSize` ("sm" | "md" | "lg"), `SpeedDialActionItem`, `SpeedDialProps`, `SpeedDialTriggerProps`, `SpeedDialActionProps`, `SpeedDialContentProps`, `SpeedDialContextValue`, compound subcomponents (`SpeedDial`, `SpeedDial.Trigger`, `SpeedDial.Action`, `SpeedDial.Content`), primary FAB with smooth 45° rotation toggle animation (`rotate(45deg)`), 4 directional action cascades ("up", "down", "left", "right") with absolute coordinate anchoring and staggered entrance/exit transitions, action item labels/tooltips with accessible screen-reader support, optional backdrop overlay (`backdrop?: boolean`) with subtle blur (`2px`) and click-to-dismiss, click-outside detection (`handlePointerDown`) and auto-close when clicking outside, controlled and uncontrolled open state management (`open`, `defaultOpen`, `onOpenChange`), full WAI-ARIA 1.2 Menu semantics (`role="menu"`, `role="menuitem"`, `aria-haspopup="menu"`, `aria-expanded`, `aria-controls`, `aria-labelledby`, `aria-orientation`), full keyboard navigation (`Escape` closes speed dial and returns focus to trigger, `ArrowUp`/`ArrowDown`/`ArrowLeft`/`ArrowRight` cycles through menu items, `Home`/`End` jumps to bounds, `Tab` closes menu), 4 futuristic visual variants ("neon", "glass", "bordered", "minimal"), 3 size presets; exports `render_speed_dial_component` in `omnistackai_agent_engine.codegen` and registers in `NextjsWebAdapter.generate()`; 100% diff-invariant across ir.description; pure codegen change, offline, 0 network, 0 model calls (16 new focused tests in test_speed_dial_component.py; 1,535 total; lint/security/env and builder:demo pass)

2026-09-11  R-346  Generated Accessible Futuristic Reusable PIN & OTP Code Input Component (components/pin-input.tsx) in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter emits a dedicated accessible futuristic PinInput compound component suite (`apps/web/components/pin-input.tsx`) providing `PinInputVariant` ("neon" | "glass" | "bordered" | "minimal"), `PinInputSize` ("sm" | "md" | "lg"), `PinInputType` ("numeric" | "alphanumeric" | "password"), `PinInputProps`, `PinInputGroupProps`, `PinInputSlotProps`, `PinInputSeparatorProps`, `PinInputContextValue`, compound subcomponents (`PinInput`, `PinInput.Group`, `PinInput.Slot`, `PinInput.Separator`), multi-slot discrete character entry with auto-advance on input and auto-retreat on Backspace, smart clipboard paste auto-distribution across slots, input types and masking/concealed mode (`mask={true}` or `type="password"`), native browser autofill support (`autocomplete="one-time-code"`), hidden input field integration for native form submissions, full keyboard navigation (`ArrowLeft`/`Right`, `Backspace`, `Delete`, `Home`, `End`), full WAI-ARIA 1.2 accessibility semantics (`role="group"`, `aria-label`, individual slot labelling with position and total count, `aria-hidden="true"` separator), 4 futuristic visual variants ("neon", "glass", "bordered", "minimal"); exports `render_pin_input_component` in `omnistackai_agent_engine.codegen` and registers in `NextjsWebAdapter.generate()`; 100% diff-invariant across ir.description; pure codegen change, offline, 0 network, 0 model calls (15 new focused tests in test_pin_input_component.py; 1,519 total; lint/security/env and builder:demo pass)

2026-09-11  R-345  Generated Accessible Futuristic Reusable Color Picker & Palette Swatch Component (components/color-picker.tsx) in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter emits a dedicated accessible futuristic ColorPicker compound component suite (`apps/web/components/color-picker.tsx`) providing `ColorPickerFormat` ("hex" | "rgb" | "hsl"), `ColorPickerVariant` ("neon" | "glass" | "bordered" | "minimal"), `ColorPickerSize` ("sm" | "md" | "lg"), `ColorSwatch`, `ColorPickerProps`, `ColorAreaProps`, `ColorSliderProps`, `ColorSwatchesProps`, `ColorPickerContextValue`, compound subcomponents (`ColorPicker`, `ColorPicker.Area`, `ColorPicker.HueSlider`, `ColorPicker.AlphaSlider`, `ColorPicker.Swatches`, `ColorPicker.Inputs`, `ColorPicker.EyeDropper`), pure mathematical color models without external packages (`hsvToRgb`, `rgbToHsv`, `rgbToHsl`, `parseHexColor`, `toHex`), interactive 2D saturation/brightness spectrum area, 1D hue slider and alpha opacity slider with pointer and touch drag tracking, format switcher toggling between HEX, RGB, and HSL with individual numeric/text controls, preset palette swatches with keyboard navigation (`role="listbox"`, `role="option"`, `aria-selected`), native browser EyeDropper API integration (`new window.EyeDropper()`) with graceful degradation when unsupported, full WAI-ARIA slider and listbox accessibility semantics (`role="slider"`, `role="listbox"`, `role="option"`, `aria-valuenow`, `aria-valuemin`, `aria-valuemax`, `aria-valuetext`, `aria-label`), keyboard navigation (`ArrowLeft`/`Right`, `ArrowUp`/`Down`, `Home`, `End`), 4 futuristic visual variants ("neon", "glass", "bordered", "minimal"); exports `render_color_picker_component` in `omnistackai_agent_engine.codegen` and registers in `NextjsWebAdapter.generate()`; 100% diff-invariant across ir.description; pure codegen change, offline, 0 network, 0 model calls (15 new focused tests in test_color_picker_component.py; 1,504 total; lint/security/env and builder:demo pass)

2026-09-11  R-344  Generated Accessible Futuristic Reusable Resizable Panels & Splitter Component (components/resizable.tsx) in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter emits a dedicated accessible futuristic Resizable compound component suite (`apps/web/components/resizable.tsx`) providing `ResizableDirection` ("horizontal" | "vertical"), `ResizableVariant` ("neon" | "glass" | "bordered" | "minimal"), `ResizablePanelGroupProps`, `ResizablePanelProps`, `ResizableHandleProps`, `ResizablePanelContextValue`, compound subcomponents (`ResizablePanelGroup`, `ResizablePanel`, `ResizableHandle`, `Resizable`), pointer and touch dragging with responsive coordinate calculation and live percentage sizing, minSize and maxSize constraints, collapsible panel support with collapsedSize, keyboard navigation per WAI-ARIA Separator (Window Splitter) pattern (`ArrowLeft`/`Right` or `ArrowUp`/`Down` with 1% step, 5% with Shift, `Home` to collapse, `End` to expand to max, `Enter` to toggle collapse), full WAI-ARIA separator accessibility semantics (`role="separator"`, `aria-orientation`, `aria-valuenow`, `aria-valuemin`, `aria-valuemax`, `aria-label`, `tabIndex={0}`), 4 futuristic visual variants ("neon", "glass", "bordered", "minimal"); exports `render_resizable_component` in `omnistackai_agent_engine.codegen` and registers in `NextjsWebAdapter.generate()`; 100% diff-invariant across ir.description; pure codegen change, offline, 0 network, 0 model calls (15 new focused tests in test_resizable_component.py; 1,489 total; lint/security/env and builder:demo pass)

2026-09-11  R-343  Generated Accessible Futuristic Reusable Carousel & Slider Showcase Component (components/carousel.tsx) in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter emits a dedicated accessible futuristic Carousel compound component suite (`apps/web/components/carousel.tsx`) providing `CarouselVariant` ("neon" | "glass" | "cards" | "minimal"), `CarouselTransition` ("slide" | "fade"), `CarouselOrientation` ("horizontal" | "vertical"), `CarouselIndicatorType` ("dots" | "fraction" | "progress" | "none"), `CarouselContextValue`, `CarouselProps`, `CarouselContentProps`, `CarouselSlideProps`, `CarouselPreviousProps`, `CarouselNextProps`, `CarouselIndicatorsProps`, `CarouselProgressProps`, `CarouselAutoplayToggleProps`, compound subcomponents (`Carousel`, `Carousel.Content`, `Carousel.Slide`, `Carousel.Previous`, `Carousel.Next`, `Carousel.Indicators`, `Carousel.Progress`, `Carousel.AutoplayToggle`), touch and swipe gesture handling with delta threshold, configurable autoplay with pause on hover/focus and play/pause toggle, keyboard navigation (`ArrowLeft`/`Right`, `ArrowUp`/`Down`, `Home`, `End`), full WAI-ARIA carousel semantics (`role="region"`, `aria-roledescription="carousel"`, `role="group"`, `aria-roledescription="slide"`, `aria-label`, `aria-hidden`, `aria-live`), controlled and uncontrolled index management; exports `render_carousel_component` in `omnistackai_agent_engine.codegen` and registers in `NextjsWebAdapter.generate()`; 100% diff-invariant across ir.description; pure codegen change, offline, 0 network, 0 model calls (15 new focused tests in test_carousel_component.py; 1,474 total; lint/security/env and builder:demo pass)


2026-09-11  R-342  Generated Accessible Futuristic Reusable Segmented Control & Mode Switcher Component (components/segmented-control.tsx) in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter emits a dedicated accessible futuristic Segmented Control and Mode Switcher component suite (`apps/web/components/segmented-control.tsx`) providing `SegmentedControlOption`, `SegmentedControlVariant` ("neon" | "glass" | "pills" | "minimal"), `SegmentedControlSize` ("sm" | "md" | "lg"), `SegmentedControlOrientation` ("horizontal" | "vertical"), `SegmentedControlProps`, `SegmentedControlOptionItemProps`, animated sliding active indicator pill with smooth cubic-bezier transitions (`cubic-bezier(0.4, 0, 0.2, 1)`), option labels, icons, disabled states, and notification badges, controlled and uncontrolled operation modes, full keyboard navigation (ArrowLeft/ArrowRight cycling, Home/End jump), full WAI-ARIA radiogroup semantics (`role="radiogroup"`, `role="radio"`, `aria-checked`, `aria-disabled`, `aria-orientation`, `tabIndex`), hidden input field integration for native form submissions; exports `render_segmented_control_component` in `omnistackai_agent_engine.codegen` and registers in `NextjsWebAdapter.generate()`; 100% diff-invariant across ir.description; pure codegen change, offline, 0 network, 0 model calls (15 new focused tests in test_segmented_control_component.py; 1,459 total; lint/security/env and builder:demo pass; committed and pushed per user instruction)
 
 2026-09-11  R-341  Generated Accessible Futuristic Reusable Radial Gauge & Activity Rings Component (components/radial-gauge.tsx) in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter emits a dedicated accessible futuristic Radial Gauge and Activity Rings component suite (`apps/web/components/radial-gauge.tsx`) providing `RadialGaugeProps`, `RadialGaugeRing`, `RadialGaugeRingProps`, `ActivityRingsProps`, `RadialGaugeVariant` ("neon" | "glass" | "gradient" | "minimal"), `RadialGaugeSize` ("sm" | "md" | "lg" | "xl"), pure SVG arc trigonometry (`polarToCartesian`, `describeArc`), configurable angle sweeps (240°, 270°, 360°), dynamic color thresholds (`resolveThresholdColor`), target goal markers with SVG ticks, glowing endpoint dots, concentric multi-ring activity mode (`ActivityRings` / `RadialGauge.Rings`), center readout slot with value & label formatting, WAI-ARIA `role="meter"` semantics (`aria-valuenow`, `aria-valuemin`, `aria-valuemax`, `aria-valuetext`); exports `render_radial_gauge_component` in `omnistackai_agent_engine.codegen` and registers in `NextjsWebAdapter.generate()`; 100% diff-invariant across ir.description; pure codegen change, offline, 0 network, 0 model calls (15 new focused tests in test_radial_gauge_component.py; 1,444 total; lint/security/env and builder:demo pass; left uncommitted per user instruction)

2026-09-11  R-340  Generated Accessible Futuristic Reusable Code Block & Syntax Presentation Component (components/code-block.tsx) in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter emits a dedicated accessible futuristic CodeBlock compound component suite (`apps/web/components/code-block.tsx`) providing `CodeSnippet`, `CodeBlockVariant` ("terminal" | "glass" | "neon" | "minimal"), `CodeBlockSize` ("sm" | "md" | "lg"), `TokenType`, `CodeToken`, `CodeBlockProps`, `CodeBlockHeaderProps`, `CodeBlockContentProps`, `CodeBlockLineProps`, `CodeBlockCopyButtonProps`, zero-dependency lexical tokenizer (`tokenizeCodeLine`) supporting TS/JS, Python, JSON, SQL, Bash, Go, Diff, multi-tab snippet switcher with keyboard navigation and active tab indicators, line numbering (`showLineNumbers`, `startLineNumber`), line highlighting supporting discrete lines and ranges (e.g. [2, 4, "7-10"]), git diff mode with emerald green additions (+) and rose red deletions (-), one-click copy-to-clipboard with smooth animated checkmark feedback, line wrap toggle (`wrapLines`), expandable/collapsible max-height view with bottom gradient fade mask, 4 futuristic visual variants ("terminal", "glass", "neon", "minimal"), WAI-ARIA accessibility semantics (`role="region"`, `role="tablist"`, `role="tab"`, keyboard scrollable `<pre tabIndex={0}>`), inline SVG vector icons (`TerminalDots`, `CopyIcon`, `CheckIcon`, `WrapIcon`, `ExpandIcon`); exports `render_code_block_component` in `omnistackai_agent_engine.codegen` and registers in `NextjsWebAdapter.generate()`; 100% diff-invariant across ir.description; pure codegen change, offline, 0 network, 0 model calls (15 new focused tests in test_code_block_component.py; 1,429 total; lint/security/env and builder:demo pass; left uncommitted per user instruction)

2026-09-11  R-339  Generated Accessible Futuristic Reusable Tag & Chip Input Tokenizer Component (components/tag-input.tsx) in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter emits a dedicated accessible futuristic Tag and Chip Tokenizer component suite (`apps/web/components/tag-input.tsx`) providing `TagItem`, `TagValue`, `TagInputVariant` ("default" | "glass" | "neon" | "bordered"), `TagInputSize` ("sm" | "md" | "lg"), `TagInputProps`, delimiter parsing on Enter/Comma/Tab, keyboard chip navigation (Backspace deletion, ArrowLeft/ArrowRight traversal), autocomplete suggestions dropdown with keyboard selection (ArrowDown/Up, Enter, Escape), validation (maxTags limit, duplicate rejection with visual warning feedback, custom validateTag predicate), clear-all action, prefix icon slot, WAI-ARIA Combobox / Listbox 1.2 compliance (`role="combobox"`, `role="listbox"`, `role="option"`, `aria-autocomplete="list"`, `aria-expanded`, `aria-activedescendant`), inline SVG icons (`TagIcon`, `XIcon`, `ClearIcon`); exports `render_tag_input_component` in `omnistackai_agent_engine.codegen` and registers in `NextjsWebAdapter.generate()`; 100% diff-invariant across ir.description; pure codegen change, offline, 0 network, 0 model calls (15 new focused tests in test_tag_input_component.py; 1,414 total; lint/security/env and builder:demo pass; left uncommitted per user instruction)

2026-09-11  R-338  Generated Accessible Reusable Hierarchical Tree View Component (components/tree-view.tsx) in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter emits a dedicated accessible Hierarchical Tree View component suite (`apps/web/components/tree-view.tsx`) providing `TreeNode` (id, label, icon, children, disabled, badge, data), `TreeViewVariant` ("default" | "bordered" | "ghost" | "lines"), `TreeViewProps`, single-select and multi-select modes with checkboxes, controlled and uncontrolled expansion state, search filter with automated ancestor branch auto-expansion and match highlighting (`<mark>`), visual hierarchy connector guide lines (`showLines`, `variant="lines"`), WAI-ARIA Tree View 1.2 compliance (`role="tree"`, `role="treeitem"`, `role="group"`, `aria-expanded`, `aria-selected`, `aria-level`, `aria-posinset`, `aria-setsize`, `aria-disabled`), complete keyboard navigation (`ArrowDown`, `ArrowUp`, `ArrowRight`, `ArrowLeft`, `Home`, `End`, `Enter`, `Space`, `*` to expand all siblings), inline SVG icons (`ChevronRightIcon`, `FolderClosedIcon`, `FolderOpenIcon`, `FileTextIcon`, `SearchIcon`); exports `render_tree_view_component` in `omnistackai_agent_engine.codegen` and registers in `NextjsWebAdapter.generate()`; 100% diff-invariant across ir.description; pure codegen change, offline, 0 network, 0 model calls (15 new focused tests in test_tree_view_component.py; 1,399 total; lint/security/env and builder:demo pass; left uncommitted per user instruction)

2026-09-11  R-337  Generated Accessible Futuristic Reusable Stat & Metric KPI Card Component (components/stat-card.tsx) in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter emits a dedicated accessible futuristic StatCard component suite (`apps/web/components/stat-card.tsx`) providing `StatCardVariant` ("default" | "glass" | "bordered" | "glow"), `StatTrend` ("positive" | "negative" | "neutral"), compound subcomponents (`StatCard`, `StatCardHeader`, `StatCardValue`, `StatCardDelta`, `StatCardSparkline`, `StatCardFooter`), pure mathematical SVG spline curve (`computeSplinePath`) generating smooth cubic-bezier `C` control points and `<linearGradient>` area fill beneath the curve, interactive pointer hover tracking dot on active coordinates, directional trend delta badge with accessible aria-label, glassmorphic backdrop filter and ambient glow border styling, WAI-ARIA `role="region"` / `role="button"` with `tabIndex={0}` and keyboard `Enter`/`Space` activation; exports `render_stat_card_component` in `omnistackai_agent_engine.codegen` and registers in `NextjsWebAdapter.generate()`; 100% diff-invariant across ir.description; pure codegen change, offline, 0 network, 0 model calls (15 new focused tests in test_stat_card_component.py; 1,384 total; lint/security/env and builder:demo pass; left uncommitted per user instruction)

2026-09-11  R-336  Generated Accessible Reusable Timeline / Activity Feed Component (components/timeline.tsx) in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter emits a dedicated accessible Timeline component suite (`apps/web/components/timeline.tsx`) providing `TimelineVariant` ("default" | "compact" | "centered"), `TimelineItemStatus` ("pending" | "active" | "completed" | "error" | "warning"), `TimelineItem`, `TimelineProps`, vertical connector line between entries with status-colored fill, built-in vector status icons (completed checkmark, error X, warning alert, active pulsing dot, pending clock) with custom icon slot override (`item.icon`) and action slot (`item.action`), timestamp rendered as semantic `<time>` element, alternating left/right layout for `centered` variant, compact variant with reduced spacing, WAI-ARIA accessibility (`role="list"` on container, `role="listitem"` on each item, `aria-label`); exports `render_timeline_component` in `omnistackai_agent_engine.codegen` and registers in `NextjsWebAdapter.generate()`; 100% diff-invariant across ir.description; pure codegen change, offline, 0 network, 0 model calls (20 new focused tests in test_timeline_component.py; 1,369 total; lint/security/env and builder:demo pass; left uncommitted per user instruction)

2026-09-11  R-335  Generated Accessible Reusable File Upload / Dropzone Component (components/file-upload.tsx) in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter emits a dedicated accessible File Upload and Dropzone component suite (`apps/web/components/file-upload.tsx`) providing `FileUploadStatus` ("idle" | "dragging" | "uploading" | "success" | "error"), `FileUploadVariant` ("default" | "compact" | "avatar"), `FileEntry`, `FileUploadProps`, drag-and-drop zone with drag-over visual styling, click-to-browse via hidden file input, keyboard trigger (`Enter`, `Space`), file validation (`accept`, `maxSize`, `maxFiles`), file preview list with formatBytes size display, image thumbnail preview, upload progressbar (`role="progressbar"`), remove button, avatar circular dropzone with camera overlay icon, WAI-ARIA accessibility (`role="button"`, `aria-label`, `aria-describedby`, `aria-live="polite"`, `aria-disabled`); exports `render_file_upload_component` in `omnistackai_agent_engine.codegen` and registers in `NextjsWebAdapter.generate()`; 100% diff-invariant across ir.description; pure codegen change, offline, 0 network, 0 model calls (20 new focused tests in test_file_upload_component.py; 1,349 total; lint/security/env and builder:demo pass; left uncommitted per user instruction)

2026-09-11  R-334  Generated Accessible Reusable Stepper / Multi-step Wizard Component (components/stepper.tsx) in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter emits a dedicated accessible Stepper component suite (`apps/web/components/stepper.tsx`) providing `StepperOrientation` ("horizontal" | "vertical"), `StepStatus` ("idle" | "active" | "completed" | "error"), `StepperVariant` ("default" | "dots" | "progress"), `StepDef`, `StepIndicator` with numeric, checkmark, and error-X states, `StepConnector`, `StepContent` tabpanel, `useStepperState` hook (`activeStep`, `goNext`, `goPrev`, `goTo`, `canGoNext`, `canGoPrev`), controlled and uncontrolled modes, WAI-ARIA tablist/tab/tabpanel pattern (`role="tablist"`, `role="tab"`, `aria-selected`, `aria-controls`, `role="tabpanel"`, `aria-labelledby`, `aria-disabled`), full keyboard navigation (`ArrowRight`/`Left`, `ArrowDown`/`Up`, `Home`, `End`), and progress-bar variant (`role="progressbar"`); exports `render_stepper_component` in `omnistackai_agent_engine.codegen` and registers in `NextjsWebAdapter.generate()`; 100% diff-invariant across ir.description; pure codegen change, offline, 0 network, 0 model calls (19 new focused tests in test_stepper_component.py; 1,329 total; lint/security/env and builder:demo pass; left uncommitted per user instruction)

2026-09-11  R-333  Generated Accessible Reusable Rating & Review Component (components/rating.tsx) in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter emits a dedicated accessible Rating and Review component suite (`apps/web/components/rating.tsx`) providing `RatingSize`, `RatingIcon`, `RatingProps`, `Rating` supporting interactive hover preview, half-star selection (`allowHalf`), read-only mode, disabled state, full keyboard navigation (Arrow keys, `Home`, `End`), built-in inline vector icons (`star`, `heart`, `thumb`) with precise fractional fill rendering via overlay clipping, numeric score display formatting (`showScore`, `formatScore`), size presets (`sm`, `md`, `lg`), WAI-ARIA slider pattern semantics (`role="slider"`, `aria-valuenow`, `aria-valuemin`, `aria-valuemax`, `aria-valuetext`, `aria-readonly`, `aria-disabled`); exports `render_rating_component` in `omnistackai_agent_engine.codegen` and registers in `NextjsWebAdapter.generate()`; 100% diff-invariant across ir.description; pure codegen change, offline, 0 network, 0 model calls (13 new focused tests in test_rating_component.py; 1,310 total; lint/security/env and builder:demo pass; left uncommitted per user instruction)

2026-09-11  R-332  Generated Accessible Reusable Progress & Spinner Component (components/progress.tsx) in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter emits a dedicated accessible Progress indicator and loading animation component suite (`apps/web/components/progress.tsx`) providing `ProgressVariant`, `ProgressSize`, `ProgressBarProps`, `CircularProgressProps`, `SpinnerProps`, `ProgressBar` (and `Progress` alias) supporting linear determinate mode (`value`, `min`, `max`, `showValue`, `formatValue`) and indeterminate animated shimmer/pulse mode, WAI-ARIA progressbar semantics (`role="progressbar"`, `aria-valuenow`, `aria-valuemin`, `aria-valuemax`, `aria-valuetext`; omitting `aria-valuenow` when indeterminate per WAI-ARIA specification), striped gradient pattern and animated stripes (`striped`, `animated`), `CircularProgress` with SVG circle stroke calculations, percentage fill, continuous spinning sweep, and center label, lightweight `Spinner` with SVG loader circle, `role="status"`, `aria-live="polite"`, and screen-reader accessible announcement label (`sr-only` span, defaulting to "Loading..."), size presets (`sm`, `md`, `lg`), and semantic color variants (`default`, `primary`, `success`, `warning`, `error`, `info`); exports `render_progress_component` in `omnistackai_agent_engine.codegen` and registers in `NextjsWebAdapter.generate()`; 100% diff-invariant across ir.description; pure codegen change, offline, 0 network, 0 model calls (13 new focused tests in test_progress_component.py; 1,297 total; lint/security/env and builder:demo pass; left uncommitted per user instruction)

2026-09-11  R-331  Generated Accessible Reusable Slider & Range Component (components/slider.tsx) in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter emits a dedicated accessible Slider and Range component suite (`apps/web/components/slider.tsx`) providing `SliderOrientation` ("horizontal" | "vertical"), `SliderValue` (number | [number, number]), `SliderMark`, `SliderProps`, single-value and dual-thumb range selection modes, range crossover prevention, interactive pointer dragging (pointerdown, pointermove, pointerup) on track and thumbs, keyboard navigation (`ArrowRight`/`ArrowUp`, `ArrowLeft`/`ArrowDown`, `PageUp`/`PageDown`, `Home`/`End`), WAI-ARIA slider pattern semantics (`role="slider"`, `tabIndex`, `aria-valuenow`, `aria-valuemin`, `aria-valuemax`, `aria-orientation`, `aria-disabled`, `aria-label`, `aria-valuetext`), tick marks and labels rendering (`marks`), and value display badge (`showValue`, `formatValue`); exports `render_slider_component` in `omnistackai_agent_engine.codegen` and registers in `NextjsWebAdapter.generate()`; 100% diff-invariant across ir.description; pure codegen change, offline, 0 network, 0 model calls (13 new focused tests in test_slider_component.py; 1,284 total; lint/security/env and builder:demo pass; left uncommitted per user instruction)

2026-09-11  R-330  Generated Accessible Command Palette / Search Menu Component (components/command-palette.tsx) in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter emits a dedicated accessible Command Palette and Search Menu component suite (`apps/web/components/command-palette.tsx`) providing `CommandItem`, `CommandGroup`, `CommandPaletteProps`, global `Cmd+K` / `Ctrl+K` keyboard shortcut listener (`triggerShortcut?: boolean`, default `true`), real-time search filtering across item `label`, `description`, `group`, and `keywords`, keyboard navigation (`ArrowDown`/`ArrowUp` traversal with boundary wrapping and disabled skipping, `Home`/`End` first/last jump, `Enter` selection trigger, `Escape` dismissal with focus restoration), WAI-ARIA combobox/listbox pattern (`role="combobox"`, `role="listbox"`, `role="option"`, `aria-autocomplete="list"`, `aria-expanded="true"`, `aria-haspopup="listbox"`, `aria-controls`, `aria-activedescendant`, `aria-selected`, `aria-disabled`), group headings under `role="group"` with `aria-labelledby`, shortcut badges (`<kbd>`), configurable `emptyMessage`, modal backdrop overlay (`role="dialog"`, `aria-modal="true"`, `backdropFilter: "blur(4px)"`), and body scroll lock management; exports `render_command_palette_component` in `omnistackai_agent_engine.codegen` and registers in `NextjsWebAdapter.generate()`; 100% diff-invariant across ir.description; pure codegen change, offline, 0 network, 0 model calls (13 new focused tests in test_command_palette_component.py; 1,271 total; lint/security/env and builder:demo pass; left uncommitted per user instruction)

2026-09-11  R-329  Generated Accessible Reusable Data Grid / Table Component (components/data-grid.tsx) in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter emits a dedicated accessible Data Grid and Table component suite (`apps/web/components/data-grid.tsx`) providing generic `ColumnDef<T>`, `DataGridProps<T>`, `SortDirection` ("asc" | "desc" | null), `SortState`, `DataGridDensity` ("compact" | "comfortable" | "spacious"), sortable column headers with WAI-ARIA `aria-sort` cycle, visual sort indicator SVGs (up/down/dual arrows), keyboard sort toggling, row selection checkboxes with header select-all (checked, unchecked, indeterminate states), `aria-selected` row attribute, display density presets with proportional cell padding and font sizes, `stickyHeader` with fixed position `<thead>` and border preservation, `striped` alternating row styling, `hoverable` row hover background transitions, loading skeleton placeholder rows with animated pulsing divs, `role="status"`, and `aria-busy="true"`, and `emptyState` fallback rendering; exports `render_data_grid_component` in `omnistackai_agent_engine.codegen` and registers in `NextjsWebAdapter.generate()`; 100% diff-invariant across ir.description; pure codegen change, offline, 0 network, 0 model calls (13 new focused tests in test_data_grid_component.py; 1,258 total; lint/security/env and builder:demo pass; left uncommitted per user instruction)

2026-09-11  R-328  Generated Accessible Reusable Date Picker Component (components/date-picker.tsx) in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter emits a dedicated accessible Date Picker and Calendar component suite (`apps/web/components/date-picker.tsx`) providing `DateFormatter`, `CalendarProps`, `DatePickerProps`, `Calendar` month grid view with month/year navigation buttons, weekday headers with `<abbr>`, WAI-ARIA `role="grid"`, `role="row"`, `role="gridcell"`, `aria-selected`, `aria-current="date"`, and `aria-disabled`, full keyboard navigation (Arrow keys, Home, End, PageUp, PageDown, Enter, Space), quick-select "Today" and optional "Clear" actions, `DatePicker` trigger button with calendar SVG icon, `aria-haspopup="dialog"`, `aria-expanded`, formatted date text, clearable button (`clearable`, `aria-label="Clear date"`), floating popover container with `role="dialog"`, `aria-modal="false"`, `aria-label="Choose date"`, dismiss on outside click, and dismiss on Escape with focus restoration, placement styling, error and helper text rendering, and zero-dependency date utilities (`formatDate`, `isSameDay`, `isToday`); exports `render_date_picker_component` in `omnistackai_agent_engine.codegen` and registers in `NextjsWebAdapter.generate()`; 100% diff-invariant across ir.description; pure codegen change, offline, 0 network, 0 model calls (13 new focused tests in test_date_picker_component.py; 1,245 total; lint/security/env and builder:demo pass; left uncommitted per user instruction)

2026-09-10  R-327  Generated Accessible Reusable Form Controls & Input Primitives (components/form-controls.tsx) in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter emits a dedicated accessible Form Controls and Input primitives component suite (`apps/web/components/form-controls.tsx`) providing `InputSize` ("sm" | "md" | "lg"), `sizeMap`, `Input` with size presets, prefix slot, suffix slot, and optional clear button (`onClear`), `Textarea` with auto/custom rows and live character counter (`showCount`, `maxLength`), `Select` with options array rendering, placeholder support, and custom SVG chevron, `Checkbox` with checked, unchecked, indeterminate states and label pairing, `RadioGroup` and `Radio` with `RadioGroupContext`, WAI-ARIA `role="radiogroup"`, `role="radio"`, `aria-checked`, and full keyboard Arrow navigation (`ArrowDown`, `ArrowUp`, `ArrowRight`, `ArrowLeft`), `Label` with optional red asterisk indicator (`*`), `FormField` compound container auto-generating unique IDs via `useId()`, linking `htmlFor`, and wiring `aria-invalid` and `aria-describedby`, `FormMessage` with `role="alert"` and `aria-live="polite"`, and `FormHelperText`; exports `render_form_controls_component` in `omnistackai_agent_engine.codegen` and registers in `NextjsWebAdapter.generate()`; 100% diff-invariant across ir.description; pure codegen change, offline, 0 network, 0 model calls (13 new focused tests in test_form_controls_component.py; 1,232 total; lint/security/env and builder:demo pass; left uncommitted per user instruction)

2026-09-10  R-326  Generated Accessible Reusable Dialog / Modal Component (components/dialog.tsx) in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter emits a dedicated accessible compound Dialog / Modal component (`apps/web/components/dialog.tsx`) providing `DialogSize` ("sm" | "md" | "lg" | "xl" | "full"), `sizeMap`, `DialogContextValue`, `Dialog` root provider with controlled and uncontrolled state support, `DialogTrigger` supporting `asChild` delegation and trigger ref capture, `DialogPortal`, `DialogOverlay` with dark tint and backdrop blur, `DialogContent` with `role="dialog"`, `aria-modal="true"`, dynamic `aria-labelledby` and `aria-describedby` wiring, Escape key dismiss listener, document body scroll lock, focus restoration upon dismiss, and optional accessible close button (`aria-label="Close dialog"` with SVG icon), `DialogHeader`, `DialogTitle`, `DialogDescription`, `DialogBody`, `DialogFooter`, `DialogClose`, and `useDialog()` hook; exports `render_dialog_component` in `omnistackai_agent_engine.codegen` and registers in `NextjsWebAdapter.generate()`; 100% diff-invariant across ir.description; pure codegen change, offline, 0 network, 0 model calls (13 new focused tests in test_dialog_component.py; 1,219 total; lint/security/env and builder:demo pass; left uncommitted per user instruction)

2026-09-10  R-325  Generated Theme Switcher / Mode Toggle Component (components/theme-toggle.tsx) in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter emits a dedicated accessible compound Theme Switcher component (`apps/web/components/theme-toggle.tsx`) providing `ThemeMode` ("light" | "dark" | "system"), `ResolvedTheme` ("light" | "dark"), `ThemeContextValue`, `ThemeProvider` context with localStorage sync and `window.matchMedia("(prefers-color-scheme: dark)")` system theme listening, `useTheme` hook with fallback defaults, `ThemeToggle` button with size presets ("sm", "md", "lg"), accessible labels, and inline SVG Sun / Moon vector icons, `ThemeSelect` segmented control with WAI-ARIA `role="radiogroup"`, `role="radio"`, and `aria-checked` semantics, and `ThemeScript` inline head injection snippet for FOUC prevention; exports `render_theme_toggle_component` in `omnistackai_agent_engine.codegen` and registers in `NextjsWebAdapter.generate()`; 100% diff-invariant across ir.description; pure codegen change, offline, 0 network, 0 model calls (12 new focused tests in test_theme_toggle_component.py; 1,206 total; lint/security/env and builder:demo pass; left uncommitted per user instruction)

2026-09-10  R-324  Generated Design Tokens & CSS Custom Properties Theming Engine (styles/tokens.css) in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter emits a dedicated, production-grade Design Tokens and CSS Custom Properties stylesheet (`apps/web/styles/tokens.css`) providing semantic colors for light (`:root`) and dark (`[data-theme="dark"]`, `:root.dark`, `body.dark`, `@media (prefers-color-scheme: dark)`) themes, scale tokens for spacing (`--space-0` through `--space-24`), typography (system sans, monospace, font sizes xs through 4xl, font weights light through bold, line heights), border radii (`--radius-none` through `--radius-full`), box shadows (`--shadow-none` through `--shadow-xl`), z-indices (`--z-dropdown` through `--z-tooltip`), and motion transitions with reduced-motion support (`@media (prefers-reduced-motion: reduce)`); updates `apps/web/app/globals.css` with `@import "../styles/tokens.css";` and root base layout styles; imports `globals.css` into `app/layout.tsx`; exports `render_design_tokens` and `render_globals_css` in `omnistackai_agent_engine.codegen` and registers in `NextjsWebAdapter.generate()`; 100% diff-invariant across ir.description; pure codegen change, offline, 0 network, 0 model calls (15 new focused tests in test_theming_tokens.py; 1,194 total; lint/security/env and builder:demo pass; left uncommitted per user instruction)

2026-09-10  R-323  Generated Accessible Reusable Popover Component (components/popover.tsx) in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter emits a dedicated accessible compound Popover component (`apps/web/components/popover.tsx`) comprising `Popover`, `PopoverTrigger`, `PopoverContent`, `PopoverClose`, and `PopoverArrow`; supports alignment (`"start"` | `"end"` | `"center"`), side placement (`"top"` | `"bottom"` | `"left"` | `"right"`), click-outside and Escape dismiss with trigger focus restoration, controlled and uncontrolled operation modes, WAI-ARIA dialog semantics (`role="dialog"`, `aria-modal="true"`, `aria-haspopup="dialog"`, `aria-expanded`, `aria-controls`), accessible close button with `aria-label="Close popover"`, and pointing orientation arrow indicator (`aria-hidden="true"`); exports `render_popover_component` in `omnistackai_agent_engine.codegen` and registers in `NextjsWebAdapter.generate()`; 100% diff-invariant across ir.description; pure codegen change, offline, 0 network, 0 model calls (11 new focused tests in test_popover_component.py; 1,179 total; lint/security/env and builder:demo pass; left uncommitted per user instruction)

2026-09-10  R-322  Generated Accessible Reusable Dropdown Menu Component (components/dropdown-menu.tsx) in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter emits a dedicated accessible compound Dropdown Menu component (`apps/web/components/dropdown-menu.tsx`) comprising `DropdownMenu`, `DropdownMenuTrigger`, `DropdownMenuContent`, `DropdownMenuItem`, `DropdownMenuSeparator`, and `DropdownMenuLabel`; supports alignment (`"start"` | `"end"` | `"center"`), side placement (`"top"` | `"bottom"` | `"left"` | `"right"`), click-outside and Escape dismiss with trigger focus restoration, full keyboard navigation (`ArrowDown`, `ArrowUp`, `Home`, `End`, `Enter`, `Space`) with active index focus cycling, WAI-ARIA 1.2 menu semantics (`role="menu"`, `role="menuitem"`, `aria-haspopup="menu"`, `aria-expanded`, `aria-controls`, `aria-labelledby`, `role="separator"`), disabled item states with focus skipping, destructive action variant, and optional icon and shortcut slots; exports `render_dropdown_menu_component` in `omnistackai_agent_engine.codegen` and registers in `NextjsWebAdapter.generate()`; 100% diff-invariant across ir.description; pure codegen change, offline, 0 network, 0 model calls (11 new focused tests in test_dropdown_menu_component.py; 1,168 total; lint/security/env and builder:demo pass; left uncommitted per user instruction)

2026-09-10  R-321  Generated Accessible Reusable Accordion Component (components/accordion.tsx) in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter emits a dedicated accessible compound Accordion component (`apps/web/components/accordion.tsx`) comprising `Accordion`, `AccordionItem`, `AccordionTrigger`, and `AccordionContent`; supports accordion types (`"single"` | `"multiple"`), collapsible mode toggle, styling variants (`"default"` | `"bordered"` | `"separated"`), controlled and uncontrolled operation modes, WAI-ARIA 1.2 accordion semantics (`aria-expanded`, `aria-controls`, dynamic `id`, `role="region"`, `aria-labelledby`, and `hidden`), rotating chevron SVG indicator (`aria-hidden="true"`, `transform: rotate(180deg)`), and disabled item states; exports `render_accordion_component` in `omnistackai_agent_engine.codegen` and registers in `NextjsWebAdapter.generate()`; 100% diff-invariant across ir.description; pure codegen change, offline, 0 network, 0 model calls (11 new focused tests in test_accordion_component.py; 1,157 total; lint/security/env and builder:demo pass; left uncommitted per user instruction)

2026-09-10  R-320  Generated Accessible Reusable Toggle Switch Component (components/toggle.tsx) in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter emits a dedicated accessible Toggle Switch component (`apps/web/components/toggle.tsx`) comprising `Toggle` and `ToggleSwitch` alias; supports sizes (`"sm"` | `"md"` | `"lg"`), controlled and uncontrolled operation modes, optional label and description slots with automated ID binding (`aria-labelledby`, `aria-describedby`), WAI-ARIA 1.2 switch semantics (`role="switch"`, `aria-checked`, `tabIndex`, focus ring), full keyboard accessibility (`Space` and `Enter` keydown toggles with `preventDefault`), and hidden `<input type="hidden">` for HTML form integration; exports `render_toggle_component` in `omnistackai_agent_engine.codegen` and registers in `NextjsWebAdapter.generate()`; 100% diff-invariant across ir.description; pure codegen change, offline, 0 network, 0 model calls (11 new focused tests in test_toggle_component.py; 1,146 total; lint/security/env and builder:demo pass; left uncommitted per user instruction)

2026-09-10  R-319  Generated Accessible Reusable Avatar Component (components/avatar.tsx) in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter emits a dedicated accessible compound Avatar component (`apps/web/components/avatar.tsx`) comprising `Avatar` and `AvatarGroup`; supports shape variants (`"circle"` | `"rounded"` | `"square"`), standard sizes (`"xs"` | `"sm"` | `"md"` | `"lg"` | `"xl"`), presence status indicators (`"online"` | `"offline"` | `"busy"` | `"away"`), 3-tier fallback cascade (Image with onError -> Initials with deterministic background color hashing -> generic SVG vector silhouette with `aria-hidden="true"`), WAI-ARIA image and status semantics (`role="img"`, `aria-label`), and `AvatarGroup` with overlapping negative margins, `max` display limit, and `+N` excess overflow badge; exports `render_avatar_component` in `omnistackai_agent_engine.codegen` and registers in `NextjsWebAdapter.generate()`; 100% diff-invariant across ir.description; pure codegen change, offline, 0 network, 0 model calls (11 new focused tests in test_avatar_component.py; 1,135 total; lint/security/env and builder:demo pass; left uncommitted per user instruction)

2026-09-10  R-318  Generated Accessible Reusable Drawer / Sheet Component (components/drawer.tsx) in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter emits a dedicated accessible compound Drawer component (`apps/web/components/drawer.tsx`) comprising `Drawer`, `DrawerHeader`, `DrawerTitle`, `DrawerDescription`, `DrawerContent`, and `DrawerFooter`; supports position presets (`"left"` | `"right"` | `"top"` | `"bottom"`), responsive sizes (`"sm"` | `"md"` | `"lg"` | `"xl"` | `"full"`), WAI-ARIA modal dialog semantics (`role="dialog"`, `aria-modal="true"`, `aria-labelledby`, `aria-describedby`), backdrop overlay click dismiss, Escape key dismiss, accessible close button (`aria-label="Close drawer"`), and body scroll locking; exports `render_drawer_component` in `omnistackai_agent_engine.codegen` and registers in `NextjsWebAdapter.generate()`; 100% diff-invariant across ir.description; pure codegen change, offline, 0 network, 0 model calls (11 new focused tests in test_drawer_component.py; 1,124 total; lint/security/env and builder:demo pass; left uncommitted per user instruction)

2026-09-10  R-317  Generated Accessible Reusable Skeleton Loader Component (components/skeleton.tsx) in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter emits a dedicated accessible compound Skeleton component (`apps/web/components/skeleton.tsx`) comprising `Skeleton`, `SkeletonText`, `SkeletonCard`, and `SkeletonTable`; supports visual shape variants (`"text"` | `"circular"` | `"rectangular"` | `"rounded"`), animation modes (`"pulse"` | `"wave"` | `"none"`), WAI-ARIA loading semantics (`role="status"`, `aria-busy="true"`, `aria-live="polite"`), and accessible visually-hidden screen reader announcements; handles `@media (prefers-reduced-motion: reduce)` to disable animation for users sensitive to motion; exports `render_skeleton_component` in `omnistackai_agent_engine.codegen` and registers in `NextjsWebAdapter.generate()`; 100% diff-invariant across ir.description; pure codegen change, offline, 0 network, 0 model calls (10 new focused tests in test_skeleton_component.py; 1,113 total; lint/security/env and builder:demo pass; left uncommitted per user instruction)

2026-09-10  R-316  Generated Accessible Reusable Alert Component (components/alert.tsx) in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter emits a dedicated accessible compound Alert component (`apps/web/components/alert.tsx`) comprising `Alert`, `AlertTitle`, and `AlertDescription`; supports visual variants (`"info"` | `"success"` | `"warning"` | `"error"`), semantic WAI-ARIA alert and status roles (`role="alert"` for error, `role="status"` for info/success/warning; `aria-live="assertive"` or `"polite"`), built-in SVG vector icons with `aria-hidden="true"`, dismissible state with accessible close button (`aria-label="Dismiss alert"`), and custom action slots; exports `render_alert_component` in `omnistackai_agent_engine.codegen` and registers in `NextjsWebAdapter.generate()`; 100% diff-invariant across ir.description; pure codegen change, offline, 0 network, 0 model calls (10 new focused tests in test_alert_component.py; 1,103 total; lint/security/env and builder:demo pass; left uncommitted per user instruction)

2026-09-10  R-315  Generated Accessible Reusable Card Component (components/card.tsx) in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter emits a dedicated accessible compound Card component (`apps/web/components/card.tsx`) comprising `Card`, `CardHeader`, `CardTitle`, `CardDescription`, `CardContent`, and `CardFooter`; supports visual variants (`"default"` | `"bordered"` | `"flat"` | `"elevated"`), padding presets (`"none"` | `"sm"` | `"md"` | `"lg"`), polymorphic element rendering (`as?: "div" | "article" | "section"` for Card; `as?: "h1".."h6" | "div"` for CardTitle); handles interactive click and keyboard states (`onClick`, `role="button"`, `tabIndex={0}`, `Enter`/`Space` keydown trigger, and hover transitions); supports `CardHeader` with title, description, and right-aligned `action` slot; supports `CardFooter` with flex alignment presets (`"left"` | `"right"` | `"between"` | `"center"`); exports `render_card_component` in `omnistackai_agent_engine.codegen` and registers in `NextjsWebAdapter.generate()`; 100% diff-invariant across ir.description; pure codegen change, offline, 0 network, 0 model calls (11 new focused tests in test_card_component.py; 1,093 total; lint/security/env and builder:demo pass)

2026-09-10  R-313  Generated Collection Table Column Visibility Dropdown / Selector Controls in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter adds interactive, accessible column visibility dropdown controls ("Columns ▾") to collection screens (`_collection_screen_page`); emits `visibleColumns` state initialized to `true` for all display fields (`useState<Record<string, boolean>>({ ... })`) and `showColumnPicker` boolean state; implements `toggleColumn` handler guarding against hiding all columns (`currentVisible.length <= 1`); renders accessible dropdown trigger button in collection toolbar with `role="button"`, `aria-haspopup="true"`, `aria-expanded={showColumnPicker}`, and `aria-label="Toggle column visibility"`; renders accessible dropdown menu with `role="menu"` and `aria-label="Column visibility options"` containing checkbox toggle controls for each display field; conditionally renders table `<th>` headers and row `<td>` cells according to `visibleColumns[field.name] !== false`; strictly preserves backwards compatibility for row selection checkboxes, Actions/Details column, and colSpan integrity; 100% diff-invariant across ir.description; pure codegen change, offline, 0 network, 0 model calls (11 new focused tests in test_column_visibility.py; 1,074 total; lint/security/env and builder:demo pass)

2026-09-10  R-312  Generated Accessible Reusable Badge & Status Pill Component (components/badge.tsx) in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter emits a dedicated accessible Badge component (`apps/web/components/badge.tsx`) conforming to WAI-ARIA status semantics: `<span role="status" aria-label={ariaLabel}>`; implements `BadgeVariant` (`"success"` | `"warning"` | `"error"` | `"info"` | `"neutral"`), `BadgeSize` (`"sm"` | `"md"`), and `BadgeProps` (`children`, `variant`, `size`, `dot`, `pulse`, `style`, `className`, `ariaLabel`); includes built-in status dot indicator with `aria-hidden="true"` and variant-matching colors; exports `render_badge_component` in `omnistackai_agent_engine.codegen` and registers in `NextjsWebAdapter.generate()`; 100% diff-invariant across ir.description; pure codegen change, offline, 0 network, 0 model calls (12 new focused tests in test_badge_component.py; 1,063 total; lint/security/env and builder:demo pass)

2026-09-10  R-311  Generated Collection Table Display Density Toggle (Compact, Comfortable, Spacious) in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter adds interactive, accessible table display density toggle controls (`"compact"` | `"comfortable"` | `"spacious"`) to collection screens (`_collection_screen_page`); emits `density` state defaulting to `"comfortable"`, computing dynamic cell padding (`densityPadding = density === "compact" ? "6px 12px" : density === "spacious" ? "16px 20px" : "12px 16px"`) and table font size (`densityFontSize = density === "compact" ? 13 : 14`); renders accessible segmented control buttons in the collection toolbar with `role="group"`, `aria-label="Table display density"`, and `aria-pressed={density === ...}` attributes; adds `data-density={density}` attribute and `fontSize: densityFontSize` to `<table>`; applies `padding: densityPadding` to table row data cells (`<td>`) across checkboxes, data fields, and action buttons; strictly preserves backwards compatibility for Actions header (`<th style={{ padding: "12px 16px", textAlign: "right", fontWeight: 600, color: "#475569" }}>Actions</th>`); 100% diff-invariant across ir.description; pure codegen change, offline, 0 network, 0 model calls (10 new focused tests in test_table_density.py; 1,051 total; lint/security/env and builder:demo pass)

2026-09-10  R-310  Generated Accessible Reusable Tabs Component (components/tabs.tsx) in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter emits a dedicated accessible Tabs component (`apps/web/components/tabs.tsx`) conforming to WAI-ARIA 1.2 Tabs design pattern: `role="tablist"` with `aria-orientation`, `role="tab"` buttons with `aria-selected`, `aria-controls`, roving `tabIndex`, `id`, and `role="tabpanel"` containers with `aria-labelledby` and `hidden={activeTab !== id}`; implements `TabItem`, `TabsProps`, and `TabPanelProps` interfaces with `variant` (`"line"` | `"pills"`) and optional badge count pills; implements full keyboard navigation: `ArrowRight` (next enabled tab), `ArrowLeft` (previous enabled tab), `Home` (first enabled tab), `End` (last enabled tab) with automatic DOM focus management; exports `render_tabs_component` in `omnistackai_agent_engine.codegen` and registers in `NextjsWebAdapter.generate()`; 100% diff-invariant across ir.description; pure codegen change, offline, 0 network, 0 model calls (12 new focused tests in test_tabs_component.py; 1,041 total; lint/security/env and builder:demo pass)

2026-09-10  R-309  Generated Accessible Reusable Pagination Component (components/pagination.tsx) in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter emits a dedicated accessible Pagination component (`apps/web/components/pagination.tsx`) conforming to WAI-ARIA 1.2 pagination structure: `<nav aria-label="Pagination">`, Previous/Next labeled buttons, `aria-current="page"` on current active page button, and `<label htmlFor="pageSizeSelect">` with `<select id="pageSizeSelect" aria-label="Select page size">`; implements `PaginationProps` interface: `page`, `pageSize`, `total`, `totalPages`, `onPageChange`, `onPageSizeChange`, `pageSizeOptions`, `disabled`, `compact`, and `itemLabel`; adds direct page number navigation button rendering with dynamic ellipsis calculation (`getPageNumbers`) in standard mode; adds `compact` mode support for narrow/constrained viewports (drawers, detail subcollections, cards); exports `render_pagination_component` in `omnistackai_agent_engine.codegen` and registers in `NextjsWebAdapter.generate()`; 100% diff-invariant across ir.description; pure codegen change, offline, 0 network, 0 model calls (12 new focused tests in test_pagination_component.py; 1,029 total; lint/security/env and builder:demo pass)
2026-09-10  R-308  Generated Collection Screen JSON Data Export & Bulk Selection Export Controls in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter adds `handleExportJson(selectedOnly)` helper function in generated collection screens (`_collection_screen_page`); adds accessible `Export JSON` button to top toolbar calling `handleExportJson(false)`, disabled when `!data || data.length === 0`; adds accessible `Export JSON ({checkedIds.length})` button to bulk action bar calling `handleExportJson(true)`; JSON blob created with `application/json;charset=utf-8;` MIME type and formatted with 2-space indentation (`JSON.stringify(itemsToExport, null, 2)`); download filename formatted as `{plural.lower()}_export.json`; Object URL lifecycle properly revoked via `URL.revokeObjectURL(url)`; user feedback via `toast.info("Exported JSON successfully")`; 100% diff-invariant across ir.description; pure codegen change, offline, 0 network, 0 model calls (8 new focused tests in test_collection_json_export.py; 1,017 total; lint/security/env and builder:demo pass)

2026-09-10  R-307  Generated Accessible EmptyState Component & Screen Zero-State Integrations in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter emits a dedicated accessible EmptyState component (`apps/web/components/empty-state.tsx`) and integrates zero-state presentation into overview and screen fallback layouts; `EmptyState` renders a semantic, centered status card conforming to WAI-ARIA (`role="status"`, `aria-live="polite"`); includes built-in vector SVG illustrations (`"folder"`, `"search"`, `"document"`, `"inbox"`) with `aria-hidden="true"`; supports primary and secondary action dispatch rendering `<Link>` (when `href` is supplied) or `<button>` (when `onClick` is supplied); exported `render_empty_state_component` in `omnistackai_agent_engine.codegen` and registered `components/empty-state.tsx` in `NextjsWebAdapter.generate()`; 100% diff-invariant across ir.description; pure codegen change, offline, 0 network, 0 model calls (11 new focused tests in test_empty_state.py; 1,009 total; lint/security/env and builder:demo pass)
2026-09-10  R-306  Generated Accessible Breadcrumb Navigation Component & Screen Hierarchy in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter emits a dedicated accessible breadcrumbs navigation component (`apps/web/components/breadcrumbs.tsx`) and integrates hierarchical wayfinding in detail screens (`_detail_screen_page`) and form screens (`_form_screen_page`); `Breadcrumbs` renders a clean semantic trail conforming to WAI-ARIA 1.2 breadcrumb design pattern (`<nav aria-label="Breadcrumb">`, `<ol>`, `<li>`, `aria-current="page"`, separator `/`); detail screens mount `<Breadcrumbs items={breadcrumbs} />` displaying Overview (`/`) &rarr; Collection (`/{col_screen.id}` when available) &rarr; Record item (`{name} #{selectedId}` or `{name} Details`), while preserving existing `&larr; Back to {plural}` link for backwards compatibility; form screens mount `<Breadcrumbs items={breadcrumbs} />` displaying Overview (`/`) &rarr; Collection (`/{list_screen.id}` when available) &rarr; New/Edit item (`Edit {name}` or `New {name}`), while preserving existing `&larr; Back to {plural}` link; 100% diff-invariant across ir.description; pure codegen change, offline, 0 network, 0 model calls (9 new focused tests in test_breadcrumbs.py; 998 total; lint/security/env and builder:demo pass)
2026-09-10  R-305  Generated Keyboard Shortcuts Help Modal & Global Discovery Affordance in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter emits a dedicated accessible shortcuts cheat sheet modal (`apps/web/components/shortcuts-dialog.tsx`) and integrates global keyboard shortcut discovery in the navigation bar (`apps/web/components/navbar.tsx`); `ShortcutsDialog` renders a clean modal dialog (`role="dialog"`, `aria-modal="true"`, `aria-labelledby="shortcuts-dialog-title"`) displaying categorized shortcuts (Global: `?` Help Modal, `Esc` Dismiss; Collection: `/` Focus Search, `Esc` Clear Search/Filters; Detail: `[` / `←` Previous Record, `]` / `→` Next Record, `e` Edit Record, `Esc` Deselect; Form: `Cmd/Ctrl+Enter` Save & Submit, `Cmd/Ctrl+S` Save & Submit, `Esc` Blur / Cancel) with styled `<kbd>` key badges, backdrop click dismiss, and Escape key dismiss; Navbar mounts `ShortcutsDialog`, wires a global `?` keydown listener (guarded outside INPUT, TEXTAREA, SELECT, contentEditable), and renders a discoverable `Shortcuts (?)` trigger button with keyboard icon in the header actions bar; 100% diff-invariant across ir.description; pure codegen change, offline, 0 network, 0 model calls (14 new focused tests in test_shortcuts_dialog.py; 989 total; lint/security/env and builder:demo pass)
2026-09-10  R-304  Generated Accessible Confirmation Dialog — Replace window.confirm() with ConfirmDialog Component in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter replaces crude, blocking `window.confirm()` browser dialogs with an accessible, styled modal confirmation dialog component (`apps/web/components/confirm-dialog.tsx`) and `useConfirm` hook across all generated screens; `ConfirmDialog` renders modal backdrop, dialog card, title, message body, Confirm/Cancel buttons with focus management (autoFocus confirm button, focus trapping, Escape dismiss, backdrop click dismiss, WAI-ARIA `role="dialog"`, `aria-modal="true"`, `aria-labelledby`, `aria-describedby`) supporting danger (`#dc2626`) and primary (`#2563eb`) intent variants; `useConfirm` hook exports `confirmAsync(title, message, options) -> Promise<boolean>`; collection screens (`_collection_screen_page`), detail screens (`_detail_screen_page`), and form screens (`_form_screen_page`) replace single delete, batch delete, subcollection child delete, and unsaved changes guards (Cancel, Escape key, Reset) with `await confirmAsync(...)` and render `<ConfirmDialog {...confirmProps} />`; 100% diff-invariant across ir.description; pure codegen change, offline, 0 network, 0 model calls (31 new focused tests in test_confirm_dialog.py; 975 total; lint/security/env and builder:demo pass)
2026-09-10  R-303  Generated Dashboard Overview Interactive Entity Links, Operational Health Badge & Metrics Chips in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter adds interactive entity summary cards, operational health badge, and summary metrics counters to generated dashboard overview pages (`app/page.tsx` via `_overview_page`); listable entities with Op.LIST dynamically map to their matching collection screen and wrap in accessible `<Link href="/{col_screen.id}">` with `aria-label="View {plural} collection"`, uppercase entity tag, navigation arrow indicator, prominent live record total, and an interactive `View all &rarr;` affordance; unlinked entities without a matching collection screen render as styled summary `<div>` cards with live count; app header section upgraded to a responsive flex layout with a live "System Operational" status pill (`#f0fdf4`, `#166534`, border `#bbf7d0`) with green pulsing indicator dot (`#22c55e`) alongside summary count badges for total entities (`{count} Entities`) and total screens (`{count} Screens`); screen navigation cards render visual navigation arrow indicators (`&rarr;`) alongside screen titles; provides an accessible zero-state fallback card when neither entities nor screens are configured; 100% diff-invariant across ir.description; pure codegen change, offline, 0 network, 0 model calls (9 new focused tests in test_overview_dashboard_links_and_health.py; 944 total; lint/security/env and both builder demos pass)
2026-09-10  R-302  Generated Collection Screen Boolean & Enum Visual Status Badges & Detail Screen One-Click Copy-to-Clipboard Affordances in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter adds visual status pill badges and detail clipboard affordances across generated screens; `_field_value_jsx(field, expr)` formats boolean fields as styled emerald/slate status pill badges ("Yes" in `#166534` on `#dcfce7`, "No" in `#64748b` on `#f1f5f9`) and enum fields as styled blue categorical pill badges (`#1d4ed8` on `#eff6ff`, border `1px solid #bfdbfe`); applied across collection table cells, master-detail subcollection cards, and detail screen subcollection tabs; detail screens in `_detail_screen_page` render an accessible one-click "Copy ID" button in the record header (`aria-label="Copy ID to clipboard"`), inline "Copy" affordance on `id` and foreign key fields in definition list `<dl>`, and status badges on boolean/enum fields; provides robust `handleCopy(text, label)` with clipboard API and fallback plus toast feedback; unconstrained fields and existing hooks remain byte-identical; pure codegen change, offline, 0 network, 0 model calls (7 new focused tests in test_status_badges_and_copy_clipboard.py; 935 total; lint/security/env and both builder demos pass)
2026-09-10  R-301  Generated Web Search Input Clear Affordances & Form Screen First-Field AutoFocus in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter adds interactive Clear (×) buttons to collection and subcollection search inputs and automatic first-field autoFocus to form screens; collection search form in `_collection_screen_page` wraps input in an accessible relative container with an inline Clear button (×) rendered when `searchInput` is non-empty (`aria-label="Clear search"`), clearing local state (`setSearchInput("")`), committing empty search (`setSearch("")`), and refocusing input (`searchInputRef.current?.focus()`) on click; subcollection master-detail search in `_subcol_controls` renders an inline Clear button (×) clearing local search state and committing to hook; form screens in `_form_screen_page` automatically emit `autoFocus` on the first editable field (text, textarea, number, select, or checkbox) to enable immediate keyboard input; unconstrained fields and existing hooks remain byte-identical; pure codegen change, offline, 0 network, 0 model calls (6 new focused tests in test_search_clear_and_form_autofocus.py; 928 total; lint/security/env and both builder demos pass)
2026-09-10  R-300  Generated Form Screen Input Constraints, Native HTML Validation & Live Character Counters in Next.js web app (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): NextjsWebAdapter derives native HTML constraints from entity field validation rules (`parse_field_rules(f)`) for generated form screens; text and textarea fields with `max_length` emit `maxLength={rules.max_length}`, helper hint ("Max {max} characters"), and a live character counter ("{length} / {max}") with amber color threshold (`#b45309`) when reaching 90% capacity; numeric fields with `min` and/or `max` rules emit native `min` and `max` attributes and a range badge ("Range: {min} to {max}"); preserves the exact `{fieldErrors.<name> && <span style={{ color: "#ef4444", fontSize: 12, marginTop: 4, display: "block" }}>{fieldErrors.<name>}</span>}` error span for test compatibility; unconstrained fields remain 100% byte-identical; pure codegen change, offline, 0 network, 0 model calls (6 new focused tests in test_form_input_constraints.py; 922 total; lint/security/env and both builder demos pass)
2026-09-10  R-299  Wired power-user keyboard navigation and shortcuts into generated Next.js form screens without changing public hook APIs or backend semantics: pressing "Cmd+Enter" or "Ctrl+Enter" anywhere in the form (including text inputs and multiline textareas) triggers form submission via form.requestSubmit(); pressing "Cmd+S" or "Ctrl+S" triggers form submission and prevents the browser's native Save Page As dialog; pressing "Escape" while focused inside editable fields (INPUT, TEXTAREA, SELECT) blurs the active field; pressing "Escape" outside text inputs triggers Cancel navigation to parent list/overview with unsaved changes confirmation when dirty; submitting guards prevent duplicate concurrent submissions while saving; proper event listener cleanup on unmount with dependency array; all existing text labels, retry buttons, skeletons, and hook signatures are strictly preserved; 100% diff-invariant across ir.description; pure codegen change, offline, 0 network, 0 model calls (7 new focused tests in test_form_keyboard_shortcuts.py; 916 total; lint/security/env and both builder demos pass)
2026-09-10  R-298  Wired power-user keyboard navigation and shortcuts into generated Next.js detail screens without changing public hook APIs or backend semantics: pressing "ArrowLeft" or "[" outside editable form elements (INPUT, TEXTAREA, SELECT, contentEditable) navigates to the previous record when available (prevItem && handleSelectId(prevItem.id)); pressing "ArrowRight" or "]" outside editable elements navigates to the next record when available (nextItem && handleSelectId(nextItem.id)); pressing "e" or "E" outside editable elements switches to edit mode for the active record when edit capability and form screen exist (can_edit && form_screen && selectedId); pressing "Escape" outside editable elements deselects the current record (handleSelectId(null)); proper event listener cleanup on unmount with dependency array; all existing text labels, retry buttons, skeletons, and hook signatures are strictly preserved; 100% diff-invariant across ir.description; pure codegen change, offline, 0 network, 0 model calls (7 new focused tests in test_detail_keyboard_navigation.py; 909 total; lint/security/env and both builder demos pass)

2026-09-10  R-297  Wired power-user keyboard navigation and shortcuts into generated Next.js collection screens without changing public hook APIs or backend semantics: pressing "/" outside of existing editable form elements (INPUT, TEXTAREA, SELECT, contentEditable) immediately focuses the collection search input and prevents "/" keystroke insertion; pressing "Escape" when focused inside the collection search input resets searchInput, clears active committed search state (setSearch("")), and blurs the input; pressing "Escape" outside text inputs when active filters are present clears all collection filters (clearFilters()); proper event listener cleanup on unmount with dependency array; all existing text labels, retry buttons, skeletons, and hook signatures are strictly preserved; 100% diff-invariant across ir.description; pure codegen change, offline, 0 network, 0 model calls (8 new focused tests in test_collection_keyboard_navigation.py; 902 total; lint/security/env and both builder demos pass)

2026-09-10  R-296  Brought the generated Next.js web application to enterprise accessibility standards (WCAG 2.1 AA / WAI-ARIA best practices) across all screen types without changing public hook APIs or backend semantics: error banners across collection list, detail screen, subcollections (both collection and detail views), and form submission emit role="alert" and aria-live="assertive" for immediate assistive announcement; search inputs emit aria-label="Search <plural>" and aria-label="Search <child_plural>"; sortable table column headers in collection screens emit dynamic WAI-ARIA aria-sort matching params.sort and params.order ("ascending", "descending", or "none"); collection pagination footer is wrapped in <nav aria-label="Pagination"> with accessible Previous/Next button labels; subcollection pagination buttons emit aria-label="Previous page" and aria-label="Next page"; and contextual empty-state list messages emit role="status" for polite announcement; all existing text labels, retry buttons, skeletons, and hook signatures are strictly preserved; 100% diff-invariant across ir.description; pure codegen change, offline, 0 network, 0 model calls (13 new focused tests in test_screen_accessibility.py; 894 total; lint/security/env and both builder demos pass; implementation 002e90a)
2026-09-10  R-295  Brought every generated data-fetch error state to parity with the collection list, which already rendered a "Retry" button (calling `refetch()`) in its error banner — so every failed load now offers a recovery affordance: the detail-screen main error banner (`_detail_screen_page`) now renders `Error loading <name>: {error.message}` inside a `<span>` beside a `<button onClick={() => refetch()}>Retry</button>` in a flex row (`refetch` is already destructured from `use<Entity>(selectedId)`), and both subcollection (master-detail) error banners — the collection master-detail block in `_collection_screen_page` and the detail block in `_detail_screen_page`, byte-identical in the generator so updated via `replace_all` — now render `Error: {<s_var>.error.message}` inside a `<span>` beside a `<button onClick={() => <s_var>.refetch()}>Retry</button>`; the Retry buttons reuse the collection error banner's flex layout and dark-red (`#991b1b` background, white text, small) styling; the collection top-level error banner (which already had Retry) is unchanged, as are the loading/empty/data-render states, delete-error toasts, and form field errors; no hook/API-client/backend/Application-IR change (`refetch` already exists on every affected hook — `use<Entity>` and `useList<Child>By<Parent>`); description-only IR generation stays byte-identical; pure codegen change, offline, 0 network, 0 model calls (8 new focused tests in `test_fetch_error_retry.py`; the existing `test_subcollection_screens.py` `commentsSubcol.error.message` assertion is preserved; 881 total; lint/security/env and both builder demos pass; implementation `2454a43`)
2026-09-10  R-294  Emitted the four Next.js App Router "special files" the generated web app was missing, giving every generated app real runtime resilience (Next.js wires these automatically, so no route wiring/IR/hook change is needed): `app/error.tsx` — a `"use client"` route-segment error boundary default-exporting `Error({ error, reset }: { error: Error & { digest?: string }; reset: () => void })` that logs via `useEffect(() => console.error(error), [error])` and renders a recovery card ("Something went wrong", an optional `error.digest` reference line, a `reset()`-bound "Try again" `<button>`, and a "Back to overview" `<Link href="/">`); `app/global-error.tsx` — a `"use client"` root-layout error boundary (`GlobalError({ error, reset })`) that renders its OWN `<html lang="en"><body>` (required because it replaces the root layout) with the same `reset()` recovery; `app/not-found.tsx` — a server-component 404 ("404" / "Page not found" / `<Link href="/">` back to the overview); and `app/loading.tsx` — a server-component route-level Suspense fallback mapping `[0..5]` skeleton cards (`height: 96, background: "#f1f5f9", borderRadius: 8, opacity: 1 - i * 0.12`) in a `repeat(auto-fill, minmax(220px, 1fr))` grid plus a header bar (`height: 32, background: "#e2e8f0"`), reusing the R-292/293 skeleton visual language; all four are static, inline-styled to match the app aesthetic (`#f8fafc`/`#0f172a`/`#2563eb`), dependency-free, and never reference `ir.name`/`ir.description` — so generation stays deterministic, description-only IR generation stays byte-identical, and the four files never enter the console-snapshot description-edit diff set; templates live as module constants (`_ERROR_PAGE`, `_GLOBAL_ERROR_PAGE`, `_NOT_FOUND_PAGE`, `_LOADING_PAGE`) with `render_error_page`/`render_global_error_page`/`render_not_found_page`/`render_loading_page` accessors exported from `codegen` (mirroring `render_toast_component`); no existing generated file, hook, API client, backend, or Application-IR change; pure codegen change, offline, 0 network, 0 model calls (9 new focused tests in `test_app_router_resilience.py`; no existing assertion changed; 873 total; lint/security/env and both builder demos pass; implementation `c49bc5c`)
2026-09-10  R-293  Completed the R-292 loading-skeleton coverage by replacing the two remaining plain "Loading..." text spots in the generated Next.js app with layout-preserving skeleton placeholders (same static inline-styled gray rounded divs — no CSS `@keyframes`, no new component/file, no dependency): the form edit-mode initial-load banner (`{isEdit && fetchingInitial && (…)}` in `_form_screen_page`) now maps `[0, 1, 2]` skeleton field bars (`height: 34, background: "#e2e8f0", borderRadius: 6, opacity: 1 - i * 0.2`) inside its existing flex-column banner instead of the "Loading `<name>` details..." text, and the detail record-selector "recent records" list (`{loadingList && (…)}` in `_detail_screen_page`) now maps `[0, 1, 2]` skeleton cards (`height: 56, background: "#f1f5f9", borderRadius: 6, opacity: 1 - i * 0.2`) in the same `repeat(auto-fill, minmax(220px, 1fr))` grid as the record cards instead of the "Loading `<plural>`..." text; the R-292 collection/subcollection/detail-main skeletons, all other loading states, empty/error states, and data rendering are unchanged; no hook/API-client/backend/Application-IR change; description-only IR generation stays byte-identical; pure codegen change, offline, 0 network, 0 model calls (6 new focused tests in `test_loading_skeletons_extra.py`; one `test_form_update_screens.py` initial-load assertion updated to the skeleton markup; 864 total; lint/security/env and both builder demos pass; implementation `9796f4a`)
2026-09-10  R-292  Replaced the plain "Loading..." text in the generated Next.js screens' data-loading states with layout-preserving skeleton placeholders (static inline-styled gray rounded bars — no CSS `@keyframes`, no new component/file, no dependency, since the generated app uses inline styles throughout and has no CSS-injection point): the collection table `{loading && !data}` cell (colSpan-spanning) now maps `[0..4]` skeleton bars (`height: 14, background: "#e2e8f0", borderRadius: 4, margin: "10px 0", opacity: 1 - i * 0.15`), the subcollection (master-detail) list `{<s_var>.loading && !<s_var>.data}` block maps `[0..2]` skeleton blocks (`height: 44, background: "#f1f5f9"`) in both the collection master-detail and detail render sites, and the detail-screen main `{loading}` block maps `[0..3]` skeleton lines of varying width (`width: `${88 - i * 14}%``); the refresh-button "Loading..." labels, empty/error states, and data rendering are all unchanged; no hook/API-client/backend/Application-IR change; description-only IR generation stays byte-identical; pure codegen change, offline, 0 network, 0 model calls (9 new focused tests in `test_loading_skeletons.py`; two `test_subcollection_screens.py` loading assertions updated to the skeleton markup; 858 total; lint/security/env and both builder demos pass; implementation `16d6c52`)
2026-09-10  R-291  Extended the R-290 optimistic delete to the subcollection (master-detail) child lists in both the generated collection master-detail (`_collection_screen_page`) and detail (`_detail_screen_page`) screens: a new `_subcol_delete_names(s_var)` helper derives `<s_var>Deleting`/`set<S_var>Deleting`, and in the child delete-handler loop (both sites emit byte-identical handler blocks) each deletable subcollection now emits `const [<s_var>Deleting, set<S_var>Deleting] = useState<string[]>([]);` plus a reconcile `useEffect(() => set<S_var>Deleting((prev) => prev.filter((did) => (<s_var>.data ?? []).some((x: any) => String(x.id) === did))), [<s_var>.data])`, the child delete handler adds `String(id)` to the overlay before `await remove<Child>(id)` and rolls it back in `catch` before the existing `toast.error`, and the child row map iterates `(<s_var>.data ?? []).filter((child: any) => !<s_var>Deleting.includes(String((child as any).id)))` — so a deleted child row disappears immediately and reappears on failure. Non-deletable subcollections are byte-identical to before, both screens already import `useState`+`useEffect`, and it is safe because mutation hooks are deduped (R-288) and the LIST_BY hook is race-safe (R-286); the mutation/list hooks, generated API client, backend, and Application IR are all unchanged; description-only IR generation stays byte-identical; pure codegen change, offline, 0 network, 0 model calls (6 new focused tests in `test_subcollection_optimistic_delete.py`; no existing assertion changed; 849 total; lint/security/env and both builder demos pass; implementation `51f33c8`)
2026-09-10  R-290  Made the generated Next.js collection screen's deletes optimistic with rollback: single (`handleDelete`) and batch (`handleBatchDelete`) removals now add the affected ids to a `pendingDeleteIds` overlay (`useState<string[]>`) before `await remove(...)` so the rows vanish immediately, and on server error roll those ids back (rows reappear) before the existing error toast; a reconcile `useEffect(() => setPendingDeleteIds((prev) => prev.filter((id) => (data ?? []).some((x: any) => String(x.id) === id))), [data])` prunes ids once `refetch` has removed them (no flash-back, no unbounded growth), and the table maps a `visibleRows = displayData.filter((item) => !pendingDeleteIds.includes(String(item.id)))` list. `pendingDeleteIds`/`visibleRows` are emitted like the existing `checkedIds` selection state (all referenced, no unused-var), the delete handlers only exist when delete is wired, and it is safe because mutation hooks are deduped (R-288) and the list fetch is race-safe (R-280); self-contained in `_collection_screen_page` — the detail-screen/subcollection delete, the mutation/list hooks, the generated API client, backend, and Application IR are all unchanged; description-only IR generation stays byte-identical; pure codegen change, offline, 0 network, 0 model calls (9 new focused tests in `test_collection_optimistic_delete.py`; two `test_collection_field_filters.py` row-map assertions updated to `visibleRows`; 843 total; lint/security/env and both builder demos pass; implementation `1ed88b6`)
2026-09-10  R-289  Made the generated Next.js subcollection (master-detail) search live and debounced (300ms), bringing it to parity with the top-level collection search (R-280) — R-281 had shipped it submit-only, and it is now safe to make live because R-286 made the `useList<Child>By<Parent>` hook race-safe (superseded LIST_BY requests are aborted): new helpers `_subcol_search_names(s_var)` (derives `<s_var>Search`/`set<S_var>Search`) and `_subcol_search_state(s_var)` emit, next to each subcollection hook declaration, a controlled `useState("")` plus a `setTimeout`/`clearTimeout` 300ms debounce `useEffect` that commits `<state>.trim()` to `<s_var>.setSearch` (guarded by `<state> !== (params.q ?? "")` so an unchanged value is not recommitted), and `_subcol_controls`'s search input becomes controlled (`value`/`onChange`) instead of uncontrolled `defaultValue`+`FormData` while the form submit still commits immediately (Enter); wired into both the collection master-detail and detail screens (both already import `useState`+`useEffect`), entities without a subcollection emit none of it, and the hook, generated API client, backend, Application IR, and the sort/filter/pagination controls are all unchanged; description-only IR generation stays byte-identical; pure codegen change, offline, 0 network, 0 model calls (5 new focused tests in `test_subcollection_search_debounce.py`; R-281 `test_subcollection_list_controls.py` search assertions updated to the controlled form; 834 total; lint/security/env and both builder demos pass; implementation `b6a1af4`)
2026-09-10  R-288  Made the generated Next.js mutation hooks (`useCreate<Entity>`, `useUpdate<Entity>`, `useDelete<Entity>`) dedupe concurrent in-flight invocations, extending the race-safety work from fetches (R-280 LIST, R-286 LIST_BY, R-287 detail GET) to writes: each hook now owns `const pendingRef = useRef<Promise<T> | null>(null)` (T = `<Entity>` for create/update, `void` for delete; `useRef` already imported), the callback's first statement is `if (pendingRef.current) return pendingRef.current;` so a rapid double-submit (double-clicked Create/Save/Delete, or a programmatic re-call) awaits the same pending promise instead of firing a duplicate POST/PUT/DELETE, the existing try/catch/finally body runs inside an IIFE captured as `pendingRef.current = request;` and returned, and `finally` keeps `setLoading(false)` and adds `pendingRef.current = null;`. Public hook names, callback signatures, and return shapes (`{ create|update|remove, mutate, loading, error, reset }`) plus the underlying `api.create|update|delete<Entity>(...)` calls are unchanged; the fetch hooks (`useList<Entities>`, `useList<Child>By<Parent>`, `use<Entity>`), the generated API client, backend, and Application IR are untouched; description-only IR generation stays byte-identical; pure codegen change, offline, 0 network, 0 model calls (6 new focused tests in `test_mutation_inflight_guard.py`; existing `test_nextjs_hooks` mutation assertions unchanged; 829 total; lint/security/env and both builder demos pass; implementation `3d6eecf`)
2026-09-10  R-287  Made the generated Next.js `use<Entity>` single-record detail hook race-safe with an internally owned `AbortController`, closing the last generated data-fetch path without cancellation (LIST was R-280, LIST_BY was R-286): `refetch` aborts the previous GET before the `if (!id)` reset (which now also clears `error` alongside data/loading), a valid id registers a fresh controller, and the GET call passes the internal signal AFTER caller options (`api.get<Entity>(id, { ...options, signal: controller.signal })`) so a caller cannot replace the cancellation signal; an aborted successful response is discarded before `setData`, `AbortError`/controller-aborted failures do not surface as user errors, only the currently active request clears loading, and effect cleanup aborts on id change or unmount. The generated API client already forwards `signal` through `request(...)`'s `...init` spread, so no client change was needed; the public hook name/params/return, `useList<Entities>` (R-280) and `useList<Child>By<Parent>` (R-286) hooks, backend, and IR are unchanged, and description-only IR generation stays byte-identical; pure codegen change, offline, 0 network, 0 model calls (13 new focused tests in `test_detail_request_cancellation.py`, one existing `test_nextjs_hooks.py` assertion updated, 823 total; lint/security/env and both builder demos pass; implementation `793804e`)
2026-09-09  R-286  Made every generated Next.js `useList<Child>By<Parent>` hook race-safe with an internally owned `AbortController`: each refetch aborts the prior request before the missing-parent early return, valid requests pass the internal signal after caller options, aborted success and `AbortError` paths cannot update current data/total/error, only the active request clears loading, and effect cleanup aborts on dependency change or unmount. Filterable hooks retain flattened server-side filter params and all hooks retain public signatures and relation path scoping; no IR, backend, dependency, database, infrastructure, network, or model change (6 focused tests, all 69 subcollection tests, 810 total; lint/security/env and both builder demos pass; implementation `3eb8bd1`)
2026-09-09  R-285  Wired generated Next.js subcollection hooks and both parent collection/detail screen variants to R-284's server-side boolean/enum LIST_BY filter contract: filterable `useList<Child>By<Parent>` hooks now carry typed IR-allowlisted filter state, expose `setFilter`/`clearFilters`, reset offset on changes, and flatten active values into API query params while preserving the parent relation ID as the path argument; both subcollection views render boolean pills, enum selects, active-filter count, Reset, and filtered-empty Clear filters recovery while mapping server-returned rows directly with no page-local filtering. Non-filterable child output and description-only IR generation remain byte-stable; no IR, backend, dependency, database, infrastructure, network, or model change (7 focused tests, all 63 subcollection tests, 804 total; lint/security/env and both builder demos pass; implementation `92c89b1`)
2026-09-09  R-284  Extended the R-282 allowlisted boolean/enum equality filters from top-level LIST to unambiguous FK-scoped LIST_BY subcollection endpoints across generated FastAPI, Go, and OpenAPI: Python relation-scoped repository helpers now compose mandatory `<relation>_id`, optional `q`, and filter predicates with `%s`-bound values for matching list/count semantics; FastAPI LIST_BY routes declare typed optional bool/enum query parameters and forward them to both repository calls; Go emits a shared `<table>By<Relation>Filters` builder that preserves relation ID as `$1`, assigns optional search next, then filter values via `len(args)+1`, with pagination arguments after every predicate, and handlers parse/forward filters only for filterable children; OpenAPI documents the same filter schemas. Non-filterable LIST_BY output remains compatible and description-only generation stays byte-stable; no IR, Next.js, dependency, database, infrastructure, network, or model change (8 focused tests, 797 total; lint/security/env and both builder demos pass; implementation `0bfb91d`)
2026-09-09  R-283  Wired generated Next.js top-level collection filters to the R-282 server-side `?<field>=<value>` backend contract so boolean/enum filtering is correct across pagination: filterable `useList` hooks now carry an allowlisted `filters` map, expose `setFilter`/`clearFilters` with page reset, flatten active filters into `ApiOptions.params`, and hydrate/sync only valid generated filter fields through the existing R-280 URL state; collection screens drive the hook from the existing pills/selects, render server-returned data directly, retain active-count/Reset/no-results UX, and remove `useMemo` plus page-local `data.filter`; non-filterable screens emit no controls/options and `LIST_BY`/backends remain unchanged; no IR field, npm/platform dependency, network, model call, or DB connection (3 new focused tests, 789 total; lint/security/both builder demos pass)
2026-09-09  R-282  Server-side boolean/enum field filters on top-level LIST endpoints in the generated backends (`services/api` Go store + FastAPI repositories/routers + `contracts/openapi.json`): a top-level LIST endpoint now accepts `?<field>=<value>` for each boolean field and each enum field (fields with an `enum:a|b|c` validation rule), composed into the SQL `WHERE` alongside the existing `q` keyword search — following the R-258 sort / R-261 search pattern with the same whitelist discipline (column identifiers come only from validated IR field names; filter values are always parameterized `$N`/`%s`). Adds a shared `field_validation.filter_fields(entity)` helper (returns `(field, kind)` for bool/enum, excludes `id`) that drives Go, FastAPI, and OpenAPI so all three agree on the same field set. Filterable entities use a dynamic `WHERE` builder: Python `data_access._python_repository` emits a `_list_filters(q, <field>=None…)` helper and `list_`/`count_` gain the filter kwargs; Go `data_access._go_entity_store` emits a `<table>Filters(q string, filters map[string]string) (string, []any)` helper (bool → `v == "true"`, enum → `v`) with correct `$N` numbering and `List`/`Count` take a `filters` map. The FastAPI router `Op.LIST` branch declares typed filter query params (bool → `bool | None`, enum → `str | None`) and forwards them; the Go handler calls a new `parseFilters(r)` helper (added to `handlers.go` only when a filterable entity exists) and threads the map into the store calls; `render_openapi` documents each filter param (boolean schema for bool, string + `enum` for enum). Non-filterable entities emit none of the filter plumbing and stay byte-identical; scoped to `Op.LIST` (FK-scoped `LIST_BY` subcollections unchanged) — the backend behind R-278's collection filters, with the frontend wiring to follow as R-283; no new IR field, no npm dependency, standard-library-only platform code, diff-invariant across `ir.description`; pure codegen change, offline, 0 network, no DB (12 new tests, 786 total; existing search/sort/pagination/total-count/wiring assertions updated for the now-filterable minimal-blog Post)
2026-09-09  R-281  Pagination, sort & search controls on subcollection master-detail lists in the generated Next.js web app (`apps/web/app/<screen>/page.tsx`): closes survey gap B — the generated subcollection lists rendered only `{sub.data.map(...)}` with no controls, even though the backend subcollection endpoints and the generated `useList<Child>By<Parent>` hook already fully support `limit/offset/sort/order/q`. Adds two shared helpers in `nextjs.py` — `_subcol_controls(sub, s_var)` emitting an uncontrolled search `<form>` (`defaultValue={sub.params.q ?? ""}` + `new FormData(e.currentTarget).get("q")` on submit → `setSearch`, so no new component state) and a sort `<select>` (`id` + the subcollection's display fields, both directions → `setSort`), plus `_subcol_pagination(s_var)` emitting a Prev / "Page X of Y (N total)" / Next footer driven by `page`/`totalPages`/`total`/`setPage` (disabled at bounds and while loading) — wired via `replace_all` into BOTH subcollection render sites (the collection master-detail block in `_collection_screen_page` and the detail-screen block in `_detail_screen_page`, which are byte-identical at the insertion points), driven entirely by the already-generated hook setters; submit-based search avoids a per-keystroke fetch storm so the subcollection hook internals are unchanged; entities without a parent→child subcollection emit no controls; no new IR field, no npm dependency, `"use client"` preserved, strict diff invariance across `ir.description`; pure codegen change, offline, 0 network, no DB (6 new tests, 774 total)
2026-09-09  R-280  Deep-linked collection list state + debounced, race-safe search in the generated Next.js web app (`apps/web/lib/hooks.ts`, `apps/web/app/<screen>/page.tsx`): the founder-chosen "best, optimised, futuristic" combination of two survey-identified gaps on one surface (`nextjs.py` `_collection_screen_page` + `_hooks_file`). The generated `useList<Entities>` hook now hydrates `sort/order/q/page/pageSize` from `window.location.search` once on mount (client-only, guarded `typeof window`, so no SSR hydration mismatch) and reflects the current params back to the URL via `new URL(window.location.href)` + `window.history.replaceState` — writing only non-default values (`sort !== "id"`, `order !== "asc"`, `page > 1`, `pageSize !== 100`, non-empty `q`) to keep URLs clean — mirroring the R-276 detail deep-link pattern, so refresh / bookmark / share restore the exact list view; `refetch` now creates an `AbortController` per call (adds `useRef` to the react import), aborts the previous in-flight request, forwards `signal` through the existing `ApiOptions` (which already extends `RequestInit`, so no `lib/api.ts` change), guards `signal.aborted` / `AbortError` (no `setData`/`setError`/`setLoading` on abort), and aborts in-flight on unmount, so out-of-order responses can no longer clobber state; the collection search input is debounced 300ms (`setTimeout`/`clearTimeout`, guarded `searchInput !== (params.q ?? "")` so it never clobbers a hydrated page offset), its `onChange` no longer calls `setSearch` on every keystroke, the form submit still searches immediately, and the hydrated `q` is reflected back into the input; the subcollection hook (`useList<Child>By<Parent>`) and subcollection UI controls are intentionally out of scope; no new IR field, no npm dependency, strict diff invariance across `ir.description`; pure codegen change, offline, 0 network, no DB (12 new tests, 768 total; also updated three existing assertion sets to the new import/refetch/onChange behavior)
2026-09-09  R-279  Global notification toast system & action feedback in generated Next.js web app (`apps/web/components/toast.tsx`, `apps/web/app/layout.tsx`): NextjsWebAdapter adds a lightweight, dependency-free global notification toast system and wires real-time feedback into generated collection screens, detail screens, and form screens; emits `apps/web/components/toast.tsx` as a client component (`"use client";`) exporting `ToastProvider`, `useToast`, `ToastType = "success" | "error" | "info"`, `ToastItem`, and `ToastContextValue`; provides `addToast(message, type, duration)`, `removeToast(id)`, and typed helpers `toast.success()`, `toast.error()`, and `toast.info()`; renders a floating viewport container fixed at bottom-right (`position: "fixed"`, `bottom: 24`, `right: 24`, `zIndex: 9999`, `aria-live="polite"`); renders toast cards with auto-dismiss timers (`setTimeout` / `clearTimeout`), manual dismiss `&times;` buttons, and distinct status color accents (emerald `#a7f3d0`/`#15803d`/`✓` for success, red `#fecaca`/`#b91c1c`/`✕` for error, blue `#bfdbfe`/`#1d4ed8`/`ℹ` for info); updates `RootLayout` in `apps/web/app/layout.tsx` to wrap `<Navbar />` and `{children}` inside `<ToastProvider>`; wires real-time action feedback into collection screens (`_collection_screen_page`): CSV export emits `toast.info("Exported CSV successfully")`, single delete emits `toast.success` / `toast.error`, batch delete emits `toast.success` with deleted item count / `toast.error`, subcollection delete emits `toast.success` / `toast.error`; wires feedback into detail screens (`_detail_screen_page`): JSON export emits `toast.info("Exported JSON successfully")`, main delete emits `toast.success` / `toast.error`, subcollection delete emits `toast.success` / `toast.error`; wires feedback into form screens (`_form_screen_page`): `handleSubmit` emits contextual `toast.success` on create or update / `toast.error` on submission failure, Reset button emits `toast.info("Form reset to original values")`; exports `render_toast_component` in `codegen`; strict diff invariance maintained across `ir.description`; pure codegen change, offline, 0 network, no DB (14 new tests, 756 total)
2026-09-09  R-278  Collection screen boolean & enum field filtering with segmented controls in generated Next.js web app (`apps/web/app/<screen>/page.tsx`): NextjsWebAdapter adds interactive, deterministic client-side filtering for boolean fields and enum fields to generated collection screens (`_collection_screen_page`); adds `_filterable_fields_for_entity(entity)` helper identifying `FieldType.BOOL` fields and fields with `enum:a|b|c` validation rules; when filterable fields are present, imports `useMemo` from `"react"`; declares `filterValues` state (`Record<string, string>`) and `handleFilterChange(field, val)` and `handleClearFilters()` handlers; computes `activeFilterCount` and `filteredData` via `useMemo` comparing loaded items against active filter values; assigns `displayData = filteredData ?? (data ?? [])`; renders an accessible filter toolbar above the table: for boolean fields renders segmented pill buttons (`[ All ] [ {Field}: Yes ] [ {Field}: No ]`) with dark active pill styling (`#0f172a`), for enum fields renders a styled `<select aria-label="Filter by {Field}">` dropdown; displays an active filter count badge (`{activeFilterCount} active`) and a "Reset" button when filters are active; connects `displayData` to table row mapping; displays a dedicated empty filter state (`"No {plural} match the active filter criteria."`) with a `"Clear all filters"` CTA button when data exists but none match active filters; clean fallback safety for entities without boolean or enum fields (zero filter state or toolbar markup emitted); strict diff invariance maintained across `ir.description`; pure codegen change, offline, 0 network, no DB (15 new tests, 742 total)
2026-09-09  R-277  Form screen dirty state tracking, unsaved changes guard & reset confirmation in generated Next.js forms (`apps/web/app/<screen>/page.tsx`): NextjsWebAdapter enhances generated form screens (`_form_screen_page`) with deterministic dirty state tracking (`isDirty`), visual warning indicators, confirmation dialogs, and native browser `beforeunload` guards protecting users against accidental data loss; imports `useMemo` from `"react"`; computes `baselineData` from `initialData` (in edit mode when loaded) or `initialValues` (in create mode); computes `isDirty` by comparing current `formData` against `baselineData` (safely treating `undefined` and empty string as equivalent to prevent false positives on initial render); renders amber "Unsaved changes" visual badge (`#fef3c7` / `#92400e`) in header when `isDirty && !success`; renders amber warning notice in form footer (`&bull; You have unsaved changes`); Cancel button links guard against leaving dirty forms with `confirm("You have unsaved changes. Discard them and leave?")`; Reset button guards against discarding changes with `confirm("Discard all changes and reset form?")` while preserving state cleanup (`setLastSavedId(null)`); registers native window `beforeunload` event listener via `useEffect` guarding page refresh and tab close when `isDirty && !submitting && !success`; post-submit success state (`success === true`) clears dirty state warnings so subsequent navigation occurs without confirmation prompts; strict diff invariance maintained across `ir.description`; pure codegen change, offline, 0 network, no DB (16 new tests, 727 total)
2026-09-09  R-276  Record selector dropdown, prev/next record navigation & deep-link sync in generated Next.js detail screens (`apps/web/app/<screen>/page.tsx`): NextjsWebAdapter enhances generated detail screens (`_detail_screen_page`) with interactive record discovery, sequential cycling, and deep-linking URL synchronization; when `Op.LIST` is wired for the entity, imports `useList<Plural>` hook and invokes `useList<Plural>()` to fetch available items; top ID section renders an interactive `<select aria-label="Select {name}">` dropdown with `"-- Choose {name} --"` placeholder and options mapped to existing records displaying the best descriptive title field (`title`, `name`, `label`, `email`, or `id`); adds `handleSelectId(newId)` helper setting `selectedId`, updating `idInput`, and updating browser URL query parameter (`?id=<id>` or removing it) via `window.history.replaceState`; renders "Clear" button when `selectedId` is active; item card header renders contextual `&larr; Prev` and `Next &rarr;` navigation buttons bound to `disabled={!prevItem}` and `disabled={!nextItem}` enabling sequential record browsing without leaving detail view; when `selectedId` is null, empty state renders a "Recent {plural}" grid of clickable card tiles allowing one-click record inspection; `handleDelete` cleans up the `id` search parameter from the URL upon deletion; clean fallback safety when `Op.LIST` is absent or entity contains only an `id` field (zero dropdowns/hooks emitted); strict diff invariance maintained across `ir.description` changes; pure codegen change, offline, 0 network, no DB (16 new tests, 711 total)
2026-09-09  R-275  Rich dashboard overview page in generated Next.js web app (`apps/web/app/page.tsx`): `_overview_page` upgraded from a static 20-line bare HTML list into a rich entity-aware client component (`"use client";`); calls `useList<Entity>({ limit: 1 })` for each entity with `Op.LIST` wired and displays live `.total` count (loading: `"…"`, error: `"—"`, live count otherwise) in styled entity summary cards (white cards with box-shadow, `#0f172a` 32px count, uppercase entity label, plural subtitle); screen navigation cards render for each primary screen (collection + form) as styled `<Link>` tiles with intent label and role badge (`#eff6ff`/`#1d4ed8` pill) for non-public screens; detail screens excluded from nav grid; Quick Actions section renders `+ Create {Entity}` blue CTAs (`#2563eb`) linking to form screens; `ir.description` removed from page body (it already lives in `README.md`) — this fixes the existing diff-invariance violation where description changes caused `app/page.tsx` to change; `test_console_snapshot.py` updated to remove `apps/web/app/page.tsx` from expected edit-diff set (description-stable page no longer appears in diff when only `ir.description` changes); clean fallback when `ir.entities` or `ir.screens` is empty; `_get_ops_by_entity`/`_screen_intent`/`_match_entity` reused from existing infrastructure; pure codegen change, offline, 0 network, no DB, no new IR field (16 new tests, 695 total)
2026-09-08  R-274  Form screen post-submit contextual CTAs, record navigation & Cancel actions (`services/agent-engine/src/omnistackai_agent_engine/codegen/nextjs.py`): `_form_screen_page` declares `lastSavedId` state (`useState<string | null>(null)`); create branch captures newly saved record ID from `create(formData)` response (`res.id`) via `setLastSavedId(String((res as any).id))`; update branch sets `setLastSavedId(editId)` after `await update(editId, formData)`; success banner upgraded from a static alert to an interactive action panel: message text preserved in `<span>{msg_jsx}</span>`; dismiss button (`&times;`, `aria-label="Dismiss"`) calling `setSuccess(false)`; contextual "View {name} →" `Link` to `/{detail_screen.id}?id=${lastSavedId || editId}` rendered when a detail screen exists for the entity (guarded by id expression check); "← Back to {plural}" `Link` to `/{list_screen.id}` rendered when a collection screen exists for the entity; "+ Create another {name}" action button in create mode resetting `setSuccess(false)` and `setLastSavedId(null)` for rapid data entry; styled `Cancel` `Link` button added in form footer next to Reset navigating to `/{list_screen.id}` (or `/` when no list screen exists); Reset `onClick` extended with `setLastSavedId(null)`; strict diff invariance maintained across `ir.description` modifications; pure codegen change, offline, 0 network, no DB, no new IR field (16 new tests, 679 total)
2026-09-08  R-273  Global responsive navigation shell & header navbar in generated Next.js web app (`apps/web/components/navbar.tsx`, `apps/web/app/layout.tsx`): NextjsWebAdapter emits persistent, responsive navigation header shell (`components/navbar.tsx`) as a client component (`"use client";`) with Next.js `usePathname` from `"next/navigation"`; defines `isLinkActive(href)` and `navLinkStyle(active)` dynamically styling active routes (`#eff6ff` background, `#1d4ed8` text, `fontWeight: 600`, subtle border); renders application branding (`ir.name`) with avatar logo badge (`ir.name[:1]`) and title linking to `/` (Overview); renders "Overview" navigation link to `/`; dynamically generates screen navigation links for primary collection, form, and generic screens from `ir.screens`, displaying screen title, role pill badges for non-public screens, and active state highlights; cleanly excludes parameter-dependent detail screens (`_screen_intent(s) == "detail"`) from top nav to keep navigation focused; detects first create form screen in `ir.screens` and renders a prominent `+ New {Entity}` / `+ Create` quick-action CTA button on the right side of the navbar; supports empty screens with clean fallback; updates `_LAYOUT` in `app/layout.tsx` to import `Navbar` from `../components/navbar` and render `<Navbar />` inside `<body>` above `{children}` with global typography and background tokens (`#f8fafc`, system font stack); preserves RootLayout server component status exporting Next.js `Metadata`; diff-invariant across `ir.description`; pure codegen change, offline, 0 network, no DB (17 new tests, 663 total)
2026-09-08  R-271  CSV data export & bulk export in generated Next.js collection screens (`apps/web/app/<screen>/page.tsx`): NextjsWebAdapter adds client-side RFC 4180 CSV export and bulk selection export to generated collection screens; `_collection_screen_page` emits `handleExportCsv(selectedOnly: boolean = false)` helper function; when `selectedOnly` is true, filters data against `checkedIds`, else exports all loaded data, with early-return guard for empty datasets; formats CSV values via strict RFC 4180 serializer `toCsvVal` handling null/undefined (`""`), internal quotes (`""`), and objects (`JSON.stringify`), wrapping each field value in quotes and joining rows by newline; includes all declared entity fields (`entity.fields`) across headers and rows; manages in-browser download lifecycle using `Blob` (`text/csv;charset=utf-8;`), `URL.createObjectURL(blob)`, dynamic `<a>` anchor element with download filename `{plural.lower()}_export.csv`, automatic `link.click()`, DOM cleanup, and memory release via `URL.revokeObjectURL(url)`; top toolbar renders "Export CSV" button disabled when data is null/empty; contextual Bulk Actions Bar renders "Export Selected ({checkedIds.length})" button when `checkedIds.length > 0`, coexisting with "Delete Selected" button when delete capability is wired; diff-invariant across `ir.description`; pure codegen change, offline, 0 network, no DB (16 new tests, 630 total)
2026-09-08  R-270  Bulk selection & batch deletion in generated Next.js collection screens (`apps/web/app/<screen>/page.tsx`): NextjsWebAdapter adds multi-record bulk selection and batch deletion workflows to collection screens; declares row selection state (`checkedIds: string[]`), `allCurrentIds`, `isAllChecked`, `handleCheckAll`, `handleToggleRow`, and `handleClearSelection`; table header `<thead>` renders master checkbox with `aria-label="Select all"`, `checked={isAllChecked}`, and `onChange={handleCheckAll}`; table body `<tbody>` rows render selection checkbox with `e.stopPropagation()` preventing interference with subcollection details; adjusts loading and empty state `colSpan` to account for checkbox column (+1); highlights selected rows with active `#f8fafc` background; conditionally renders contextual Bulk Actions Bar above the table when `checkedIds.length > 0` displaying selection count (`{checkedIds.length} selected`), `Clear selection` button, and `Delete Selected ({checkedIds.length})` button when `Op.DELETE` is wired; batch delete executes with confirmation dialog (`confirm("Are you sure you want to delete {count} {name/plural}?")`), progress state (`batchDeleting`), concurrent execution via `Promise.all(checkedIds.map(id => remove(id)))`, selection reset, automatic `refetch()`, and dismissible `batchDeleteError` alert banner; individual `handleDelete(id)` cleans up deleted ID from `checkedIds`; diff-invariant across `ir.description`; pure codegen change, offline, 0 network, no DB (16 new tests, 614 total)
2026-09-08  R-269  Page size selector & contextual empty state CTAs in generated Next.js screens (`apps/web/app/<screen>/page.tsx`, `apps/web/lib/hooks.ts`): NextjsWebAdapter adds configurable page size selection and contextual empty state call-to-actions; `UseListState<T>` interface declares `setPageSize: (size: number) => void;`; `useList<Entities>` and `useList<Children>By<Rel>` hooks implement `setPageSize` resetting offset to 0 and clamping page size to minimum 1, returning `pageSize` and `setPageSize` in hook state; collection screens (`_collection_screen_page`) destructure `pageSize` and `setPageSize` and render an accessible `<select id="pageSizeSelect">` dropdown (10, 25, 50, 100 per page) next to pagination buttons in footer; collection screen table empty state renders search mismatch message with interactive `Clear search` CTA button (`onClick={() => { setSearchInput(""); setSearch(""); }}`) when search is active, and `+ Create first <Entity>` CTA link when `form_screen` is available; subcollection empty states render `+ Add first <Child>` CTA link when `child_form` is available in both collection and detail views; strict diff invariance maintained across `ir.description`; pure codegen change, offline, 0 network, no DB (15 new tests, 598 total)
2026-09-08  R-268  Subcollection child item deletion & mutation feedback in master-detail views (`apps/web/app/<screen>/page.tsx`): NextjsWebAdapter adds child item deletion, confirmation dialogs, loading/disabled states, error feedback alerts, and automatic subcollection refetching to master-detail views in generated Next.js screens; `_subcollections_for_parent` sets `can_delete = True` on `SubcollectionInfo` when child entity supports `Op.DELETE`; fallback entity matching for `DELETE` endpoints lacking explicit `response_schema` (e.g. `ApiEndpoint(HttpMethod.DELETE, "/comments/{id}")`) across API client and hooks; collection screens (`_collection_screen_page`) and detail screens (`_detail_screen_page`) import and wire `useDelete<Child>` hooks from `"../lib/hooks"`; emits `handleDelete<Child>` handlers prompting confirmation dialog (`confirm("Are you sure you want to delete this <Child>?")`) with try/catch guard and triggering `<subcol>.refetch()`; child cards render styled Delete button with disabled state during mutation and `stopPropagation`; renders mutation error feedback alert banner (`{delete<Child>Error && ...}`) directly above child items if deletion fails; non-deletable subcollections omit deletion code; diff-invariant across `ir.description`; pure codegen change, offline, 0 network, no DB (13 new tests, 583 total)
2026-09-08  R-267  Foreign-key relation selectors & parent auto-population in generated Next.js forms (`apps/web/app/<screen>/page.tsx`): NextjsWebAdapter replaces raw foreign key text inputs with typed parent relation selectors (<select> dropdowns) and automatic query parameter pre-population; `_parent_relations_for_entity` helper detects `RelationKind.MANY_TO_ONE` relations and foreign key fields on child entities where parent entity has `Op.LIST`; child form screens (`_form_screen_page`) import and wire `useList<ParentPlural>()` from `"../lib/hooks"`; renders accessible `<select>` dropdowns displaying parent title/name options (`{String(item.title ?? item.name ?? item.id)}`), loading state placeholder (`Loading <parents>...` vs `Select <parent>...`), and required HTML5/ARIA attributes; displays contextual parent linkage badge (`&bull; Selected <Parent> linked`) when foreign key is selected; pre-populates foreign keys from `searchParams` on mount, supporting parameter aliases (`<field>`, `<rel>_id`, `<rel>Id`, `<rel>`); client-side required validation highlights unselected foreign keys with field-level errors; clean fallback safety for entities without relations (zero dropdowns/hooks emitted); diff-invariant across `ir.description`; pure codegen change, offline, 0 network, no DB (12 new tests, 570 total)
2026-09-08  R-266  Update/edit mode in generated Next.js forms & collection screen edit actions (`apps/web/app/<screen>/page.tsx`): NextjsWebAdapter connects generated frontend to existing backend update handlers (PUT/PATCH) and typed React mutation hooks (`useUpdate<Entity>` and `use<Entity>(id)`); form screen (`_form_screen_page`) operates in dual-mode (Create and Edit) when `Op.UPDATE` is wired, importing `use<Entity>`, `useUpdate<Entity>`, and `useSearchParams` from `"next/navigation"`, extracting `editId = searchParams.get("id")`, computing `isEdit = Boolean(editId)`, synchronizing initial data into `formData` via `useEffect`, branching submission to call `update(editId, formData)` in edit mode vs `create(formData)` in create mode, and dynamically updating headers, submit button labels, loading indicator, and success alerts; collection screen (`_collection_screen_page`) renders an "Edit" action link (`/{form_screen.id}?id=${(item as any).id}`) with `onClick={(e) => e.stopPropagation()}` to avoid toggling table row selection; subcollection master-detail view renders `+ New <Child>` link (`/{child_form.id}?{foreign_key}=${selectedId}`) when complementary form screen exists for child entity; fallback safety for entities without `Op.UPDATE` (zero update code emitted); diff-invariant across `ir.description`; pure codegen change, offline, 0 network, no DB (12 new tests, 558 total)
2026-09-08  R-265  Subcollection navigation & master-detail views in generated screens (`apps/web/app/<screen>/page.tsx`): NextjsWebAdapter connects parent collection screens to nested child subcollections; `_subcollections_for_parent` identifies relations where `rel.target_entity == parent_name` and an `Op.LIST_BY` endpoint exists; collection screens conditionally import subcollection hooks (`useList<Children>By<Rel>`) and child entity types (`import type { <Child> }`) on dedicated lines preserving substring match for parent imports; collection screen manages `selectedId` state with table row click selection and "View Details" / "Hide Details" button; renders master-detail subcollection section with live count badges (`{subcol.total}`), tabbed navigation for multi-subcollection parent entities, child item lists with key display fields, loading/error/empty states, and refetch; dedicated detail screen generator (`_detail_screen_page`) for `intent == "detail"` screens; fallback safety for entities without subcollections (zero subcollection code emitted); diff-invariant with respect to `ir.description`; pure codegen change, offline, 0 network, no DB (19 new tests, 546 total)
2026-09-08  R-264  Field-level validation & error feedback in generated Next.js forms (`apps/web/app/<screen>/page.tsx` & `apps/web/lib/api.ts`): NextjsWebAdapter emits `extractFieldErrors(error: unknown): Record<string, string>` normalizing Go backend structured errors (`{"errors": [...]}`) and FastAPI structured errors (`{"detail": [...]}`); form screens import `extractFieldErrors` and manage `fieldErrors` state (`Record<string, string>`); client-side pre-validation validates required fields, string `max_length`, numeric `min`/`max` constraints, and enum options before network requests, halting and setting `fieldErrors` on failure; submission failure extracts server errors via `extractFieldErrors(err)` and highlights inputs; inputs render red borders (`#ef4444`), `aria-invalid="true"`, and per-field error messages below inputs; reactive error clearing clears field errors on input edit (`onChange`); enum fields render interactive `<select>` dropdowns with options; reset button clears `fieldErrors` and form data; warning banner rendered when field errors exist; diff-invariant with respect to `ir.description`; pure codegen change, offline, 0 network, no DB (12 new tests, 527 total)
2026-09-08  R-263  Interactive screen component generator with real data binding (`apps/web/app/<screen>/page.tsx`) in NextjsWebAdapter: screen pages upgraded into interactive React client components with `"use client"` and React 18 hooks (`useState`); automatically matches screens to IR entities via multi-token scoring (`_match_entity`); detects screen intent (`_screen_intent`) as collection, form, or generic; collection screens bind to `useList<Entities>()` with live search input (`setSearch`), sortable headers (`setSort`), pagination controls (`setPage`, Previous/Next), loading/error/empty states, delete action (`useDelete<Entity>`) when `Op.DELETE` is wired, and navigation to complementary editor screens; form screens bind to `useCreate<Entity>()` with schema-derived inputs (checkbox for bool, textarea for text, number input for int/float, datetime-local for datetime, text for string), required indicators/attributes, submit handling (`create(formData)`), success banner, and reset/cancel controls; graceful fallback screen without broken imports when unwired; byte-identical output across IR description changes preserving diff stability; public `render_screen_page` exported in `codegen`; pure codegen change, offline, 0 network, no DB (9 new tests, 515 total)
2026-09-08  R-262  React data-fetching & mutation hooks generation (`apps/web/lib/hooks.ts`) from Application IR: NextjsWebAdapter emits strongly-typed, idiomatic React hooks using `"use client"` and React 18 built-ins (`useState`, `useEffect`, `useCallback`) with zero extra npm dependencies; exports shared interfaces UseListParams, UseListState<T>, UseDetailState<T>, UseMutationState<TData, TResult>; generates useList<Entities> managing data, total count, loading, error, pagination math (page, pageSize, totalPages, setPage), search (q, setSearch), sorting (sort, order, setSort), and refetch; generates use<Entity> for detail fetch by ID; generates useCreate<Entity>, useUpdate<Entity>, and useDelete<Entity> mutation hooks; generates useList<Entities>By<Rel> for parent-scoped subcollections; exports public render_hooks in codegen and registers in codegen.__init__; pure codegen change, offline, 0 network, no DB (15 new tests, 506 total)
2026-09-08  R-261  Full-text and keyword search filtering (`q` query parameter) on Op.LIST and Op.LIST_BY endpoints across Go, FastAPI, Next.js, and OpenAPI 3.1 targets: data_access._searchable_fields identifies fields of type STRING and TEXT; Go store emits `WHERE (field1 ILIKE $1 OR ...)` in List and Count methods with positional argument reuse for parameterized `"%"+q+"%"`, and subcollections scope with relation foreign key (`WHERE <rel>_id = $1 AND (...)`); Go handlers emit `parseSearch(r *http.Request) string` and pass `q` to store calls; FastAPI repositories emit `WHERE (col1 ILIKE %s OR ...)` in list_<table> and count_<table> passing `f"%{q}%"` wildcards; FastAPI routers declare `q: str | None = None` on list endpoints; Next.js lib/api.ts options.params includes `q?: string` for all list and list_by methods; OpenAPI 3.1 generator documents `q` query parameter on all LIST operations; non-text entities safely omit search clauses with 0 SQL errors; pure codegen change, offline, 0 network, no DB (16 new tests, 491 total)
2026-09-08  R-260  OpenAPI 3.1 specification generation (`render_openapi`, `render_openapi_json`) from Application IR: translates ApplicationIR into a canonical OpenAPI 3.1.0 document (Brief Section 43: Contracts: OpenAPI + generated clients, packages/contracts/openapi/); components.schemas maps entities to typed properties with validation metadata (maxLength, enum, minimum, maximum, required) plus standard ValidationError/Response and ErrorResponse schemas; components.securitySchemes declares BearerAuth; paths maps all endpoints with path parameters, query parameters (limit, offset, sort, order on LIST endpoints), request bodies, and responses with X-Total-Count header; security requirement attaches BearerAuth and required roles; assemble_project emits contracts/openapi.json in customer monorepos; backend_go and backend_python emit openapi.json in backend project root; test_console_snapshot updated for openapi.json in diff; pure codegen change, offline, 0 network, no DB (14 new tests, 475 total)
2026-09-08  R-259  Total count database queries and X-Total-Count HTTP response header on Op.LIST and Op.LIST_BY endpoints across Go, FastAPI, and Next.js targets: Go store emits Count<Entity> (`SELECT COUNT(*) FROM <table>`) and Count<Entity>By<Rel> (`SELECT COUNT(*) FROM <table> WHERE <rel>_id = $1`); Go handlers in internal/handlers/*.go query count before listing and emit `w.Header().Set("X-Total-Count", strconv.Itoa(total))`; Go corsMiddleware adds `Access-Control-Expose-Headers: X-Total-Count`; FastAPI repositories emit `async def count_<table>() -> int` and `count_<table>_by_<rel>(<rel>_id: str) -> int`; FastAPI routers inject `response: Response`, query count, set `response.headers["X-Total-Count"] = str(total)`, and expose in CORSMiddleware; Next.js lib/api.ts exports `PaginatedResult<T> { data: T; total: number }`, adds helper `requestWithMeta<T>`, and emits `list<Entity>WithCount` / `list<Entity>sBy<Rel>WithCount` returning `Promise<PaginatedResult<<Entity>[]>>`, keeping standard `list*` methods returning `Promise<<Entity>[]>` for complete backward compatibility; pure codegen change, offline, 0 network, no DB (15 new tests, 461 total)
2026-09-08  R-258  Query parameter sorting (`sort` and `order=asc|desc`) with SQL injection whitelist protection across Go and FastAPI backends and Next.js typed API client: Go store whitelists sort column against declared entity fields via switch statement (falling back to "id" default) and validates order direction ("DESC" on case-insensitive "desc", else "ASC"), formatting `ORDER BY %s %s LIMIT $1 OFFSET $2`; Go handlers emit `parseSort(r *http.Request) (string, string)` in shared handlers and pass sort and order to store.List and store.ListBy; FastAPI routers declare `sort: str = "id", order: str = "asc"` on Op.LIST and Op.LIST_BY and forward to repositories; Python repositories declare `ALLOWED_SORT_FIELDS`, whitelisting sort column (falling back to "id") and validating sort direction (falling back to "ASC"); Next.js typed API client (`apps/web/lib/api.ts`) types options params with `limit?: number; offset?: number; sort?: string; order?: "asc" | "desc"`; pure codegen change, offline, 0 network, no DB (14 new tests, 446 total)
2026-09-08  R-257  Full-stack connectivity — Next.js typed API client (`apps/web/lib/api.ts`) and backend CORS middleware (Go & FastAPI): NextjsWebAdapter emits a strongly-typed TypeScript API client using native fetch, supporting BASE_URL (`process.env.NEXT_PUBLIC_API_URL || ""`), ApiOptions (token for Bearer auth, query params for URLSearchParams), ApiError (status + data), request helper, typed CRUD functions for all wired entities (list with limit/offset pagination, get, create, update, delete, list_by subcollections), deterministic fallbacks for custom routes, and exported `api` namespace object; Go backend `main.go` gains corsMiddleware wrapping mux with Access-Control-Allow-Origin (* or env CORS_ALLOWED_ORIGIN), Methods, Headers, and OPTIONS 204 preflight; FastAPI backend `app/main.py` adds CORSMiddleware; .env.example files updated in all three adapters; additive, offline, 0 network, no DB (9 new tests, 432 total)
2026-09-08  R-256  Wired PUT handlers (full-replace update) in Go and FastAPI backends: route_wiring extends Op.UPDATE to match `method in ("PATCH", "PUT")` when endpoint ends in a single path parameter and `request_schema` matches a known entity; backend_go wires `Put<Entities><Id>` handler (body decode -> validateStruct if entity has rules -> store.Update<Entity> -> 404/200); backend_python wires `@router.put` (id_param + payload -> update_<table> -> 404 on None); negative shapes stay 501 scaffold; rule-free entities emit no validateStruct; example IRs unchanged; additive, offline, 0 network, no DB (14 new tests, 423 total)
2026-09-08  R-255  Added query parameter pagination (`limit` & `offset`) across both generated backends: Go `internal/handlers/handlers.go` emits `parsePagination(r *http.Request) (int, int)` returning limit (default 100, parsed if >0) and offset (default 0, parsed if >=0) via `strconv.Atoi`; Go wired `Op.LIST` and `Op.LIST_BY` handlers extract limit and offset and pass them to `store.List<Entity>` and `store.List<Entity>By<Rel>`; Go store emits `SELECT ... ORDER BY id LIMIT $1 OFFSET $2` (and `LIMIT $2 OFFSET $3` for filtered lists); FastAPI routers emit `limit: int = 100, offset: int = 0` query parameters on `@router.get` endpoints and pass them to repository calls; Python repositories parameterize `LIMIT %s OFFSET %s` with `(limit, offset)`; clients omitting parameters get default 100 items at offset 0; additive, offline, 0 network, no DB (11 new tests, 409 total)
2026-09-08  R-254  Replaced the flat "validation_failed" string with structured JSON error bodies in the generated Go backend: field_validation.go_validate_file() now emits a validationError struct {Field/Rule/Message string} and validateStruct(w http.ResponseWriter, v any) bool — on failure it iterates validator.ValidationErrors, builds []validationError{field:fe.Field(), rule:fe.Tag(), message:fmt.Sprintf(...)}, writes {"errors":[...]} via writeJSON(w, http.StatusBadRequest, ...) and returns false; on success returns true; handlers use "if !validateStruct(w, m) { return }" — a single line replacing the old two-line status+msg pattern; "fmt" added to validate.go imports; "validation_failed" string gone; FastAPI/Pydantic already return structured 422 detail by default (no change needed); rule-free gate, example IRs, and all other handler behaviour unchanged; additive and offline, no IR field, no platform dep, no install/build/run/DB (24 new tests, 398 total)
2026-09-08  R-253  Closed the missing PATCH/update CRUD verb in both generated backends: route_wiring gains Op.UPDATE; data_access emits Go UpdateEntity + Python update_table; backend_go wires Op.UPDATE handler (decode -> validateStruct -> store.Update -> 404/200); backend_python wires @router.patch; all SQL parameterized; rule-free no validate; example IRs unchanged; additive and offline (24 new tests, 374 total)
2026-09-08  R-252  Made the R-251 Go validate:"..." struct tags actually enforced at request time (FastAPI already enforced via Pydantic at construction; Go was the gap): codegen/field_validation.py gained VALIDATOR_REQUIRE (github.com/go-playground/validator/v10 v10.22.1) and go_validate_file(), which emits internal/handlers/validate.go — a shared var validate = validator.New() plus a validateStruct(v any) (int, string) helper returning http.StatusBadRequest/"validation_failed" on a tag violation ("" when valid); backend_go now computes has_validation (any wired CREATE handler whose entity carries rules) and, only then, adds the validator require to the generated go.mod, emits validate.go, and inserts a validateStruct(m) guard in each such create handler right after the JSON decode and before the store.Create call (400 on failure); rule-free projects and both example IRs emit none of it and stay byte-identical to R-251, the validator dependency lives only in the generated project (no platform Python dependency), and validation now holds at three layers — request model, request handler, and DB schema; additive and offline, no DB connection (5 new tests)
2026-09-08  R-251  Extended IR field validation to the Go backend and added numeric bounds: the codegen/field_validation parser gained min:<n> / max:<n> rules (raw literals, non-numeric ignored) and a go_validate_tag helper, so validation now flows into all three targets — the PostgreSQL schema renders numeric CHECK (col >= n)/CHECK (col <= n) (alongside the R-250 VARCHAR/enum CHECK), the FastAPI Pydantic models render Field(ge=n, le=n) beside max_length, and the Go models carry a go-playground validate:"max=,oneof=,gte=,lte=" struct tag on each field with rules (rule-free fields keep a plain json tag); tags are declarative (no go.mod dependency and no validator.Struct call this task — enforcement is a follow-up; the schema already enforces at the DB for both backends); additive and offline, example IRs unchanged, no DB connection (8 new tests)
2026-09-08  R-250  Made the IR Field.validation tuple meaningful with a small deterministic rule syntax (max_length:<int>, enum:<a>|<b>|<c>) threaded into two generated targets via codegen/field_validation.py: the PostgreSQL schema renders a STRING with max_length as VARCHAR(n) and an enum as CHECK (<col> IN ('a','b')) (values single-quote-escaped, id PK unaffected); the FastAPI Pydantic models render Field(max_length=n) (Field(default=None, max_length=n) when optional) and a Literal[...] type, importing Field/Literal only when used; unknown rules are ignored (forward-compatible), Go request-validation tags are deferred (the schema constrains Go writes at the DB); additive and offline with example IRs unchanged, no DB connection (10 new tests)
2026-09-08  R-249  Added schema indexes + unique constraints from the Application IR: Field gains a unique flag (a single-column UNIQUE constraint, never applied to the id primary key) and Entity gains indexes — a new Index record (fields + unique + optional name) whose fields are validated against the entity's own fields at construction; both round-trip through to_dict/from_dict with false/empty defaults (no schema-version bump); render_postgres_schema emits UNIQUE on unique non-id columns and one CREATE [UNIQUE] INDEX <name> ON <table> (<cols>); per entity index with a deterministic default name (<table>_<cols>_idx, _key when unique) when unnamed; rideshare-favourites gains a demo index on driver(name); pure/deterministic/offline, schema text only, nothing connects to or runs a database (11 new tests)
2026-09-08  R-248  Added honest seed data from explicit Application IR fixtures: a new Fixture record (entity + rows of column->JSON value) that is validated (entity + columns must be declared; a required column missing from a row is a WARNING) and round-trips through to_dict/from_dict/normalize_ir (additive IR field, empty default, no schema-version bump); render_postgres_seed (codegen/seed_sql.py) emits a deterministic migrations/0002_seed.sql with one INSERT per row using ONLY the declared columns/values — never inventing, defaulting, or guessing — and owns the codebase's first SQL literal quoter (single quotes doubled, bool->TRUE/FALSE, None->NULL, dict/list->jsonb); both backends emit the file only when the IR has fixtures and database_strategy=postgres (0001_init.sql unchanged), minimal-blog gains example fixtures and builder:demo prints the seed; deterministic and offline, no DB connection or migration run, no fabricated value (14 new tests)
2026-09-08  R-247  Made the real offline builder visible in the dependency-free static console: a new metadata-only console snapshot composes the R-245 ProjectPlan for the bundled Rideshare Favourites IR (apps/web + services/api with preview URLs and verification ladders) and an R-246 hunk-level unified patch produced from actual old/new Minimal Blog IR assemblies (five modified generated files); the responsive UI renders both with textContent-only DOM construction and same-origin JSON alongside the model fabric, with no generated app execution, database connection, external request, service, dependency, or secret value (5 new tests)
2026-09-07  R-246  Added hunk-level edit diffs + rename detection on top of the R-237 file-level ProjectDiff: diff_report(old, new) classifies each path as added/modified/deleted/renamed and attaches a git-style unified (hunk) diff for content changes, collapsing an exact-content delete+add into a single RENAMED record (old_path -> path); unified_patch(old, new) concatenates them into one byte-stable git-style patch string with rename headers, so an edit reads as a focused review-ready patch; standard-library difflib only, additive (diff_projects/apply_diff/plan_edit unchanged), pure and deterministic with no disk write or run (6 new tests)
2026-09-07  R-245  Added the combined project-plan surface: build_project_plan(ir) composes the assembler app layout (R-232), runtime preview plans (R-233), verify gate ladders (R-235), and optional deploy plans (R-234) into one per-app ProjectPlan a console/CLI can render — each AppPlan pairs an assembled app with its preview + verify plans (and a deploy plan only when a key-activated DeploymentProvider is passed); ProjectPlan.to_dict is JSON-serializable and secret-free, render() is a readable summary, and task plan:show prints it for an example IR; pure and deterministic, nothing is installed/run/verified/deployed and no key value is included (6 new tests)
2026-09-07  R-244  Wired the sub-collection GET pattern /<parents>/{parentId}/<children> to a parent-scoped list, clearing the main class of remaining 501 stubs: when the child entity (response_schema) is a repository entity with exactly one foreign-key relation (many_to_one/one_to_one), the endpoint lists it filtered by <rel>_id = the path parameter; route_wiring gains an Op.LIST_BY + fk_relations(ir) helper, and the data-access layer emits a filtered list per FK relation (Python list_<table>_by_<rel>, Go List<Entity>By<Rel>) with parameterized values and a fixed identifier column; Python routers and Go handlers call it; ambiguous endpoints (0 or >1 FK relations, multi-param, unknown entity) stay labelled 501; deterministic and offline, nothing runs; seed/fixture data deferred (the IR carries no seed values) (11 new tests)
2026-09-07  R-243  Added per-endpoint role enforcement on top of the R-242 JWT authentication: the Application IR ApiEndpoint gains an optional required_roles (role ids; a non-empty value implies auth, each must be a declared Role — validate_ir errors on an undeclared role) that round-trips through to_dict/from_dict (first additive IR change, default (), no schema-version bump); the generated guard returns 403 when the verified token's roles claim includes no required role — Python emits a require_roles(*required) dependency factory and role-gated routes declare dependencies=[Depends(require_roles("..."))], Go refactors to a shared verifyToken plus RequireRoles(next, required...)/hasAnyRole and main wraps role-gated endpoints; endpoints without required_roles keep the R-241/R-242 auth guard; deterministic and offline, nothing runs (6 new tests)
2026-09-07  R-242  Upgraded the generated auth guard from a bearer-presence check to real JWT verification: it decodes and verifies a JWT (HS256) using JWT_SECRET read from the environment (401 on a missing/invalid/expired token, 500 when the secret is unset), never hard-coding or defaulting the secret; Python require_auth uses PyJWT (jwt.decode with algorithms=["HS256"]) and returns the verified claims, Go RequireAuth uses github.com/golang-jwt/jwt/v5 and rejects non-HMAC tokens; the generated projects gain the JWT dependency (PyJWT in requirements.txt / golang-jwt in go.mod) and an empty JWT_SECRET in .env.example; the IR roles constant is retained for future per-endpoint authorization; platform code stays standard-library only (the JWT dep lives in the generated project); deterministic and offline, nothing signed/verified at generation time (3 new tests)
2026-09-07  R-241  Made the IR per-endpoint auth flag real: every auth=true endpoint enforces an authentication guard that rejects a request with no Authorization: Bearer credential (HTTP 401) before the handler runs; Python emits app/auth.py (require_auth dependency) and auth routes declare dependencies=[Depends(require_auth)], Go emits internal/handlers/auth.go (RequireAuth middleware) and main wraps exactly the auth=true registrations with handlers.RequireAuth(...); the IR roles are surfaced as a generated constant (Python ROLES, Go Roles) and token verification (signature/expiry/roles) is an explicit TODO in the generated code — never faked, no secret fabricated; emitted only when the IR declares an auth=true endpoint, public routes untouched; deterministic and offline, nothing runs (9 new tests)
2026-09-07  R-240  Wired the generated backends' HTTP handlers to the R-239 repository layer for the unambiguous CRUD shapes (route_wiring.wire_endpoint: GET collection->list, GET /x/{id}->get, POST with request_schema->create, DELETE /x/{id}->delete; entity from response_schema else request_schema; everything else stays a labelled 501 scaffold); Python routers import the repository/model and await the call (create takes a Pydantic body), Go handlers became methods on a Handlers struct holding *sql.DB (internal/handlers/handlers.go with New + writeJSON) with main opening store.Open() and registering h.<Handler>; emitted only when entities + database_strategy=postgres, non-DB backends keep the plain scaffolds unchanged; deterministic and offline, nothing runs (12 new tests)
2026-09-07  R-239  Added a per-entity data-access/repository layer so the generated backends read/write the R-238 tables: Python (FastAPI) gets app/db.py (async psycopg) + app/repositories/<entity>.py (list/get/create/delete) and psycopg in requirements.txt; Go gets internal/store/store.go (database/sql via pgx) + internal/store/<entity>.go (List/Get/Create/Delete scanning into the generated model structs) and a pgx require in go.mod; all query values are parameterized (%s / $N) with only fixed IR-derived identifiers inline, an id-only entity creates via DEFAULT VALUES; emitted only when entities + database_strategy=postgres; nothing connects to or queries a database; standard-library only, deterministic and offline (7 new tests)
2026-09-07  R-238  Added PostgreSQL schema/migration generation from the Application IR: render_postgres_schema turns entities + relations into deterministic SQL DDL (one CREATE TABLE per entity, snake_case names, FieldType->PG column types, NOT NULL for required fields, a UUID primary key from the entity id or a surrogate, foreign-key columns for many_to_one/one_to_one relations, and a join table per many_to_many); the FastAPI and Go backends emit it as migrations/0001_init.sql exactly when the IR has entities and database_strategy=postgres, changing no previously emitted file; byte-stable so the R-237 edit loop diffs it on IR change; nothing connects to or runs a database; standard-library only, deterministic and offline (11 new tests)
2026-09-07  R-237  Added the IR-diff -> patch-apply edit loop (edit an existing app): diff_projects classifies added/modified/deleted/unchanged between two generated projects (modified on content or executable change); plan_edit(old_ir, new_ir) assembles both IRs and diffs them so an IR change becomes exactly the files to rewrite; apply_diff writes added/modified and removes deleted files inside a path-safe target (escapes refused, empty dirs pruned) and returns an ApplyReport; commit_edit applies + commits as the customer identity via an additive git_service.commit_all, preserving history; deterministic and offline (diffing pure, applying a bounded path-safe disk step), no network or code execution (9 new tests)
2026-09-07  R-236  Expanded the model-provider catalog: added first-class OpenAI-compatible adapters for DeepSeek, xAI (Grok), Mistral, Together, and Fireworks alongside OpenAI/Anthropic/Google/OpenRouter/Groq, plus a generic env-driven custom-provider path (OMNISTACKAI_CUSTOM_PROVIDERS + per-provider BASE_URL/MODEL/API_KEY) so any OpenAI-compatible endpoint plugs in with no code change; resolve_provider_specs merges built-ins + custom and bootstrap/overview iterate it so every provider is key-activated, selectable as the cloud tier or a fallback, and listed in the metadata-only overview; illustrative price-book defaults for the priced new providers (openrouter/custom unpriced); keys stay env-only and never logged/stored/shown; standard-library only, deterministic and offline (14 new tests)
2026-09-07  R-235  Added the verifiable-engineering verify-plan layer: a deterministic per-target gate ladder (install/typecheck/lint/test/build) that generated code is engineered to pass, as pure validated data classified by gate kind; verify_plans_for_ir maps one Application IR to the verify plans for its assembled monorepo apps (apps/web, services/api) via a new additive assembled_targets; run_verify is an opt-in fail-fast executor returning a structured VerifyReport; task agent-engine:verify-plan prints a ladder; standard-library only, deterministic and offline (9 new tests), nothing installed/built/run by the platform
2026-09-07  R-234  Added a single OMNISTACKAI_TIER switch (0/1 local, 2 cloud) that resolves the runtime + deploy providers, and gave every declared provider a driver that emits a real command plan (deploy: vercel/netlify/render/fly; sandbox: e2b/daytona/fly-machines) with the key read from env at run time and never placed in a plan or logged; platform_status/format_status + task platform:status report the active tier and key presence, run_deploy executes a plan opt-in; deterministic and offline (13 new tests, task verify stays network-independent), nothing run or deployed
2026-09-07  R-233  Added the runtime & deployment provider layer: a local preview provider (Tier 0/1, no keys) with deterministic per-target preview plans and an opt-in executor, plus key-activated cloud sandbox (e2b/daytona/fly) and deploy (vercel/fly/render/netlify) providers via an env registry (Tier 2/3 = add a key); nothing is run or deployed by the platform and no key is logged/stored
2026-09-06  R-232  Added the customer project assembler: one Application IR is assembled into a complete customer monorepo (apps/web Next.js + services/api Go/Python) as a single GeneratedProject with root README/.gitignore, and materialized via the Git service into one customer-owned repo (default_registry preloads all three adapters); deterministic, offline
2026-09-06  R-231  Added an Application IR semantic validator (unknown API schema references = error; strategy/platform mismatch = warning), a deterministic canonical normalizer (idempotent, lossless), and example IR fixtures (rideshare-favourites, minimal-blog) that validate clean and generate via all three adapters
2026-09-06  R-230  Added the Go backend framework adapter: one Application IR now emits web (Next.js) + Python (FastAPI) + Go (net/http) — Go structs from entities, Go 1.22 method+pattern routes grouped by resource with r.PathValue params, main + health; standard-library only, deterministic, offline, no install/build/disk
2026-09-06  R-229  Added the Python (FastAPI) backend framework adapter: one Application IR now emits web (Next.js) + backend (FastAPI) together — Pydantic models from entities, routers from APIs grouped by resource with typed path params, main app + health; deterministic, offline, no install/build/disk
2026-09-06  R-228  Added the git service: materialize a GeneratedProject to disk and initialize a customer-owned Git repository with a first commit (explicit identity, no global config, writes only inside the target, offline) — completes the first end-to-end builder slice (IR -> app -> owned repo)
2026-09-06  R-227  Added the first framework code adapter (Next.js web): turns an Application IR into a real Next.js App Router TypeScript project (config, TS interfaces from entities, App Router API routes with {param}->[param], a page per screen, overview) as a deterministic in-memory GeneratedProject; no install/build/disk
2026-09-06  R-226  Added the code-generation boundary: an in-memory generated file-set (GeneratedFile/GeneratedProject, path-safe, deterministic) and the FrameworkAdapter contract + registry that turn an Application IR into a GeneratedProject per target; no disk writes or code execution
2026-09-06  R-225  Added the framework-neutral Application IR v1 (Brief 9) — the source of truth for code generation: validated, versioned records for application/strategy/roles/entities/APIs/screens/acceptance with cross-reference checks and lossless serialization; first real product primitive
2026-09-06  R-224  Deferred the Next.js console upgrade — the Next.js SWC binary install times out in this sandbox (attempted 3x); recorded a resume plan; no application code committed; the R-222 static console remains the working slice
2026-09-06  R-223  Wired the cross-provider fallback chain and circuit breaker into build_gateway_from_env via environment configuration (OMNISTACKAI_FALLBACK_PROVIDERS + breaker threshold/cooldown); the console overview surfaces the resilience settings; single-provider default unchanged
2026-09-06  R-222  Added the first platform console slice (apps/console-web): a dependency-free static console rendering providers, the routing ladder, the price book, and the usage/cost dashboard from a metadata-only Python-exported snapshot (no secrets, strict CSP, no external calls); Next.js upgrade deferred (SWC install times out in this environment)
2026-09-06  R-221  Added explicit allowlist-driven cross-provider fallback and a per-provider circuit breaker to the gateway: retriable failures fail over along the configured chain (never silent, never on non-retriable errors), the breaker opens/cools-down/half-opens, and every attempt is accounted
2026-09-06  R-220  Added true per-provider Server-Sent-Events streaming for the cloud adapters (OpenAI-compatible, Anthropic, Gemini): ordered delta events plus a final event with measured usage, reusing the HTTP-safety bounds (new IDs continue after the workbook backlog's R-010..R-219)
2026-09-06  R-009  Added deterministic usage and cost accounting for the model gateway: immutable metadata-only usage records (no content/secret), a configurable Decimal price book (local Ollama zero, unknown unpriced), and an aggregating ledger with per-provider/model breakdowns, p50/p95 latency, and cost per successful call
2026-09-06  R-008  Added key-activated cloud ModelProvider adapters (Anthropic, OpenAI, Gemini, OpenRouter, Groq) behind the shared boundary with an env bootstrap; standard-library only, no vendor SDK, keys env-only, local Ollama stays default and no key means no cloud call
2026-09-06  R-007  Added the deterministic Balanced Model Gateway router — escalation ladder (L0 refused), sub-L3 local Ollama routing, L3/L4 escalation-required, conservative context-budget guard, and no silent cloud fallback — with 14 offline tests
2026-09-06  R-006  Added the bounded loopback-only Ollama ModelProvider adapter with conservative model eligibility, offline conformance tests, and live generation/streaming proof
2026-09-06  R-005  Added the vendor-neutral Python ModelProvider contract, validated model-call records, deterministic registry, and CI-equivalent tests
2026-09-06  R-004  Added the minimal Go control-plane with typed config, PostgreSQL readiness, structured logs, graceful shutdown, tests, and live Compose verification
2026-09-06  R-003  Added loopback-only local Ollama configuration, model discovery, lifecycle commands, and live inference verification
2026-09-06  R-002  Added pinned local PostgreSQL+pgvector Compose bootstrap, versioned initial migration, and deterministic lifecycle/verification commands
2026-09-06  R-001  Bootstrapped the Section 74 monorepo skeleton and Stage 0 start, verification, state, and handoff contract

## [R-334] Generated Accessible Reusable Stepper / Multi-step Wizard Component – 2026-09-11

### Added
- `components/stepper.tsx` emitted in every generated Next.js web application
  - **Types**: `StepperOrientation`, `StepStatus`, `StepperVariant`, `StepDef`, `StepperProps`, `StepContentProps`
  - **Stepper root**: horizontal and vertical orientation via CSS flex; controlled (`value`/`onChange`) and uncontrolled (`defaultValue`) modes
  - **StepIndicator**: numeric (idle), checkmark SVG (completed), X SVG (error), animated focus ring (active); dots variant
  - **StepConnector**: solid line that fills (primary colour) for completed steps; adapts to orientation
  - **StepContent**: `role="tabpanel"`, `aria-labelledby`, `hidden` attribute, renders children only when active
  - **useStepperState hook**: `goNext`, `goPrev`, `goTo`, `canGoNext`, `canGoPrev`, `currentStep`
  - **Progress variant**: `role="progressbar"` bar above the track showing % completion
  - **WAI-ARIA**: `role="tablist"`, `aria-orientation`, `role="tab"`, `aria-selected`, `aria-controls`, `role="tabpanel"`, `aria-labelledby`, `aria-disabled`
  - **Keyboard**: `ArrowRight`/`ArrowLeft` (horizontal), `ArrowDown`/`ArrowUp` (vertical), `Home`, `End`
  - **Zero external dependencies**; 100% diff-invariant across `ir.description` changes
- `render_stepper_component()` exported from `omnistackai_agent_engine.codegen`
- 19 new unit tests in `test_stepper_component.py`; total test count: 1,329

## [R-335] Generated Accessible Reusable File Upload / Dropzone Component – 2026-09-11

### Added
- `components/file-upload.tsx` emitted in every generated Next.js web application
  - **Types**: `FileUploadStatus`, `FileUploadVariant`, `FileEntry`, `FileUploadProps`
  - **Drag-and-drop zone**: `onDragOver`/`onDragLeave`/`onDrop` with animated dashed-border drag state
  - **Click-to-browse**: hidden `<input type="file">` triggered by zone click / Enter / Space
  - **Validation**: `accept` MIME/extension check, `maxSize` byte limit, `maxFiles` cap — all with inline error messages
  - **FileRow list**: filename + formatted size (B/KB/MB), image thumbnail preview, upload progressbar (`role="progressbar"`), accessible remove button (`aria-label`)
  - **Avatar variant**: 96 × 96 circular crop with `CameraIcon`, hover overlay, drag target
  - **Compact variant**: reduced padding, no upload icon
  - **WAI-ARIA**: `role="button"`, `aria-label`, `aria-describedby`, `aria-disabled`, `aria-live="polite"` status region
  - **Keyboard**: `Enter` / `Space` opens file picker
  - **Zero external dependencies**; 100% diff-invariant across `ir.description` changes
- `render_file_upload_component()` exported from `omnistackai_agent_engine.codegen`
- 20 new unit tests in `test_file_upload_component.py`; total test count: 1,349
